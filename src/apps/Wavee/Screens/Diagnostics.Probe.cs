// ── Screens/Diagnostics.Probe.cs ───────────────────────────────────────────────────────────────────────────────────
// the CLI probe arms: --perf-bench, --startup-bench, --crash-probe, --lyrics-advance-probe (ch 22 (a) — the env-var
// switch is deleted), --qr-dump (ch 28 §1.5), NotificationSimulator (ch 14, +195), the process receipts
//
// Role: SHELL
// Owner: S
// Wave: 6
// Budget: 800 lines
// Spec: ch 27 §9.3 (400) + ch 14 §9 (195) + ch 28 (93) + ch 22 (DERIVED)

namespace Wavee;

public static partial class Diagnostics
{
}
