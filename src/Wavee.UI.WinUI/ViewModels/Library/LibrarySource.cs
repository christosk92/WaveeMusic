namespace Wavee.UI.WinUI.ViewModels;

/// <summary>
/// Library surfaces that can be viewed either as explicitly saved entities or
/// as virtual entities synthesized from the user's liked songs.
/// </summary>
public enum LibrarySource
{
    Saved,
    FromLikedSongs
}
