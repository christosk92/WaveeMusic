<?xml version="1.0" encoding="utf-8"?>
<winuiex:WindowEx
    x:Class="Wavee.UI.WinUI.Floating.DebugFloatingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winuiex="using:WinUIEx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Wavee — Debug"
    MinWidth="780"
    MinHeight="560"
    Width="1200"
    Height="800"
    PersistenceId="WaveeDebugFloating">

    <winuiex:WindowEx.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt" />
    </winuiex:WindowEx.SystemBackdrop>

    <Grid x:Name="RootGrid" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title-bar drag region. ExtendsContentIntoTitleBar=true is set in
             code-behind so this Border occupies the title-bar area; the right
             edge stops short of the system caption buttons. -->
        <Border Grid.Row="0"
                x:Name="DragRegion"
                Height="32"
                Background="Transparent">
            <TextBlock Text="Wavee — Debug"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>

        <!-- DebugPage is a Page; host it via a Frame so it gets the Page
             lifecycle (OnNavigatedTo/From) the rest of the app gives it. -->
        <Frame Grid.Row="1"
               x:Name="RootFrame"/>
    </Grid>
</winuiex:WindowEx>
