<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Wavee.UI.WinUI.Controls.PageErrorState"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420">
        <StackPanel Spacing="14" HorizontalAlignment="Center" Padding="32">
            <FontIcon Glyph="&#xE7BA;"
                      FontSize="40"
                      HorizontalAlignment="Center"
                      Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
            <TextBlock x:Name="TitleText"
                       Text="{x:Bind Title, Mode=OneWay}"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>
            <TextBlock x:Name="MessageText"
                       Text="{x:Bind Message, Mode=OneWay}"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Visibility="{x:Bind HasMessage, Mode=OneWay}"/>
            <Button x:Name="RetryButton"
                    Content="Retry"
                    HorizontalAlignment="Center"
                    Click="OnRetryClicked"
                    Style="{ThemeResource AccentButtonStyle}"
                    Visibility="{x:Bind ShowRetry, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
