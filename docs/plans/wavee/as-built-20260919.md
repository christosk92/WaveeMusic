# As built — 2026-09-19 session (D1–D4, B4, B6, podcast P1–P2)

Running notes kept by the orchestrator while the waves were written (nothing was compiled; the owner builds). Each
section names deviations from the plans and what later waves must know. The plans link here from their "As built"
sections.

## P1-R (Show.Rules.cs) as built
- ListenNext serial anchor = OLDER of (resume, newest finished); forward walk then backfill oldest unfinished behind anchor; "finished" = >= 0.90 excluded from resume AND up-next in every order; non-serial orders = newest unfinished first.
- ShowLedger scaling: Episodic/Recent only, total > resident > 0, OLDEST resident (tail neighbour) played, dates aligned & last is oldest; else counts are resident-only (tail never "to go"). Fresh = pct <= .01 and published > lastPlayedAt. Helpers IsFresh, LastPlayed, TailAssumedPlayed. WinUI never scaled (plan claim corrected in doc).
- ShowCadence bands: <=1.5d Daily, 5-10d Weekly, 10-20d Fortnightly, 20-45d Monthly, 1.5-5d Unknown on purpose; weekday = UTC modal, >= half the sample, tie => null.
- ShowReaderShape: publishedAt aligned with viewSlots (not by slot); non-row items carry (-1,-1); Group Slot = first row's slot; MaxItems(n) = 5 + 2n; GroupKeyOf/YearOf/MonthOf helpers.
- EpisodeNeighbours.Of(slotsNewestFirst, self) — order parameter DROPPED (next = newer, previous = older in every order; labels come from the show).
- ShowViewPrefs: base62 id only (':'/';'/empty rejected), blob string?, negatives -> 0.
- P3 notes: pass Completed(p,d) ? 1f : Pct; spans newest-first; ShowVisit.Of(played+inProgress>0, toGo, inProgress) only after progress settled; Returning may yield Resume -1/Count 0 -> head fallback.
## P1-A/B/C/F as built
- ShowFlags FactsMask/RatingMask; flags persisted in TWO columns (flags, rating_flags) so a Facts-only upsert never wipes Rating bits. Column `consumption_order` (order is SQL keyword). Tone uint 0xAARRGGBB.
- Episode kind column is `Variant` (Table.Kind clash); handle/staged still `Kind`. No per-shape version: DDL fingerprint = the bump (new file name).
- Decode: episode number field 65 is sint32 (ZigZag); consumption_order proto enum already 1-based (no +1); Mixed only when media_type present.
- ProgressOf(false,_,0) = int.MaxValue (per §5.8). Completed requires progressMs > 0; long arithmetic.
- OPEN: Completed vs Played(pct) disagree for <25-min episodes (<=30 s left but <98%). P3 must feed rules `Completed(p,d) ? 1f : Pct(p,d)`.
- P5: EpisodeFields.Detail = 1<<9 COLLIDES with Progress; use 1<<10+. Paywalled/PreviewOnly/HasTranscript should move to P5's own mask/column.
- Seed: sh0 trailer is its own row spotify:episode:sh0trailer; sh2 rating 4.3 + my 5 stars; sh0-2 CanRate; sh3-8 Facts/Rating known-empty. Home seed lists sh1 as played 3 days ago (cosmetic mismatch).
- Budgets: Show.cs 437 (300), Episode.cs 502 (330).
## D2-U (sidebar) as built
- ShouldEnsureCount deleted; count = PlaylistFields.TrackCount row fact, fallback to resident membership Total (MembershipTotal) — free.
- Mosaic asks (rootlist walk + pins) at Prefetch, one span per build. Orchestrator also removed ResolveLivePin's count ask (plan gave it to F2) so no count-driven read remains anywhere.
- Orchestrator: s_show ShowV4 route now carries ShowFields.Facts (pulled forward from P4 per P1-data's note).
## D1-S1 (Store) as built
- DropCatalog does NOT delete the file: one tx clears kind tables (all scopes), palette, non-library edges/edge_state; keeps intent/meta/scope/5 library relations; incremental_vacuum + checkpoint(TRUNCATE). Reason: the intent journal.
- Recovery once per store SESSION (Boot resets) — sign-in cycles/tests get one each. Rebuild = owed flag serviced by the loop between jobs (queue can't refuse). Intent ids carry file generation in bits 48+. s_meta cleared after rebuild (ledgers -> next sync full walk). StoreHealth.RecoverySpent added.
- Loop exits on its own queue completed; Enqueue treats completed queue as refusal; Shutdown cleans memory-only store.
- Not written: "foreign WAL beside a good file" test (non-deterministic).
- Log lines: store.open / store.dropped / store.fault code= recovery= / store.recovered / store.reap / store.cleared.
## D1-S2 as built
- Shell.AcquireInstance() in Main before RegisterShapes/Setup/Store; Run throws if not acquired; boot.handoff line. ClearMetadataCache wired in App.cs. Setup witness = legacy library.db OR library.*.db. Orchestrator: headless --store now calls App.RegisterShapes() first (was naming a near-empty schema).
## P1-D1 (wire check) as built
- Spotify.Api.PodcastQueries (nested in Api) + per-op Query builders (reusable by P5); hashes verified vs 0d0429a0 PathfinderOperations.cs; variables vs *Variables.cs DTOs.
- RunWireCheckAsync via Api.RunAsync; herodotus via PostEncoded + Bearer|ClientToken|Identity|ContentProtobuf, own route literal (Telemetry's constant is private); limit 10, 30-day lookback.
- Card PodcastWireCheckView in Diagnostics.UI.cs (Runtime page); disabled unless Shell.Auth == Live.
- Transcript status is a stand-in (GetText hides status). No tests (helpers private; no InternalsVisibleTo).
- Captures land in %LOCALAPPDATA%\Wavee\logs\podcast-wire\.
## P1-O (controls + loc) as built
- Controls.Words: Word(Label, Count?, Visible Prop<bool>?, Code, Chevron) ; Rail(..., role = AutomationRole.Button (per-word; engine has no Toolbar/TabList/Link roles), onReselect, onSelect); Links. Big = Display 20/26, 300->400, tracking -6.
- LIBRARY MOTION CHANGE: underline now SLIDES 167 ms between words (was an 83 ms in-place fade). Resting pixels identical. Revert = BrushTransitionMs = Design.Motion.Faster + drop SlideBar layout effect. Underline colour no longer fades on accent change.
- Podcast controls: Chip(text, glyph, tone, toneOf), LedgerBar(played, progress, tone) animates only on value change, StarRow(value, rate, size, tone), Door(DoorData incl. Tone, Numeral string?), FindBox (no glyph; TextBox has no slot). Door label NOT upper-cased (Design.Type rule).
- P3/E: Role=Link must be AutomationRole.Hyperlink.
- Loc: extra keys countOf, nothingMatchesHint, rate.stars; caughtUpNext{day} is a SELECT on lowercase English weekday; wire-check strings English-only in ko/nl (fallback).
- Unverified engine timing: UseLayoutEffect slide same frame; door numeral ZStack sizing.
## D2-L (lists on disk) as built
- Store.Lists.cs: ListRow(Uri, ItemId, AddedAt, AddedBy, ChartStatus, ChartPos, ChartPrev, Flags, RootlistKind Kind, Depth, WirePos, FolderId, FolderName); ListWrite.MayPersist(state, rows, total, wholeAnswer, anyPending, revision) + IsWellFormedRevision ("<uint digits>,<40 lc hex>") + Stamp/Trusts (written_by "g<Generation> <version>"; bump Generation invalidates old lists w/o DDL change) + RootlistKey.
- Store.SaveList(scope, relation, parent, revision) / ReadList(scope, relation, parentId, then). list_item has wire_pos (rootlist skipped items still count a position). Revision saved = the one the SAME batch staged. Sweep deletes a playlist's list with its row. DropCatalog drops list tables.
- Disk read lands via the wire's own commit path (whole-list run of text uris + StagedPlaylist Known=TrackCount, Thin authority / StagedRootlist), committed only if still Unknown, never written back. Side effect: Applied stamps playlist row FetchedAt => a later row ask skips the disk leg (one extra batchable network ask).
- Edge door: AskDisk/AfterDisk; EdgeTableBase.WasDiskAsked/MarkDiskAsked. PlaylistShape.CountColumn; TrackCount==0 mask deleted; CommitPlaylists TrackCount group writes its own value. PlaylistShape never had a revision column.
- Rootlist route diffs already; unchanged answer keeps disk list Complete; no Spotify.Api change needed.
- OWED (D3 L2): Spotify.Decode.Playlist.cs PlaylistFormatAttributes stages revision only when attributes present -> must stage field-1 revision whenever contents staged. Replayer lands whole-list run + staged revision (WriteBehind persists); never SaveList from RootlistConfirmed.
- OWED (D4 F1): AfterDisk ends in Pump() -> drain conversion.
- Orchestrator removed stale SidebarProjectionTests comment about the load-time mask.
## D3-pure as built
- Spotify.Playlist.Ops.cs: PlaylistOps.TryApply<TRow,TAccess>(list, ops, opItems, lists, access, out attrs, out tally, out misfit) (copy; untouched on misfit) + TryApply(List<WireItem>, Batch,...). IRowAccess<TRow>{SameUri,HasItemId,SameItemId,Holds,TryPatch}; WireItemAccess. Op factories; Place {Index,First,Last,AfterItem} (no before). WireItem, ListAttributeChange(Name,Description,Unset), Tally.Reconciles, Misfit(OpIndex,Kind,Why).Refusal, Batch(Ops,Items,Lists).AddedUris.
- Decoders: DecodeDiff -> DiffAnswer{Unchanged|Replay|Contents|FullRead} (field 20 first; refuses advancing 0-op diff, multiple_heads); DecodePush -> PushAnswer(...HasReplayableOps); TryDecodeChanges; TryDecodeContents.
- ListFreshness (WindowMs 300000, BlockingBudgetMs 1500; first open/dirty -> RevalidateThenPaint; window -> Paint; past window rolling -> RevalidateThenPaint else PaintThenRevalidate) + ReaskHeader. ListPush.Decide (never compares counters) + DecideRootlist (dedupe by revision).
- Proto: MetaItem 7 (bytes, capabilities shape) / 9 (uint64=400), ListAttributes 16, ItemAttributes 17 (UUID msg), Add 6 (unobserved) / 7, SelectedListContent 23 — lossless types, neutral names.
- Fixtures: 29 .bin + 2 .expected.txt + README (scrubbed; 1,065 needles, 0 hits). Tests: PlaylistOps 40, PlaylistDiffDecode 23, ListFreshness 10, ListPush 9. `using static Wavee.PlaylistOps;` inside the namespace (Concert.cs Wavee.Place shadow).
- Build watch: List.InsertRange(int, ReadOnlySpan); generated names Unknown7CapabilitiesShape etc.; byte-exact round-trip test.
## D4-F1 as built
- Fetch.Drain(), CanSend, WakeForDrain (host posts OnDrainWake via marshaller; NextWakeAt=now while owed; held bucket not a deadline). Register() owes a drain. Reset() clears CanSend. Playback ask pumps inline and takes same-shape rows. Bucket key w/o priority; max priority; emptied bucket resets priority/attempt/wait (old stale-attempt bug fixed). Send order: priority, rows before edges, FIFO.
- fetch.send ticket= provider= subject= kind= edge= offset= rows= need= prio= attempt= waitedMs= / fetch.answer ticket= status= ms= rows= unfilled= [retry=].
- Tests: Drain added across Fetch*/EdgeDoor/SidebarProjection tests; Playback_outranks test now uses 3 shapes; alloc facts measure Plan+Drain at Warning log level.
- Orchestrator: L3 wires CanSend + Pump in Spotify.Library.cs. Quick reconnect (<30 s) skips SyncNow, per-tick Drain covers it.
## Verifier (Opus) fixes applied by orchestrator
- Spotify.Api.Podcast.cs CS0165 (atComments) fixed; JSON scan wrapped in try/catch JsonException.
- Sidebar.cs PlaylistRow folds PlaylistTracks.Version when !Knows(TrackCount) (count fallback repaint). OWED: pin path fallback in Sidebar.Host.cs (after F2).
- Store.FileGlob deleted (unused) + its test; census uses "library.*", witness "library.*.db".
- Palette.Host.cs comments -> Fetch.Drain().
- Deferred to P3: Show.UI.cs:126 literal podcast.empty; TitleSansNumber/LeftLabel. P5: Episode Identity arm writes whole Flags column. Nit: User.WordRail builds Word[] + Loc.Bind per call.
## D4-F2 as built
- Sidebar.Host ResolveLivePin Album/Artist/Show identity asks -> _pinEnsureAlbum/Artist/ShowSlots flushed once in ResolvePins.
- Playback.Host.Context EnsureRestoredIdentities: episodes span per pass (ArrayPool<Episode>).
- Playback.Host EnsureRow collects into s_ensureRowTrack/EpisodeSlots (dedupe, cap 4); FlushRowEnsures() once at end of Publish() at Playback priority.
- Not converted (no need — per-tick drain coalesces): Rail.UI now-playing single ask; Home.cs EnsureSection (callers Browse.Page.cs:596 / Home.Rules.cs:1370 loop per band).
- Orchestrator: pin count fallback (TrackCount row fact, else resident MembershipTotal) in ResolveLivePin — matches WalkRootlist.
## D3-L3 (dealer) as built
- LibraryPush.Playlist = 1<<6; LibraryPushRules.PlaylistTopicPrefix/PlaylistIdChars(22)/PlaylistId(topic); strict classify.
- Spotify.Library now partial; NEW Spotify.Library.Lists.cs: ListStamps (NowMs never 0, IsDirty/MarkDirty/RevalidatedAtMs/MarkRevalidated/ForgetAll/Changed + Count/ClockOf/CollectDirty/ForgetExcept), ListPushReplay (pure), host list section. Keys: EntityId.Text playlist uri / ListWrite.RootlistKey(user uri). ForgetAll on scope switch/sign-out.
- open = Shell.Current.Peek() route Playlist with subject slot == playlist (rootlist always open).
- Push: decode on dealer thread (exceptions caught), post in order; not folded into the 250 ms settle. RevalidateNow skipped while list Unknown. ApplyInPlace: snapshot, TryReplay, total vs edges.Total, revision well-formed, stage/commit/publish/WriteBehind, MarkRevalidated. A push with a list-attribute change -> not applied in place (reason=header). Added tracks not fetched (page demand does it). Rootlist: dedupe by held + last re-asked revision.
- Reconnect (second SyncNow of same scope, past the 30 s guard): RefreshEdge Prefetch for open + dirty Complete playlists; ForgetExcept(keep); line list.reconnect revalidate= forgot=.
- CanSend in Install; WatchSession pumps Fetch on every transition into Online; SyncNow Resume()+Pump().
- list.push list= verdict= ops= reason=.
- Orchestrator: Spotify.Connect.cs now forwards the payload (empty when Truncated).
- OWED: L2's Store.SnapshotList / Store.StageList(staging, relation, uri, rows, revision) — StageList must stage whole-list run + playlist row Revision + TrackCount that can overwrite. MarkRevalidated on revalidation landing (orchestrator hook after U1).
## D3-U1 (open) as built
- ListOpenPolicy (pure: Surface Page/Revisit/Queue, Ask None/Ensure/Refresh, Plan, Decide, Observe, Holds, Classify, IsRolling, InFlightMs 30 s) + ListOpen (UI state: Open, Holding, WouldHold, Observe, Settled; one budget timer; list.open / list.settle / list.hold.spent). Unknown list -> one EnsureEdge (disk leg + chained /diff), never Refresh; open joins an in-flight revalidation.
- Hold wired via Playlist.HeldRows table source (State Unknown/Count 0 while held -> TableRules.RowsPending keeps skeleton) + PageStamp.Holding -> MetaLoading. WouldHold for the first frame. Revisit never holds.
- Queue: EnsureContext one span ask + ListOpen.Open(Queue) Prefetch — NEW traffic: an Unknown context playlist's membership at Prefetch (usually disk).
- No library pane opens a playlist list (Collection browser has Album.Pane/LibraryShowPane only) — no change.
- Orchestrator: moved ListOpenPolicy/ListOpen to NEW Entities/Playlist.Open.cs; Fetch.ListSettled hook (EdgesAnswered/EdgesFailed) installed in Playlist.InstallPages.
- Known limitation: lists revalidated by another surface (sidebar mosaic, L3 RevalidateNow/reconnect) aren't stamped -> first page open sends one extra (usually 304) /diff. Stamping on answer deliberately NOT done (would hide a mid-flight push's dirty mark).
## D3-L2 (replay arm) as built
- DiffVerdict/ListDiff DELETED; ReadList(kind, id, revision, baseline, ref outcome) + ListRead + FullRead + LogReplay; ListReplay.Decide (pure) -> Outcome{Unchanged|Applied|Contents|FullRead, Why, Answer, Rows, Attrs, Tally}. RecentsEdge uses DecodeDiff(...).Answer == Contents. Behaviour change: 0-op diff beside contents = fullread:Contradictory.
- Store.Lists: ListWrite.IsSettled; SnapshotList (null unless Complete, rows==total, no pending, all identities; gid uris formatted); StageList(s, relation, parentUri, rows, revision) -> bool (whole-list run + revision + TrackCount at Authority.Full; rootlist via StagedRootlist; any thread; rewinds on failure); disk reader renamed StageDiskList (count still Thin); catalog uris stage as packed gids.
- Decode.Playlist: revision staged from field 1 whenever contents present (own no-group row); header-only answer no longer moves the revision. PlaylistReplay / RootlistReplay.
- Extra guards: contents w/o revision -> full; 0-op diff naming another revision -> full; to == held refused; header op needs both name+description stated (else fullread:header); rootlist add of a decoder-skipped row refused (rootlist-item).
- list.replay list=<playlist|rootlist|recents> ops= kinds= verdict= ms= (category library).
- Orchestrator: Fetch.cs Baselines field/alloc/call/Recycle; deleted SpotifyApiTests DiffVerdict facts (862-927); stale DiffVerdict comments updated.
- POISON PATH found -> fix agent: optimistic row edits keep the held revision => our own edit replayed twice; RootlistFailed refresh against rejected optimistic list; ApplyInPlace ignores StageList bool.
- Notes: header op as Identity group can mark a never-loaded row Identity-known without cover/owner; unset description stays on disk (PlaylistShape.Save keeps old when empty); full read answering no revision keeps old revision (unobserved).
## D3 fix (optimistic edits forget revision) as built
- Entities.ForgetListRevision(scope, relation, parent) in Edges.cs (memory only; ClearText playlist Revision / SetRootlistRevision Empty).
- PlaylistEdits.LandOptimistic (RemoveRows, MoveRows, Restore — revert keeps revision forgotten). Library.LandRootlistTree -> OptimisticRootlist(Base, Version) (WriteRootlist, StartCreate, PlaylistEdits.Delete); ReadRootlistBack (RootlistFailed, CreateFailed, CreateConfirmed unfiled/409, Delete refreshes); AdoptRootlistHead guarded by pure OptimisticHead.MayAdopt (held base, reply counter == base+1, edge version unchanged, no re-arm). Second concurrent rootlist write now reads a fresh head.
- Library.LandPushReplay: StageList false -> reason=stage, dirty/revalidate, no stamp.
- Adds (AddTracks/AddRecommendation/Deposit) are not optimistic — /diff replays ADD once. Header edits keep revision.
- OPEN: optimistic edit on a list still loading in pages — a later page re-arms the revision over mixed rows (pre-existing; PlaylistFormatAttributes stages revision for any contents page; Playlist.cs:765 commit ungated). Fix: refuse row edits until Complete, or decode/commit side.
- No list.replay line for no-revision reads (ReadList returns before LogReplay) — proof is the wire.call full GET.
## B4 as built
- Inputs ApDropped/DealerDropped/ApRetry/DealerRetry; per-transport LinkPhase (Down/Opening/Up/Waiting) Session.Ap/Dealer, ApAttempt/DealerAttempt, BackoffMs(uint) 3/6/12/24/30 s; LoggedIn flag. Session.Epoch = session+dealer epoch (name kept for 4 outside readers); new ApEpoch. Events stamped per transport; LinkOf(kind); IsStale first in Step.
- Effects CloseAp / CloseDealer (+Connect.Clear) / ApBackoff / DealerBackoff; CloseAll = both. OpenDealer only when LoggedIn && dealer Down. Login over running session -> CloseAll.
- Online = signed in + dealer holds a connection id; AP drop while Online keeps Online; dealer drop -> Reconnecting (resync/feeds/SessionOnline fire on Online edge + epoch).
- Shell: 3 cancellation sources (AP, dealer+keepalive, session); deferred dealer start; two retry timers epoch-stamped. FIXED: catch(OperationCanceledException) swallowed HttpClient timeouts -> dead threads with no reconnect.
- Lines: ap.exit reason= epoch= / dealer.exit / ap reconnecting in / dealer reconnecting in / session.stale / dealer connected (host, epoch N) / logged in (…, ap epoch N).
- Evidence: 8 of 12 `ap channel failed` came 11-57 ms after our immediate Pong (cmd=0x49).
- Left for B6: reconnect welcome with late ProductInfo -> Unknown tier -> ends login (D12); B3 repeat-id compares arena slices not bytes.
## P2-S as built
- RouteKind.Episode after Concert; s_routes row; Shell.For Episode; InstallLazyGroupFor includes Episode (Album group); IsEntityVerb "episode"; TryParseSpotifyUri episode -> open. Omnibar ChoosePlays still plays episodes (explicit user action). Drag.cs unchanged.
- EpisodePagePlaceholder (Album.Page.cs, registered in Album.InstallPages): title/uri, eyebrow, Play via PlayContext, Go to show.
- ActionId MarkPlayed/MarkUnplayed/GoToShow; TargetKind Show/Episode; ActionTarget.ShowUri + ForShow/ForEpisode; ActionRules.RouteFor Show/Episode + ShowRouteFor. NO AppActions.Register yet (P5 episode menu owner wires them to Entities.MarkEpisode).
- Tests: ShellRoutesTests (count 31, episode rows/parse), BootOrderingTests RouteKindCount 30->31.
## P2-M as built
- DetailKind.Episode; Config.Episode (Show with Kind Episode, Heart None, RailScope Show); Eyebrow Episode = Strings.Nav.Episode (+ · year). No RailScope.Episode (ordinal indexes 5 rail cells).
- Detail.cs §8b: RailSlotSet, RailBadgeRow {None, BesideEyebrow, OwnRow, AfterMeta}, RailLayout (constants + IsPodcast, LeadsWithAttribution, BadgeRowFor, RowsFor, HeightOf, CtaHeight). Skeleton.RailPlan trailing fields; RailPlanFor(..., RailSlotSet slots = default); RailDescriptionLinesFor.
- FrameSlots +6 (bits 4096..131072); FrameHost trampolines; PlayPill(accent, onClick, label?, glyph?); RailSatellite / RailSatelliteSave / RailSatelliteMore.
- Episode rail: Attribution (show link) LEADS above title (rail:lead) replacing eyebrow; badges under meta. Show: eyebrow+badges → title → attribution(publisher) → rating → meta → ledger → CTA → topics → blurb. Podcast kinds: Attribution is the after-title row even w/o billed artists. Vertical header (<540) hosts slots (no topics).
- P3/P5 notes: Rating/Ledger/Topics get rail content width (NaN in vertical header); Primary via Detail.PlayPill(accent, onClick, label, glyph: Icons.Add for Follow); Badges = one wrapping row of Chips Gap RailLayout.BadgeGap; Satellites keyed, fixed length per slot set, builder builds only (skeleton counts it); values via signals; P3 declares Attribution for publisher and removes publisher from Text.ShowMeta.
## B6 as built
- ApKeepAlive (Spotify.cs §1c, pure): State(Phase, DeadlineMs), Step(in State, Input, nowMs); inputs Connected/PingReceived/PongAckReceived/Tick; effects None/SendPong/Dead(NoFirstPing|NoPongAck|NoPing). Constants from librespot core/src/session.rs: PongDelay 60 s (:685), PongAckTimeout 20 s (:686), PingTimeout 80 s (:684), FirstPingTimeout 20 s (:683). go-librespot/librespot-java pong at once (echo payload); librespot's delay since PR #1359 (2024-10). Pong payload 4 zero bytes.
- ApPulse shell: lock + one one-shot Timer re-armed to DueInMs; held pong on captured stream/socket/token + s_apCodec; Dead closes socket -> ApKeepAliveException -> ap.exit reason=keepalive-timeout. Authenticate no longer pongs (records first ping). ApPingIntervalMs deleted; ApReadTimeoutMs 300 -> 160 s (backstop).
- Follow-up A: reconnect with Unknown tier keeps known tier/product; Free still ends login. Follow-up B: DealerOnline repeat check SameText (bytes).
- Lines: ap.keepalive ping|pong|ack (Info) / dead (Warn); logged in (…) says "product late".
- Before/after: 2026-09-19 12 `ap channel failed … 10054` in 3.6 h; 8 within 11-57 ms of cmd=0x49. After: count ap.exit reason=dropped + ap channel failed 10054 per hour; separately keepalive-timeout; check gap pong -> reset; ack follows each pong; sinceLastPingMs ~120000. Risk: repeated dead reason=no-ping => server times ping from ack.
- Orchestrator: WelcomeMarket (pure) — a reconnect's late 0x1b keeps the held market for the same account (scope no longer switches); test added.
## P2-T (progress) as built
- **WRONG — CORRECTED 09-19 by the fresh-client captures (`findings-podcast-wire.md` SUMMARY 1–2): ResumePoint is a google.protobuf.Duration {int64 seconds=1; int32 nanos=2}, and "completed" is a oneof marker, not an omitted resume point. A fix agent was running at handoff time (handoff-20260919-session2.md §1).** Original note: ResumePoint unit = MICROSECONDS, field 2 int64 (writes ms*1000; capture notes in wavee-fluentgpu-playback-plan.md §8.1; 0.2.9 HerodotusClient.cs:114). WinUI's position_seconds=1 never filled. Completed = revision with NO resume point.
- Telemetry: CurrentStates(since, s, ct) -> n / -1; FoldCurrentStates (pure; Full; ProgressOf(hasPoint, µs/1000, 0) -> completed int.MaxValue); CurrentStatesFilter; PlayedAtOf (create_time before update_time); HydrateProgress(scope) (one in flight/scope); ReconcileProgress (newer wire revision promotes over Local; equal/older dropped); ProgressSettled (new(Platform.Args.Fake)) / ProgressFailed; ResumeRevision builder; ResumeMs + ListRoute DELETED.
- Library.SyncNow calls HydrateProgress. Playback.Host.Context: local mirror pause/end (LeaveEpisode), 15 s ArmProgressMirror/ProgressMirrorTick, EpisodeStartMs replaces LookUpResumePoint (deleted). LoadHost: fromMs==0 && Claim/Advance => EpisodeStartMs(id).
- Episode.Progress.cs: EpisodeProgress (MirrorIntervalMs, Marked, UnixSeconds, ResumeFromMs, HydrateLanding), Entities.StageLocalProgress / MirrorEpisodeProgress / MarkEpisode(Episode e, bool played).
- Lines: podcast.progress.hydrate / .mirror / .mark / .resume.
- FOLLOW-UP (done 09-19): since-stamp DELETED (ProgressStamp, ProgressOverlapMs, Keys.PodcastProgressSince) -> every hydrate asks HydrateSince(now) = now - 180 d, limit 1021 (truncated=1 in the line). EpisodeShape.Load stages one row per distinct authority (pure RunsOf; identity/about/progress each at its own rung; no DDL change). Save writes authority 0 never NULL; orchestrator also fixed Store.PrepareUpsert to max(coalesce(col,0),coalesce(excluded.col,0)) (sqlite max() is NULL when any arg is NULL — affected every shape). Offline settle: Telemetry.ProgressUnreachable(phase) = Reconnecting|Failed (not Offline = boot state); pure SettlesUnreachable; Library.WatchSession posts SettleProgressUnreachable -> Settled=true, Failed=true, line `podcast.progress.hydrate unreachable`; a later landing clears Failed. Paused Claim: EmitLoad sets PosMs = EpisodeStartOf(id) for Claim/Advance at 0 (Playback.Transitions.cs, public); `podcast.progress.resume` moved to the Started report. ResumeAt/DoResumeAt/ResumeTailMs DELETED. Orchestrator removed LoadHost's redundant resolve + EpisodeStartMs + the `why` parameter.
- OPEN: Show/Artist/Album/Track/Playlist/User/Concert shapes still reload the whole row at the highest group authority (wire rungs only: a Thin group reloads at Full). Play on the already-playing episode resumes from the last 15 s mirror tick.
- P5 note: fromMs 0 means resume — "Start from beginning" needs its own intent. Natural end sends end position (both fold to completed). First hydrate may create up to 1021 progress-only episode rows.

## P3 (show reader) as built
- Show.Page.cs rewritten (~1200 lines vs 900 budget): PageHost owns ReaderModel (Status/Order seeded from ShowViewPrefs, persisted by a tracked effect; Find session-only; ONE Snap = UseComputed — the snapshot lives on the PAGE, not ReaderHost, so rail primary/ledger/tone and the reader never disagree); `ShowReaderRules` (pure CORE decisions). Show.UI.cs rewritten (~650 vs 380): rail body, visit head, header/empty, month groups, foot, seeds, publisher line, MetaOf; `podcast.empty` literal -> Strings.Podcast.Empty.
- Episode.UI.cs rewritten: ReaderRow, RowItem(Episode, Version, RowMarks), RowContext, IsNowPlaying, Invoke, OpenPage, Enqueue, pure ReaderPct/ReaderPctOf, LeftMinutes, LeftLabel, DurationWords, DurationLabel, TitleSansNumber; Fixed(RowItem) for the head's eager rows; SeedReaderRow; ProgressRule. DELETED Row, BoundRow, Card, PlayCircle, MinutesLabel, SeedRow.
- NEW Episode.Menu.cs: play next / play after / mark played|unplayed / go to show / Share submenu; registers ActionId.MarkPlayed/MarkUnplayed (-> Entities.MarkEpisode) and GoToShow (-> ActionRules.ShowRouteFor); ShareMenu + CopyLink reused by the show's more-menu.
- Tree: Detail.Frame(Config.Show) rail slots Badges, Attribution (publisher), Rating (+ rate flyout, stars disabled with reason), Ledger, Primary, Satellites [heart, trailer, bell (disabled, Notify.Unavailable), share, more]; right = ReaderHost -> SkelRegion -> ItemsView.CreateBound<ReaderItem> (PersistentPrefixCount 1, ItemClipTopInset 48, fade 24) -> [0] RAW sticky rail (filter words + counts, FindBox, sort words; horizontal scroll under 720) -> VisitHead (New doors+about | Returning continue hero + up-next + new-since + mark all played | CaughtUp + caughtUpNext(day) | Unavailable when the hydrate failed and nothing shows progress) -> header -> month groups -> rows -> foot.
- Deviations: two width thresholds (rows narrow < 540, rail scrolls < 720); no rail plate; a heart satellite (hidden while Follow is primary); episode-1 door/PlayFirst only when the list is complete; copy from existing keys (capitalised filter/sort words, "Resume · N min left" composed from two keys, about facts as one meta line); ledger numbers not bold; no hero number badge; trailer icon Icons.TvMonitor; MarkPlayed registered with IconKey Save (the menu overrides the glyph).
- Needs: loc (lowercase podcast.filter.*/sort.*; podcast.playLatest, playFirst{n}, resumeLeft{left}, follow, fact labels, trailerTitle{name}, trailerWhy, bellHint); ActionIcons check/played key; CopyLink disabled for show/episode targets (reads a track set); Detail frame wash/accent still grade from the cover first (CardAccent only a fallback); skeleton counts hidden heart/trailer satellites; file split Show.Rail.cs / Show.Head.cs for the budget; no focus-within in the engine (hover-only action cluster unreachable by Tab).
- Build watch: typed CreateBound with a persistent prefix; Episode.Fixed's RowScope with discard lambdas; Assert.Equal collection expressions vs Item[]; `with { Visible }` on Controls.Chip. The Write tool turns backslash-u escapes into literal chars — replaced with (char)0x constants.
- LIVE DATA: the episode list still comes from the ShowV4 field-70 route, which never answers on live data (handoff-20260919-session2.md §5.2-5) — P3 shows episodes in --fake only until the playlist/v2/show fix lands.
- --fake check: spotify:show:sh0 (Returning serial: Resume #9 at 46 %, up next #10-12, new since #13-14, rating 4.8, amber, trailer), sh1 (New: doors + about; the seed follows all shows so the primary reads "latest"; unfollow to see Follow), sh2 (CaughtUp, exclusive + video badges), sh7 (load more), sh8 (empty). Widths 1440/1120/760/480.

## Herodotus resume-point fix (P0, after the fresh-client captures) as built
- Wire facts re-decoded from vc1 #47/#585/#658 and vc2 #46/#308/#359 (`findings-podcast-wire.md`, `findings-wavee-vs-official.md` §3.3).
- herodotus.proto: imports google/protobuf/duration.proto (like l5_hashcash.proto). `CurrentStateEntry { entity_uri = 1; repeated CurrentStateRevision revisions = 2; }` — REVISIONS ARE REPEATED (the CEL filter says `cs.resume_point_revisions`; vc1 #47 has an episode with a newer 131 s position and an older {4}; the old singular declaration merged them). `CurrentStateValue { entity_uri = 1; oneof state { google.protobuf.Duration resume_point = 2; StateMarker marker_3 = 3; StateMarker marker_4 = 4; bytes context = 12; } item_uri = 6; }`. `ResumePoint` message DELETED. `ListResumePointRevisionsResponse.next_page_token = 2`; `BatchCreateResumePointRevisionsResponse` = repeated `BatchCreateResult{entity_uri = 1, CreateResumePointRevisionResponse result = 2}`. Unknown fields round-trip (tested).
- Write: `ResumeDurationOf(ms)` = {Seconds = ms/1000, Nanos = (ms%1000)*1_000_000}; negative -> 0 (`{}`). `ResumeRevision` omits value.entity_uri like the official client; for 820 912 ms it produces EXACTLY the official vc2 #308 bytes (uri swapped). `MicrosPerMs` and the `Completed = -1` sentinel DELETED. LeaveEpisode's signature unchanged.
- Read: `PositionMsOf(Duration)` = s*1000 + nanos/1e6, saturating (negative -> 0, nanos past a second -> 999 ms). `CurrentRevisionOf(entry)` = newest by update_time else create_time (tie: earlier on the wire). `ArmOf(value)` -> `Episode.Rules.ResumeArm { None = 0, Position = 2, Marker3 = 3, Marker4 = 4, Context = 12 }`. `Episode.Rules.ProgressOf(arm, positionMs, durationMs, out progressMs)` (bool try): 2 -> clamped position; 3 -> 0 (in progress at 0, NOT completed); 4 -> duration (int.MaxValue when unknown = completed); 12 / none -> false, nothing staged.
- PROVISIONAL: marker 3 = "started" and marker 4 = "finished" are inferred (official wrote {3} at a fresh play start; the only {4} is an old revision whose timing fits "finished"; the official client never wrote {4} in either capture). A 2-minute capture (mark played, mark unplayed, natural end) settles them.
- Previously corrupted server values ({2:4699000}, {2:998943000}) now read as 4 ms / 998 ms — what the server holds and every client shows; no repair (the next write fixes each episode).
- Hydrate: limit 1021 -> 1000; the 180-day filter KEPT on every sync (the answer is ordered by kind then uri, not recency, so a cut at 1000 drops arbitrary rows; the window limits that; an episode last touched > 180 days ago stays unknown).
- Mark played: `EpisodeProgress.TryMarkedResumePoint` — played sends Duration = the episode's duration (reads back completed); unplayed sends 0; played with an unknown duration sends NOTHING (0 would mean unplayed) but marks the local row. `MarkEpisode` logs `wire=0/1`.
- Tests: Duration theory (0 … 7 200 999, -5), a 0–10 000 ms + large-value loop (nanos < 1e9, ToTimeSpan ok, exact round trip), saturation, pinned official #308 bytes + `{}`, write->fold round trips, captured arms, markers 3/4, no arm/context stage nothing, two revisions newest wins either order, unknown-field + context round trip, limit 1000; EpisodeRulesTests ProgressOf per arm; EpisodeProgressTests TryMarkedResumePoint.
- Not touched: `src/apps/_old/Wavee/SpotifyLive/Herodotus/HerodotusClient.cs` (parked 0.2.9, not compiled, still writes µs — deletion candidate). No `hm://herodotus/...` dealer push handling exists.
