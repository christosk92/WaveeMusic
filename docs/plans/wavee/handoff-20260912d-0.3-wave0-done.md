# Handoff — Wavee 0.3: Waves 0–3 green, FLAC decoder + catalogue in (updated 2026-09-13)

Supersedes `handoff-20260912c-0.3-wave2.md`. The earlier attempt (Wave 0 skipped, a temporary `Wavee.V3` /
`Wavee.V3.Tests` pair, a `<Compile Remove>` in `Wavee.csproj`) was rejected by Christos and discarded; it survives only
as a git stash in the main repo ("0.3 old worktree snapshot (wave 1 + Wavee.V3 scaffold), superseded 2026-09-12").

## Where the work is

Worktree **`C:\WAVEE\wavee-0.3`**, branch **`feat/0.3-structure`**, forked from `main` @ `90b56e82` (the 0.2.10
"Breaker" release). Everything below is staged, **nothing is committed**.

## What Wave 0 did (plan §5 Wave 0, §3)

- **The fold.** `git mv` of the 0.2.9 tree to `src/apps/_old/`: `Wavee/{Actions,App,Backend,Components,Design,
  Diagnostics,Features,Platform,SpotifyLive,Program.cs,WaveeApp.cs}`, all of `Wavee.Core/*` (its csproj deleted), all of
  the old `Wavee.Tests/*` (535 files; the csproj stayed and was rewritten). `_old` is reference material for porting
  and is compiled by nothing. **When the plan or a chapter says `_old/Wavee/X.cs`, that path is now real.**
- **Protos** moved to `src/apps/Wavee/Spotify/Protos/` (42 files); `Wavee.csproj` generates from there.
- **§3 project changes.** `Wavee.csproj` has no `Wavee.Core` reference and compiles the default glob (the whole 0.3
  tree) + the P16b `System.GC.RegionSize` trial knob. `Wavee.Tests.csproj` is a 30-line project reference to
  `Wavee.csproj` (+ `FluentGpu.WindowsApi`). `Wavee.ReleaseTool.csproj` cherry-picks `Screens/ReleaseNotes.cs` +
  `Screens/ReleaseNotes.Model.cs` by name. `Wavee.slnx` and `ops/build/generate-third-party-notices.ps1` no longer list
  `Wavee.Core`. The PlayPlay `Exists()`-guarded block in `Wavee.csproj` is untouched: its sources bind to the 0.2.9
  audio stack, so **do not create the junction in this worktree until Wave 2 re-homes it**.
- **The skeleton.** All 136 files of §2 exist: 17 are Wave 1's real code, 2 are the ported ReleaseNotes core, the
  other 117 are empty partials with a header (path, concern, Role, Owner, Wave, Budget, Spec). `App.cs` carries §3.5's
  `Main` verbatim over empty `Boot()` stubs in `Platform/Platform.cs` (also `Scope => CatalogScope.Fake()`),
  `Spotify/Spotify.Session.cs`, `Playback/Playback.Host.cs`, `Platform/Modules.Host.cs`, `Shell/Shell.Host.cs`
  (`Run()`), and `Glyphs.Register()` in `Platform/Design.cs`. The app is dark by design (plan §7).
- **ReleaseNotes port.** The 12 pure files of `_old/Wavee.Core/ReleaseNotes/` are nested verbatim inside
  `public static partial class ReleaseNotes` across the two `Screens/` partials (1,579 + 333 lines; the first is over
  its 1,000 budget because the port is verbatim — owner R trims in Wave 6). `Wavee.ReleaseTool` uses
  `using static Wavee.ReleaseNotes;`.

## Wave 1 (unchanged code, new home)

`src/apps/Wavee/Entities/` 17 files, 10,439 lines; the 21 test files now sit directly in `src/apps/Wavee.Tests/`
(namespace `Wavee.Tests`). One edit: `Queue.cs`'s `static class Queue` became `partial` so `Queue.UI.cs` can exist.

## Gates run (2026-09-12)

`dotnet build Wavee.slnx` Debug and Release: 0 warnings, 0 errors. `dotnet test src/apps/Wavee.Tests`: 450/450.

## Wave 2 — Spotify (done 2026-09-13, all gates green)

- `Spotify/`: `Spotify.cs` (CORE: Session state machine, Shannon, DH handshake, hashcash, PKCE, dealer frame, the
  request fold), `Spotify.Session.cs` (SHELL: AP/dealer/keepalive threads, login5, tokens, audio keys; reads
  credentials, device id and locale from `Platform` directly), `Spotify.Api.cs` (one function per request),
  `Spotify.Audio.cs` (fileId ladder incl. FLAC/FLAC-24, CDN, AES-CTR stream, the PlayPlay key SEAM only),
  `Spotify.Connect.cs` (dealer → `Decode` → a bounded `Connect.Inbox` Wave 3 drains), `Spotify.Telemetry.cs`
  (Gabo/Herodotus), `Spotify.Decode.cs` + `.Pathfinder.cs` + `.Connect.cs` (CORE: span decoders into `Staging`,
  a `ProtoReader` — never the generated message objects on the receive path; pathfinder over `Utf8JsonReader`).
- The Wave 1 gaps Wave 2 exposed were filled under `Entities/`: `Edges.Staging.cs` (the staged-edge buffer,
  `EdgeRun`, `CommitEdges`), `StagedId` (packed `EntityId` or arena text) on every staged row, `CommitHome`,
  `SectionCards`, `Recents` + `RecentsMembers`, the `TrackFields.Tags` commit arm, `Playlist.ShareUrl`, staged
  palette gradings in `Palette.cs`. `EdgeTable` keeps the server-STATED total in `_total` (0 = unstated) and derives
  the scroll extent on read — a page never completes off an unstated total (D7).
- `Platform/Platform.cs` + `Platform.Host.cs` first cut: settings door + `Platform.Keys`, credential slot (DPAPI),
  `Log` seam, `ZoomAutoPolicy.MigrateMode`, `Glyphs`, `RuntimePhase`. Owner S's Wave 6 share is listed at the bottom
  of the file; the file is at 1,012/1,100 already — S names `+Platform.Settings.cs` on day one.
- Engine: `StringTable.Intern(ReadOnlySpan<byte>)` landed in `..luent-gpu` (uncommitted there; 4 tests).
- Gates: Debug + Release 0 warnings; `Wavee.Tests` 777/777 (Wave 1 450 + Platform 39 + Spotify 79 + F 74 +
  decoder/staging 60 + …). NOT run: the login smoke (network, needs the `Diagnostics.Probe --login-smoke` arm from
  Wave 6 and a real console — Christos runs it).
- Open: `Decode.PutState` is a named stub waiting on Wave 3's `Playback.Snapshot`; `Browse.Commit` is unwired (nothing
  stages browse rows yet); `Scope.ReleaseText` does not walk the synthetic tables (owner A).
- FLAC: `docs/plans/wavee/wavee-0.3-flac-implementation.md` (research in progress) — full lossless support ships in
  0.3, implemented in parallel with Wave 3.

## Wave 3 — Playback + FLAC (done 2026-09-13, all gates green)

- `Playback/Playback.cs` (CORE 2,221 — the reducer: `State`, `Input`, `Effects` with one slot per kind, `Step`, the
  ownership fold with the fence (C5) and the 2026-09-11 Connect incidents pinned as tests, `Snapshot`,
  `SmtcTimelineCoalescer`, `TimeFormat`, `LiveRail`, `LiveEdgeState`), `Playback.Host.cs` (the `Post` loop, signals,
  `Pending`, the 1 s ticker, the Connect device roster), `Playback.Audio.cs` (the pump, five `AudioSource`s, gapless +
  crossfade, epoch cancel, decoder factory over the engine's `IAudioDecoder`: Vorbis, MP3, **FLAC**; AAC dropped —
  see the file), `Playback.Video.cs` (decode/host only), `Playback.Os.cs` (ch 14: SMTC, taskbar, jump list, media
  keys, power). `Decode.PutState` is real now (`Spotify.Decode.Connect.cs`, 737 lines — split candidate
  `Spotify.Encode.Connect.cs`).
- **FLAC** (`docs/plans/wavee/wavee-0.3-flac-implementation.md`): `Playback/Playback.Audio.Flac.cs` — a from-scratch
  CORE decoder (1,247 lines; zero-alloc after open, exact seek in ≤ 4 probes, CRC-8/16, MD5 oracle), 22 xiph vectors
  under `Wavee.Tests/Fixtures/flac/`, 30 tests bit-exact. Catalogue side: `TrackFields.Files`, `TrackFlags.Lossless /
  Lossless24`, `TrackTable.OriginalAudio` (+16 B/row, the footprint gate raised to 1.47 MB with the reason recorded),
  `Edges.TrackFormats`, the kind-5 planner bucket in `Fetch.cs`, `Decode.AudioFiles`. `Spotify.Audio.cs`: FLAC skips no
  0xa7 header, never asks the AP for a key (no account-wide latch), `Ctr.Validates` accepts `fLaC`. `FlacBox` removed.
  Still to land with their owners: the drawer's format ladder (M, 4.5), the Lossless rung in Settings (R, 6), local
  FLAC via `Modules` (T, 6), `--flac-probe` (S, 6). Decisions taken pending Christos: lossless is opt-in via the rung
  + the metered cap; the engine's linear resampler stays for 0.3; no exclusive-mode output.
- Gates: Debug + Release 0 warnings; `Wavee.Tests` **1,138/1,138**. NOT run: the login smoke and a 10 s playback smoke
  (network + device; Christos runs them — the app is still dark until Wave 4.5).
- Over budget, by §5's rule not yet a partial: `Playback.cs` +24 %, `Spotify.Decode.cs` 1,811/1,600 (+13 %),
  `Playback.Video.cs` +3 %.

## Still open from Wave 0

- The orchestrator-owned first cut of `Platform/Platform.cs` (settings store, credential slot, `Glyphs`,
  `ZoomAutoPolicy.MigrateMode`, the `WaveeLog` seam — §5 Wave 1 note). Wave 2 reads credentials through it.
- Engine: `StringTable.Intern(ReadOnlySpan<byte>)` (§3.4), made and gated in `..\fluent-gpu`. Wave 2's decoder needs it.
- G1 golden captures: skipped by decision; the kept 0.2.9 build at `C:\WAVEE\wavee-0.2.9\...\publish\Wavee.exe` remains
  the parity reference.
- The Base62 rewrite issue for 0.2.x is drafted, not filed.

## Rules (unchanged)

Subagents never build, test, or touch git; the orchestrator runs one Debug build, one Release build and one test run
per wave. Opus for intricate surfaces, Sonnet for mechanical ports and docs, Fable orchestrates and verifies. Work only
in the worktree — Christos works in `C:\wavee\waveemusic` at the same time. P1–P16 / C1–C10 bind every CORE file.
CLAUDE.md's out-of-scope paths are off limits.
