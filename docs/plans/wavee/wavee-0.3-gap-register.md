# Wavee 0.3 — gap register

The single source of truth for what stands between the worktree and a working end-to-end Wavee 0.3. Written by the gap
auditor from the 93 agent reports, the plans, and the code; every row was checked against the code before it was
listed. The orchestrator dispatches fix batches from §4 and resumes the auditor after each landing.

Baseline of this pass: worktree `C:\WAVEE\wavee-0.3` @ `201cd247` ("wip(0.3): wave 4 UI compiles; composition root,
loc keys, sidebar customizer, tray rules") plus the live edits of H-W (audio/session wiring), tray W-C and the engine
video branch. Stage B (I1–I3, J1–J2, K1–K3), tray W-B, the tray engine seams (`fluent-gpu` `eba1285b2`, Engine.Tests
434/434, Windows.Tests 289/289) and headless H-X are complete. **Build state:** the Wavee app assembly compiles clean in
Debug against the pinned engine `C:\WAVEE\fluent-gpu-pin` @ `eba1285b2` (the orchestrator fixed 23 stage-B errors);
`Wavee.Tests` still fails on 4 errors in files H-W and H-X are editing; no Release build and no test run since stage A
(3,543/3,543).

Conventions. **Category**: defect · wiring (missing wiring) · impl (missing implementation) · deferred (by plan to a
later wave) · data (data gap) · engine (engine gap) · parity (divergence from 0.2.9) · decision. **Severity**: BLOCKS
(blocks e2e) · DEGRADES · COSMETIC. **Owner** letters are plan §2's (A–T; X = headless implementer; U = tray; V =
video rules; O* = orchestrator). **Status**: open · in flight: `<agent>` · fixed-unverified · verified. Line numbers
are as of 2026-09-13 20:30 and drift where a file is in flight. Miner detail lives in the auditor's scratch notes
(`scratchpad\gaps\miner-{audio,core,video,sidebar,ui}.md`, `sweep-code.md`, `auditor-own-findings.md`).

---

## 1. Definition of end to end (plan §8 + G1–G6), with today's state

| # | Step | State today |
|---|---|---|
| E1 | Debug + Release build clean (`TreatWarningsAsErrors`), `Wavee.Tests` green | GREEN: round 1 at `021c1610` (Debug + Release, 4,636/4,636); B0 at `771fce00` (Debug, 4,636/4,636). Round 2 (B1b, B3, WP-4.5) is unbuilt |
| E2 | Launch → window paints the shell frame (chrome row, masthead, sidebar in all three designs, rail, player bar, overlays) | RENDERED (first launch, `--fake --profile <temp> --frames 120 --screenshot`): chrome row, collapsed sidebar rail with seeded covers, player bar, localized strings. Two findings: the chrome says "Sign in" under `--fake` (G-065), the sidebar opens collapsed at 1,770 px (G-066) |
| E3 | Sign in: stored 0.2.x credential resumes automatically; a fresh profile can sign in (PKCE/device code) and the profile chip shows name + avatar; Premium gated | Auto-login with a stored credential (B0) + the sign-in flows (B5: PKCE loopback + device code, the sign-in door, Premium gate, reject ladder) landed — unverified against keymaster/live; profile fetch in flight (B1b) |
| E4 | Welcome → market/scope switch, library sync (rootlist with folders, liked, albums, artists, shows, pins), recents | Welcome sets market/scope + `LastAccount`; rootlist/libraryV3 decoders emit folders with `FolderId`; the edge door exists (B1); the provider routes are in flight (B1b); the login-time sync host and every write are B2 (not started) |
| E5 | Every 0.2.9 page renders its loaded state: home, home-section, browse, search, album (+prerelease), playlist (+local), liked, library ×3, artist, discography, show/episode, recents, history, concerts ×3, queue (rail + stage), settings, whatsnew, diagnostics ×2, module | 2 of 30 route kinds have a page (history, sidebar-customize); 58 of 159 app files are 14–16-line skeletons (Entities 38, Screens 17, Modules 3) |
| E6 | Playback: Ogg 96/160/320 and FLAC from the head file; seek inside/outside the head; gapless and crossfade; normalization; next-track prefetch; disk cache; device-format recovery; play a context; shuffle/repeat/autoplay | **Rewritten 2026-09-17 — the pass-1 text below was stale in every clause.** Ogg plays; the deriver IS installed (G-014); a FLAC opens, parses and reports gain/peak. The remaining lossless break is a **CDN body refusal**, not a key: `audio.resolve why=refused` ×10 over 52 s, then 56 s of `audio.underrun`, then an unhandled decoder-seek exception, then a skip — 57 s of silence on an account that HAS the entitlement (G-277, fixed 2026-09-17: typed `Fault.Refused`, a 3 s first-body deadline, refused-vs-slow split in `StarvePolicy`, and the HTTP status finally on `audio.resolve`). Still owed: the live re-run that proves the ~1 s demotion, and why Spotify refuses an entitled file id. *(Stale pass-1 text, kept for the audit trail: "Single-track play path exists (unbuilt). Gapless hand-off makes every following track play twice; no play-a-context path; FLAC needs a key deriver nobody installs; normalization double-applied; starve ends the track")* |
| E7 | Connect both ways: device visible, transfer to Wavee resumes the context at position, transfer away, remote volume | Hello sent; `Decode.ConnectLoad` decodes play/transfer with TransferState (B1); the volume/logout topics are read (B5); the host halves (apply the load, fold the volume) are in flight (B3) |
| E8 | Video: switch on/off carries position, seek (planner), PiP, fullscreen, pop-out, override manager | **Rewritten 2026-09-17 — "the reducer still never drives the video host" is FALSE and was the single most misleading line in this file.** Video plays end to end on this box: `switch.begin → open.ok → license.ok → attach.ok → first.frame (sinceSwitchMs=2477, natural=568x320) → ABR to 1280x720 → clean detach`, zero faults in any log. The engine branches were combined long ago; the pin carries the video rework. What was actually wrong, and is fixed 2026-09-17: (a) the switch was a **cold rebuild every time** (`plan=Rebuild warm=false`) with the licence ~80 % of the 2477 ms — now a warm keeper pre-acquires the licence for the playing and next queued row, default ON with a user off-switch; (b) **every switch opened two protected sessions and leaked one** (prefetch raced the load it was fetched for) — now claim-before-work + adopt-in-flight, with three closed paths so none can strand; (c) `Video.Phase`/`FirstFrame`/`Buffered` had **zero consumers** and the surfaces used a blind `DepKey.Empty` timer that spent its 400 ms grace on the first join and flashed the ring on every later one — now event-driven (§6.2 K, finally built). Still owed: the §4.3 manual gate, and the audio hand-off (§3.1.4 `AudioCut`/`VideoGo`/`SetVolumeRamp` do not exist, so audio is silent across the switch). *(Stale text: "Engine rework DONE and green on `feat/0.3-video-runtime` (`e4a88e3ab`) but not combinable with the tray-seams pin without Christos (G-164); `Track.VideoGid` decoded (B1); the reducer still never drives the video host (B3 routes by kind; B7 after the merge)")* |
| E9 | Lyrics: rail, stage pane, NPV peek, sync, upgrade, inspector | `Lyrics.Boot` never called (no provider); `Lyrics.Store.ToUi` inline |
| E10 | OS surfaces: SMTC, taskbar overlay/thumb buttons, jump list, toasts, tray | `Os.Activate` + `Notify.HostInstall` now called from the frame effect (B8); `Notify.Rebuild` still has no producer (no toast can raise until a feed host exists); tray host landed (W-C) — W-D settings group, both on-box checklists and VerticalSlice owed |
| E11 | Settings (7 tabs), setup wizard, what's new, feedback, diagnostics + logs | All skeletons except `ReleaseNotes.cs` CORE and the runtime card |
| E12 | Headless smoke (`--headless --script ops/headless/login-smoke.wh`, ogg320, far-seek-disk, flac, gapless) | H-X landed: `Diagnostics.Headless.cs` (1,145), `Diagnostics.Probe.cs` (797), 102 tests, seven `.wh` scripts + `Invoke-WaveeHeadless.ps1`; never run; the scripts' track ids are unverified placeholders |
| E13 | `--fake` renders every route's loaded state (G2), ReuseGuard silent, zero alloc on scroll | `--fake` parsed, SEED-CORE seeded, silent endpoint reaches `Ended`, first launch renders the frame; Home content host empty (expected until WP-5); SEED-SURFACES owed |
| E14 | Perf tour vs 0.2.9; `local-update-e2e.ps1` both scenarios; `wavee-release.ps1 -DryRun`; MSIX both arches; signing; notices; CHANGELOG issue refs | Version 0.3.0-beta.1, bench script, `engine checkout` + PlayReady-export preflights (B10; helpers untestable and blind to the local props, G-205), `EngineRoot.local.props` pin (B0; slnx still wrong); no updater/install-on-quit (E2E cannot pass); junction blocked on D3 |
| E15 | `src/apps/_old` deleted, parity checklists (2,650 items) logged | Not started (Wave 6) |

---

## 2. The register

**Totals (pass 4, 2026-09-14): 238 rows — 37 BLOCKS · 154 DEGRADES · 47 COSMETIC.** By category: wiring 53 ·
defect 53 · parity 40 · deferred 30 · impl 24 · data 16 · decision 13 · engine 9. By status: **open 81 · in flight 4 ·
fixed (build/tests green, not all run) 141 · verified 12.** **Blockers still open (7):** G-265 (`Wavee.ReleaseTool` no longer
compiles — NEW) · G-164 (the two engine branches, D30) · G-144 (video headers, B7) · G-191 (Wave-5 pages — WP-5.M/N/O/P
in flight) · G-110 (local files/modules, WP-6.T) · G-014 (key deriver, D3) · G-018 (the headless smoke never run).
Blockers with a fixed-part status that still need a run: G-141/G-142 (video: the `SetVideoPlacement` caller and
`OnVideoDemoted` are B7's), G-190 (Wave 4.5 landed; rows blank until G-259). Pass-3 totals for the record: 193 rows —
38 · 125 · 30; open 103. Pass-2: 185 — 36 · 120 · 29; open 169.

### 2.1 Composition root, boot, build (files: `App.cs`, `Shell/Shell.Host.cs`, `Directory.Build.props`, `Wavee.slnx`, loc)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in (owner, wave) | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-001 | `Shell.InstallUi()` was never called; window painted an empty box — **pass 3: `Shell.InstallUi()` at App.cs:38; the frame has still not been rendered on a box** — **pass 3b: FIRST LAUNCH `--fake --profile <temp> --frames 120 --screenshot` renders the frame — chrome row, collapsed sidebar rail with seeded covers, player bar, localized strings (`scratchpad\fake-second.png`)** | `App.cs:34` now calls it; `Shell.UI.cs:49`; `Shell.Host.cs:606` | wiring | BLOCKS | App.cs (O*) | verified (launch) | Verify with `--frames 60 --screenshot`: frame renders |
| G-002 | No auto-login at launch: nothing calls `Spotify.Login()` except the chip's Sign in/Reconnect buttons; a stored 0.2.x credential is never used — **pass 3: still no launch-time `Spotify.Login()` — the only callers are the chip/offline-strip buttons (`Shell.UI.cs:867,1159`) and B5's `SignInDoor` (opens only with NO credential); B0** — **pass 3b: B0 (`771fce00`) — auto-login when a credential is stored, not under `--fake`** | `Spotify.Session.cs:256`; `Shell.UI.cs:838,1130`; `_old/Wavee/Program.cs:437-440` | parity | BLOCKS | App.cs / Shell.Host.cs first-render effect (O*) | fixed (build+tests green) | After the marshallers are attached: `if (Platform.HasStoredCredential()) Spotify.Login();`. Test: pure boot-plan fact + headless `login-smoke.wh` |
| G-003 | `Spotify.Api.Boot()` never called → `Fetch.Register(Transport)` never runs; the planner skips every demand until the first `Api.Run` (a playback/Connect call) — **pass 3: still no `Api.Boot()` caller in the GUI; B0** — **pass 3b: B0 — `Spotify.Api.Boot()` in the composition** | `Spotify.Api.cs:109-128`; `Fetch.cs:559` | wiring | BLOCKS | App.cs (O*) | fixed (build+tests green) | `Spotify.Api.Boot();` after `Spotify.Boot()`. Test: FetchTests fact "a demand planned after composition reaches the provider" |
| G-004 | `Sidebar.Boot()` never called: design/width/collapse/V3 state never restored, `sidebar-layout.json` never loaded, `s_loaded` false so no mutation commits — **pass 3: `Sidebar.Boot()` at App.cs:36, `Sidebar.Shutdown()` at :40** | `Sidebar.Host.cs:80,87-121,774` | wiring | BLOCKS | App.cs (O*) | fixed (build+tests green) | `Sidebar.Boot()` after `Entities.Boot`. Test: change design, restart → persisted |
| G-005 | `Sidebar.Activate(post)` never installed: `Sidebar.ToUi` inline → store `WriteCompleted` and source `Report`s write signals from pool/network threads (C1/C9) — **pass 3: `Sidebar.Activate(post)` at `Shell.Host.cs:273`** | `Sidebar.Host.cs:64,123-133,586-594`; `Shell.Host.cs:241-265` | wiring | BLOCKS | Shell.Host.cs `InstallMarshallers`/`AttachUiPoster` (I/O*) | fixed (build+tests green) | Add `Sidebar.Activate(post)` beside `Store.Post = post` |
| G-006 | `Sidebar.InstallActionSeams()` never called → pin/unpin descriptors permanently disabled | `App.cs:33` now calls it; `Sidebar.UI.Menus.cs:33-45` | wiring | DEGRADES | App.cs | verified (build) | Test: a FixedEntity pin binding resolves Available |
| G-007 | `Store.Use(library.db)` / `Store.Shutdown()` never called: graph is memory-only, no `KindShape` registered, no WAL checkpoint — **pass 3: still no `Store.Use(` caller; B0** — **pass 3b: B0 — `Store.Use(library.db)` outside `--fake`; `Store.Shutdown()` in the tail** | `Store.cs:633-660,771-773` | wiring | DEGRADES | App.cs + kind owners + exit tail (O*) | **fixed FOR REAL 2026-09-15** — the earlier "fixed" status covered ONLY the `Store.Use`/`Store.Shutdown` half; the kind-owner half (the thing that makes the file hold anything) was never done, so every launch stayed cold and `library.db` sat at 36 KB / 0 rows against 0.2.10's 30.9 MB / 5,620. Now landed: all eight `KindShape`s (Show/User/Episode/Track/Album/Artist/Concert/Playlist) registered from `App.RegisterShapes()` before `Entities.Boot`; `RowWriter.Id` for cross-reference columns; the five `LibraryEdge` appliers (Liked/SavedAlbums/FollowedArtists/SavedShows/Pins, the last cross-kind) + `SaveEdges` called from `WriteBehind`; `Store.MetaGet/MetaSet` warmed at boot. Rootlist stays network-only on purpose (`StringId` payload cannot survive the process). VERIFIED: the live db now has 13 tables incl. all eight catalog tables. NOT yet verified: real row counts after an interactive sign-in | `Store.Use(Path.Combine(Platform.LocalFolder,"library.db"))` before `Entities.Boot` (skip under `--fake`/headless per plan §2.8); `Store.Shutdown()` in the tail. StoreTests cover the round trip |
| G-008 | Lyrics stack never composed: `Lyrics.Boot(...)` has no caller (no provider → every read "unknown"); `Lyrics.Store.ToUi` not installed; the Spotify-native source needs a bearer absolute-URL GET `Spotify.Api` does not expose — **pass 3: unchanged (no `Lyrics.Boot` caller, no `Lyrics.Store.ToUi`); B0 + B1b (`GetText`)** — **pass 4: `Lyrics.Boot(Shell.ResolveLyricsRequest, Api.GetTextAsync, SpclientBaseUrl)` at App.cs:60-61 (not under fake); `Lyrics.Store.ToUi` at `Shell.Host.cs:294`; `Api.GetText/GetTextAsync` gated by `IsSpotifyUrl`; `LyricsRequestTests` (8). Remains: the resolver polls (G-252); no fake provider (G-238); runtime unverified** | `Lyrics.Host.cs:1638,1772-1788`; `Spotify.Api.cs:1207-1211` | wiring | DEGRADES | App.cs (O*), Spotify.Api.cs (F: `Api.GetText(url, ct)`), Shell.Host.cs (ToUi) | fixed (build+tests green) | Add `Api.GetText`; compose `Lyrics.Boot(resolve, (u,ct)=>Task.Run(()=>Api.GetText(u,ct)), Spotify.SpclientBaseUrl)`; install `Lyrics.Store.ToUi = post`. LyricsHostTests fact over a fake GET |
| G-009 | `Notify.HostInstall(post, deepLink, icon)` never called ("from Shell.Run, after the window exists"); Run only calls `HostShutdown` — **pass 3: `Notify.HostInstall(post, ApplyDeepLink)` at `Shell.UI.cs:308` (B8)** | `Notify.Host.cs:47-52`; `Shell.Host.cs:124-207` | wiring | DEGRADES | Shell.Host.cs `AttachUiPoster` (I/O*) | fixed (build+tests green) | Call with `ApplyDeepLink`; test a toast activation reaches the deep-link intake via the NotifyTests seam |
| G-010 | `Playback.Os.Activate(hwnd, state)` has no explicit caller; `Publish` self-activates with `FluentApp.WindowHandle` on the first reducer effect — SMTC/taskbar/jump list/keep-awake arm only if that happens after the window exists — **pass 3: `Playback.Os.Activate(FluentApp.WindowHandle, Playback.Snap())` in the mount-once frame effect, `Shell.UI.cs:313` (B8)** | `Playback.Os.cs:73-90,116-120`; `Shell.Host.cs:589-591` | wiring | DEGRADES | Shell.UI.cs first-render effect (I) | fixed (build+tests green) | If the first publish precedes the HWND, call `Playback.Os.Activate(FluentApp.WindowHandle, Playback.Snap())` once on first render |
| G-011 | `Playback.FrameNowMs` left at `TickCount64` | `App.cs:30` now binds `Design.FrameTime.NowMs` | wiring | COSMETIC | App.cs | verified (build) | — |
| G-012 | Exit tail incomplete: `Spotify.Telemetry.Shutdown` (last gabo batch lost), `Playback.Audio.Shutdown` (WASAPI), `Playback.Video.Shutdown` (pump/CDM), `Store.Shutdown`, `DiskCache.Shared.Dispose`, `Fetcher.Shared.Dispose`, `Sidebar.Flush`+`Store.FlushNow` never called — **pass 3: `Tray.Host.Shutdown()` and `Sidebar.Shutdown()` now in the tail; `Telemetry/Audio/Video/Store.Shutdown` and the cache/fetcher disposals still have no caller; B0** — **pass 3b: B0 — tail: `Sidebar.Shutdown`, `Telemetry.Shutdown`, `Audio.Shutdown`, `Video.Shutdown`, `Store.Shutdown`, DiskCache/Fetcher dispose, `Platform.Shutdown`** | `Shell.Host.cs:196-206`; `Spotify.Telemetry.cs:240-250`; `Playback.Audio.cs:345-355`; `Playback.Video.cs:326`; `Sidebar.Host.cs:783-787,1243` | wiring | DEGRADES | Shell.Host.cs finally / App.cs after Run (O*) | fixed (build+tests green) | Order: sidebar flush → telemetry flush → audio → video → store → cache/fetcher dispose → platform. Check: exit log tail shows the drains |
| G-013 | UI marshallers are installed inside `Shell.Run`, after `Entities.Boot`/`Spotify.Boot`/`Playback.Boot`; once `Store.Use` lands, `Store.Warm` answers commit inline on the store thread — **pass 3: unchanged; B0** — **pass 3b: B0 — `InstallMarshallers` public, first in the GUI boot** | `App.cs:28-35`; `Shell.Host.cs:130,241-253`; `Store.cs:908-927` | defect | DEGRADES | App.cs + Shell.Host.cs (O*) | fixed (build+tests green) | Make `Shell.InstallMarshallers()` public and call it first in `Main` (its queue-until-root poster handles pre-window posts). ShellHostTests fact: a post before attach is delivered in order |
| G-014 | `Spotify.Audio.KeyDeriver` never assigned by any public file (0.2.9 bound it under `#if WAVEE_PLAYPLAY_LOCAL`): FLAC and every AP-refused Ogg answer `NoDeriver`/`NoKey` unless the private sources self-install — **pass 3: unchanged; D3** — **pass 5 (2026-09-17): THIS ROW WAS STALE AND COST A SESSION. D3 landed in `02f22cce` itself: `App.cs:74-76` installs it under `#if WAVEE_PLAYPLAY_LOCAL`, and the symbol IS defined in this worktree because `Wavee.csproj:161-171` gates on `Exists('..\Wavee.PlayPlay\Client\PlayPlayHost.cs')` and the junction is linked. PROVEN AT RUNTIME: a FLAC opened with gain/peak read and `CanDerive` true, and `NoDeriver`/`NoKey` appear ZERO times in any log. The real lossless failure is a CDN body refusal — see G-277** | `App.cs:74-76`; `Wavee.csproj:161-171`; `Spotify.Audio.cs:145,149`; `_old/Wavee/SpotifyLive/Audio/AudioPlaybackStack.cs:30-67` (NOT `Program.cs:333` — that is a CLI probe block, a bad citation carried since pass 1) | decision / wiring | BLOCKS (lossless; refused keys) | App.cs guarded line (O*) + private repo | **verified (live, 2026-09-17)** | `#if WAVEE_PLAYPLAY_LOCAL` install before `Playback.Boot()`; public build keeps `NoDeriver` + 320 fallback (G-101). Check: `audio.head … key=` log on a deriver build; `CanDerive == false` on public |
| G-015 | `--fake` is not parsed; `Platform.Scope` has no fake arm; `Entities.Fake.cs` is empty (SEED-CORE was due in Wave 4 as an orchestrator cut); bundled covers/fixtures have no consumer → CLAUDE.md's offline demo, the Wave-4/5 gates and the customizer miniature all lack their seed — **pass 3: `--fake` parsed (`Platform.Args.Fake`, `Clock.SeedEpoch`), `Entities.SeedFake` (461 lines, `EntitiesFakeTests`), silent endpoint under `--fake` (App.cs:30,34). Still owed: ch 31 SEED-SURFACES (home/search/browse/concerts/recents/lyrics/video fixtures, the W13 `Sample` vocabulary beyond playlist/artist, clock-anchored surfaces); the sign-in door must stay closed under `--fake` (B5: a fresh profile's scope is also "fake" — verify at the first `--fake` launch); pins are seeded playlists-only (no kind bit)** — **pass 4c: `SeedQueue` added (`Entities.Fake.cs:399-429`); WP-5.M/N/O/P each own a `Seed{Album,Artist,Library,Home}Surfaces` hook. Still owed: the gate's holes (G-273: no AlbumArtists/TrackArtists edges, no `AlbumFields.Release/Publishing/Availability/TopTrack`, no `TrackFields.Tags/Video`, no gradings), lyrics (G-238), pins playlists-only** | `Platform.cs:1006`; `Entities/Entities.Fake.cs:1-14`; `Shell.Host.cs:209-224`; `Sidebar.Customizer.UI.cs:73-79` | deferred | DEGRADES (blocks every offline verification gate) | `Screens/Diagnostics.Probe.cs` or Platform.cs (`--fake` arm, O* first cut), `Entities/Entities.Fake.cs` (Q), `Shell/Sidebar.Customizer.UI.cs` samples | fixed (SEED-CORE + queue) / open (SEED-SURFACES) | Parse `--fake` in the probe arm table → `Platform.Scope = Fake`, `Clock.SeedEpoch` pinned, `Entities.SeedFake()` per ch 31 (rootlist with folders, four collection sets, pins, 166 liked, 16 covers). Tests: `EntitiesFakeTests` (determinism) |
| G-016 | Headless arm: `Diagnostics.Headless.cs` (1,145/700 — over 30 %, needs the named partial `Headless.Json.cs`), `Diagnostics.Probe.cs` (797/800 — owner S's Wave-6 arms need a partial), `HeadlessTests.cs` (102 facts, 1 compile error open), seven `.wh` scripts + wrapper; `Platform.ProfileRoot` declared (+ tests); plan §5 and handoff-d lines rewritten. Open: the scripts' track ids are Web-API doc examples (the gapless album id least certain); `parse-only.wh` has no plan section; headless plan still "DRAFT", §8 answers recorded only in a code comment — **pass 3: unchanged — the smoke has not been run; the placeholder ids and the CORE split are still owed** — **pass 4c: `Diagnostics.Headless.Json.cs` (244) split out; `parse-only.wh` now has plan §4.1a. Remains: `Diagnostics.Headless.cs` 925/700 (G-202), no smoke run, placeholder ids, Q8** | `App.cs:21-27`; H-X report; `ls ops/headless` | impl | DEGRADES (was BLOCKS) | H-X (done) → O* (split, run) | fixed (part) | Run `parse-only.wh` offline, then the five smoke scripts on the orchestrator's profile; replace the placeholder ids; split the CORE file; record §8 answers in the plan like the tray plan's §13 |
| G-017 | Localization tables never loaded (`Localization.LoadFolder`/`SetCulture` "left for S"): English resolves through the generator's neutral table, ko-KR/nl and `localization.culture` never apply — **pass 3: `LoadFolder` still only in the Platform.cs comment** — **pass 3b: B0 — `HostOpenLocale` loads `assets/loc` and selects the culture** — **pass 4c: `Platform.Host.cs:96-99` (`LoadFolder` + `TrySetCulture`); localized strings rendered** | `Platform.cs:1007-1008` (LEFT FOR S); csproj:194 comment; `_old/Wavee/App/AppLocale.cs:19-29` | deferred | DEGRADES | Platform.Host.cs `HostOpenLocale` (S; first cut can land now) | verified (launch) | Load `assets/loc` + `TrySetCulture(Locale.UiCulture)` in `Platform.Boot`. Smoke: a ko-KR label resolves |
| G-018 | Wave 2 and Wave 3 real-traffic gates (login smoke decoding a live `getAlbum`; 10 s through the real pump) were never run — every wiring gap in §2.2–2.5 is invisible to the unit suite — **pass 3: still not run (the seven scripts exist)** | `handoff-20260912d:63-64,89-90` | deferred | BLOCKS (verification) | ops/headless + Probe (H-X, O*) | open | Run `login-smoke.wh`, `ogg320.wh`, `far-seek-disk.wh`, `flac.wh`, `gapless-album.wh` on the orchestrator's profile once G-016 lands |
| G-019 | Build/test verification: the app assembly now compiles in Debug (23 stage-B errors fixed by the orchestrator); `Wavee.Tests` fails on 4 errors in H-W/H-X files (AudioStreamTests 436/437, PlaybackAudioTests 493 xUnit1031; HeadlessTests 96 CS0649 reported fixed, unbuilt); no Release build; no test run since stage A — so none of the ≈ 30k lines written since (stage B, audio swap, FLAC SIMD, headless, tray, App.cs) has run a single fact — **pass 3: Debug + Release clean against the pin `eba1285b2`; `Wavee.Tests` 4,636/4,636 at `021c1610`** | orchestrator's build report; `git status` | defect | BLOCKS | H-W, H-X, then O* | verified | After H-W lands: Debug + Release (`-p:EngineRoot=C:\WAVEE\fluent-gpu-pin\`), `dotnet test` (baseline 3,543 + ≈ 700 new); hand failures to owners by file |
| G-020 | 40 pending stage-B/tray loc keys were unmerged (11 referenced: `Strings.Lyrics.*` → CS0117; `runtime.sig.title`, `sidebar.customizer.tooNew/tooNewSub` literal) — **pass 3: build green with the generated members** | orchestrator merged all 40 into `en-US.json` (commit `201cd247`) | wiring | BLOCKS (compile) | `assets/loc/en-US.json` (O*) | verified | Release build confirms the generated `Strings.Lyrics.*`/`Strings.Tray.*` members; ko-KR/nl keep falling back (G-017) |
| G-021 | `Modules.Boot()` is `{ }`; `Shell.LinkModules`, `Shell.MatchLink`, `Playback.Audio.ModuleOpen/LocalPath` unassigned; `module:` route has no page; csproj still stages YouTube/Twitch/Radio with nothing to host them | `Modules.Host.cs:15`; `Shell.Overlays.UI.cs:63,71`; `Playback.Audio.cs:2354,2358` | deferred | DEGRADES (modules, local files) | `Platform/Modules*.cs` (T, W6) | open | Port the 0.2.9 module host/router/catalog + `_old/Wavee.Tests/Modules/*` (15 files) |
| G-022 | Default `EngineRoot` = `..\fluent-gpu` = `C:\WAVEE\fluent-gpu` = (case-insensitive) `C:\wavee\fluent-gpu`, the unfinished video-rework branch (`feat/0.3-video-runtime`, 2 wip commits over `869b1c65e`); `Wavee.slnx` hardcodes the same path; builds pass only because the orchestrator passes `-p:EngineRoot=C:\WAVEE\fluent-gpu-pin\` (a second worktree, now @ `eba1285b2` = the tray seams); `wavee-release.ps1` and a plain `dotnet build Wavee.slnx` would build the wrong engine — **pass 3: B10 added the release script's `engine checkout` hard check (resolves like the props, records the commit); the props/slnx pin itself is B0 (in progress)** — **pass 3b: B0 — git-ignored `EngineRoot.local.props` imported by `Directory.Build.props`; a plain build resolves `C:\WAVEE\fluent-gpu-pin`. Still open: `Wavee.slnx` lists `../fluent-gpu` engine projects by path (IDE/`dotnet build Wavee.slnx` load the wrong engine), and the release script's `engine checkout` check resolves `EngineRoot` without reading the local props (B10b)** — **pass 4: `Wavee.slnx` no longer lists engine projects (they come through `$(EngineRoot)`); `Resolve-EngineRoot` in `Wavee.Build.psm1:417-432` reads `EngineRoot.local.props`; release script uses it (:555). Remains: G-237 (notices generator, store submit, perf tour, publish/pack callers still default to `..\fluent-gpu`)** | `Directory.Build.props:36`; `Wavee.slnx:5-9`; `git branch` in each checkout | decision | BLOCKS (reproducibility) | `Directory.Build.props`, `Wavee.slnx`, release script (O*) | fixed (props, slnx, release script) / open (four scripts) | Worktree-local props override to the pin + slnx via the property; release preflight asserts the resolved engine commit |
| G-023 | `Spotify.Telemetry.Boot()` never called (lazy on first event) and `Shutdown` never called (see G-012); play registration itself unwired (G-076) — **pass 3b: `Telemetry.Boot()` on Online goes with B3's registration hooks** — **pass 4: telemetry boots lazily on the first registration event (`Spotify.Telemetry.cs:229`) and G-076 now produces them; `Shutdown` in the tail (App.cs:70). Remains: no explicit `Telemetry.Boot()` on Online; the resume-point ticker starts only after the first gabo event** | `Spotify.Telemetry.cs:209,242` | wiring | DEGRADES | App.cs (O*) | fixed (part) | `Telemetry.Boot()` on Online (with G-076) |
| G-024 | `Playback.FrameNowMs` now reads `Design.FrameTime.NowMs`, the UI frame clock: it FREEZES while the window is idle, minimized or tray-hidden and is last-writer across hosts (the pop-out window's own `RunFrame` can step it back), yet the audio thread's position throttle, the 1 s ticker, the video watchdog, SMTC rebuild throttles and Connect snapshots all stamp with it off the UI thread — **pass 3: App.cs:32 still binds `Design.FrameTime.NowMs`; B0 per D22** — **pass 3b: B0 — Stopwatch-ms `FrameNowMs` (D22)** | `App.cs:30`; `Design.cs:1500-1506`; engine `Hooks/Context.cs:69-73`; readers in `Playback.Audio.cs` (position throttle), `Playback.Host.cs`, `Playback.Video.cs`, `Playback.Os.cs:593-602` | defect | DEGRADES (background playback position/timeline; tray-hidden playback) | App.cs (O*) + render sites (K/I) | fixed (build+tests green) | Monotonic Stopwatch ms clock for `FrameNowMs`; render sites that want vsync-lattice time convert `FrameTime.NowQpc` explicitly. Test: minimize 30 s while playing → SMTC timeline advances |
| G-025 | `Entities.Now` is never written in the GUI (only the headless tick sets it) → `Fetch`'s backoff `ReadyAt` never expires and `Palette`'s TTL never ages — **pass 3: `Entities.Now = Store.ToApp(...)` on `FrameCompleted`, `Shell.Host.cs:284` (W-C)** | `Fetch.cs:385,551,682`; `Palette.cs:119,400`; grep `Entities.Now =` in the app → none | wiring | DEGRADES (a 429/5xx backoff never re-sends) | Shell.Host.cs `FrameCompleted` (the orchestrator asked the tray W-C agent to add it) | fixed (build+tests green) | `Entities.Now = unixSeconds` on the frame tick + the idle timer of G-026; FetchTests fact: an expired backoff re-plans |
| G-026 | `Fetch.Pump()`/`Palette.Tick()`/`Entities.Now` ride `FluentApp.FrameCompleted`, which fires only on RENDERED frames → an expired backoff or grading debounce waits while the window is idle, minimized or tray-hidden (exactly when background playback needs the next fetch) — **pass 3: unchanged; B0 per D23** — **pass 3b: B0 — `Fetch.NextWakeAt` + one idle wake timer in `Shell.Host` (D23)** | `Shell.Host.cs:255-264`; `Fetch.cs:545-547` | decision / defect | DEGRADES | Shell.Host.cs (I/O*) | fixed (build+tests green) | One named one-shot timer armed to the earliest `ReadyAt` that posts `Fetch.Pump()` through `ToUi`; test by forcing a 429 with the window idle |
| G-027 | The action platform is never composed: `Actions.Registry.Build` has no caller (palette shows only its 19 builtins; bound sidebar actions resolve nothing), `AppActions.Register` has no caller (every `Actions.Menu.Row(id)` returns null → context menus have NO verb rows), and the `ActionServices` verbs other than confirm/clipboard/post/pins are null (`Go`, `CurrentRoute`, `CurrentDestination`, `Play`, `PlayNext`, `AddToQueue`, `StartRadio`, `IsSaved`, `SetSaved`) → bound actions render HostUnavailable, the player-bar play-next drop silently returns — **pass 3: B8 wired `Registry.Build` + `Services.Go/CurrentRoute/CurrentDestination` (`Shell.UI.cs:79`); `AppActions.Register` still has NO caller (menus have no verb rows) and `Play/PlayNext/AddToQueue/StartRadio` (Q), `IsSaved/SetSaved` (O) are null — WP-4.5/WP-5** — **pass 4: `Queue.UI.cs:65-69` sets `Play/PlayNext/AddToQueue/StartRadio` + `Shell.OnPlayContext`; `AppActions.Register` now has 20 callers (`Queue.UI.cs:1009-1059`, `Track.InstallActions` App.cs:55). Remains: `IsSaved`/`SetSaved` never assigned (O, WP-5.O). No longer BLOCKS** — **pass 4c: `AppActions.Register` callers in `Queue.UI.cs` (7) and `Track.Menu.cs` (13); the O half (`IsSaved/SetSaved`, `Controls.Library` — G-269) is WP-5.O's; `Queue.UI.cs:56-59` comment stale (G-250)** | `Actions.cs:239-257,775-781,1033`; `Shell.Palette.cs:307,354`; `Sidebar.UI.cs:524`; `Shell.Overlays.UI.cs:94-104`; `Shell.PlayerBar.UI.cs:645` | wiring | DEGRADES (was BLOCKS; pass 4) | Shell.UI.cs `InstallUi` (I: Build + Go/CurrentRoute/CurrentDestination), Queue owner Q (Play/PlayNext/AddToQueue/StartRadio), User.cs O (IsSaved/SetSaved), each entity owner (`AppActions.Register` of its verbs) | fixed (part) — O half open | Build once at install; ActionsTests: `Current` holds the 13 built-ins after Build; `Actions.Resolve` becomes Available once the seams are set |
| G-028 | A rejected stored login still wipes the credential: `AuthRejected` → `HasCredential = false`, `ClearCredential + CloseAll` effects — one transient rejection on a live profile loses the sign-in (0.2.9's probes used `clearStoredOnReject:false`); `Disconnect` protects only the exit path — **pass 3: B5 `OnApReject` ladder — one retry, then definitive; new `SessionFault.LoginRefused` keeps the credential** | `Spotify.cs:241-246`; `Spotify.Session.cs:219` | decision / defect | DEGRADES | Spotify.cs (D; after H-W) | fixed (tests green) | Clear only on a definitive bad-credentials reject after one retry against a fresh AP; a transient/unknown reject keeps it; pure `Spotify.Step` test |
| G-029 | Headless-found tidy-ups: the headless device shows `Environment.MachineName` (needs owner G's name seam); a headless run beside the GUI rewrites the whole shared `store.json` per process; without `--connect` local play still sends the reducer's Connect state updates; stdin/pipe reads can stall the loop | H-X report | defect | COSMETIC | Playback.Host.cs (G), Platform.Host.cs (S), Diagnostics.Probe.cs (X) | open | Name seam on the device identity; per-process store section or read-modify-write with a lock |

### 2.2 Sign-in, session, account

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-030 | A fresh profile cannot sign in: `Shell.Auth` is never written (`FoldAuth` has no caller), nothing routes to Setup, `Setup.UI.cs` is a skeleton, and there is no PKCE/device-code/QR flow (`Spotify.Pkce` unused) — only an already-stored blob logs in — **pass 3: B5 landed `Spotify.OAuth.cs` (PKCE loopback + device code, token in memory only), `Spotify.SignIn`, `Setup.UI.cs` (392: sign-in facet, QR plate), `Setup.SignInDoor()` mounted in the overlays; `Shell.Auth = FoldAuth(...)` at `Shell.UI.cs:296`. UNVERIFIED on a fresh profile: whether keymaster accepts a loopback redirect on an OS-assigned port (0.2.9 flagged it too); the pairing code is the confirmed path** | `Shell.cs:1355-1366`; `Shell.Host.cs:86`; `Screens/Setup.UI.cs` (14 lines); `Spotify.cs:800-839`; `Spotify.Session.cs:68-93`; `_old/Wavee/WaveeApp.cs:343-368`, `_old/.../LiveSessionHost.cs:160-164,1110-1117` | impl | BLOCKS (fresh machine) | `Spotify.cs` + `Spotify.Session.cs` (D: pure steps + runner), `Screens/Setup.*` (R, W6), `Shell.UI.cs` gating effect (I) | fixed-unverified | Port both 0.2.9 flows ending in `Platform.SaveCredential(OAuthToken)` → `Spotify.Login()`; a UI-thread effect writes `Shell.Auth = FoldAuth(...)` and the frame mounts the sign-in surface while `SignInRequired`. Facts: device-code poll fold, PKCE loopback parse, `SetupGating` |
| G-031 | `session.lastAccount` never written → `ResolveScope`'s fallback never fires; account change undetectable; the 0.2.9 local-library reset on account change is missing — **pass 3: written on welcome, on change only (`AccountMemoryTests`); the register's "0.2.9 library reset" claim was wrong — 0.2.9 never wrote the key** | `Platform.cs:245,954`; `Spotify.Session.cs:229-247` (`AdoptWelcome`) | wiring | DEGRADES | Spotify.Session.cs `AdoptWelcome` (D; after H-W) | fixed (tests green) | `Settings.Set(Keys.LastAccount, account)` on change; SpotifySessionTests fact over a memory settings store |
| G-032 | Free/unknown-tier accounts are not gated: `SessionFault.NotPremium` never produced; PutState announces `license=premium` regardless — **pass 3: Free → `NotPremium` + clear; Unknown tier refused but credential kept (B5's reading of D12 + D24)** | `Spotify.cs:81,221-230`; `Spotify.Decode.Connect.cs:628-631`; `_old/.../LiveSessionHost.cs:177-188` | parity | DEGRADES | Spotify.cs (D) | fixed (tests green) | `Step(Welcome)` with `Tier != Premium` → `Failed/NotPremium` + clear/close; SpotifyCoreTests fact |
| G-033 | The account's own profile (name, avatar) is never fetched: `CatalogKindOf(User)` Unknown, no kind-15 decode arm, `Api.Profile` uncalled, `hm://identity/user-profile-changed` unhandled; the chip reads `User.Me` Identity — **pass 3: decode half landed (`Decode.Profile`, kind-15 arm, B1); the Api fetch + `Ensure(User.Me, Identity)` on welcome are B1b's** — **pass 4: kind 15 in the batch (`Spotify.Api.cs:877`), REST fallback (:884-895), `market=from_token`; `Ensure(User(me), Identity)` on every sync (`Spotify.Library.cs:193`). Remains: `hm://identity/user-profile-changed` not handled** | `Spotify.Api.cs:392-401,555-561,1264-1268`; `Spotify.Decode.cs:893-919`; `Shell.Overlays.UI.cs:427-432` | data | DEGRADES | Spotify.Api.cs (F), Spotify.Decode.cs (E), Spotify.Connect.cs (topic) | fixed (part) | Map User→`UserProfile(15)`, `Decode.UserProfile` → `StagedUser`, REST fallback, `Ensure(User.Me, Identity)` on welcome, coalesced push. DecodeTests fixture |
| G-034 | `Audio.ResetKeyLatch()` never called ("Session calls it on a fresh login"): an AP refusal latch and the key cache survive logout/account switch — **pass 3: in the welcome effect (B5)** | `Spotify.Audio.cs:460-465` | wiring | DEGRADES | Spotify.Session.cs welcome effect (D) | fixed (tests green) | Call inside the Welcome effect; SpotifyAudioTests latch fact |
| G-035 | Client token never refreshed: minted once per AP attempt, expiry stored but unchecked, the 401 retry re-mints login5 only — **pass 3: `TokenDue`, 120 s lead, serialised on one gate, 60 s backoff (B5)** | `Spotify.Session.cs:811-868,1027-1051`; `Spotify.cs:239-244` | defect | DEGRADES | Spotify.Session.cs (D) | fixed (tests green) | `ClientToken()` re-mints under `TokenGate` when `Now >= expiry − 120 s`; fact behind an injectable clock |
| G-036 | Logout/drop/epoch bump never reaches Playback or Connect: no `Playback.Release(Logout)` caller, `Connect.Clear()` never called, `DrainConnect` ignores `Item.Epoch` → a dead session's clusters are folded, no BecameInactive — **pass 3: session half done — closing an epoch empties the Connect mailbox, sign-out posts `Input.Release(Logout)` when a host exists. Still open: the BecameInactive announcement on logout cannot go out because the connection id is cleared in the same step (B5 report)** — **pass 4: closed-session items/intakes dropped (`Playback.Host.cs:419`, `Playback.Host.Remote.cs:113`); sign-out resets the deck (`Playback.cs:1990-2017`). Still open: `SignOutTeardown` posts `Release(Logout)` (`Spotify.Session.cs:266`) AFTER `CloseAll` → `Connect.Clear()` (:250), so `Flush` returns on an empty connection id (`Spotify.Connect.cs:328-329`) and the BecameInactive PUT never leaves — reorder the two effects** | `Playback.cs:87`; `Spotify.Connect.cs:123-128`; `Playback.Host.cs:334-349` | wiring | DEGRADES | Playback.Host.cs (G), Spotify.Session.cs (D) | fixed (tests green) / open (BecameInactive) | On Logout/Dropped post `Input.Release(Logout)` + `Connect.Clear()`; drop `item.Epoch != Current.Epoch`. PlaybackStepTests fact |
| G-037 | Sign-out teardown never called: `Playback.Os.SignedOut` (jump list) and `Notify.SignedOut` (unschedule drops/daylist, image cache) have no caller (ch 14 DATA GAP 8, decided fix) — **pass 3: new `SessionEffects.SignedOut` calls both, only when a window exists (B5)** | `Playback.Os.cs:105-109`; `Notify.Host.cs:89-95`; `Shell.Overlays.UI.cs:602`; `Spotify.cs:275` | wiring | DEGRADES | Shell.Host.cs logout handler or Spotify.cs Logout effect | fixed (tests green) | Call both on `SessionEventKind.Logout`; manual parity 43 |
| G-038 | Any TRACK_V4/EPISODE_V4 failure (transport 0, 401-after-retry, 5xx, empty) opens as `Fault.Restricted` — a terminal "unavailable" instead of a retryable network fault — **pass 4: pure `MetadataFault(http, entity, hasPayload)` (`Spotify.Audio.cs:420-426`): 0/401/408/429/5xx → Network; used by TRACK_V4 and EPISODE_V4** | `Spotify.Audio.cs:344-364,673-685` | defect | DEGRADES | Spotify.Audio.cs (F) | fixed (build+tests green) | Return the status; `0/429/≥500` → `Fault.Network`, 200-empty/404 → Restricted. SpotifyAudioTests status-map facts |
| G-039 | Headless `Logout` semantics: today closes sockets AND clears the credential; headless plan wants a `Disconnect` that closes without clearing; the store wrapper is the only protection — **pass 3: `Disconnect` exists and the probe uses it** | `Spotify.cs:275-279`; headless plan §8 Q3 | decision | COSMETIC | Spotify.cs (D) | verified (build) | Add `SessionEventKind.Disconnect` |

### 2.3 Data: fetch planner ↔ `Spotify.Api` ↔ decode (the path every page needs)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-040 | The fetch provider ignores `batch.Wanted`: POSTs one extension kind per uri whatever the page asked; `TrackFields.PlayCount/Audio/Tags/Video/Publishing`, `ArtistFields.Overview/Chart`, `AlbumFields.TopTrack`, `PlaylistFields.Saves` never arrive; and because `Table.Applied` clears `Inflight` when any group lands, the next `Ensure(Row)` re-POSTs the same V4 (the sidebar's per-rebuild `Fill` → a request loop) — **pass 3: `Table.Asked` per scope; planner buckets by what each row still needs; a terminal failure un-asks only the failed groups (`FetchTests`); the provider half is B1b** — **pass 4: provider half — `Serves`/`RoutesFor` skip unservable routes, `MetadataKinds` + `AnswerRows` send one mixed-kind POST (`Spotify.Api.cs:644-693,839-860`), `FetchOutcome` + stale-epoch guard** | `Spotify.Api.cs:577-597`; `Entities.cs:1295-1304`; `Spotify.Decode.cs:461`; `Sidebar.Host.cs:2108,2129`; `Rail.UI.cs:455` | impl | BLOCKS | Spotify.Api.cs (F) + Fetch.cs (C) | fixed (build+tests green) | Pure `Api.RoutesFor(EntityKind, wanted)` → extension kinds sent as ONE mixed-kind POST per uri; non-XM groups (overview → pathfinder, chart, popcount) dispatched per subject; groups no route serves are sealed. FetchTests: "a partial answer does not re-ask" |
| G-041 | `wavee:home`, `wavee:search:*`, `wavee:browse` map to `EntityProvider.None` → the planner abandons them; `Api.Home`/`Api.Search` folds have no caller; `HomeSection`/`Browse*` have no decoder — **pass 3: `Fetch.SubjectOf`/`ProviderFor` route synthetic subjects to the scope's catalogue, bucket key includes the subject, `Entities.BrowseSection` (B1); the pathfinder provider is B1b** — **pass 4: Home/HomeSection/BrowseAll/Page/Section/Search routed through `Decode.Export` (`Spotify.Api.cs:923-950`)** | `Entities.cs:741-751`; `Fetch.cs:469-475`; `Home.cs:406-408,519-523`; `Search.cs:148,363-367`; `Spotify.Api.cs:848-853,1074-1082` | impl | BLOCKS | Entities.cs routing (A), Spotify.Api.cs pathfinder provider (F), Spotify.Decode.Pathfinder.cs (E) | fixed (build+tests green) / open (facets, paging → G-232) | Route the synthetic prefixes to a pathfinder provider that runs Home/HomeSection/Search/Browse* and answers through `Fetch.Answer`; PathfinderDecodeTests already hold `home.json` |
| G-042 | Relations are never fetched and there is no edge-fetch door: `Api.Playlist/PlaylistDiff/Rootlist/RootlistChanges/Recents/RecentsDiff/CollectionPage/CollectionDelta/Album/ArtistOverview/Discography` have no caller; `Decode.PlaylistV2/PlaylistRevision/CollectionPage/Recents/LibraryV3` have no production caller (`LibraryV3` reachable only from the offline `Export` path) → `Edges.Rootlist/Liked/SavedAlbums/FollowedArtists/SavedShows/Pins/ArtistPopular/FeedSection` never fill; no library sync on login; every library/liked/sidebar surface skeletons forever — **pass 3: `Entities.EnsureEdge/RefreshEdge` → `Fetch.PlanEdge` (`Fetch.Edges.cs`, `EdgeDoorTests`), `FetchRoutes.ForEdge/LibrarySetOf` (`Fetch.Routes.cs`); provider routes are B1b; the login-time library sync host is B2. Until B1b lands the provider answers null for subject/edge batches (surfaces show vacant, not skeleton)** — **pass 4: edge walk `AnswerEdge` (`Spotify.Api.Library.cs:194-305`, revision-gated list reads, collection walk lands Liked + SavedAlbums), sync host `Spotify.Library.cs:167-257` (`WatchSession`/`SyncNow`/`AfterPublish`), dealer hook `Spotify.Connect.cs:240` → `OnDealerPush`; `Spotify.Library.Install()` at App.cs:52. Runtime unverified** | `Spotify.Api.cs:1345-1398,1508-1524`; `Spotify.Decode.cs:1349,1503,1728`; `Spotify.Decode.Pathfinder.cs:994-1071`; `User.cs:319` (`ReplaceRootlist` no caller); `Sidebar.Host.cs:2085,2120,2243,2996` | impl | BLOCKS | Fetch.cs + Edges.cs (C/B: `Entities.EnsureEdge(relation, parent, page)`), Spotify.Api.cs (F: provider routes), a login-time library sync host (new SHELL file, see B2) | fixed (build+tests green) / open (persistence → G-230, playlist diff → G-233) | Plan edges like rows (inflight per parent); routes: playlist v2 + revision diff (`%2C`), collection paging 300/set, rootlist `?decorate=revision`, recents diff, pathfinder album/artist/discography; dealer pushes → deltas. EdgesStagingTests-style facts from captured fixtures |
| G-043 | No request-body encoders for any library/playlist write (`Col.PageRequest/WriteRequest`, `Pl.ListChanges`, create, signals, rootlist changes, autoplay), plan's `Api.CollectionAdd/Remove` absent, `Store.Journal` has no network drain; and `Sidebar.LibraryWrites` (every move/deposit/create/rename/delete in the sidebar, tab DnD) is never installed and has no Spotify-side composer (0.2.9's `TryBuildMove`/`PlaylistDiffApplier` dropped) — **pass 3: unchanged — B2; B6 changed the contract: `SidebarLibraryWrites.RenameFolder` now receives the NEW name** — **pass 4: `Spotify.Encode.cs` (1,009: all encoders), `CollectionAdd/Remove` (`Api.Library.cs:515-540`), every `LibraryWrites` member implemented (`Spotify.Library.cs:494-937`), `User.Dispatch` routes; EncodeTests 24 + 18 playlist-wire fixtures, RootlistSlotToOp 9, RootlistDropScenario 16. Remains: the playlist page's `Track.MenuSeams.RemoveRows/MoveRows` never assigned (O), no playlist-signals encoder, `Store.Journal` dead (D-J below), "New folder" never asks a name (G-235). Severity drops to DEGRADES** | `Spotify.Api.cs:1298-1302,1362-1402,1508-1524`; `Store.cs:1381`; `Sidebar.Host.cs:3076-3116`; plan §4:953 | impl | DEGRADES (was BLOCKS; pass 4) | `+Spotify.Encode.cs` (E, new named partial: pure encoders), Spotify.Api.cs (F), the library sync host (B2), App.cs installs `Sidebar.LibraryWrites` | fixed (part) | Port `CollectionWriteMapper`, `PlaylistWireMapper.Build{Changes,RootlistChanges,CreateChanges}`, `SpotifyIds.NewGroupId`, folder markers, signals; optimistic edge write + settle + undo toast; byte-exact facts vs 0.2.9 fixtures; restore `RootlistDropScenarioTests`/`RootlistSlotToOpTests` op-order facts |
| G-044 | Extended-metadata kinds the API asks for are dropped by the decoder: 15 (user profile), 186 (credits), 151 (recommended playlists), 98 (audio associations), 237 (waveforms) → `Api.UserProfiles/TrackCredits/AlbumRecommendations/TrackExpansion` answer with nothing staged — **pass 3: kinds 15/186/151/98/237 decode (`Spotify.Decode.Traits.cs`) into the user row, `Edges.TrackCredits`, `TrackVersions`, `TrackWaveform`, `AlbumRecommendations`** | `Spotify.Decode.cs:46-61,893-919`; `Spotify.Api.cs:515-561` | data | DEGRADES | Spotify.Decode.cs (E) | fixed (tests green) | Port the projectors (`Backend/Hydration/Projectors/*`): prerelease under 3 keys, waveform 220 columns, recommendations Take(12). DecodeTests from generated protos |
| G-045 | No decoder for: browseAll/browsePage/browseSection, HomeSection, queryNpvArtist, queryAlbumMerch, similarAlbums, whatsNewFeed, userTopContent, artistDiscographyAll, ArtistTopTracksExtended, the 12 concert calls (`StagedConcert` has no producer), content filters, friend feed/presence, gander notifications, popcount, profile REST, ContextResolve, Autoplay — **pass 3: landed — gander `Decode.Notifications`, `Decode.Rootlist`, `BrowseAll/BrowsePage/BrowseSection/HomeSection`, `ContextResolve` (+ autoplay answer + `AutoplayRequest` body), `Decode.Profile`, `Decode.ConnectLoad` (B1, fixtures added). Still owed: concerts (`StagedConcert` producer), merch, similar albums, what's-new, friends feed/presence, content filters, popcount, ArtistTopTracksExtended, userTopContent, NpvArtist, discography-all — each with its WP-5 page owner** — **pass 4: + `Decode.Popcount`, `PermissionBase`, `ArtistTopTracks`, `DiscographyAll`, `LibrarySet`, profile REST (`Spotify.Decode.Entry.cs`). Still owed: concerts, merch, similar albums, what's-new, friends feed/presence, content filters, userTopContent, NpvArtist, search genres/suggestions (`Spotify.Api.cs:639-643` unserved)** | `Spotify.Api.cs:855-1298`; `Entities/Concert.cs:380-407`; grep `Spotify.Decode*.cs` | impl | DEGRADES (BLOCKS the pages that need them) | Spotify.Decode.Pathfinder.cs / Spotify.Decode.cs (E) with each page owner (M/N/O/P/Q, W5) | fixed (part) | One `Utf8JsonReader` fold per op into `Staging` with a fixture each; wire into the pathfinder provider |
| G-046 | Rootlist decode never emits folder markers or names: every row is `RootlistKind.Item`, `edge.Text` (FolderName) never set, folder nodes have no `EntityKind` so `Stage` drops them; `SidebarProjection.WalkRootlist` expects FolderStart/FolderEnd — **pass 3: FolderStart/FolderEnd with names from both decoders; a folder is no longer staged as a user row (B1)** | `Spotify.Decode.Pathfinder.cs:1031-1038`; `Edges.Staging.cs:446`; `Sidebar.cs:4254-4297` | data | BLOCKS (folders, rail flyout, Move to folder, outdent) | Spotify.Decode*.cs (E) | fixed (tests green) | Emit `FolderStart(depth, name, groupId)`/`FolderEnd` from `spotify:start-group:<hex>:<name>`/`end-group`; staging test over a two-level folder fixture |
| G-047 | Folder identity is position-derived and prefix-mismatched: `RootlistEdge` has no `FolderId`, ids are `"folder:"+Position` (0.2.9: `FolderId` = bare group hex, `Id = ForFolder(hex)`); ids move with the folder, persisted `v3.expandedFolders` and 0.2.9 folder pins never match, `PinSyncRules` would sync `spotify:folder:<position>`, `LibraryWrites.MoveRootlist/RenameFolder/DeleteFolder` get ids Spotify cannot resolve; `ResolveUnlistedPin` uses the bare convention while the projection uses the prefixed one — **pass 3: `RootlistEdge.FolderId` = bare group hex on both markers; `SidebarProjection.FolderIdOf`, folders travel as `spotify:folder:<hex>` (B1 + B6). Owed: the fake seed passes no `FolderId` (WP-fake), and `Relation.Rootlist`/`User.ReplaceRootlist` store folder strings without AddRef** — **pass 4: `User.ReplaceRootlist` AddRefs `FolderName`/`FolderId` (`User.cs:390-395`), commit arm retains both (`Edges.Staging.cs:451-465`). Remains (fake seed only): `FolderStart` passes no `FolderId` and RetainText + ReplaceRootlist double-AddRef the name (`Entities.Fake.cs:383-389`) — a small leak under `--fake`** | `Edges.cs:551`; `Sidebar.cs:4258,4273-4274,4584,5552,5581-5582,5629-5630`; `_old/.../SidebarProjection.cs:174,182` | data / defect | BLOCKS | Edges.cs (B: `RootlistEdge.FolderId StringId`), Spotify decode (E), Sidebar.cs `WalkRootlist` (J) | fixed (build+tests green) | Add the column, decode the group hex, `Id = SidebarPinId.ForFolder(hex)`, `FolderId = hex`, `ParentFolderId = parent hex`; projection test: a moved folder keeps its id |
| G-048 | Rootlist rows are staged `Authority.Thin` with no capability block → every row reads not-owner/not-editable (deposits refuse "can't edit", Rename/Delete/Access absent, no V3 qualifier chips); nothing Ensures the sidebar's model on mount (ch 25 §7), V3 dropped 0.2.9's seven `LibraryStore.Ensure*` warmers — **pass 4: rootlist `meta_items` owner/header zipped by index (`Spotify.Decode.Library.cs:158-202`), kind-205 `source` → `OwnerUri` (`Spotify.Decode.cs:821`), `LibraryCaps.StageOwned` from `AfterPublish`, rootlist rows asked `PlaylistFields.Row`. Remains: both wire assumptions uncaptured (G-236); collaborators' capabilities never derived; no PaneHost mount effect (sync covers login)** | `Spotify.Decode.Pathfinder.cs:1031`; `Sidebar.cs:4318,4334,4345-4353`; `Playlist.cs:70,401-403`; grep `Entities.Ensure(` in Sidebar*.cs | data / wiring | DEGRADES | rootlist decode (E: caps from `decorate` attributes) + Sidebar.UI.cs PaneHost effect (J) | fixed (part) | Decode owner/capabilities; one effect Ensures the four relations + `PlaylistFields.Identity` + `Capabilities` at Visible priority. Projection test: an owned playlist reports ByYou + CanEdit |
| G-049 | Pins never sync with Spotify's ylpin set (read or write): `SidebarPinSync` bridge not ported, `Edges.Pins` never folded into `SidebarPinStore.ApplyRemote`, `OnLocalPinChanged` never becomes a collection write, `PinsMigratedToServer` unread, `User.Dispatch` unimplemented — **pass 4: `LibraryPinSync` bridge (`Spotify.Encode.cs:921-1009`), server→local `ApplyServer`, local→server ylpin writes + `MirrorPin`, 60 s grace, migration latch; `SidebarPinSyncTests` 14. Runtime unverified against a live ylpin set** | `Sidebar.Host.cs:1423-1425,1443,1563-1587`; `Platform.cs:100`; `User.cs:328`; `_old/Wavee/App/SidebarPinSync.cs` | parity | DEGRADES | new bridge in Sidebar.Host.cs (J) + collection write (B2) | fixed (build+tests green) | Port SidebarPinSync (settled/delta fold, `PinSyncRules.IsSyncable`, migration latch); restore the 12 dropped `SidebarPinSyncTests` |
| G-050 | `EdgeState` has no Failed member → `PublishState` never reports Failed; V3's error banner, full-page Retry vacancy and parity item 73 unreachable — **pass 3: `EdgeState.Failed` via `EdgeTableBase.Readiness` (never persisted)** | `Edges.cs:58-66`; `Sidebar.Host.cs:2933-2936`; `Sidebar.UI.LibraryV3.cs:541,567,609-612` | data | DEGRADES | Edges.cs (B) + binder (J) | fixed (tests green) | Per-relation failure bit (or Fetch error state) mapped to the binder; V3 test on a failed rootlist with zero rows |
| G-051 | `Browse.Commit` is not in the commit chain → a decoded browse page never lands — **pass 3: `CommitBrowse` in the commit chain** | `Browse.cs:241-244`; `Entities.cs:1925-1945` | wiring | DEGRADES | Browse.cs + Entities.cs (B/P) | fixed (tests green) | `static partial void CommitBrowse(Staging s)` beside `CommitHome`; BrowseTests through `Entities.Commit` |
| G-052 | Retiring a scope leaks the synthetic tables' text: `Scope.Tables` lists the 8 kind tables only, so Homes/Sections/Searches/Browses strings and `Edges.RecentsRevision` are never released on `Entities.Switch` — which the welcome effect now runs on every login — **pass 3: `ScopeRetirementTests`; the four synthetic tables + both revisions released** | `Entities.cs:1448,1460-1463,1775-1786` | defect | DEGRADES | Entities.cs (A) + Edges.cs (B) | fixed (tests green) | Add the synthetic tables to `Tables` / release in `Scope.ReleaseText`; EntitiesTests fact counting StringTable refs across a Switch |
| G-053 | Small spclient semantics dropped: `LikedContentFilters` has no `If-None-Match`/304 path; search has no 8 s deadline; `/permission/base` GET missing — **pass 4: `If-None-Match`/ETag (`Spotify.Api.cs:284,332`), 8 s search deadline (:1560-1573), `PlaylistPermissionBase` (:1776). `LikedContentFilters` still has no consumer (G-045)** | `Spotify.Api.cs:1067-1072,1215-1216,1470-1488` | parity | COSMETIC | Spotify.Api.cs (F) | fixed (build+tests green) | Etag in/out, linked 8 s CTS, `PlaylistPermissionBase(id)`; SpotifyApiTests header facts |
| G-054 | `ZstdSharp.Port` referenced "for zstd /changes + /diff" but nothing decodes zstd and `DecompressionMethods.All` does not cover it → playlist diffs will not decode once wired — **pass 4: `IsZstd`/`Unzstd` one-shot then streamed (`Spotify.Api.Library.cs:150-183`); D8 answered: decode, keep the package** | `Wavee.csproj:176-177`; `Spotify.Api.cs:148,1353-1366` | impl | DEGRADES (latent) | Spotify.Api.cs (F) | fixed (build+tests green) | Decode zstd bodies on the two playlist routes, or drop the package |
| G-055 | `Palette.Host.Filler` unassigned → cover gradings come only from kind-179 metadata; washes/placeholders neutral otherwise — **pass 4: Filler effect while Online and not fake (`Shell.Host.cs:783-786`), `GradeCovers` + `Queries.DynamicColors` (`Spotify.Api.cs:1050,1418-1428`), pump via `Palette.Tick`** | `Palette.Host.cs:47-51` | wiring | DEGRADES | Spotify.Api.cs + composition (F/L) | fixed (build+tests green) | Filler over the extended-metadata colour endpoint installed after login; PaletteHostTests cover the pump |
| G-056 | No manifest id in the catalogue: TrackV4 `original_video[0].gid` never decoded, no `VideoGid` column, none of 0.2.9's four resolver tiers; `Manifest.Resolve` has no caller → video can never be opened — **pass 3: TrackV4 field 38 → `Track.VideoGid` (B1); `Manifest.Resolve` still has no caller and the four-tier resolver is B7's** | `Spotify/Protos/metadata.proto:98-104`; `Spotify.Decode.cs:1210-1243`; `Track.cs:170-173` | data | BLOCKS (video) | Spotify.Decode.cs (E), Track.cs (A: `Column<StringId> VideoGid` under `TrackFields.Video`), Playback.Video.cs resolver (H) | fixed (decode) / open (resolver) | Decode field 38 gid → hex; resolver: own gid → counterpart's gid → TrackV4 on the counterpart. Decode unit over a captured TrackV4; pure resolver-tier test |
| G-057 | `HasVideo` narrower than 0.2.9 (only when the association carries `associated_uri`; 0.2.9: `files > 0 or counterpart`, aliases via kinds 182/212) — **pass 3: `HasVideo` also set when kind 99 carries files; the 182/212 alias recovery is still owed** | `Spotify.Decode.cs:1242` | parity | DEGRADES | Spotify.Decode.cs (E) | fixed (part) | Also set on non-empty `files`; port the 182/212 recovery as a follow-up fetch. Decode unit |
| G-058 | No `Track.VideoW/VideoH/LiveVideo` columns (ch 24 DATA GAP 1): the cap seeds 16:9 then refits — **pass 3: `VideoW/VideoH` + `TrackFlags.LiveVideo` (B1); the cap does not read them yet (B7)** | `Track.cs:113-127,170-173`; `Video.UI.cs:409-412` | data | COSMETIC | Track.cs (A), Playback.Video.cs, Video.UI.cs | fixed (tests green) | Two `ushort` columns + flag under `TrackFields.Video`, read first by the cap/PiP fit |
| G-059 | Cover-less playlist mosaic missing (`MosaicTiles = null` for playlists; only folders) | `Sidebar.cs:4114-4116,4327` | data | COSMETIC | Playlist.cs (Mosaic0..3) + decode + projection | open | Per ch 25 §7 DATA GAPS; projection test with a 4-cover playlist |
| G-060 | Unknown recents content type loses its wire token (`RecentsContentType` is a byte enum; ch 16 §8 wants "the wire token IS the label") — **pass 3: `RecentsItem/RecentsRow.RawContentType` decoded; `Recents.cs` still needs the `RawType` field to commit it (P, WP-5)** | `Recents.cs:42` | decision | COSMETIC | Recents.cs + Decode (P/E) | fixed (decode) / open (commit) | Keep the byte, add `StringId RawType` written only when `None` |
| G-061 | Dead staging fields: `SectionTable.Duplicates` has no producer; `StagedSection.ParentUri` unused | `Home.cs:228,540` | defect | COSMETIC | Home.cs (P) | open | Dedupe at stage time or delete both |
| G-062 | Staging/store follow-ups found by B1 and WP-fake: `Relation.Pins` does not exist in `Edges.Staging.cs` (the staged-commit pipeline cannot write pins; the seed uses `User.Replace` directly; a live pin sync needs the arm); pins carry no kind bit (`User.cs:227-234`); the `Relation.Rootlist` arm and `User.ReplaceRootlist` store folder strings without `AddRef`; the store does not persist rootlist payloads, so folder markers are lost on a warm read; `TrackFields.Publishing` has no route (kind 183 is album-only); `Home.cs` still says the synthetic-text leak is open — **pass 4: `Relation.Pins` + commit arm (`Edges.Staging.cs:49,469-484`), `PinKind` written by `User.Add` (`User.cs:92,360`), AddRef closed (G-047), `Publishing` deliberately routeless (`Fetch.Routes.cs:187`). The persistence half is wider than stated → G-230 (no edge of any relation is persisted)** | B1 + WP-fake reports; `Edges.Staging.cs`; `User.cs:227-234`; `Store.cs` | data / defect | DEGRADES | Edges.Staging.cs + User.cs (B), Store.cs (C), Track.cs (A) | fixed (part) | Add the `Relation.Pins` arm + a pin kind bit (B2 needs both); AddRef the folder strings; persist rootlist runs with their markers; seal or re-home `Publishing`; fix the comment |
| G-063 | B1b prerequisites B1 found: `Api.Home/Album/Track/ArtistOverview/Search` hand a root-positioned reader to folds that expect to be inside `data.*`, so as written they decode nothing; `PathfinderOp.FetchPlaylist` has no persisted-query hash, so the playlist groups routed to it (accent, daylist, chart, tuning) are sealed — **pass 4: folds go through `Decode.Export` (`Spotify.Api.cs:907-949,1182-1231`). Remains: no `fetchPlaylist` persisted hash — accent/daylist/chart/tuning stay sealed (`Spotify.Api.cs:642`, `Fetch.Routes.cs:114-116,225`); no longer BLOCKS** | B1 report | defect | DEGRADES (was BLOCKS; pass 4) | Spotify.Api.cs (B1b) | fixed (part) | Go through `Decode.Export` or the raw-JSON entry points; add the hash |
| G-064 | The footprint gate has about 2.5 % headroom: Track rows grew 12 B (`Asked` + the three video columns), about 1,434 KB against a 1,470 KB budget; `Entities.cs` (2,104) is past budget | B1 report; `FootprintGateTests` | decision | COSMETIC (the next column breaks the gate) | Entities.cs / Track.cs (A) | open — decision D31 | Raise the budget with a reason, or pack the flags |
| G-065 | Under `--fake` the chrome shows "Sign in": the fake scope never presents as signed in — `Shell.Auth` folds from `Spotify.Status`, which has no fake arm (WP-fake expected B5's `Session.Phase := Authenticated` + `Me := MeSlot` under fake; B5 did not add it) — and per B5 the sign-in door may open on a fresh fake profile — **pass 4: `FakeOnline` step (`Spotify.cs:363-375`), `BootFake` (`Spotify.Session.cs:380-392`) at App.cs:62; auth fold passes `Args.Fake or HasStoredCredential()` (`Shell.UI.cs:325`); the door is not mounted under fake (`Setup.UI.cs:60`); VERIFIED by the orchestrator's launch (signed-in chip)** | first `--fake` launch (orchestrator); `Shell.UI.cs:296` (`FoldAuth`); `Spotify.Session.cs` (no fake arm) | wiring | DEGRADES (blocks the `--fake` chrome/profile-chip parity items, and every fake surface gated on Auth) | Spotify.Session.cs `Boot`/`Login` fake arm (D) + `Shell.cs` `FoldAuth` | fixed (build+tests green) | Under `Platform.Args.Fake`: `Status` publishes Online with a fake profile row (`Entities.FakeAccount`), `SignIn.Requests` never bumps; SessionStepTests fact; re-take the screenshot |
| G-066 | A fresh profile opens with the sidebar collapsed to the 56-DIP rail at a 1,770-px window — the pane-state default (or `Shell.Ui.SidebarPresentedCompact`'s responsive fold) disagrees with 0.2.9, which opened expanded at that width — **pass 4: D's pure fact `FreshProfile_At1770_RestoresExpandedAndUncollapsed_G066` (`SidebarDesignTests.cs:113`); `--fake` settings are memory-only now (`Platform.Settings.cs:58-61`); 0.2.9 used the same 720 threshold. The orchestrator's re-launch rendered the Classic sidebar expanded** | first `--fake` launch (orchestrator), `scratchpad\fake-second.png`; `Sidebar.Host.cs:87-121` (Boot defaults); `Shell.UI.cs` (compact fold) | parity / defect | DEGRADES (first impression; ch 25 parity) | Sidebar.Host.cs defaults (J) or Shell.cs `FrameRules` (I) — whichever reads wrong against `_old` `SidebarPreferencesService` defaults | verified (launch: Classic sidebar expanded at the second launch) | Compare the seeded `sidebar.collapsed`/width defaults and the narrow-band threshold with 0.2.9; pure fact on the default pane state at 1,770 px |

### 2.4 Playback reducer/host, Connect, telemetry, OS bridges, notifications

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-070 | No play-a-context path: `PlayNow` needs a row + `QueueCursor`; `Queue.Replace` and `Api.ContextResolve` have no caller, no context-resolve or autoplay decoder, `Shell.OnPlayContext` null (deep link `wavee://play?ctx=`, omnibar ▶, every "play this" intake inert) — **pass 3: `Decode.ContextResolve` + the autoplay body landed (B1); `Queue.Replace` still has no GUI caller, `OnPlayContext` null — Q (WP-5) + B3** — **pass 4: `Playback.PlayContext` (`Playback.Host.Context.cs:75-110` → resolve :189-212 → `StartContext` :256-329); GUI callers `Queue.UI.cs:65,69,1044`, `Track.Menu.cs:433`. Remains: the table's own play funnel bypasses it (G-253); context paging (G-242); inert under `--fake` (G-249)** | `Queue.cs:344-355`; `Spotify.Api.cs:1291-1298`; `Shell.Host.cs:400-421` | deferred | BLOCKS (plan DoD 4: play/next) | Queue.cs (Q: `ContextQueue.Build`, headless plan §3.1), Spotify.Decode.cs (E), Shell.Host.cs install (I) | fixed (build+tests green) | ContextResolve decode → `Queue.Replace` → `PlayNow(first, context, CursorOf(first))`; install `OnPlayContext`; ShellRoutesTests deep-link fact + queue seed test |
| G-071 | An inbound Connect transfer or play claims ownership and announces but plays nothing: `RemoteCommand` carries no context uri, `skip_to`, `play_origin`, position or the transfer's `TransferState`; no host code resolves anything (the phone shows Wavee active and silent) — **pass 3: `Decode.ConnectLoad` → `RemoteLoad` (play + transfer incl. base64 TransferState) over a pooled buffer (B1); the host half is B3's** — **pass 4: glue `Spotify.Connect.Commands.cs:74-89` (`ConnectLoad` → `RemoteLoadArrived`, no item); host `Playback.Host.Remote.cs:49-51,122-139` + `StartContext` (start index uid→uri→index, transfer position extrapolation, paused). Runtime unverified; supersede (G-244); paging (G-242)** | `Playback.cs:1810-1819`; `Spotify.Decode.Connect.cs:395-397,425-467`; `Playback.Host.cs:341-345` | impl | BLOCKS (DoD 4: transfer to Wavee) | Spotify.Decode.Connect.cs (E), Playback.Host.cs (G), the context resolver (G-070) | fixed (build+tests green) | Decode `command.context.uri`, `options.skip_to{track_uri,track_index,track_uid}`, `seek_to`, `transfer.data` into a pooled value; the host builds the queue and posts `Input.Play(…, fromMs)` under the claim epoch. ClusterDecodeTests from captured request bodies |
| G-072 | Hello PUT (`PublishReason.NewDevice/NewConnection`) never sent → device invisible, roster empty — **pass 3: hello + `SnapshotForConnect` in; runtime still unverified (needs the smoke)** | now `SessionEffects.AnnounceDevice` → `Connect.AnnounceDevice()` → `Playback.HelloReason` (`Spotify.Session.cs:226`, `Spotify.Connect.cs:256`, `Playback.Host.cs:282-293,451-453`) | wiring | BLOCKS | H-W G1 | fixed (tests green) | Smoke: another client's picker lists the device |
| G-073 | Inbound Connect volume and logout topics dropped: only `cluster` pushes and REQUESTs handled; 0.2.9 subscribed `hm://connect-state/v1/connect/volume`; PutState advertises `supports_logout` — **pass 3: `ItemKind.Volume` mailbox item + `connect/logout` → `Spotify.Logout()` (B5, `DealerTopicTests`); the reducer's `WireVolume` fold is B3's** — **pass 4: volume fold `Playback.Transitions.cs:836-849` (never forwarded), mailbox arm `Playback.Host.cs:428-431`, field 8 at `Spotify.Decode.Connect.cs:737`. Remains: `LastCommandMessageId` never ages out (G-246)** | `Spotify.cs:915-917`; `Spotify.Connect.cs:190-191`; `Spotify.Decode.Connect.cs:710`; `_old/Wavee/Backend/ConnectCommand.cs:197-217` | parity | DEGRADES | Spotify.cs (topic), Spotify.Connect.cs (F), Playback.Host.cs (G) | fixed (build+tests green) | Classify the volume topic, decode varint field 1 → `Input.WireVolume`; logout topic → `Spotify.Logout()`. DealerFrameTests + PlaybackStepTests |
| G-074 | Controller queue/context writes not applied: AddToQueue/SetQueue only claim; UpdateContext/SetOptions ignored — **pass 4: `add_to_queue` → `EnqueueRemote` (`Playback.Host.Context.cs:356-363`); `set_options` → verbs (`Spotify.Connect.Commands.cs:92-104`); `set_queue`/`update_context` applied (`Playback.Host.Remote.cs:149-209`, `SpliceQueue`); the queue body is handed off with no command item (`Spotify.Connect.Commands.cs:55,66,109-124`). Remains: `update_context` does not reshuffle (G-245) and folds no claim/announce; controllers never see the applied queue (G-240); stale comments (G-250)** | `Playback.cs:1870-1879` | deferred | DEGRADES | Playback.Host.cs (G) + Queue.cs (Q, W5) | fixed (build+tests green) | Decode and apply through Queue in the drain; facts over captured commands |
| G-075 | PutState never carries the current item's queue uid (`CurrentUid()` = "") — **pass 4: `UidBook` (`Playback.Transitions.cs:329-409`) keeps 16-hex/20-hex/`q2` uids; `CurrentUid` → `w.Str(2, snap.Uid)`. Only the current row's uid reaches the wire (G-240)** | `Playback.Host.cs:456-458` | impl | COSMETIC | Playback.Host.cs (G) | fixed (build+tests green) | Carry the server-minted uid on `QueueEdge`; PutStateEncode fact |
| G-076 | Play registration (gabo) and resume points (herodotus) entirely unwired: no caller of `Telemetry.Boot/Started/Paused/Resumed/Seeked/Ended/PlayHistory/ResumePoint/ResumeMs/Shutdown` → no royalties, no Recently Played, no Wrapped, no episode resume — **pass 4: registration Started/Paused/Resumed/Seeked/Ended in `Report` (`Playback.Host.Context.cs:561-594`), one report per Step; episode resume write/read (`:571,578,622-632` → `DoResumeAt`). Runtime unverified. Side effect: G-077 is now live** | `Spotify.Telemetry.cs:209,334-438,608-696`; grep | wiring | DEGRADES (must ship) | Playback.Host.cs (G) + App.cs | fixed (build+tests green) | A `Registration` per load epoch; epoch-matched Started/Pause/Resume/Seek/Ended/Transfer/Stop → Telemetry; `ResumeMs` before an episode load. Pure facts over a recorded Step trace |
| G-077 | `Telemetry.Enqueue` (any thread) writes `Settings.Set(GaboGlobalSequence)` whose facade bumps `SettingsEpoch` and a Signal (C1) — **pass 4: with G-076 wired, `Telemetry.Enqueue` writes `Settings.Set(GaboGlobalSequence)` per event (`Spotify.Telemetry.cs:231`); the facade bumps `SettingsChanged` on every write (`Platform.cs:879-884`) → a track boundary costs ~10 registry writes and re-renders every surface subscribed to `SettingsChanged` (`Queue.UI.cs`, `Stage.UI.cs`, `Lyrics.UI.cs`). Now an idle-GPU/re-render issue, not only C1** | `Spotify.Telemetry.cs:227-235`; `Platform.cs:828-833` | defect | DEGRADES (raised, pass 4) | Spotify.Telemetry.cs (F), Platform.cs (S) | open — severity raised | Interlocked sequence, persisted on flush through `Spotify.Post`; DEBUG UI-thread assert in the facade |
| G-078 | Launch restore of the deck missing: `SnapshotSession` is an unimplemented partial, no restore input (0.2.9 restored a paused session) — **pass 4: `DoRestore` (`Playback.Transitions.cs:812-831`, paused, parked, audio, no claim), `Restore` (`Playback.Host.Context.cs:525-545`), installed at boot (`Shell.Host.Deck.cs:44-49`), first Resume loads at the position. Remains: the restore lands in the BOOT scope and is orphaned by the welcome's `Entities.Switch` on every real launch (G-241). 2026-09-16: the QUEUE ROWS are now persisted too — `SessionDeckDto.Rows` (`SessionQueueRowDto`: uri, bucket, provider, item id; ≤ `SessionDeck.MaxRows` = 200 around the deck, `SelectRows`/`DeckIndex` in `Shell.Host.Deck.cs`), `RestorePoint.Rows`, laid back by `Playback.Restore` → `LayPersistedRows` BEFORE any context seed, so an autoplay or hand-built queue survives a restart ("when I restart the app the queue is also gone")** | `Playback.Host.cs:518-521`; `_old/Wavee/App/PlaybackBridge.cs:1688`; `Playback.cs:89-90` | parity | DEGRADES | Playback.Host.cs (G) + Shell.Host.cs session doc (I) | fixed (part) | `Input.Restore(row, context, cursor, posMs)`; PlaybackOwnershipTests pin `AllowsLoad` **2026-09-16 refill batch:** `Restore` posts `Input.ContextPages(ctx, false)` after `Input.Restore`; `DoRestore` arms a prefetch (never a prepare) and runs `CheckRefill`; `Rebind` re-ensures every re-laid row's identity in the new scope; the panel's `EnsureRows` key carries the scope epoch and the context entity is `Ensure`d for "Playing from" (`Queue.EnsureContext`). |
| G-079 | `Shell.PlayLog.Append` has no caller → jump list recents, History, recents recency, sidebar "played" feeds never fill — **pass 4: `Playback.PlayStarted` → `Shell.Host.Deck.cs:42,77-92` → `PlayLog.Append` (:91)** | `Shell.Host.cs:1040` | wiring | DEGRADES | Playback.Host.cs (G) via an `Action` seam set by Shell.Host.cs (I) | fixed (build+tests green) | On epoch-matched Started post `PlayLog.Append(track, context, UnixNowMs(), title)`; ShellHostTests fact |
| G-080 | Local shuffle flips a flag only (no `QueueOrder` reorder); autoplay never runs (`EndOfQueue` ends the phase although `AutoplayEnabled` defaults true and `Api.Autoplay` exists) — **pass 4: shuffle reorders/unshuffle restores the NextUp run (`Playback.cs:1565-1575` → `ReorderQueue` `Playback.Host.Context.cs:366-422`); autoplay early request + run-out + append (`Playback.Transitions.cs:619-631,886-908`, `Playback.Host.Context.cs:432-518`). Remains: context paging (G-242)** | `Playback.cs:1496-1507,1578-1587`; `Spotify.Api.cs:1298-1302`; `Platform.cs:205` | parity | DEGRADES | Queue.cs (Q), Playback.cs + Host (G) | fixed (build+tests green) | Shuffle applies `QueueOrder` to NextUp; `Effects.Autoplay` at end-of-context → Api.Autoplay (+ encoder G-043) → rows appended as `QueueProvider.Autoplay`; PlaybackStepTests **2026-09-16 refill batch:** asked by the pure `Playback.Refill` rule (75 % of the run consumed or ≤ 3 rows ahead; at once for a complete context) instead of only at the endgame; offline or 401/403 → `Autoplayed(retry)` → `AutoplayPhase.Deferred`, re-asked on `Input.SessionOnline` (permanent `Spotify.Status` watch in `Playback.Host.Autoplay.cs`). `DoEndingSoon`/`EndOfContext` keep the None-guarded fallback. Tests: `RefillTests`, `PlaybackStepTests` refill region. |
| G-081 | Suspend pauses a foreign Connect device: `PowerPolicy.CanPauseOnSuspend` never set (default true), `OnSuspendUi` posts `Input.Pause` which `DoPause` FORWARDS as `RemoteCmd.Pause`; the local-only `DoSuspend`/`ReportSuspend` is unreachable — **pass 4: `OnSuspendUi` → `ReportSuspend` (`Playback.Os.cs:889-896`) → local-only `DoSuspend` (`Playback.cs:2042-2056`); `CanPauseOnSuspend` deleted** | `Playback.Os.cs:741,791-796`; `Playback.cs:1509-1511,1972-1983`; `Playback.Host.cs:636` | defect | DEGRADES | Playback.Os.cs (H), Playback.cs (G) | fixed (build+tests green) | `OnSuspendUi` → `Playback.ReportSuspend()`; `DoSuspend` also sets `fx.Smtc`; delete `CanPauseOnSuspend`. Step fact: Suspend with Foreign owner emits no `SendRemote` |
| G-082 | Resume does not re-announce to Connect (0.2.9 `AnnounceNewConnection` after `PBT_APMRESUMEAUTOMATIC`); `Resume_` is a no-op — **pass 4: `OnResumeUi` → `ReportWake` (`Playback.Os.cs:898-904`) → `DoWake` announces NewConnection (`Playback.cs:2062-2066`)** | `Playback.Os.cs:798-804`; `Playback.cs:1371,1987-1990` | parity | DEGRADES | Playback.Os.cs, Playback.cs | fixed (build+tests green) | `OnResumeUi` → `ReportWake()`; `Resume_` → `Announce(NewConnection)`. Step fact |
| G-083 | SMTC reads the Track table for episodes (`new Track(s.Current.Slot)` without checking `Kind`) — **pass 4: `PushDisplay` switches on kind via `CardShapeFor` (`Playback.Os.cs:231-272,382-387`)** | `Playback.Os.cs:197-205`; `Playback.Host.cs:423-424` | defect | DEGRADES | Playback.Os.cs (H) | fixed (build+tests green) | Switch on `Current.Kind`; pure `SmtcDisplayFor(kind, …)` facts in PlaybackOsTests |
| G-084 | SMTC metadata never refreshes after late hydration (display pushed only on identity change; a remote/Connect track with unknown Identity keeps an empty card) — **pass 4: `CardKey(Id, Knows, Image)` dedupe (`Playback.Os.cs:394-412,193-204`); `WatchRow` re-publishes on a row version bump (`Playback.Host.cs:808-822`)** | `Playback.Os.cs:189-208`; `Playback.Host.cs:415-425` | defect | DEGRADES | Playback.Os.cs + Host | fixed (build+tests green) | Dedupe key = (id, knowsIdentity, imageId); re-publish when the row's Identity group commits |
| G-085 | Display keep-awake during video reads `Playback.VideoActive` (never true; one drain stale) — **pass 4: `AwakeFor(phase, routesLocal, kind)` from the published state (`Playback.Os.cs:826-829,861-874`)** | `Playback.Os.cs:737,763-776`; `Playback.Host.cs:408,550` | wiring | DEGRADES | Playback.Os.cs | fixed (build+tests green) | Decide from `in State` (`s.Kind == Video`) in `OnStateChanged`; table fact |
| G-086 | Ambient power cadence (2 s `GetSystemPowerStatus` poll, debounce, 30/24 Hz, energy saver) exists only as constants — **pass 4c: `AttachAmbientPower/DetachAmbientPower` (`Platform.Host.cs:331,346`), attached from the GUI arms; the launch log shows `ambient cadence plugged loopHz=30`. On-box battery check owed** | `Design.cs:1524-1536`; grep | parity | DEGRADES | Platform.Host.cs (S, W6) | fixed (build+tests green) | Port poll + debounce + `DefaultLoopHz` apply; pure `Plugged(status)` facts |
| G-087 | Jump list may publish before `JumpList.Aumid` is known (set later by `Notify.HostInstall`, no rebuild) — **pass 4: `JumpList.Aumid` setter republishes when active (`Playback.Os.cs:622-633`)** | `Playback.Os.cs:81,550-555`; `Notify.Host.cs:69`; `Shell.UI.cs:283` | defect | COSMETIC | Playback.Os.cs | fixed (build+tests green) | `Aumid` setter calls `Attach()` when active |
| G-088 | `Notify.Rebuild` has no external caller → `HostConsider`/escalation never runs; no Windows banner can ever raise (the panel shows local toasts only); the feed sources (gander, what's-new, update) have no decoder/host — **pass 4c: `Update.FeedRow` (`Update.Host.cs:144`) has no caller; the only `Notify.Rebuild` caller is the simulator (`Diagnostics.Probe.Arms.cs:703`) — the feed host (WP-5.P) must merge the update row, gander and what's-new** | `Notify.cs:1005-1026,1080`; `Spotify.Api.cs:1220-1250` (raw, no decoder) | wiring / impl | DEGRADES (toasts BLOCKED) | a feed host (W5/W6) + Notify.Host.cs; Spotify.Decode.Pathfinder.cs (gander/whatsNew folds) | open | Each source fetch calls `Notify.Rebuild(update, social, releases, activity)`; assign `RefreshFeeds/ActivityMarkAllRead/ClearActivity/UndoActivity/UpdateCommand` (G-092). NotifyTests escalation facts exist; manual [SIM] parity 27 |
| G-089 | Pre-save drop and daylist schedulers never attached (`ReleaseDrops.Attach/OnSavedChanged/SimulateSchedule`, `Daylist.Note`) — Settings "Send event" cannot reach them — **pass 4: `ReleaseDrops.Attach` on the first sync, `RequestReconcile` on saved-album changes, `OnSavedChanged` after a prerelease save (`Spotify.Library.cs:194-198,270-275,411-415`). Remains: `Daylist.Note` has called from Home.Host.cs NoteDaylist (stale note fixed 2026-09-17) outside Notify.Host (P); Settings "Send event" → `SimulateSchedule` only from the probe arms (R to confirm)** | `Notify.Host.cs:333,379,458,471,593` | wiring | DEGRADES | prerelease owner (M), home daylist hydrator (P), Settings notifications page (R) | fixed (part) | Attach `Resolve` at login; `Daylist.Note` on card hydration; Send event → `SimulateSchedule` |
| G-090 | `Notify.Dismiss`/`ClearLive` have no caller: in-app dismiss / Mark all read leave Action Center banners — **pass 3: Mark-all-read → `ClearLive()`, row open → `Dismiss(id)` (B8)** | `Notify.Host.cs:612-623`; `Shell.UI.cs:1429` | wiring | COSMETIC | Notify.cs | fixed (build) | MarkAllRead → `ClearLive()`; row dismiss → `Dismiss(id)` |
| G-091 | Toast image cache rooted at `%LOCALAPPDATA%\FluentGpu\toastimg`, unbounded, cleared only by the never-called `SignedOut`; `TagFor` uncapped at 64 chars (over-long tag → silently dropped banner) — **pass 3: own `ToastImageCache("Wavee")` + 50 MB LRU trim; `Notify.TagIds.Clamp` (B8)** | `Notify.Host.cs:94,175,300,570`; engine `ToastImageCache.cs:48-57` | parity | COSMETIC | Notify.Host.cs | fixed (tests green) | `new ToastImageCache("Wavee")` + cap; tag = hash when > 64; pure `TagFor` length fact |
| G-092 | Notification panel verbs unassigned (`RefreshFeeds`, `ActivityMarkAllRead`, `ClearActivity`, `UndoActivity`, `UpdateCommand`) and two updater seams exist (`Shell.UpdateVerb` from I3, `Notify.UpdateCommand` from I1) with nothing behind either; activity rows have no journal (ch 19 DATA GAP 3) — **pass 3: B8 collapsed the two updater seams (`Shell.UpdateVerb` deleted; every non-WhatsNew toast action → `Notify.UpdateCommand` via `VerbFor`); the feed/journal seams stay unassigned until the feed host (WP-5/6) and the update host (WP-6) exist** | `Notify.cs:1086-1100`; `Shell.Overlays.UI.cs:77`; I1 report | wiring | DEGRADES | feed host + update host (W6) | open (reduced) | Route one seam into the other via `Notify.VerbFor`; assign the five seams from the feed/update hosts |
| G-093 | No app updater at all: no AppInstaller/package update service, scheduler or check; the `wavee-stable` feed would never be polled; `ShutdownUpdatePolicy`/install-on-quit absent → `local-update-e2e.ps1 -Scenario inapp` waits for 0.2.9 log lines that 0.3 never writes — **pass 4c: `Update.Host` service, scheduler, simulation, install-on-quit (`Update.Host.cs:481-600`); harness sentences verbatim (`install-on-quit: staging <q>` :559 = `local-update-e2e.ps1:1899`); relaunch line `Platform.Host.cs:131`. Remains: both E2E scenarios not run; dev builds no longer poll (D40); `Update.Host.BeforeApply` unassigned (T)** | `Notify.cs:323-363,772,1100`; `Shell.Overlays.UI.cs:77`; `_old/Wavee/App/AppInstallerUpdateService.cs`; `ops/release/tests/local-update-e2e.ps1:1896-1910` | parity | DEGRADES (BLOCKS release gate E14) | a W6 update host (R/S) + App.cs; harness wording | fixed (build+tests green) | Port `AppInstallerUpdateService` + scheduler + install-on-quit; install both seams; keep the log wording or update the harness in the same change; run both E2E scenarios |
| G-094 | 0.2.9 startup services not ported: factory-reset apply, relaunch broker (`--relaunch-after`), crash report writer + pending-report arm, RunMarker/CrashPromptPolicy, WER dump probe, StartupPreflight notice, preferred-GPU LUID seed, `FluentApp.DiagnosticRun` (census hook, benches), NavigationFrameWatch/MemorySampler, DealerArchive, AttachParentConsole — **pass 4c: crash writer (`Platform.Host.cs:144-160`), run marker + prompt (`Diagnostics.Host.cs:206-223`, WerDump rule proven at launch), factory reset plan/apply (`Platform.cs:1046`, `Platform.Host.cs:176-187`), report dialog + crash card (`Feedback.UI.cs:120-129,595`)** | `_old/Wavee/Program.cs:51-74,78,82,130,153-195,365-401,418-433,499-514,532,566,629-697`; grep → key declarations only | parity | DEGRADES | Screens/Diagnostics*.cs, Screens/Feedback*.cs, Platform.Host.cs, App.cs (R/S, W6) | fixed (build+tests green) | Port per plan §2 rows; port `CrashPromptPolicyTests`, `RunMarkerTests`, `FactoryResetTests`, `ShutdownUpdatePolicyTests` |
| G-095 | Setup wizard never armed: `SetupBootstrap`/`SidebarBootstrap` not ported, `setup.pending`, `sidebar.onboarding.seen`, `*.bootstrap.version` never written → first-run wizard and design chooser can never appear; `SidebarDesignGating` has no consumer — **pass 3: `Setup.Bootstrap` ported with fresh-install detection folded in (sidebar D6); `Setup.BootstrapInstall()` at App.cs:28 outside `--fake`; `SetupBootstrapTests` restore the two dropped chooser facts** — **pass 4c: `Setup.WizardChrome()` mounted (`Shell.Overlays.UI.cs:125`); wizard rules in `Setup.cs` (R2). Launch check of a first-run wizard owed** | `Platform.cs:82-110`; `Sidebar.Modes.cs:178-187`; `SidebarDesignTests.cs:408-411` | parity | DEGRADES | Screens/Setup.cs (R), Sidebar.Host.cs (J), App.cs | fixed (build+tests green) | Port SetupBootstrap; run in `Platform.Boot`; port the two dropped SidebarDesign facts |

### 2.5 Audio: decoders, stream, pump, gain (files: `Playback.Audio*.cs`, `Spotify.Audio*.cs`, `Playback.cs`)

Note: the audio swap and the FLAC SIMD pass were never built; H-W is editing `Playback.Audio.cs`, `Spotify.Audio.Stream.cs`, `Playback.Host.cs`. Every row below is verified in code but not at runtime.

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-100 | A committed gapless join or crossfade never advances the reducer: the pump posts `Started` stamped with A's epoch at position 0, the reducer's `Started` arm does not move the cursor, no hand-off input exists → the UI keeps showing A while B plays; when B ends, `Ended → Advance` loads B AGAIN, and so on for every track. Default settings take this path (crossfade off = gapless; B prepared at every load). The reducer's own model ("Ended advances in the same drain") contradicts the pump's join — **pass 4: D4 as decided — pump posts `ReportHandedOff` from `AnnounceGaplessJoin`/`CommitCrossfade` (`Playback.Audio.cs:2385,2445,2925-2929`), `AdoptHandOff` via `PumpAdopt`; reducer `DoHandedOff` (`Playback.Transitions.cs:650-683`); Execute adopt order (`Playback.Host.cs:508-509`). Runtime check owed (`gapless-album.wh`)** | `Playback.Audio.cs:1795-1825,1871,2021-2044`; `Playback.cs:129-152,1612-1623,1705-1709`; `PlaybackStepTests.cs:269` | wiring | BLOCKS | Playback.cs (G), Playback.Audio.cs (H) | fixed (build+tests green) | `AudioSignal.HandedOff(preparedId)`: reducer advances the cursor, bumps the epoch without `fx.Load`, returns the new epoch through an effect the pump adopts. Step fact "a hand-off advances the cursor and emits no Load"; pump fact for the posted input |
| G-101 | A refused FLAC key (`NoDeriver`/`NoKey`) fails the whole track instead of re-running the ladder at VeryHigh320 (plan gate: public-only build plays the 320 rung) — **pass 4: `LosslessFallback.Decide/Effective` (`Spotify.Audio.cs:159-169`), re-ladder at 320 remembered per track (:756-766), `PreferredQuality` (:734-738) — D7** | `Spotify.Audio.Stream.cs:1207-1211`; `Spotify.Audio.cs:653-663`; `Playback.Audio.cs:2272-2277,2291`; flac plan :2006-2009 | defect | BLOCKS (lossless) | Spotify.Audio.cs (F) | fixed (build+tests green) | In `Open`, on Flac + NoDeriver/NoKey re-`Choose(track, lossless:null, VeryHigh320)`; pure `LosslessFallback.Decide(fmt, fault)` + test |
| G-102 | An 8 s wait for bytes ENDS THE TRACK: `Ring.ReadAt` returns −1 at its bound, both adapters treat `n <= 0` as EOF, the engine goes Ended, the reducer advances; and ranges are published only after the whole 512 KiB arrives, so at ≲64 KB/s the first fill after the 80 KiB head exceeds 8 s (≈2 s into a cold start). 0.2.9 degraded to a blocking read with a ~90 s budget — **pass 4: `Ring.ReadAt` returns `Starved` never EOF (`Spotify.Audio.Stream.cs:763-803`); adapters retry; `FetchRange` publishes each completed 64 KiB slot (:1426-1435); `FoldStall` reports Reconnecting at 1.5 s and fails at 90 s (`Playback.Audio.cs:150-164,2767-2793`) — D5. Runtime check on a throttled link owed** | `Spotify.Audio.Stream.cs:546-551,903-936,380-390,720-726`; `Playback.Audio.cs:1179-1180,1258,2853-2857,2917`; `_old/.../PrefetchingReadStream.cs:56-73` | parity / defect | DEGRADES (skips tracks on slow links) | Playback.Audio.cs adapters + `RingSource` (H), Spotify.Audio.Stream.cs (F) | fixed (build+tests green) | Return 0 only when `ReadAt` returns 0; on a starve post Buffering/`RecoveryKind.Network`, wait up to ~90 s, then `<0` → `Fault.Network`; `Land` each completed 64 KiB slot inside the read loop. Tests: a body stalled 10 s then resumed must not end; a throttled source records `Starves == 0` |
| G-103 | `Prefetching.Read` (module byte seam) returns 0 at its 8 s deadline although its doc says it degrades to the blocking read → slow module/radio bodies truncate — **pass 4: `Prefetching.Read` polls fast for 8 s then every 100 ms up to 90 s (`Playback.Audio.cs:3187-3245`)** | `Playback.Audio.cs:2366-2368` vs `:2409` | defect | DEGRADES | Playback.Audio.cs | fixed (build+tests green) | Fall back to a blocking `_inner.Read`; extend the transient-miss fact |
| G-104 | Every track plays ≈ +4 dB too loud: `SeedFromSettings` sets engine `Normalization = Track` with `default` ReplayGainInfo → `ScalarLinear(default, Track, −14) ≈ 1.585` applied to every voice on top of the decoders' Spotify gain — **pass 4: `EngineNormalization = NormMode.Off` (`Playback.Audio.cs:145,483`) — D6** | `Playback.Audio.cs:336-337,1770,1840`; engine `AudioSources.cs:19-24`, `MediaEffects.cs:177`, `PcmAudioPlayer.cs:241-245,598` | defect | DEGRADES | Playback.Audio.cs (H) | fixed (build+tests green) | Decoders own the gain: seed `Normalization = Off` (or `ReferenceLufs = TagReferenceLufs`); pure fact `ScalarLinear(default, seeded, ref) == 1` |
| G-105 | A FLAC can play at up to +30 dB: with catalogue gain 0, `OpenBody` reads the Ogg-only gain at byte 144 of the head (STREAMINFO/SEEKTABLE bytes in a FLAC); `NormalizationFactor` clamps only to ±30 dB — **pass 4: `GainFor` reads the header gain for Ogg only, `SaneGain` ±30 dB (`Spotify.Audio.cs:298-320`)** | `Spotify.Audio.Stream.cs:1214-1216`; `Playback.Audio.cs:790-801`; `Spotify.Audio.cs:267-268` | defect | DEGRADES | Spotify.Audio.Stream.cs (F) | fixed (build+tests green) | Read the head gain only for Ogg; pure `GainFor(fmt, catalogueGain, head)` with a FLAC head fixture (0) and an Ogg one (−3.5) |
| G-106 | Ogg peak cap not applied (`GainLinear` passes no peak; the float at head byte 148 never read) — **pass 4: peak reaches every decoder (`Playback.Audio.cs:1065-1067,1430,1918,3793`)** | `Playback.Audio.cs:784-785,796-801`; `Spotify.Audio.cs:751-754`; `Spotify.Audio.Stream.cs:813` | wiring | DEGRADES | Spotify.Audio.cs, Stream, Playback.Audio.cs | verified (tests) | `HeadGain` → (gainDb, peak) carried on `Body`/`Opened`; `GainLinear(gain, peak)`; AudioAdapterTests with a real header |
| G-107 | An Ogg track is un-normalized when the head GET fails on an uncached file (gain baked at `TryOpen`) — **pass 4: chunk 0 teaches gain + peak when no header was at hand (`Stream.cs:1550-1558`); the Vorbis adapter re-reads after the header packets (`Playback.Audio.cs:1477`)** | `Spotify.Audio.Stream.cs:1184-1216`; `Playback.Audio.cs:1030-1034` | defect | DEGRADES | Spotify.Audio.Stream.cs | fixed (build+tests green) | Read gain from the first decrypted range before returning `Opened` (bounded), or accept a late gain; `GainFor` empty-head fact |
| G-108 | A device format change leaves the session silent: the pump posts `DeviceLost`, `DoDeviceLost` returns unless the owner is Foreign, nothing reloads; deck stays "Playing" in silence (0.2.9 soft-reloaded at the saved position) — **pass 4: `ReportDeviceReload` on a graph rebuild (`Playback.Audio.cs:2874-2881,2937-2941`), reducer `DoDeviceReload` → `ReloadHost` (`Playback.Transitions.cs:739-757`)** | `Playback.Audio.cs:2142-2166`; `Playback.cs:1926-1928`; engine `PcmAudioPlayer.cs:1402,1637` | parity | DEGRADES | Playback.cs (G), Playback.Audio.cs (H) | fixed (build+tests green) | `AudioSignal.DeviceReload` → `fx.Load` current row with `LoadFromMs = Position(now)`, intent kept; Step fact + `DeviceRecoveryPlan` table facts |
| G-109 | `wavee:episode:` (WaveePodcast provider) routes into the Spotify track ladder → TRACK_V4 fails → `Restricted/Unavailable`; nothing maps an episode enclosure to an `ExternalUrl` FileChoice — **pass 4: `RouteOf(provider)` + `PodcastSource` → `ExternalChoice` (`Playback.Audio.cs:3018-3073`, `Spotify.Audio.cs:786-788`). Remains: `Playback.Audio.PodcastEnclosure` (:2997) never assigned (M, WP-5.M) — episodes still answer Unavailable at runtime** | `Playback.Audio.cs:2241-2243,2259-2285`; `Spotify.Audio.cs:655-663`; `Entities.cs:747-748` | wiring | BLOCKS (Wavee podcasts) | Playback.Audio.cs (H) + the podcast owner's episode store (M) | fixed (part) | Resolve enclosure URL → `FileChoice(ExternalUrl, Mp3)` → `Open(in choice)`; routing switch test |
| G-110 | Local files cannot play: `LocalPath`/`ModuleOpen` seams never assigned; `PlayableLinks.ProbeFlac`, `Keys.LocalFileTags`, `wavee:local:art:` not written; `Shell.OnFilesDropped`/`PickAndPlayFile` unassigned | `Playback.Audio.cs:2226-2230,2301-2302`; `Shell.Host.cs:103`; `Shell.Overlays.UI.cs:56` | deferred | BLOCKS (local files) | Modules.cs/Modules.Host.cs (T, W6), Playlist.Page.cs local arm (O, W5), Drag.cs rules (L) | open | Set `Playback.Audio.LocalPath` at boot from `PlayableLinks`; pure `ProbeFlac` (flac plan §6) + probe fact over `Fixtures/flac`; install the drop/pick verbs |
| G-111 | Silent sink (`--fake`/`--silent`): session was never connected, unpaced, transport routed to `s_player`, seek zeroed the start offset, fake load probed WASAPI — **pass 3: the silent session now reaches `Ended` — `PacedSilentEndpoint.Follow(mixer)` drops the drained mixer's silence so the buffer empties (test `A_followed_endpoint_drains_the_tail…`); the 1 ms wake while stopped fixed by the orchestrator. The underlying ENGINE rule is G-135** | now: `OpenSilentSession` connects (`Playback.Audio.cs:677-688`), `PacedSilentEndpoint` (`:307,321,347-366,713-714`) | defect | BLOCKS (the `--fake` playing-bar gate) | H-W H2 | fixed-unverified | `PlaybackSilentSinkTests`: a 3 s fake track ends in ~3 s; pause/resume reach the session |
| G-112 | Next track is fully opened at the current track's LOAD (head, resolve, key, range 1, tail, 30 s ring) and never re-armed: the "ending soon, unprepared" nudge posts a plain `Position`; a failed `TryPeek` at load never retries; queue/shuffle/repeat changes neither re-prepare nor invalidate the slot; `Spotify.Audio.Prefetch` (warm-only) has no caller — **pass 4: reducer `ArmNext` prefetches at load and prepares in the endgame (`Playback.Transitions.cs:584-631`), re-arms on queue/shuffle/repeat (`Playback.Host.cs:468-475`); pump `EndingSoon` once per load, re-arms after a seek back, `Prepare` replaces/ignores, `CancelPrepared`, `Prefetch` (`Playback.Audio.cs:537-575,2544,2573,2659-2665`)** | `Playback.cs:1417,1474,2055-2061,1625-1631`; `Playback.Audio.cs:2011-2037,374-379,1721-1753`; `Spotify.Audio.Stream.cs:1231-1249` | wiring | DEGRADES (memory, wrong-track joins) | Playback.cs (G), Playback.Audio.cs (H) | fixed (build+tests green) | `AudioSignal.EndingSoon`; reducer prepares when `dur − pos ≤ EndingSoonMs`; re-emit `PrepareNext`/`CancelPrepared` on next-row change; pump replaces the slot when the id differs; at load only `Prefetch`. Step facts **2026-09-16 refill batch:** two rows warmed (`Effects.Prefetch` + `Prefetch2`, farther first), prefetch allowed while Parked, `HeadCache` 2 → 4; `SessionOnline` re-issues prefetches dropped offline. |
| G-113 | The gapless join frame comes from catalogue `duration_ms`, not the voice's exact end (`ExactVoiceEndFrame`); Ogg `ExactFrames` is almost never known at open (tail queued after range 1's Content-Range; `_tail` picked up only by `Seek`) → a gap/overlap of (duration_ms − true length) — **pass 4: `JoinFrameExact` + `ActiveExactFrames` (engine exact end else `VorbisAudioDecoder.LateExactFrames`, `Playback.Audio.cs:2293-2341`); prepare waits ≤ 2 s for the tail; commit uses the exact boundary. Runtime `exact>=1` owed** | `Playback.Audio.cs:604,1078-1080,1289-1290,1768-1769,1812,1857,1937-1938`; `Spotify.Audio.Stream.cs:830-833,1033-1040`; engine `VoiceLengthIsExact/ExactVoiceEndFrame` | defect / deferred | DEGRADES | Playback.cs `GaplessJoinClock`, Playback.Audio.cs; engine (no API to update a voice's total after open) | fixed (build+tests green) | `JoinFrameExact` overload when `pcm.VoiceLengthIsExact`; wait for `TailGranule` (≤ 2 s) before `PrepareAsync` |
| G-114 | No shared read-ahead budget: each `Body` pins its whole slot ring at construction (up to 16 MiB + 256 KiB POH for the 600 s tier / FLAC24), and B's body is built at A's start → ≈33 MiB pinned per lossless session (0.2.9: 24 MiB total / 12 per stream); rings not pooled; read-ahead tier fixed at construction, `metered` never passed, throughput sampled once — **pass 4: 24 MiB shared budget + 64-slot pool (`Stream.cs:146-231`), `Promote` grows the joined ring, metered flag per open (installed `Diagnostics.UI.cs:79`). Remains: the tier is re-evaluated only at `Promote` (accepted for 0.3)** | `Spotify.Audio.Stream.cs:451,477-493,513-522,783-801,1222-1223`; `Playback.Audio.cs:1730` | deferred | DEGRADES | Spotify.Audio.Stream.cs (F), Playback.Audio.cs `Prepare` (H) | fixed (part) | Process-wide slot budget shared by active + prepared, slots pooled; pass metered; re-evaluate the tier in `TryPlan`. Pure `SlotCount` under a budget |
| G-115 | Expired CDN URLs never re-resolved inside a `Body` (`_mirrors` fixed at open; all-refused only drops the cache entry + 250 ms backoff) → after a long pause / for a prepared track / a seek past the TTL, dead URLs retry until the 8 s starve — **pass 4: all-refused → `RequestResolve` off the fetch thread, 5 s backoff, swap and retry (`Stream.cs:1447-1451,1484-1513`)** | `Spotify.Audio.Stream.cs:765,783-788,938-941`; `Spotify.Audio.cs:404-445` | impl | DEGRADES | Spotify.Audio.Stream.cs (F) | fixed (build+tests green) | On all-mirrors-refused run `Resolve(fileIdHex)` on an api thread, swap `_mirrors`, retry; fake source refusing the first URL set |
| G-116 | Seek inside the clear head still cancels the fill and issues a probe (`Body.IsResident` unused); every non-final probe starts a 512 KiB fill the next probe cancels; `Ring.Retarget` sizes the probe from its aligned start (RingSource compensates) — **pass 4: head-resident seek = epoch bump only (`Stream.cs:1352-1357,827-849`), fill held until `ResumeFrom`, probe end aligned up (:832)** | `Spotify.Audio.Stream.cs:563-580,640-652,684-694,861-872`; `Playback.Audio.cs:1400-1409` | defect | DEGRADES | Spotify.Audio.Stream.cs (F) | fixed (build+tests green) | Head-resident probe → epoch bump only; `_seeking` flag gates `TryPlan`; `end = AlignUp(probe + bytes)`. AudioStreamTests: seek inside head = 0 requests; far seek with 2 probes → requests == 3, cancels == 0 |
| G-117 | Superseded loads are never cancelled (`LoadCoreAsync`/`PrepareCoreAsync` CTS never cancelled; up to 4×20 s deadlines run to completion, start range 1 + tail, then close); the Host header claims `Audio.Load` cancels the previous epoch — **pass 4: `Supersede` holders (`Playback.Audio.cs:169-185,293`), `Load` cancels loads and prepares (:523-525), `Stop` (:631-633), `ReleaseBlockedChain`** | `Playback.Audio.cs:541-543,1729`; `Playback.Host.cs:36-37` | defect | DEGRADES | Playback.Audio.cs (H) | fixed (build+tests green) | Keep `s_loadCts`; `Load()` cancels + replaces synchronously; pass the token into `Open`. Holder fact: ten loads → nine cancelled |
| G-118 | A 200 reply to a non-zero `Range` is accepted as that range (external podcast hosts ignoring Range corrupt every range after the first) — **pass 4: `IRangeReply.Start` + `RangeReply.SkipFor` applied in `FetchRange` (`Stream.cs:238-270,1419-1424`). Remains: a host that ignores Range serves only the first 4 MiB (`MaxSkipBytes`); later ranges read as dead mirrors, an external body has no re-resolve → `Fault.Network` after ~4 min of a 128 kbit/s episode; ranges before 4 MiB re-download from byte 0** | `Spotify.Audio.Stream.cs:97-100,907-918` | defect | DEGRADES | Spotify.Audio.Stream.cs | fixed (part) | Accept 200 only at `start == 0`, else skip or refuse; fake 200-only source |
| G-119 | External episode needs a HEAD with Content-Length (`HeadLength` → 0 → `Fault.Network`) — **pass 4: `ProbeLength`: HEAD then `Range 0-0`; unnamed length opens on the duration estimate (`Stream.cs:1862-1885`)** | `Spotify.Audio.Stream.cs:1162-1165`; `Spotify.Audio.cs:697-711` | defect | DEGRADES | Spotify.Audio.Stream.cs | fixed (build+tests green) | Fall back to `Range: bytes=0-0` + `Content-Range` |
| G-120 | A fully cached file still needs storage-resolve (`!mirrors.Ok` fails the open even when `disk.KnownSize` covers the file; plan §5.4: 0 requests) — **pass 4: a cached open asks neither head nor resolve; mirrors resolve on the first miss (`Stream.cs:1789-1817,1833,1403-1408`)** | `Spotify.Audio.Stream.cs:1173-1212` | defect | DEGRADES | Spotify.Audio.Stream.cs | fixed (build+tests green) | Lazily resolved mirrors on the first miss; extend `Cached_file_needs_no_request` to the open |
| G-121 | The engine's seek does not cancel a read blocked in the ring (`SeekFrameAsync` applied after the current decoder read returns → a seek during a stall waits the 8 s bound); `RingSource.Cancel` DISPOSES the body | engine `PcmAudioPlayer.cs:1136-1165`; `Playback.Audio.cs:1417-1421` | engine | DEGRADES | `..\fluent-gpu` (`ICancellableAudioSource.InterruptPendingRead()`), Playback.Audio.cs | open — engine issue to file | Engine calls the interrupt before `SeekFrameAsync`; app maps it to an epoch bump |
| G-122 | Audio cache root moved without migrating 0.2.9's (`%LOCALAPPDATA%\Wavee\cache\audio` vs `…\Wavee\Wavee\Cache\audio`, auto budget ≤ 128 GiB): the old root is orphaned, never trimmed; no Storage tab to see or clear it — **pass 4: `DiskCache.DefaultDirectory() => DirectoryUnder(Platform.LocalFolder)` = `%LOCALAPPDATA%\Wavee\Wavee\Cache\audio` (`Stream.cs:358-362`) — 0.2.9's root (CORRECTION: this row had the two paths reversed; 0.2.9 was `…\Wavee\Wavee\Cache\audio`). Storage-tab half: see G-192/WP-6.R** — **pass 4c: the Storage card sizes/clears/relocates the cache (`Settings.Host.cs:93`). Remains: `Settings.LicenseKeyCount/ClearLicenseKeys` unassigned (D9)** | `Spotify.Audio.Stream.cs:155`; `_old/.../AudioBodyDiskCache.cs:30-34`; `Platform.cs:232-237`; `Settings.UI.Storage.cs` (14 lines) | parity | DEGRADES | Spotify.Audio.Stream.cs `DiskCache.DefaultDirectory` (F); Settings Storage (R, W6) | fixed (build+tests green) | Reuse the 0.2.9 root (keys already identical) or delete once; pure path test |
| G-123 | The persistent audio key cache (0.2.9 DPAPI `LicenseKeyDiskCache`, 30 d, 4096) became a 256-entry in-memory cache; `audio.cache.keys.enabled` orphaned → every launch re-requests/re-derives every key; a disk-cached replay still pays the key RTT | `Spotify.Audio.cs:449-519`; `Platform.cs:233`; `_old/.../LicenseKeyDiskCache.cs:14-50` | parity | DEGRADES | Spotify.Audio.cs (F) + Platform protector | open — decision D9 (stores derived keys) | Port behind `AudioKeyCacheEnabled` or delete the key; pure round trip with a no-op protector |
| G-124 | The audio backend boots ON THE UI THREAD: `Load`/`SetVolume` call `Boot()` from `Execute`, and `WasapiPcm.CreateBackend → ProbeFormat` opens an endpoint synchronously (seconds on a cold Bluetooth/USB device) — **pass 4: backend built on the chain only (`Playback.Audio.cs:364-377,394-422,782`); warmed at +3 s by `Shell.Host.cs:426-437` (`AudioWarmDelayMs = 3_000`)** | `Playback.Audio.cs:302-315,363-366,420-422`; `Playback.Host.cs:392,399`; engine `WasapiPcm.cs:32-35` | parity | DEGRADES | Playback.Audio.cs (H) | fixed (build+tests green) | Boot on the pump chain; warm after the startup quiet period; `SetVolume` stores intent before boot |
| G-125 | The format label is interned off the UI thread (`OpenSessionAsync` on the pump's pool thread calls `Entities.Strings.Intern`; the `StringTable` is single-writer, C1) — **pass 4: label interned through `ToUi` (`Playback.Audio.cs:903-909`)** | `Playback.Audio.cs:614`; engine `StringTable.cs:38` | defect | DEGRADES | Playback.Audio.cs, Playback.cs | fixed (build+tests green) | Post a label code and intern in `DoFormat`, or pre-intern the fixed label set at boot |
| G-126 | No local output-endpoint enumeration or routing: `EnumerateEndpoints` never assigned (0.2.9 enumerated via COM), `Select` only persists, the engine's WASAPI leaf opens only the default endpoint; `FilterForPicker`/`Fold(FormFactor)` not ported | `Playback.Audio.cs:140-203`; engine `WasapiAudioDevice.cs:405-475`; `Stage.UI.cs:758,809` | engine / parity | DEGRADES | `..\fluent-gpu` (`WasapiAudioDevice(deviceId)` + MMDevice enumeration), Platform/Playback.Audio.cs (H) | open — decision D18 | Engine issue for 0.3.x; ship the enumerated list + persisted selection with a "follows the Windows default" note (0.2.9 parity); `DevicePickerItemsTests` |
| G-127 | WASAPI `FormatSink`/`DiagSink` unwired → device-open failures and the negotiated format silently dropped — **pass 4: `WasapiAudioDevice.FormatSink ??= / DiagSink ??=` before the first open (`Playback.Audio.cs:399-400`)** | grep; `Platform.Host.cs:88` | wiring | DEGRADES | Platform.Host.cs (S) or Playback.Audio.cs Boot (H) | fixed (build+tests green) | Wire both to `Log` before the first open |
| G-128 | Live radio: ADTS sniffs to `Unknown` → Vorbis adapter; no AAC decoder (0.2.9 `AacSampleSource` over `MfAacDecoder`), no reconnecting live HTTP ring; a dropped stream reads as Ended (skips the station) | `Playback.Audio.cs:2463-2468,774-779,2640-2735` | parity | DEGRADES | Playback.Audio.cs (H) + module host (T, W6) | open | `AacAudioDecoder` over `FluentGpu.Media.Windows.MfAacDecoder` with 8-frame priming; live wrapper with reconnect budget; drop → `Fault.Network` |
| G-129 | Spotify MP3 skips 167 bytes on the format's say-so (`HeaderBytesFor(Mp3) = 0xa7`); librespot applies the header only to Ogg, go-librespot opens MP3 at 0 — would lose ID3/Xing/LAME | `Spotify.Audio.cs:158-163`; SYMPH/WEBFLAC reports | decision | DEGRADES (MP3 episodes) | Spotify.Audio.cs, Stream | open — decision D19 | Sniff decrypted chunk 0 (ID3/0xFFEx at 0 → 0; OggS at 0xa7 → 0xa7); verify on one real Spotify MP3 episode |
| G-130 | Lossless rung unreachable and metered cap ignored: Settings ▸ Playback is a stub (nothing sets `playback.quality = 3`), `PreferredQuality()` never applies `MeteredQualityCap`, no `NetworkPolicy`/metered detection — **pass 4: `PreferredQuality` applies `Platform.Network.EffectiveQuality()` per open (`Spotify.Audio.cs:734-738` → `Platform.Settings.cs:139-162`). Settings ▸ Playback rung UI: see WP-6.R verification** — **pass 4c: rungs gated on `Spotify.Audio.CanDerive` (`Settings.UI.Playback.cs:78,250,273`); the Lossless rung stays unreachable until D3 installs a deriver (G-014)** | `Settings.UI.Playback.cs` (14 lines); `Spotify.Audio.cs:643-647`; `Platform.cs:193` | deferred | DEGRADES | Settings.cs/Settings.UI.Playback.cs (R, W6), Platform.cs `NetworkPolicy.EffectiveQuality` (S), Spotify.Audio.cs | fixed (build+tests green) | Fourth rung offered only when `CanDerive`; `EffectiveQuality(user, cap, cost)` table + test |
| G-131 | Per-track format override and the drawer's format ladder not wired (no override map on the Host; `SpotifySource` always uses the global quality; `Track.Drawer.cs` empty); FLAC badge shows "FLAC 24-bit" not "FLAC 24/44.1", bitrate never published | `Playback.Audio.cs:2268,2791,2827,2281,614,2220-2221` | deferred | COSMETIC | Playback.Host.cs (G), Playback.Audio.cs (H), Track.Drawer.cs (M, W5) | open | uri→format map on the Host; `Open(uri, quality, …)` with the override; publish the opened decoder's label + kbps |
| G-132 | Crossfade, EQ, normalization toggles have no settings UI (applied only from `SeedFromSettings` at boot; crossfade only from the palette; a normalization change applies from the next track at best) — **pass 4c: EQ/crossfade rows → `Playback.Audio.SetEqualizer/SetCrossfade` (`Settings.UI.Playback.cs:358`); `Settings.ApplyNormalization ??= SetNormalization` (per D6 applies from the next track)** | `Playback.Audio.cs:330-337,784-785`; `Shell.Palette.cs:288`; Settings stubs | deferred | DEGRADES | Screens/Settings*.cs (R, W6) + `Playback.Audio.SetNormalization(bool)` | fixed (build+tests green) | Bind rows to `SetCrossfade`/`SetEqualizer`/`SetNormalization` |
| G-133 | Stream `Stats` and pump `Metrics` doors (heads, resolves, cache hits, first-audio ms, seek latency by kind, xruns, gapless exact/degraded, decode ×realtime) — **pass 3: `Playback.Audio.Metrics.Read()` compiles against the probe** | `Stream.Stats` landed (`Spotify.Audio.Stream.cs:96-109`); `Playback.Audio.Metrics.Read()` is read by `Diagnostics.Probe.cs:463` and the app assembly compiles, so H1 is in the uncommitted `Playback.Audio.cs` | impl | COSMETIC (blocks measurement) | H-W F1/H1 | verified (build) | The `ogg320.wh`/`gapless-album.wh` runs are the check (`xruns==0`, `exact>=1`) |
| G-135 | ENGINE: `PcmAudioSession` moves `Playing → Ended` only when the mixer has no voices AND the sink buffer is empty, but `RtRenderOnce`/`RenderBlock` keep writing the finished mixer's silence into the sink, so a buffered sink whose audio thread keeps up never empties — the silent endpoint needed a Wavee-side workaround (G-111) and, by code reading, the WASAPI device's `Write` refills the buffer every period too, so the LAST track of a real queue (no prepared successor) would stay `Playing` forever and `Ended → Advance/EndOfQueue` never fires. Not yet observed on a device; no engine test covers Playing → Ended with an audio thread and a buffered endpoint | silent-Ended report; engine `PcmAudioPlayer.cs:1376-1377`; `PlaybackQualityTests.cs:131` (ends from Buffering only) | engine | DEGRADES (likely BLOCKS end-of-queue on a device) | `..\fluent-gpu` `PcmAudioSession` (+ a test with a buffered fake endpoint) | open — engine issue to file | Stop rendering once the mixer is finished and no commands are pending, draining like the pause path; or end once played frames reach the frames submitted at mixer-finish. Verify on the box with `ogg320.wh` (`next` at the last track) |
| G-134 | Audio tidy-ups: prepare overlap hard-coded Audio→Audio (`AllowCrossfade(Audio, Audio)`); decoder buffers allocated per track (192 KiB pinned window, 256 KiB Ogg buffer, 0.3–0.7 MB Vorbis tables → LOH/POH churn); refused head splice re-reads the same offset instead of restarting at 0; `NoPage` probe window lands without reading one more window; `ApplySkip`/`SetSkip` legacy path kept; `NU1701` NoWarn outlived FlacBox; `VorbisComment` reader not shared; `OpenBody` parallel head/resolve/key and `Prefetch` untested; `--flac-probe`/`--vorbis-probe` absent; empty `Cancel()` on the file and ICY sources — **pass 4: done — `Prepare(row,id,kind)` + `AllowCrossfade(activeKind, kind)`, `SetSkip/ApplySkip` deleted, `OpenSeams` injected (`Stream.cs:1751-1764`), cancellable module/ICY sources. Not done: splice restart at 0, NU1701, shared Vorbis/FLAC comment reader, `--flac-probe`/`--vorbis-probe`, FLAC buffer pooling** | `Playback.Audio.cs:1748,1047-1049,978-989,1303-1307,553-571,2421-2423,2550,2793`; `Wavee.csproj:14`; `Playback.Audio.Flac.cs:196,251`; `AudioStreamTests.cs:123-596` | defect / deferred | COSMETIC | Playback.Audio.cs, Flac/Vorbis, csproj, tests (H/F/S) | fixed (part) | Pass `PlayableKind` with `PrepareNext`; pool two adapters' buffers; splice restart; `NoPage` read-ahead; delete `SetSkip`; drop NU1701 if clean; shared comment reader; inject seams into `OpenBody` + the cold-start fact; probe arms in W6 |

### 2.6 Video (Wavee host + surfaces) and the engine rework

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-140 | The reducer never drives the video host: `Execute()` calls only `Audio.*`; `Audio.Load` opens an audio session whatever the `LoadKind`; `Playback.Video.Load/Stop/Play/Pause/SetVolume` have no caller; the video element's play/pause/seek, SMTC, thumb buttons and media keys all reach the AUDIO host — **pass 4: `Execute` routes Load/Stop/Pause/Resume/Seek by `s_hostKind` (`Playback.Host.cs:507-513,547-599`); video loads resolve on an api thread and fall back through `AudioSignal.VideoUnavailable`. Unreachable in production until G-141's caller exists** | `Playback.Host.cs:383-411`; `Playback.Audio.cs:363-369,515-557`; `Video.UI.cs:175-188`; `Playback.Os.cs:274-290,501-507` | wiring | BLOCKS (video) | Playback.Host.cs + Playback.cs (G) | fixed (build+tests green) | Route `Load` by `LoadKind` (Video → resolve source then `Video.Load(src, epoch, fromMs)`), and `Stop/PauseHost/ResumeHost/Seek/Volume` by `s_state.Kind`. Step+Execute facts with fake audio/video seams |
| G-141 | Nothing ever produces `PlayableKind.Video`: `MediaSwitch.KindOf` has no caller; the placement toggle writes only `Video.State.Surface` and posts nothing to Playback; no `Effects` video slot; a null manifest resolve has no failure path → every surface sits on poster + "Loading…" forever — **pass 4: `KindOfRow` (`Playback.Transitions.cs:550-558`), `DoVideoPlacement` switches hosts at the carried position (:767-778). Remains: `Playback.SetVideoPlacement` (`Playback.Host.cs:903`) has NO caller — `Shell/Video.Host.cs` never posts it (B7)** | `Playback.cs:472-475`; `Video.Host.cs:87-105`; `Playback.Host.cs:592`; `Video.UI.cs:282-311,480-481,656,845,992` | wiring | BLOCKS (video) | Video.Host.cs (K: post `Input.VideoPlacement(active)` on the `IsActive` edge), Playback.cs (G: fold `Kind = KindOf(active && HasVideo, isLocal)`, emit `Load` at `Position(now)`), Playback.Host.cs | fixed (reducer) / open (K caller) | Plan §6.2 G1–G3; on null resolve `FoldAvailability(false)` + `Notify.Say`. Step facts: placement on for a HasVideo track emits `fx.Load` kind Video with `fromMs = pos`; off emits Audio |
| G-142 | Video faults never recover or fall back (0.2.9: one retry per uri, then audio fallback when Connect-originated, else toast + `NotifyVideoUnavailable`); 0.3 folds `Failed` generically, the watchdog just posts a fault — **pass 4: one retry then demote to audio at the position with `fx.VideoDemoted` (`Playback.Transitions.cs:784-797`). Remains: `Playback.OnVideoDemoted` (`Playback.Host.cs:603`) never installed — the demotion is silent (B7)** | `Playback.Video.cs:653-657,737-744` | parity | DEGRADES | Playback.cs (G), Video.Host.cs (K) | fixed (reducer) / open (K install) | One re-resolve + reload; on a second fault demote placement, `Load` Audio at the carried position, `Notify.Say`. Step sequence fact |
| G-143 | `ConnectVideoFacts` never published (one extra PutState when a badge lands); `track_player` derives from `snap.Kind`, never Video → remote controllers never see the video offer — **pass 4: late-badge offer `ObserveVideo` → `Input.VideoOffer` → `DoVideoOffer`; encoder writes `associated_video_id` + `media.*` (`Spotify.Decode.Connect.cs:700-710`). Remains: `signals` switch-to-video and `restrictions.disallow_signals` (G-240); no wire test for the video keys** | `Video.cs:675-700`; `Spotify.Decode.Connect.cs:670` | parity | DEGRADES | Playback.cs (G), Video.cs (K) | fixed (part) | Observe `(uri, HasVideo, gid)` on the Video group landing; on a real gain `Announce(PlayerStateChanged)`; unit on `Observe` + Step fact |
| G-144 | Manifest/licence request headers diverge from 0.2.9: `Origin` = spclient base, no `Referer`, no `Accept: */*`; licence POST lacks `Content-Type: text/xml; charset=utf-8` and `SOAPAction` → server rejection likely (unverified) | `Playback.Video.cs:792-801,1112-1120`; `Spotify.Api.cs:329-345`; `_old/.../SpotifyLicenseRelay.cs:32-35`, `SpotifyVideoResolver.cs:39-41` | parity | BLOCKS (likely) | Spotify.cs `HeaderSet` bits, Spotify.Api.cs (F), Playback.Video.cs (H) | open | `XpuiOrigin` (Origin + Referer xpui) and `ContentSoapXml` header bits on both routes; pure header-fold unit |
| G-145 | Licence relay blocks the CDM thread (synchronous `Api.Send` inside the delegate, completed `ValueTask`; the branch runtime calls it synchronously on the KeyMessage thread); and the relay is per-load (`RelayForward` reads `s_activeRelay` set in `ResetPerLoad`) so a prefetched/next-track licence sees null or the wrong relay | `Playback.Video.cs:544,563-574,1103-1107,1121`; engine `ProtectedVideoRuntime.cs:612-648` | defect | DEGRADES | Playback.Video.cs (H) | open | `new ValueTask<LicenseResponse>(Task.Run(...))`; pass the source's relay per open (`MediaOpenOptions.LicenseRelay`), delete `RelayForward`; fact with a blocking fake sender |
| G-146 | No manifest memo/single-flight/prefetch: every resolve refetches; `PrefetchSchedule` unused; 0.2.9's 10-min `SingleFlightMemo` + `VideoCdnWarm` absent (warm ≤ 300 ms target unreachable) | `Playback.Video.cs:786-819`; grep | impl | DEGRADES | Playback.Video.cs `Prefetch` (H), Playback.cs `Effects.PrefetchVideo` (G) | open — decisions D15 (Q3 prefetch policy) | Plan §6.2 H2/G1: memo keyed by manifest id, `PrepareAsync` on the protected backend, emitted from Step per `Decide`; Step fact "ending-soon emits PrefetchVideo why=next" |
| G-147 | Manifest duration fallback stricter than 0.2.9 (both `*_time_millis` required on the content object; 0.2.9 read the root, start default 0) → zero duration → `ToDescriptor` null ("no video") | `Playback.Video.cs:855-857,944-947`; `_old/.../SpotifyVideoManifest.cs:91-97` | parity | DEGRADES | Playback.Video.cs | open | Port the fallback; `Manifest.Parse` fixture with end-only on the root |
| G-148 | Local video overrides and module clear video never become a source (`VideoSource.LocalFile/Clear` have no callers); quality setting not applied live (`SetPreferredHeight` uncalled), metered cap absent | `Video.cs:844-1093`; `Settings.UI.Video.cs:55-285`; `Playback.Video.cs:306-323,556-559` | wiring | DEGRADES | Playback.Video.cs resolver (H), module owner (T), Settings.UI.Video.cs, Platform.cs | open | Resolver tier 1 `Overrides.Decide(uri)` → `LocalFile`; module `form: video` → `Clear(url, isLive)`; settings write → `SetPreferredHeight` |
| G-149 | Host behind the video plan: carried position applied after start (no `OpenAsync(source, MediaOpenOptions)` — exists only in the video branch); no `Phase/FirstFrame/Buffered` signals (poster drops on player presence); seek planner fed an empty index, `KeyframeMs` never passed, no `seek.plan/seek.done`; audio hand-off (`AudioHandoff`, `Effects.AudioCut/VideoGo`, `FadeOutAndPark`) unimplemented (silence across the switch); always-on `VideoLog` lines and the native `LogSink` not routed; descriptor misses `SegmentLengthMs/DurationMs/SegmentsStartWithKeyframe`; `PlayReassertBudget` arm still present | `Playback.Video.cs:93-102,179,277-288,388,431,463,464,638-647,669-680,723-724,1002-1022`; `Video.UI.cs:172-193`; `Playback.Video.Rules.cs`; `Playback.cs:501` | deferred | DEGRADES | Playback.Video.cs (H), Playback.cs/Host (G), Playback.Audio.cs (H), Video.UI.cs (K) — after the engine (G-162) | open | Plan §6.2 H1–H6, G1–G4, K; plan §4.1 tests |
| G-150 | Placement preference never persisted/restored (`video.placement` key, `PlacementPersistence.Save/Load` uncalled; boots `Music`); availability not re-folded at track boundaries (`FoldAvailability` only from the rail's OpenAt) | `Platform.cs:178`; `Video.cs:634-665,732-754`; `Rail.UI.cs:214`; `Video.Host.cs:141-142` | parity / wiring | DEGRADES | Video.Host.cs, Shell.Host.cs, Shell.UI.cs effect (K/I) | open | Load at shell boot into `Preferred`, save in `Commit`; effect on `Playback.Current` + the row's Video group → `FoldAvailability(hasVideo)` with `commitUpgrade` at boundaries; `BoundaryFold` pure helper + facts |
| G-151 | Module watch-page stage never mounts (`Video.WatchStage` no caller; `ActiveStagePlayable` only cleared) | `Video.UI.cs:69-99`; `Shell.UI.cs:736` | wiring | DEGRADES | Modules.UI.cs (T, W6), Shell.UI.cs | open | The watch page mounts `WatchStage(uri, poster, onPlay)` and writes `ActiveStagePlayable` while active |
| G-152 | `Wavee.csproj`'s native build target is stale against the video branch: `Inputs` name the deleted `PlayReadyNative.cpp`, omit `PrRuntime/PrLicense/PrSession.cpp`; arch rule duplicated instead of importing the branch's `PlayReadyNative.props`; the shipped DLL follows whichever engine root resolved (base has only `out\arm64`) — **pass 4: `Assert-PlayReadyNativeExport` → `Test-PeExport` (`Wavee.Build.psm1:393-396`, `Wavee.Build.Tests.ps1`). Remains: `Wavee.csproj` native `Inputs` still name the deleted `PlayReadyNative.cpp` and no `PlayReadyNative.props` import (B7, after D30)** | `Wavee.csproj:75-96`; engine `ops/tools/playready-native` listing; `FluentGpu.WindowsApp.csproj:29-40` | defect / decision | DEGRADES (x64 DRM; rebuilds every build) | Wavee.csproj (O*), release preflight | fixed (preflight) / open (csproj) | Import `PlayReadyNative.props` when present; `Inputs = *.cpp;*.h;build.cmd`; release script asserts both arch DLLs export `FgPrRuntimeCreate` |
| G-153 | K3 leftovers: fullscreen exit button duplicated locally instead of K1's chrome; keyframe-accurate scrub preview impossible (no index/phase from the host); pop-out and fullscreen titles do not update when metadata arrives late; the lyrics store sets `Prefs.Lyrics.Available` for untimed docs (stale "second line" toggle); stage-rect probe, lyrics inspector, `--lyrics-advance-probe` → W6 — **pass 4c: lyrics inspector rules/dialog/bundle in `Diagnostics.UI.cs` (`LyricsInspectorRulesTests`), button mounted `Rail.UI.cs:262`. Remains: lyrics probes P2/P4 (G-275), the K3 leftovers** | K3 report; `Lyrics.Host.cs:1729` | deferred | COSMETIC | Video.UI.cs, Lyrics.UI.cs (K), Diagnostics (S, W6) | fixed (part) | Title signals from the row's Identity; `Available` only for timed docs |
| G-160 | ENGINE (in flight): x64 native DLL is the old ABI (2026-08-29 `FgPlayReady*` exports) while the managed side P/Invokes `FgPr*`; only arm64 rebuilt — **pass 3: both arches rebuilt clean, 23 exports present (`e4a88e3ab`); Wavee cannot load it until the branches combine (G-164)** | `C:\wavee\fluent-gpu\ops\tools\playready-native\out\x64\` | engine | BLOCKS (x64 video) | engine video agent + release script | fixed (engine tests green) | `build.cmd x64`; release gate on the export |
| G-161 | ENGINE (in flight): protected-session seams (`Phase`, `FirstFrameEpoch`, `GetKeyframes/GetBuffered`, `SeekAsync(pos, mode, hint)`, `PrefetchAsync`) live on `IProtectedVideoPlayer` inside `ProtectedMediaSession`; `IMediaPlayer` only gained `OpenAsync(source, options)` → Wavee cannot reach any of them — **pass 3: reachable as `MediaPlayer.Session as ProtectedMediaSession` → `.Player` (`Phase`, `FirstFrameEpoch`, `IsSeeking`, `LastSeekLandedMs`, `IndexEpoch`, `GetKeyframes/GetBuffered`, `ForwardBufferedMs`), `.SeekAsync(to, mode, keyframeMs)`; `ProtectedMediaBackend.PrepareAtAsync/PrepareAsync/StartLicense`, `ProtectedVideoRuntime.LogSink`; `MediaOpenOptions {StartPosition, StartPaused}` — B7 codes against these** | engine `IProtectedVideoPlayer.cs:42-93`; `IMediaPlayer.cs:138-144` | engine | BLOCKS (plan targets) | `FluentGpu.Engine/Media/Playback/{IMediaPlayer,MediaPlayer,IVideoSurfaceSession}.cs`, `ProtectedMediaSession.cs` | fixed (engine) | Optional session interface (phase/first-frame signals, index epoch, `CopyKeyframes/CopyBuffered`, `SeekAsync(to, mode, hint)`) surfaced by `MediaPlayer` + a public prepare entry; facade fact over `FakeProtectedVideoPlayer` |
| G-162 | ENGINE (in flight): the branch is two wip commits + uncommitted edits (`PrInternal.h`, `PrLicense.cpp`, `build.cmd`, two csprojs, untracked `PlayReadyNative.props`), never built or run; no VerticalSlice protected gate; the real-DLL runtime test (plan §4.2) absent; native behaviours (attach while licence pending, `MESourceStarted` start time, swapchain handle, `SetSource("")` detach, WinRT HTTP `Completed`) unproven; KeyMessage HttpHeaders dropped before the relay — **pass 3: branch builds Debug + Release, `FluentGpu.Windows.Tests` 464/464, Engine.Tests 409, `check-canon` 0, pushed `e4a88e3ab`. Not possible: a VerticalSlice protected gate (VerticalSlice cannot reference WindowsApi — a unit test covers the zero-alloc pump) and a real-DLL test. Owed on the box: Media Lab bring-up/attach/first-frame, detach with no successor (`SetSource("")`), video→song→video re-attach, scrub/far-seek timings, 30 s teardown, and the real-stream checks (paused open, switch at 1:23 never 0:00, keyframes vs Spotify's edit list, pending licence at attach, swap-chain handle stability)** | `git status` in the engine; reports a2af9ef, a551b61 | engine | BLOCKS | `C:\wavee\fluent-gpu` | fixed-unverified | Plan §6.3: Debug + Release, `FluentGpu.Windows.Tests`, VerticalSlice (+ the protected gate), `check-canon.ps1`, both DLL arches, then §4.3 manual gate with `[video.native]` lines |
| G-163 | ENGINE defects found by the engine tests (in flight): PSSH parse overflows on a hostile KID count (`LicenseCachePolicy.cs:145-149`); Rebuffer/Opening publish `BufferingInfo.None` (`ProtectedMediaSession.cs:432-445,624`); demuxer ignores `elst`, merges traks, reads the first `traf` only (`CencMediaSource.h:287-293,384-385`); licence events match handle 0 loosely and Expired skips `AcceptCompletion` (`ProtectedVideoRuntime.cs:530-556`); `WaitBufferedAsync` cancel leaks waiters (`:453-461`); `IsAvailable` checks load not exports (`:109,774-790`) — **pass 3: PSSH bounds, buffering rule, edit list + per-track demux + `trex` defaults + `senc`/`saiz`/`saio`, handle-0 events dropped, expiry must name its handle, `WaitBufferedAsync` returns `Task<bool>`, `IsAvailable` checks every export; plus three bugs found while wiring (prefetch wait registered late, prepared session never pumped, deferred start after a failed init)** | as cited | engine | DEGRADES | engine files | fixed (engine tests green) | The written tests name each fix |

| G-164 | Wavee builds against `fluent-gpu-pin` @ `eba1285b2` (`feat/0.3-tray-seams`) while the finished video rework sits on `feat/0.3-video-runtime` @ `e4a88e3ab` — the two branches have never been combined, so B7 cannot code against G-161's API and the tray host cannot run on the video branch; combining them and re-pinning is a decision for Christos | branch heads in `C:\wavee\fluent-gpu`; the pin worktree | decision / engine | BLOCKS (video) | `..\fluent-gpu` (Christos), then `fluent-gpu-pin` + `EngineRoot.local.props` (O*) | open — decision D30 | Rebase `feat/0.3-video-runtime` onto `feat/0.3-tray-seams`, run the engine gates once (Debug + Release, Engine/Windows/SourceGen tests, VerticalSlice, `check-canon`), fast-forward the pin, then B7 |

### 2.7 Sidebar platform and pane (files: `Shell/Sidebar*.cs`)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-170 | Several "+"/drop commits are silent no-ops without the `LibraryWrites` seam (header "+" drop, folder "+" drop, `CreatePlaylistFlow`, `NewPlaylistInFolder` just `?.Invoke`; `accepts:` still arms the cue) — contract says a null seam refuses with a toast — **pass 3: `SidebarCreateDropRules` + `SidebarDropRefusal.WritesUnavailable` + `PaneView.RefuseWrite` (B6)** | `Sidebar.UI.Drop.cs:421,432-433,443,452`; `Sidebar.UI.cs:1968`; `Sidebar.UI.Menus.cs:573` | defect | DEGRADES | Sidebar.UI.Drop.cs, Sidebar.UI.cs, Sidebar.UI.Menus.cs (J) | fixed (tests green) | Gate `accepts` on the seam member; route the null arm through `RefuseDrop(Unavailable)`; extract `SidebarCreateDropRules.Accepts(payload, seams)` + test |
| G-171 | Rename folder passes the OLD name (no dialog/inline editor); playlist row menu has no "Access ▸" (builder exists) — **pass 3: rename opens `Controls.Prompt`, `SidebarFolderRename.Commit`; Access ▸ still absent (no ActionId for collaborative; O/I)** — **pass 4: `Spotify.Library.RenameFolder(newName)` (`Spotify.Library.cs:672-684`)** | `Sidebar.UI.Menus.cs:236-263,282-283,591-598`; `Actions.UI.cs:339-353` | impl / deferred | DEGRADES / COSMETIC | Sidebar.UI.Menus.cs (J); Access after owner O ships set-public/collaborative | fixed (rename end to end) / open (Access) | Shared text-input dialog (0.2.9 `FolderActions.Rename`); insert `Actions.Menu.Access` for owner rows |
| G-172 | Recents / "Jump back in" feeds hard-wired empty although `Shell.History.Store` and `Shell.PlayLog.Recency` exist (binder returns `Array.Empty`, `SidebarRecency.Empty`, no `lastPlayed`, versions pinned 0) — **pass 3: `ISidebarRecencyLogs`/`ShellRecencyLogs`, `SidebarRecencyFold`, both log versions as trigger lanes (B6)** | `Sidebar.Host.cs:2631-2632,2679,2687-2691,2975-2976`; `Shell.Host.cs:612-634,967-997` | wiring | DEGRADES | Sidebar.Host.cs binder + Sidebar.UI.cs `PumpBinder` (J) | fixed (tests green) | Build `SidebarRecency` from History entries, played contexts from PlayLog, pass `Recency` as `lastPlayed`; fold both Version signals into triggers |
| G-173 | Queue / Now Playing sections never refresh (sources read playback only inside `Fill`, never raise `Changed`; `PlaybackEpoch` 0; pump reads no playback/queue signal); queued episodes skipped; top-tracks/concert rows never re-plan when their fetch lands — **pass 3: bigger cause found — the pane's plan key only followed the entries cell; `SidebarProjectionBinder.InputVersion` + the pump reads `Playback.Current`, the queue, tracks, episodes, top tracks, concerts; `FromEpisode` (B6)** | `Sidebar.Host.cs:2020-2081,2119-2152,2312-2347,2986`; `Sidebar.UI.cs:117-143` | wiring | DEGRADES | Sidebar.UI.cs `PumpBinder`, Sidebar.Host.cs (J) | fixed (tests green) | Read `Playback` current/queue version signals; add `Tracks.Changed`/`ArtistPopular`/`FeedSection`/`Concerts` reads; `FromEpisode` mapper |
| G-174 | New releases and Concerts have no fetcher (`RegisterAll` called with null `SidebarFeedFetch`/`SidebarConcertsFetch`); unresolved releases dropped instead of stubbed; concert rows navigate to NotFound (bare `spotify:concert:` uri as route key; table parses `concert:<bareId>`) — **pass 3: concert rows keyed by route, venue/date on the row, unresolved release → dimmed stub, `Report` always notifies; `Sidebar.NewReleasesFetch`/`ConcertsFetch` seams exist but the feed owners (N/R) must still set them** | `Sidebar.UI.cs:107`; `Sidebar.Host.cs:2203-2377,2343-2345`; `Shell.cs:159,276` | wiring / defect | DEGRADES | Sidebar.Host.cs (J) + Concert/WhatsNew owners (N/R) | fixed (part) | Key rows `Shell.NameOf(Shell.For(uri))`; wire both seams when the feeds land; dimmed stub row for an unresolved release |
| G-175 | `sidebar-layout.json` moved from `%LOCALAPPDATA%\Wavee\WaveeMusic\` (0.2.9 and 0.3's other shell documents) to `%LOCALAPPDATA%\Wavee\` with no migration → upgraders lose layout, pins, V3 order, expanded folders; `SidebarStoreTests.cs:429` pins the new path — **pass 3: back under `WaveeMusic\` via `PathUnder` (B6)** | `Sidebar.Host.cs:1070`; `_old/.../SidebarLayoutStore.cs:110-114`; `Shell.Host.cs:634,798,999` | parity (data loss) | DEGRADES | Sidebar.Host.cs, SidebarStoreTests (J) | fixed (tests green) | `Path.Combine(Platform.LocalFolder, "WaveeMusic", "sidebar-layout.json")` |
| G-176 | Persisted pane scalars written but never read back (consequence of G-004); no sidebar flush on shutdown (last edit inside the 300 ms debounce lost; `Sidebar.Flush`/`FlushNow` have no caller) — **pass 3: `Sidebar.Shutdown(timeoutMs)` called from App.cs:40; the persisted scalars read back via `Boot` (G-004)** | `Sidebar.Host.cs:89-111,256-282,335-338,783-787,1243,1369`; `Shell.UI.cs:222-238,505` | wiring | DEGRADES | App.cs (G-004), Shell.Host.cs finally (G-012) | fixed (build) | Covered by G-004/G-012; store fact Commit → FlushNow → WaitForWrites → file on disk |
| G-177 | `Controls.NowPlaying` seam never installed (sidebar ♪ and the now-playing card overlays never light; `RefreshPlayState` returns before subscribing when null); unresolved bound-action label falls back to "Manage extension" not the raw `providerId.actionId` (ch 26 §7.2); the registry itself is G-027 | `Controls.Art.cs:70`; `Sidebar.UI.cs:1200-1216`; `Sidebar.UI.Menus.cs:687-715` | wiring | DEGRADES | Playback/Controls owner (G/L: NowPlaying), Sidebar.UI.Menus.cs (label) | open | Install at boot; `label = binding.ActionKey` when unresolved; W22 fact |
| G-178 | Rail folder flyout subscribed to the never-published static `Sidebar.Entries` cell → an open flyout did not refresh | orchestrator changed `Sidebar.UI.Rail.cs:302` to `Binder?.Entries` (commit `201cd247`) | defect | DEGRADES | Sidebar.UI.Rail.cs (J) | verified (build) | Consider deleting the static `Sidebar.Entries` (no legacy paths); manual: create a playlist with the flyout open |
| G-184 | J2 leftovers: after "Remove section" inside the options popover the subject clears but the popover stays open ("Select a section…"); palette drops and card-menu moves go straight to `Sidebar.Dispatch` so a refused one is silent; three chapter-vs-code conflicts followed 0.2.9 (Divider Behavior group, 280-wide palette search, design labels) need ch 26 amended; the item picker's Library tab reads `Binder.Entries` rather than ch 26 §7.2's `User.Me` edge scans; Esc inside an unhandled text box also leaves the page — **pass 3: popover closes when its subject is gone (`OptionsSubjectWatch`), palette drops refuse with a caption, `Edit.Apply` toasts rejections (B6); the three chapter conflicts still need ch 26 amended** | J2 report items 2–3, "Left out" | defect / parity | COSMETIC | Sidebar.Customizer.UI.cs (J), ch 26 §10 | fixed (build) | Close the popover when `Edit.OptionsSection` goes null; route refused palette drops through the refusal caption; strike/amend the three parity items |
| G-179 | Binder subscription goes stale on `Entities.Switch` (signals captured at effect time from the replaced scope) — latent until login switches scope (now does) | `Sidebar.UI.cs:120-132`; `Entities.cs:1751,1775-1786` | defect | DEGRADES → BLOCKS after login | Entities.cs (A: a scope-epoch signal), Sidebar.UI.cs (J) | open | Read a `Signal<uint>` scope epoch first in `PumpBinder`; test: switch scope, stage a rootlist edge → Revision advances |
| G-180 | Every table change anywhere forces a full three-pass library rebuild (unconditional `Invalidate()+Sync()` on `Playlists/Albums/Artists/Shows.Changed`, materialising `EntityUri.Text` per row) — **pass 3: unconditional `Invalidate()` gone; `SidebarLibraryFingerprint` per-row versions + feed counters; not render-census checked** | `Sidebar.UI.cs:128-131,146-151`; `Sidebar.Host.cs:2687-2713` | defect (perf) | DEGRADES | Sidebar.UI.cs, Sidebar.Host.cs (J) | fixed (tests green) | Fold per-table publication counters into `SidebarBinderTriggers`; drop the unconditional Invalidate; render-census check |
| G-181 | Customizer leftovers after J2 (done, 2,612 lines; Ctrl+Z/Y and Esc landed; `PropertyPanel` compiles): template miniature has no deterministic sample (`MiniaturePlaylist/Artist/ShortcutCount` unassigned — needs `SeedFake`, G-015); palette extension group reads the local `SidebarDataSourceTable`, no `WaveeExtensionRegistry` (the nine sources are not in `Actions.Registry`) | `Sidebar.Customizer.UI.cs:73-79`; `Sidebar.Host.cs:2362-2407`; `Actions.cs:727` | deferred / impl | COSMETIC | Entities.Fake.cs (Q), Actions.cs + Sidebar.Host.cs (I/J) | open | Register the nine sources into `Actions.Registry` (first wins); `SidebarSourcesTests` `RegisterAll` fact |
| G-182 | Design chooser / Settings design picker / Setup page absent (`SidebarDesignGating` no consumer; no Appearance row; W21, parity 80) — **pass 4c: three-card picker via `SidebarDesignGating` (`Settings.UI.Appearance.cs:448-473`) + "Customize sidebar"; launch check owed** | `Sidebar.Modes.cs:178-187`; grep `sidebar` in Screens → none | deferred | DEGRADES | Screens/Settings*.cs, Setup*.cs (R, W6) | fixed (build+tests green) | Port the three-card picker + row form |
| G-183 | Sidebar test debt: no store test seam (`Sidebar.Store` bound to the real profile → binder integration facts unported, tests create `%LOCALAPPDATA%\Wavee`); dropped 0.2.9 facts (SidebarPinSyncTests 12, SidebarBootstrapTests ~20, `RootlistDropScenarioTests`, op-order half of `RootlistSlotToOpTests`, populated now-playing, `PinnableRoutes`); no pure rule for "missing seam refuses"; `SidebarPaneInvariant` has no consumer; binder drag-freeze API (`BeginDragSession/…`) uncalled; Liked cover ignores the treatment pref; stale test headers — **pass 3: `Sidebar.UseStore` seam, freeze API deleted (D5), missing-seam rule + tests, binder facts, `PinnableRoutes` fact, headers fixed (B6). Still open: pin-sync tests (G-049), bootstrap tests, drop-scenario tests (B2), `SidebarPaneInvariant` consumer (S), Liked cover (O); the static `Sidebar.Entries` cannot be deleted while `Sidebar.Customizer.UI.cs` reads it in 3 places** — **pass 4: B2 restored `SidebarPinSyncTests` 14, `RootlistDropScenarioTests` 16, `RootlistSlotToOpTests` 9. Remains: bootstrap tests, `SidebarPaneInvariant` consumer (S), Liked cover (O), the static `Sidebar.Entries`, and the stale header `SidebarDropTests.cs:163-167` (says no `TryBuildMove` exists — `Spotify.Encode.TryBuildMove` does)** | `Sidebar.Host.cs:69,1066-1070,2589-2604,2738-2741`; `SidebarProjectionTests.cs:36,44-54`; `SidebarDocTests.cs:18-21`; `Sidebar.UI.Rows.cs:492-505` | impl / decision | COSMETIC | Sidebar.Host.cs (`Sidebar.UseStore`), tests (J/Sonnet) | fixed (part) | `UseStore` with a lazy default; port the facts as their production halves land |

### 2.8 Shell, platform, screens, tests, ops

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-190 | Wave 4.5 not started: `Detail.cs`, `Detail.UI.cs`, `Track.UI.cs`, `Track.Table.cs` are skeletons (`Track.cs` has columns only); the ten `detail.rail.*` keys orphaned; the rail/page convergence facts absent — **pass 4c: all WP-4.5 files filled (`Detail.cs` 1,050, `Detail.UI.cs` 1,535 + `Hero` 812, `Track.UI.cs` 1,375, `Track.Table.cs` 2,098 + `Chrome` 1,407, `Track.Rules.cs`, `Track.Menu.cs`), `Detail.RailPolicy.KeysFor` reads the rail keys. Blocking: G-259 (reveal ramp overflow → blank rows); G-258 (tab label); deferred items 23/32/41/42/45/49/52/55-56 (O/M, WP-5)** | file sizes; `Platform.cs:154-168`; `Prefs.cs:249` | deferred | BLOCKS (every detail page) | M, Wave 4.5 | fixed (part) — the gate renders its frame but NO ROWS | Work package WP-4.5 (§4) |
| G-191 | Wave 5 not started: 34 Entities page/UI files are skeletons (Album, Artist/Discography, Browse, Concert, Episode.UI, Home ×6, Playlist, Queue.UI, Recents, Search, Show, Track.Drawer, User ×7, Entities.Fake) → 28 of 30 route kinds paint not-found; `Rail.QueueBody`/`Stage.QueuePaneBody`, `Omnibar.Source`, `Shell.OnNewPlaylist/OnNewFolder`, `Drag.LikedChipArt`, `Controls.PreSave`, `Controls.Library` unassigned — **pass 4: `Rail.QueueBody`/`Stage.QueuePaneBody` installed (`Queue.UI.cs:62-63`, App.cs:54); Queue.UI 1,075 lines, `Queue.Rules.cs`; the other 34 Wave-5 files are still skeletons (Album, Artist/Discography, Browse, Concert, Episode.UI, Home ×6, Playlist, Recents, Search, Show, Track.Drawer, User ×7)** | file sizes; `Shell.cs:123-162`; `Rail.UI.cs:46`; `Stage.UI.cs:60`; `Shell.cs:1910`; `Shell.Palette.cs:239,242`; `Drag.cs:500-505`; `Controls.cs:58-68` | deferred | BLOCKS | M/N/O/P/Q, Wave 5 | open — queue part fixed | Work packages WP-5.* (§4) |
| G-192 | Wave 6 not started: Settings ×7, Setup.UI/Host, Diagnostics ×4, Feedback ×2, ReleaseNotes.UI/Host, Modules ×3 are skeletons; `Shell.ReportDialogOpener`, `Setup.Runtime.Host` unassigned; 55 settings keys never referenced, 36 read with no writer; what's new, after-update dialog, logs panel, FPS overlay, lyrics inspector, crash-reports card absent — **pass 4c: R/S screens all filled (Settings ×7 tabs, Setup, What's new, Feedback, Diagnostics ×4, `Platform.Settings.cs`); seams assigned in `Diagnostics.Install`. Remains: Modules ×3 (T); unassigned `Settings.MetadataStats/ClearMetadataCache` (C), `LicenseKeyCount/ClearLicenseKeys` (D9), `Update.Host.BeforeApply` (T), video-override toast "Manage" → `Settings.OpenVideoOverrides()` (K, D41); the ReleaseTool build break (G-265)** | file sizes; `Shell.Overlays.UI.cs:80,104,201`; `Setup.UI.Runtime.cs:100,206,249`; `Platform.cs:70-330` | deferred | BLOCKS (Settings, sign-in) / DEGRADES | R/S/T, Wave 6 | fixed (part) | Work packages WP-6.* (§4); every key gets a writer or is deleted; `SettingsCatalogTests` |
| G-193 | `Controls.RouteForUri` unassigned → every rich-text anchor is styled but does not navigate (needs only the existing route codec) — **pass 3: `Controls.RouteForUri` at `Shell.UI.cs:60` (B8)** | `Controls.cs:63` | wiring | DEGRADES | Shell.UI.cs `InstallUi` (I) | fixed (build) | `RouteForUri = uri => Shell.NameOf(Shell.For(uri))`; ControlsTests fact |
| G-194 | Friends panel host absent: `Rail.Friends.SetActive/Refresh` unassigned, state starts Offline, Retry inert; `FriendFeed/FriendPresence` have no caller or decoder | `Rail.cs:374-380`; `Spotify.Api.cs:1220,1231` | impl | DEGRADES | a friends host (SHELL, K) + Spotify.Decode.Pathfinder.cs (E) | open | Host polls the feed while active, stages friend edges, assigns both seams; RailTests pin `SurfaceFor` |
| G-195 | `Stage.NowPlayingMenu` unassigned (right-click on the stage identity raises nothing) — **pass 3: `Actions.NowPlayingMenu()` + `Actions.InstallUi()` (thin; the full track menu stays WP-4.5's)** | `Stage.UI.cs:64` | wiring | COSMETIC | Actions.UI.cs install (I) | fixed (build) | Build from the action platform at install |
| G-196 | `Shell.SeedPalette` unset (falls back to `Tok.Use(kind)`; 0.2.9 `Tok.Use(Tok.NeutralPalette, kind)`) — **pass 3: `Design.SeedPalette`/`Design.Install()` from `Shell.InstallUi` (B8)** | `Shell.Host.cs:107,316-317`; `_old/Wavee/Design/WaveeTheme.cs:15,28` | wiring | COSMETIC | Design.cs (L) | fixed (build) | Install `SeedPalette = k => Tok.Use(Tok.NeutralPalette, k)` |
| G-197 | I1 leftovers: `RouteNoted` seam stands in for `NavigationFrameWatch` (no 0.3 home); activity rows have no kind glyph/status chip/track list (no journal, ch 19 DATA GAP 3); omnibar rows have no "⋯"/context menu; tab DnD depends on `LibraryWrites.ResolveTracks/DepositTracks` (G-043); the offline strip is a deliberate divergence from ch 29 that still needs an issue; masthead insets hard-coded 36/32; duplicate tint owner (`Shell.TintOwner`/`Claim` vs L's `TintOwnership`); stale comments (`Video.State.HostCapability` doc; the seam comments in Playback/Spotify/Store/Palette still say App.cs sets the marshaller) — **pass 3: updater-seam duplicate collapsed (B8); the `TintOwner`/`TintOwnership` duplicate, the masthead insets, `NavigationFrameWatch` and the stale seam comments remain** — **pass 4c: `NavigationFrameWatch` subscribed (`Diagnostics.UI.cs:64-65`, `Diagnostics.Host.cs:265`). Remains: `Shell.TintOwner/Claim` duplicate (`Shell.cs:1299-1313`), masthead insets, stale seam comments** | I1 report; `Shell.UI.cs:70-72` | deferred / defect | COSMETIC | Shell.UI.cs, Shell.cs (I), Diagnostics (S) | fixed (part) | Delete the duplicate tint owner; port NavigationFrameWatch in W6; fix comments |
| G-198 | `WaveeVersionInfo` not ported: the csproj stamps Codename/Channel/PackageVersion/Commit/FeedRelease/UpdateBaseUrl/StoreId "read back through `WaveeVersionInfo.Parse`" but the type does not exist → About, crash headers, update checker, what's-new gating have no version source (sidebar uses `AssemblyName.Version`) — **pass 4c: `WaveeVersionInfo` (`Platform.Settings.cs:530`) read by `Platform.Version` (`Platform.Host.cs:239-257`), `PlatformWave6Tests`** | `Wavee.csproj:22-53`; `Sidebar.Host.cs:1407-1415` | impl | DEGRADES | Platform.cs (S) or ReleaseNotes.Model.cs (R; keep in a named partial so `Wavee.ReleaseTool` can include it) | fixed (build+tests green) | Port `_old/Wavee.Core` `WaveeVersionInfo` (+`NormalizeUpdateBaseUrl`) with tests |
| G-199 | Platform (S, W6) not written: `NetworkPolicy` + 3 composition sites, ambient power policy (G-086), `Clock.SeedEpoch`, `AppLocaleBootstrap` engine half (G-017), ch 28 D3 `RuntimeStatus/ProvisioningOutcome/ProgressFraction/ShortHash`, `LogCapturePolicy`, `WaveeLogSessions`; the log facade builds strings on filtered hot paths (no interpolated-handler overloads); `Platform.cs` at budget → needs `+Platform.Settings.cs` — **pass 4c: `Platform.Network` + `EffectiveQuality`, `LogCapturePolicy` (`Platform.Settings.cs:273`), `WaveeLogSessions` (:325), ambient power. Remains: the log facade still builds strings on filtered hot paths; `Platform.Host.cs` 852/700** | `Platform.cs:1003-1013,653` | deferred | DEGRADES | Platform.cs / Platform.Host.cs (S) | fixed (part) | Per ch 29 §9.10 / ch 28 D3 with ported tests |
| G-200 | Tray: W-B done (`Platform/Tray.cs` 380, `TrayTests` 58, four keys default `WhileHidden`, six `.ico` + generator, 29 loc keys merged); engine seams E1–E8 committed `eba1285b2` (Engine.Tests 434/434, Windows.Tests 289/289) but VerticalSlice and the 12-step engine on-box list NOT run (`RunFrame`'s gate changed; `AppInstance.GetActivatedEventArgs` for `StartupTask` needs live validation). Absent: `Platform/Tray.Host.cs` (W-C), the `Playback.Os.Publish` hook, `Shell.Host.cs` edits (`--tray` in `ParseArgs`, `StartHidden`, `CloseRequested`, `Tray.Host.Shutdown()`, `RegisterStartup(args)`, `WakeFor` before showing on a redirect), `DeepLinkKind.Quit` / `wavee://quit`, the Settings ▸ General group (W-D), the `(#152)` CHANGELOG bullet, the 13-step app on-box checklist; the tray plan's §4.1/§8 still say the default is Always (code follows §13) — **pass 3: `Platform/Tray.Host.cs` (506) + `TrayHostTests` (8), `Shell.Host.cs` (`Arm`, `StartHidden`, `SyncStartupRegistration`, `Tray.Host.Shutdown()` first in the tail, wake rule in `ApplyDeepLink`), `DeepLinkKind.Quit` + `wavee://quit`, `Tray.Host.Publish` in `Playback.Os.Publish`, `ForegroundLatch`. Still owed: W-D Settings ▸ General group (R, must call `Tray.Host.OnSettingsChanged()`; Start-on-login row → `Shell.SyncStartupRegistration()`), the `(#152)` CHANGELOG bullet, the 8-step app + 12-step engine on-box checklists, VerticalSlice, the plan's §4.1/§8 default text; `Tray.cs` 401/380** — **pass 4c: W-D group at `Settings.UI.cs:418-467` (every writer → `Tray.Host.OnSettingsChanged()`; Start on login → `Shell.SyncStartupRegistration()`). Remains: `(#152)` CHANGELOG bullet, both on-box checklists, VerticalSlice, the tray plan's §4.1/§8 text** | `ls`; tray reports; `Shell.Host.cs:163-224,340,355-359`; `Shell.cs:439`; tray plan :233,:1248 vs :1567 | deferred | DEGRADES (tray) | W-C (U + I/H edits), W-D (R, W6), plan text | fixed (W-C, W-D) / open (checks) | Tray plan §7.4–7.6, §12.2; then VerticalSlice + both checklists |
| G-201 | Tests missing for real CORE: `Screens/ReleaseNotes.cs` (1,912 lines, compiled by `Wavee.ReleaseTool`, 0.2.9 had `ChangelogParserTests` + `ReleaseNotesStoreTests`), `Entities/Recents.cs` (five 0.2.9 files), video host rules (`Video.Plan`, `Manifest.Parse/ToDescriptor/Address`, `StartWatchdog`; 0.2.9 `VideoSwitchPolicyTests`, `SpotifyVideoManifestTests`), `Playback.Video.cs` host, `Notify.Host.cs`, `Home.Host.cs`, `Entities.Fake` — **pass 4c: ReleaseNotes ×3, Update, Feedback, SetupWizard, SettingsCatalog, DiagnosticsCore, ProbeArms, PlatformWave6, EntitiesFake tests exist. Remains: `RecentsTests` (WP-5.P), `VideoHostRulesTests` (B7), `Notify.Host`/`Home.Host` tests** | grep `Wavee.Tests` | impl | DEGRADES (release gate runs untested rules) | `Wavee.Tests/ReleaseNotesTests.cs`, `RecentsTests.cs`, `VideoHostRulesTests.cs` (Sonnet) | fixed (part) | Port the 0.2.9 files against the 0.3 names |
| G-202 | Over-budget files not split (`Playback.cs` 2,221/1,790; `Playback.Audio.cs` 3,010/2,470; `Spotify.Decode.cs` 1,811/1,600; `Spotify.Decode.Connect.cs` 737/520; `Playback.Video.cs` 1,137/1,100; `Sidebar.cs` 6,397/5,000; `Sidebar.Host.cs` 3,116/2,500; `Shell.Host.cs` 1,313/1,000; `Video.cs` 1,412/900); `ReleaseNotes.cs` 1,579/1,000 while the ReleaseTool compiles exactly two partials by name (a third breaks it) | `wc -l`; `Wavee.ReleaseTool.csproj:23-24` | deferred | COSMETIC | owners | open | Name partials on the next edit that crosses 30 %; add a ReleaseTool build to the wave gate |
| G-203 | Release/ops: `Wavee.Version.props` still 0.2.10 "Breaker" build 11; `launchSettings.json` carries `--real-backend` and no `--fake`; csproj comments claim loc/cover loading; `publish-wavee-aot.ps1`, `Wavee.Store.psm1`, `pack-wavee-msix.ps1` reference `src/apps/Directory.Build.props`, `ops/diag/README.md`, `Wavee.Core\Versioning\StoreVersion.Quad` (none exist); `bench-wavee.ps1` launches `--fake --perf-bench` (neither parsed) and passes its output dir through `$env:WAVEE_BENCH_OUT` (banned env-var switch); no `ops/headless/**` — **pass 3: version `0.3.0-beta.1`/Crest, launch profiles (`Wavee (live)`, `Wavee (demo)` = `--fake`), csproj comments, `publish-wavee-aot.ps1`/`Wavee.Store.psm1`/`pack` comments, bench script → `--fake --perf-bench --probe-out <dir>` (the arms themselves are still W6 S), `ops/build/README.md`; `ops/headless/**` exists. Pester 330/330 (B10)** | `Wavee.Version.props:5-9`; `launchSettings.json:5,12`; `Wavee.csproj:193-195`; `publish-wavee-aot.ps1:12-15`; `Wavee.Store.psm1:38`; `pack-wavee-msix.ps1:78`; `bench-wavee.ps1:6,40,47` | defect / parity | DEGRADES (perf gate) / COSMETIC | ops scripts, props (O*/S) | fixed (build) | `0.3.0-beta.1`/Crest; `--perf-bench --probe-out <dir>` with the probe arm; repoint the comments; `ops/headless` with H-X |
| G-204 | Release pipeline gates the PlayPlay junction (`wavee-release.ps1:241,541`) and the worktree has none; the junction can be created now that Wave 2 re-homed the audio stack, but G-014 must be answered first | `wavee-release.ps1:241,541`; handoff d :23-24 | decision | DEGRADES (release rehearsal) | O* (`.\link-playplay.ps1`) | open — decision D3 | Create the junction after D3; `-DryRun -SkipTests` |

| G-205 | The release script's new checks are untestable: `Get-PeExportedNames` (PE export-directory parser), `Assert-PlayReadyNativeExport` (opens the MSIX, asserts `FgPrRuntimeCreate`) and the `engine checkout` resolution live as script-scoped functions inside `wavee-release.ps1`, which no Pester file may dot-source (it would run the build/sign/git/gh phases); `Get-PeExportedNames` was only sanity-checked by hand against `kernel32.dll`; the engine resolution also ignores B0's `EngineRoot.local.props` — **pass 4: `Get-PeExportedNames`, `Test-PeExport`, `Resolve-EngineRoot` are `Wavee.Build.psm1` functions (:325,393,417); the script imports the module (:88); `Wavee.Build.Tests.ps1` new. Caveat: `Resolve-EngineRoot` reads the props by regex (a pin written with MSBuild property functions would come back unexpanded)** | `ops/release/wavee-release.ps1:880,940,1018,546-558`; B10 report | impl (test) | DEGRADES (release gate unverified; wrong engine asserted) | `ops/build/Wavee.Build.psm1` (+ Pester in `ops/release/tests/`), `wavee-release.ps1` (callers) | fixed (Pester 343/0/1 per B10b; not re-run this pass) | Move the pure halves into `Wavee.Build.psm1` beside `Get-PeMachine`/`Test-PeMachine` (`Get-PeExportedNames`, `Test-PeExport`, `Resolve-EngineRoot` that reads `EngineRoot.local.props` like MSBuild does); the script calls them; Pester facts over a tiny fixture PE (or the built engine DLL) and a fake EngineRoot tree |

### 2.9 Stage-B leftovers, engine UI gaps, stage-A deferrals (from the wave-4 reports, verified 20:35)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-210 | Device picker has no height cap: a dozen Connect devices overrun the window (ch 20 §9 trap, parity 81 "0.3 must") — **pass 3: `Shell.DeviceRoster.PickerMaxHeight` (window − 96 DIP) via `TemplateParts`/`ScrollEl.MaxHeight` (B8)** | `Shell.PlayerBar.UI.cs:1050-1090` | defect | DEGRADES | Shell.PlayerBar.UI.cs (I) | fixed (tests green) | Max-height scroller (window height − 96 DIP) |
| G-211 | Play-next drop has no batch cap, no "Added the first N" truncation toast, and is silent on a null seam (ch 19 #87 "nothing answers with silence") — **pass 3: cap 100, "Added the first N", warning toast when `PlayNext` is null (B8)** | `Shell.PlayerBar.UI.cs:633-645` | parity | DEGRADES | Shell.PlayerBar.UI.cs + Shell.PlayerBar.cs (I) | fixed (tests green) | 0.2.9's cap + toast; a toast when the seam is null |
| G-212 | Player-bar/overlay parity: the error title is always "Can't play this track" (`Fault` carries no message); no mosaic cover on the bar's art tile (parity 85); module playables never detected (`isModulePlayable = false`); the theme toggle is duplicated (`Shell.Overlays.UI.cs:563-566` vs `Shell.Palette.cs:249`); the update toast calls `Toast.Show` directly (bypasses the one `Notify.Say` announce rule); profile header has no email line (no `User` contact column); chip skeleton is a static fill, not a shimmer; runtime folder-picker filter names hard-coded English (`Setup.UI.Runtime.cs:358`) — **pass 3: fault → loc-key map (`PlayerBarRules.FaultTitleKey`), theme-toggle duplicate removed, update toast through `Notify.Say(title, customContent)` (B8). Still owed: mosaic cover (I), `isModulePlayable` (T), profile email (O + decode), chip shimmer (I), runtime folder-picker strings (I3's file)** | `Shell.cs:1265-1268`; `Shell.PlayerBar.UI.cs:599,607`; `Shell.Overlays.UI.cs:316,430`; I2/I3 reports | parity / defect | COSMETIC | Shell.PlayerBar.cs (`Fault` → loc-key map + test), Shell.Overlays.UI.cs, Modules.cs (T), User.cs + decode, Notify.cs (`Say` with title/content) | fixed (part) | — |
| G-213 | NPV credits section has no data: no credits edge (`EdgeTable<CreditEdge> TrackCredits`, `TrackFields.Credits`; kind 186 also dropped by the decoder, G-044) — **pass 4: `Edges.TrackCredits` (`Edges.cs:807`), kind 186 decoded (`Spotify.Decode.Traits.cs:132-164`), edge route (`Fetch.Routes.cs:363`). Remains: nothing asks for or renders credits in the NPV rail (`Rail.UI.cs`, K)** | `Edges.cs:610-612` (doc comment only); `Rail.UI.cs:454-455` | data (ch 21 G5) | DEGRADES | Edges.cs (B), Spotify.Decode.cs (E), Rail.UI.cs (K) | fixed (data) / open (UI) | Add the edge + field group; parity 16 |
| G-214 | Rail/stage rows use a local compact row instead of `TrackRow.ArtCard` and restate the pivot constants (until Wave 4.5 lands `Track.UI.cs`/`Detail.cs`); device-kind glyph order and `FriendEdge.ContextSlot` (assumed playlist) are assumptions — **pass 4c: `Track.ArtCard` exists; `Rail.UI.cs:737,745` still use the local `RailRow`, `Stage.UI.cs:192` restates the pivot constants (K → R4-4)** | `Rail.UI.cs:922,956`; `Stage.UI.cs:191`; K1 report | deferred | COSMETIC | Rail.UI.cs, Stage.UI.cs, Rail.cs (K) after WP-4.5 | open — unblocked | Swap to the shared row/band constants; key the glyph off the device-kind enum |
| G-215 | Deck: no stream bitrate/sample rate published (kbps "---", kHz shows the codec badge; ch 23 GAP 4); no blur-hash first paint (no `ImageHash` column); reduced motion does not stop tape/disc/meter/level motion in a mounted deck (parity 55); the Winamp marquee pauses when not playing (ch 23 #66 vs the idle-GPU rule) | `Deck.Faces.cs:1035-1039,1197-1201`; `Deck.UI.cs:390`; `Deck.cs:1574`; `Playback.Host.cs:140` | data / parity | COSMETIC | Playback.Host.cs + Playback.Audio.cs (H), Track.cs + decode (A/E), Deck.Faces.cs (K), ch 23 §10 | open — decision D27 (marquee) | Publish bitrate + rate with the format; `Column<StringId> ImageHash` → `Controls.Artwork(blurHash:)`; snap poses under `Design.Reduced` |
| G-216 | Immersive stage host lives in `Stage.UI.cs` (K1/K3) while ch 22 puts it in `Lyrics.UI.cs` — the coordinator settled it as K1's; the chapter is not amended | `Stage.UI.cs` (backdrop/drift/Escape/shield); ch 22 §9 | decision (settled) | COSMETIC | ch 22 §9 + plan §2 A12 note | open (doc only) | Record `Stage.UI.cs` as the host |
| G-217 | Session document has no playback section (`SessionDto` = Nav + Shell) — pairs with G-078; recent-surface pin reason and the activity/undo journal tables absent from `Store.cs` (the notification panel's Undo/Clear have no journal) — **pass 4: `SessionDto.Deck`/`SessionDeckDto`/`CaptureDeck`/`DeckSection` (`Shell.Host.cs:934-965,1099-1112`), shape rules `Shell.Host.Deck.cs:95-148`. Journal tables in `Store.cs` untouched** | `Shell.Host.cs:747-752`; grep `pin_reason` / `activity` in Store.cs → none | deferred | DEGRADES | Shell.Host.cs (I) + Playback (G); Store.cs (C) | fixed (playback section) / open (journal) | `SessionPlaybackDto`; journal tables + the Notify journal seams (G-092) |
| G-218 | `NotificationSimulator` ("Send event") absent → ch 27 parity 40b-c, ch 19 36-37/71-74 cannot be driven — **pass 4c: `NotificationSimulator` (`Diagnostics.Probe.Arms.cs:631`) → `Settings.SendTestEvent` (`Diagnostics.UI.cs:71`); simulated library activity has no journal (G-217)** | plan §2 `Diagnostics.Probe.cs` row; no code | deferred | DEGRADES (verification) | Diagnostics.Probe.cs (needs the named partial, G-016) or Diagnostics.Host.cs (S, W6) | fixed (build+tests green) | Port `_old/Wavee/App/NotificationSimulator.cs` over `Notify.Rebuild` |
| G-219 | Lyrics: the CJK and Musixmatch network sources and the DES decrypt were not ported (only Amll, LrcLib, SpotifyNative); a pre-save revoked while Wavee was closed is never unscheduled (ch 14 gap 10) | `Lyrics.Host.cs:1780-1782`; Wave-4-owner-I/K reports | parity | DEGRADES / COSMETIC | Lyrics.Host.cs (K), Notify.Host.cs (I) | open — decision D28 | Drop Musixmatch/DES, keep the parsers, CJK on demand; `RemoveGroup("wavee.release-drops")` before the launch re-derive |
| G-220 | Video overrides are not mirrored into `Track.LocalVideo`/`VideoOverride` columns (the roster changes never commit a Staging); Storage-tab sources absent (`Audio.CacheStatus()`, `Audio.Relocate(...)`, licence-key count; the resident-census row undecided) — **pass 4c: the Storage tab reads the cache through `DiskCache` (`Settings.Host.cs:93`); the census row was deleted (needs a CHANGELOG line, G-277). Remains: the video-override → `Track.LocalVideo` mirror; the licence-key seam** | Wave-4-owner-K report; grep `CacheStatus` / `Relocate` / `Census(` → none; `Store.cs:612-616` (`Store.Stats` exists) | deferred / data | DEGRADES | Entities.cs commit path + Video.Overrides.cs (A/K); Spotify.Audio*.cs (F) for the Storage sources (R mounts) | fixed (part) | Commit a Staging on `Overrides.Changed`; add the two audio calls; re-point the census row at `Entities.Census()` |
| G-221 | Owner L deferrals: the drag COMMIT half (deposit/reorder execution), `SelectionBar` commands, the `RowSwipe` wrapper + digitizer probe (`TouchCapable` defaults `true`), palette persistence (no palette table in `Store.cs`) — **pass 4c: selection-bar commands (`Track.Table.Chrome.cs:1274-1390`), drag commit via `TableProfile.Deposit/MoveRows` (`Track.Table.cs:1547-1563`). Remains: `Deposit/MoveRows` have no implementer (WP-5.O); no `RowSwipe` belt; palette persistence** | `Controls.cs:855-859`; Wave-4-owner-L report | deferred | DEGRADES | User.cs / Sidebar.Host.cs (O/J), Track.UI.cs (M), Platform.Host.cs (S), Store.cs (C) | fixed (part) | Each owner's wave; `Palette.Filler` + a palette table (G-055) |
| G-222 | Engine UI gaps: engine boxes have no accessible-name property (glyph-only buttons carry names only as tooltips; UIA `AutomationName` absent); `ProgressBar` ignores reduced motion (the player-bar top-edge sweep keeps sweeping, ch 20 parity 70) | I2/K2/L reports (UIA not inspected by the miner) | engine | DEGRADES / COSMETIC | `fluent-gpu` UIA provider, `ProgressBar` | open — engine issues to file | `AutomationName` mapped in the UIA provider; honour `Motion.Reduced` |
| G-223 | No test pins the player-bar art size (48 above Medium, 40 below) — the 56 bug I2 found was invisible to the suite (I1 added `ShellPlayerBarArtSizeTests`; verify it covers all six tiers) — **pass 4c: `ShellPlayerBarArtSizeTests` covers all six tiers (`ShellTabStripRulesTests.cs:66-78`)** | `Shell.cs:1139`; `ShellTabStripRulesTests.cs` | impl | COSMETIC | Wavee.Tests | verified (tests) | `[Theory]` over the six tiers |
| G-224 | Chapter rulings owed (doc-only): window placement not persisted (ch 18 #83 — keep 0.2.9's 1180×760); first-frame accent = OS ramp on frame 0 (ch 30 #75); `PlayLinkRules` lives in `Actions.Rules.cs` though ch 19 files it under `Modules.cs`; ch 26's three J2 conflicts (G-184); ch 23 #66 (G-215); ch 22 stage host (G-216) | miner-ui decisions 8–9, 11–13, 17 | decision | COSMETIC | the chapters' §10/§11 | open | Record each ruling in the chapter's audit log |

---

### 2.10 Pass-4 findings (round 2/3 reports verified in code, 2026-09-14)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-230 | No edge relation is ever persisted or warmed: `Store.SaveEdges<TEdge>` and `Store.RegisterEdges` have no caller, so `Store.Warm`'s six account-relation reads post pages to a null applier → a cold or offline launch shows an empty library (rootlist with folders, liked, saved albums, artists, shows, pins) until the Online sync answers (supersedes G-062's "rootlist runs not persisted") | `Entities/Store.cs:653-654,908-927,1208-1235,1364` | wiring | DEGRADES (E4 warm start; offline library) | Edges.Staging.cs `Land` → `Store.SaveEdges` per landed account relation (B); appliers registered at boot (C); rootlist payload keeps Kind/Depth/FolderName/FolderId text, pins their `PinKind` byte | open | One applier per `EdgeRelation` replaying `EdgePage` into the table (folder text interned + AddRef'd). StoreTests round trip: save rootlist with folders + pins, reopen, warm, assert markers/folder ids/pin kinds |
| G-231 | The pathfinder collector drops a wrapper element's nested list: `Collect.Item` pops everything since its mark for a node with no uri, so `artistOverview.discography.albums/singles/compilations.items[].releases.items[]` stage no `ArtistAlbums` members (only `popularReleasesAlbums` survives; B1b worked around it for `DiscographyAll` only) → the artist page loses its album/single/compilation shelves | `Spotify.Decode.Pathfinder.cs:566-591` (`if (node.Uri.IsEmpty) s.Edges.Pop(mark)` :584), :678-691, :720-730 | defect | DEGRADES (artist page shelves) | Spotify.Decode.Pathfinder.cs (E) | open | Keep the nested list's pushes for a wrapper (second mark before the nested `Collect`, or skip the pop when `wrapper`); PathfinderDecodeTests over an artistOverview fixture with wrapped releases |
| G-232 | Four fetches are deliberately sealed because their folds cannot land them: home facets (`Decode.Home` always stages on `wavee:home`), album tracks past 50 (`getAlbum` lands at offset 0), search "load more" (`Decode.Search` rewrites the whole list), album "More by" (no run) | `Spotify.Api.cs:702-708` (`ServesEdge`), :926, :1162 (`AlbumPageSize` 50), :1495 | impl | DEGRADES (home chips, albums > 50 tracks, search paging, More by) | Spotify.Decode.Pathfinder.cs folds take a subject + offset (E), then open the gates in Spotify.Api.cs (F) — WP-5.P/M | open | Decode facts at offset > 0; flipped `ServesEdge` InlineData |
| G-233 | Playlist membership refresh always does the full read: only the rootlist and recents carry a held revision (`FillRevisions` → `StringId.Empty` for playlists), `PlaylistRead` has no revision branch although `ListDiffRoute(Playlist)` exists; 0.2.9 used `/diff` | `Entities/Fetch.cs:852-865`; `Spotify.Api.Library.cs:95-96,224` | parity | DEGRADES (bandwidth/latency on large playlists) | Fetch.cs (C: a `PlaylistRevision` column feeds `FillRevisions`), Api.Library.cs (F: `PlaylistTracks` through `ReadList(ListKind.Playlist)`) | open | FetchTests fills the revision; a `ReadList` verdict fact |
| G-234 | Dead request kinds and route tables after B1b: `Spotify.Build`'s path arms for `PlaylistRead/PlaylistDiff/RootlistRead/RecentsDiff` never build a path; `FetchRoutes.For(FetchBatch…)`/`ExtensionKinds` are called only by tests; `Fetch.cs:72` still documents `For(batch)` as what the provider sends | `Spotify.cs:1575-1625`; `Fetch.Routes.cs:330`; `FetchRoutesTests.cs:17,24,159,163` | defect (hygiene) | COSMETIC | Spotify.cs (D), Fetch.Routes.cs/Fetch.cs (C) | open | Delete the arms/helpers or align the `Build` spellings with the captured routes; move the facts onto `Api.RoutesFor`/`Api.ListRoute` |
| G-235 | "New folder" never asks for a name: the seam has no name parameter and the host creates "New Folder"; 0.2.9 prompted first; the pane already has `Controls.Prompt` (rename) | `Sidebar.Host.cs:3474-3476`; `Spotify.Library.cs:634-668`; `Sidebar.UI.Menus.cs:280,568,590` | parity | COSMETIC | Sidebar.Host.cs seam signature + Sidebar.UI.Menus.cs prompt (J); `Spotify.Library.NewFolderWith(name)` (F) | open | Pure name-commit rule fact; drop-to-create keeps the default name |
| G-236 | Owner capabilities rest on two uncaptured wire assumptions: (a) rootlist `meta_items` are zipped to playlist rows by index assuming folder markers carry no MetaItem — if they do, every row after a folder takes its neighbour's owner and `StageOwned` grants/withholds owner capabilities wrongly; (b) `ListMetadataV2.source` (kind 205) is read at Full authority as an owner USERNAME, but 0.2.9 painted it as a display name — if so, every `PlaylistFields.Row` answer overwrites the rootlist's correct Thin owner with `spotify:user:<display name>` | `Spotify.Decode.Library.cs:188-202`; `Spotify.Decode.cs:802-807,821`; `_old/.../ExtendedMetadataSource.cs:471`; no rootlist-read capture in `Wavee.Tests/Fixtures` | data | DEGRADES (sidebar edit/deposit/rename affordances) | a headless capture (S/X), fixtures, then Spotify.Decode*.cs (E) | open — decision D32 | Capture a decorated rootlist with a folder + a kind-205 answer for an owned and a foreign playlist; zip over all items if markers carry MetaItems; drop case 7 if `source` is a display name. DecodeTests over the captured bytes |
| G-237 | Four scripts still ignore the engine pin: `generate-third-party-notices.ps1:42` defaults to `..\fluent-gpu` (called without `-EngineRoot` from `pack-wavee-msix.ps1:304` and `publish-wavee-aot.ps1:110`), `wavee-store-submit.ps1:563`, `perf-tour.ps1:242` → notices from the wrong engine's packages, the Store submission records the sibling engine's commit, the perf tour stamps the wrong revision | as cited | defect | DEGRADES (release provenance, notices) | those four scripts (O*/S) | open | Import `Wavee.Build.psm1` and call `Resolve-EngineRoot -RepoRoot <root> -Override $env:EngineRoot`; Pester fact via a `-RepoRoot` parameter on the notices generator |
| G-238 | `--fake` has no lyrics provider: `Lyrics.Boot` is skipped under fake and ch 31's seeded lyrics document (GAP 7) has no owner file → every lyrics surface reads "unknown" in the demo | `App.cs:60-61`; `Lyrics.Host.cs:1642`; ch 31 :1384 | deferred | DEGRADES (E13 lyrics parity) | Entities.Fake.cs seed + `Lyrics.Store.Attach(seeded aggregator)` under fake (Q, WP-5) | open | A seeded 40-line document attached through `Store.Attach`; fake-seed fact that the playing track resolves a synced document |
| G-239 | The headless host composes `Api.Boot` but not `Spotify.Library.Install` or `Lyrics.Boot`, and no `.wh` script touches the library → the smoke cannot confirm G-042/043/049/008 live | `Screens/Diagnostics.Probe.cs:286`; `App.cs:25-29`; `ops/headless/*.wh` | wiring | DEGRADES (verification) | Diagnostics.Probe.cs (S/X) + `ops/headless/library-sync.wh` | open | Install the library host in the headless composition; script steps: wait `Edges.Rootlist` Complete, print folder/pin counts; HeadlessTests parse fact |
| G-240 | Thin PutState parity: 0.3 writes the device constants, context uri, ONE track (uri, uid, `track_player`, video keys, provider "context"), speed, position, duration, playing/paused/buffering, three options and the claim fields, and OMITS everything else 0.2.9's `ConnectStateBuilder` sent — `last_command_sent_by_device_id`; DeviceInfo `is_private_session`, `audio_output_device_info`; PlayerState `context_url`, `context_restrictions`, `play_origin`, `index`, `playback_id`, `restrictions` (already_paused/not_paused/no_prev_track, speed, modes, disallow_signals switch-to-audio/video), `suppressions`, `prev_tracks`/`next_tracks` (≤ 50 each, per-row provider/uid/metadata), `context_metadata` (`player.arch=2`), `session_id`, `queue_revision`, `playback_quality`, `signals` (interact, automix-preview, speed-preview, switch-to-video), `session_command_id`, unknown #38, `options.modes`; and the current row's metadata (title, artist_name, album_title/uri, artist_uri, context/entity_uri, image_url ×4, is_queued, autoplay, skip actions, ids). Values that differ: `is_playing` false while paused (0.2.9 true), `playback_speed` 1.0 while paused (0.2.9 0.0), provider hard-coded "context" → controllers show no queue/prev/next/restrictions/"Playing from", read a pause as a stop, private session not honoured on the wire, no video switch offer | `Spotify.Decode.Connect.cs:667-742` (speed :714, provider :711), `Playback.cs:2265-2270`; 0.2.9 `_old/Wavee/SpotifyLive/ConnectStateBuilder.cs:86-116,170-286,291-318,352-463`, `_old/.../DeviceStatePublisher.cs:34-35,379-455,484-506`; `Protos/player.proto:11-42` | parity | DEGRADES (every Connect controller) | Spotify.Decode.Connect.cs encoder (E); `Playback.cs` `Snapshot` + `Playback.Host.cs` `SnapshotForConnect` (G) for the queue window, ids, revision | open | Extend `Snapshot` with a bounded prev/next window (≤ 50 each: packed id, uid via `UidBook`, provider byte), `queue_revision` (bump on `Queue.Version`/options), session/playback ids per claim, context index, private-session flag; port the capture-proven constants verbatim into the zero-alloc writer; `is_playing = Playing or Paused`, `speed = Paused ? 0 : 1`. Port `_old/Wavee.Tests/ConnectStateBuilderTests.cs` as PutStateWireTests over decoded bytes; a warm encode with 50+50 rows allocates nothing |
| G-241 | Playback slot refs are never re-resolved across `Entities.Switch`: the boot scope has market "" and Tier 0, so the first welcome ALWAYS switches scope; the launch restore (`Queue.Replace` + `Input.Restore`, before login) and any deck started from store-warmed pages land in the boot scope; after the switch the queue is empty and `s_state.Current` names a slot in the new, unrelated Tracks table → the bar shows no/wrong row, Next ends, the persisted cursor is stale (G-078's restore is lost on every real launch) | `Platform.cs:1015`; `Spotify.cs:91,386-392`; `Spotify.Session.cs:311-315`; `Entities.cs:1817-1829`; `Queue.cs:113`; `Playback.Host.Context.cs:525-545`; Playback never reads `Entities.ScopeEpoch` | defect | DEGRADES (restore lost; wrong row after login) | Playback.Host.cs / Playback.Host.Context.cs (G), Spotify.Session.cs `AdoptWelcome` (D) | open | Before `Entities.Switch` capture the deck as ids (`RestorePoint` + packed queue ids/uids/buckets); after it a host `Rebind()` re-slots `Current` from `CurrentId`, re-lays the queue, bumps the load arm — or defer `Playback.Restore` until the first welcome's scope is adopted. Test: restore in scope A, switch to B, `Snap().Current` resolves to the same id and the queue count matches **Fixed 2026-09-16:** `Rebind` rents an `EntityRef[]` alongside the packed refs and calls `EnsureRestoredIdentities` for every carried row (hot 30 at Playback priority, rest Prefetch). |
| G-242 | Context paging is not followed: a resolve lands one page (≤ 500 rows), `ContextPage.NextPageUrl` is decoded but never read; a playlist larger than its first page runs into autoplay; a station/radio context ENDS because autoplay refuses `:station:` — which matters now that Start radio plays a station context | `Spotify.Decode.Remote.cs:354-392`; `Playback.Host.Context.cs:196-202`; `Playback.Transitions.cs:119,168-174,886-908` | parity | DEGRADES | Playback.Host.Context.cs (G) + an Api page fetch (F) | open | Keep the page url with the context; in `DoEndingSoon`/`EndOfContext` ask the next page before autoplay; append as Context NextUp; page an `update_context`'s urls too. Step facts: a context with a next page emits `fx.Page` not `fx.Autoplay`; a station pages indefinitely **Fixed 2026-09-16:** the next page is asked at 75 % of the laid page / ≤ 3 ahead (`RefillKind.PageContext`); `State.PagesKnown` gates the rule until `ContextPages` lands; the endgame ask remains the fallback. |
| G-243 | Queued rows replay after Previous: `QueueOrder.Follow` re-buckets every row after the cursor with provider Queue as UserQueue and rotates it ahead, so after Previous (or a repeat-context wrap) an already-played queued row becomes upcoming again; 0.2.9 consumed a queued row once played | `Entities/Queue.Rules.cs:162-186`; called every drain `Playback.Host.cs:486-497` | defect | DEGRADES | Entities/Queue.Rules.cs (Q) | open | Mark a queued row consumed when the cursor passes it (played bit on `QueueEdge`, or re-provider as Context history); `Follow` never rotates consumed rows. QueueOrderTests: advance past a queued row, Previous twice, it is not next |
| G-244 | A newer inbound play cannot supersede an inbound play whose resolve is in flight: the hold gates every mailbox item and later intake, so the second waits up to 3 s, then the first starts and plays briefly | `Playback.Host.Remote.cs:78-100`; `Playback.Transitions.cs:539-543` | defect | COSMETIC | Playback.Host.Remote.cs / `IntakeQueue` (G) | open | Let `TryTake` release a newer `Load` past the hold, bump `s_contextSeq` so the older `StartContext` drops (`Playback.Host.Context.cs:259`). IntakeTests: two loads, only the second's Play is stepped |
| G-245 | `update_context` does not re-apply shuffle (`RunQueue` splices in context order and clears the saved unshuffle order while shuffle is on) and folds no claim/announce | `Playback.Host.Remote.cs:176-199` (`s_orderRefs = null` :195); `Playback.cs:1838-1917` (no `UpdateContext` arm) | parity | COSMETIC | Playback.Host.Remote.cs (G) | open | When `Shuffle`, `Reorder(..., shuffle: true)` the spliced NextUp run before `Queue.Replace`; IntakeTests fact |
| G-246 | `LastCommandMessageId` never ages out (set by every controller command/volume message, cleared only on sign-out) → every later PUT, including local changes minutes later and the inactive snapshot, names the last controller command; 0.2.9 dropped it after 10 s | `Playback.cs:1836,2263,2270`; `Playback.Transitions.cs:840`; `_old/.../DeviceStatePublisher.cs:283-287` | defect | COSMETIC | Playback.cs (G) | open | Stamp with `NowMs`; `Snapshot.Of` writes it only within 10 s. Step fact: a local Next 11 s after a remote pause carries 0 |
| G-247 | No re-announce after a rejected PUT: a non-OK put-state (422 "no registration") only posts `PutVerdict(false)`; 0.2.9 re-announced NewConnection (30 s rate limit) → the device stays off every picker until something else re-announces | `Spotify.Connect.cs:343-353`; `_old/.../DeviceStatePublisher.cs:340-347,368-377` | parity | DEGRADES | Spotify.Connect.cs (F) | open | For PlayerStateChanged/VolumeChanged rejections invoke the hello, rate-limited by a pure `ReannounceGate(nowMs, lastMs)`; pure fact + a dealer test with a fake 422 |
| G-248 | Outbound queue writes to a foreign device are missing: while another device owns playback, queue edits are refused with a toast; 0.2.9 forwarded `add_to_queue` and `set_queue`; the endpoint table has neither | `Playback.Host.cs:722-734`; `Queue.UI.cs:76,1036`; `_old/Wavee/Backend/OutboundEnvelope.cs:148,183` | parity | DEGRADES | Spotify.Connect.cs command body (F), Playback.cs Forward (G), Queue.UI.cs (Q) | open | `AddToQueue` (track uri) + `SetQueue` (next_tracks + queue_revision) forwarding: a local PlayNext/AddToQueue while Foreign → `fx.SendRemote`; body tests over the 0.2.9 envelope bytes |
| G-249 | `--fake`: Play on a context is inert — `PlayContext` always goes through `Api.ContextResolve`, which has no fake arm (WP-5Q removed the local-members fallback on purpose) → the demo's Play buttons log "nothing to play" | `Playback.Host.Context.cs:87-98,189-212`; `Spotify.Api.cs:1810-1814`; `Queue.UI.cs:1050,1064` (`LocalMembers` exists) | deferred | DEGRADES (the `--fake` demo gate) | Playback.Host.Context.cs (G) + Entities.Fake.cs (Q) | open — decision D33 | In a fake scope (`Entities.Current.Key.Provider == "fake"`) build rows from the seeded edges (`Queue.LocalMembers`) and call `PlayRows`; real scopes unchanged. Fact: fake `PlayContext(album)` posts one Play with the album's rows |
| G-250 | Stale comments and a missing glue test after B3b/B3c: the Connect glue header/enum doc still say the queue body is "LOGGED, not applied"; `Playback.cs:1909-1910` says set_queue/update_context/set_options are undecoded; `Queue.UI.cs:56-59,1007` say StartRadio is unset (it is set at :68); `Track.Table.cs:259` profile doc; `Track.Menu.cs:584` promises a park + toast the code does not do. No dealer-level test asserts a set_queue frame enqueues no item and reaches the intake | `Spotify.Connect.Commands.cs:18-19,45`; `Playback.cs:1909-1910`; `Queue.UI.cs:56-59,1007`; `Track.Table.cs:259`; `Track.Menu.cs:584`; `SpotifyConnectTests.cs:199-200` | defect | COSMETIC | owners F, G, Q, M | open | Rewrite the comments; add `SpotifyConnectDealerTests.A_set_queue_frame_enqueues_nothing_and_reaches_the_host_intake` via a test hook on `Playback.Host.Remote.cs` |
| G-251 | Start radio parity: song/artist radio → `Playback.StartRadio` → `PlayContext("spotify:station:…")` plays IMMEDIATELY through context-resolve (unverified for station uris, and depends on paging G-242); no success/failure toast (the "Radio started / Open playlist / Couldn't start radio" keys exist with no caller); a refused resolve is silent. 0.2.9 resolved seed → inspiredby-mix playlist, parked it after the current track, raised the toast | `Playback.Host.Context.cs:115-120`; `Playback.Transitions.cs:179-183`; `Track.Menu.cs:580-590`; `en-US.json:2082-2084`; `_old/.../PlaybackController.cs:1040-1080`, `RadioLaunch.cs:17-43`, `LiveContextResolver.cs:262-280` | parity | DEGRADES | Playback.Host.Context.cs (G), Track.Menu.cs (M), Spotify.Api.cs (F) | fixed (D34 ported, 2026-09-16): `Spotify.cs` `RequestKind.RadioSeed` + `Spotify.Api.RadioSeed`, `Spotify.Decode.Remote.cs` `RadioPlaylistUri`; `Playback.Host.Context.cs` `StartRadio(seed, done)` → `LandRadio`/`ParkRadio`; `Playback.Transitions.cs` `RemotePlan.RadioSeedUri/RadioParks/RadioAfterCurrent` + `DoSwitchContext`; `Playback.cs` `Input.SwitchContext`; `Queue.UI.cs` `RadioToast`; `Artist.Page.cs` `Radio()`; facts in `PlaybackIntakeTests`, `PlaybackStepTests`, `ClusterDecodeTests`, `SpotifySessionTests` | `StartRadio` reports its outcome through a callback; the verb raises `RadioStarted` (+ "Open playlist" when a playlist uri is known) or `RadioUnavailable`. Fact: a refused resolve raises the warning toast |
| G-252 | The lyrics request resolver polls: `ResolveLyricsRequest` re-probes an unknown track row every 50 ms for up to 4 s (each probe allocates a TCS, a closure, a posted publish-wrapped action and a `Task.Delay`, waking the UI loop ~20×/s while a lookup waits) because Track columns are UI-thread-only and nothing signals when the row's Identity commits | `Shell/Shell.Host.cs:357-387` (`LyricsResolveTimeoutMs = 4_000`, `LyricsResolvePollMs = 50`) | impl | COSMETIC (idle wakes) | Shell.Host.cs (I) | open | Signal-free waiter: a small bounded list of pending `(trackSlot, tcs)` on the UI thread, checked right after `Entities.Publish()` (the `PublishAfter.Run` tail :347-354 and the frame tick :303-313 already run there); complete when `Knows(Identity)` or the fetch settles; a timer completes null at 4 s. Fact: a pending resolve completes on the publish with zero probes |
| G-253 | `Track.Table.StartVisible` bypasses `Playback.PlayRows`: it builds refs/rows itself and calls `Queue.Replace` + `PlayNow` → (i) the user's waiting queued rows are dropped, (ii) `s_contextSeq` is not bumped so an in-flight `PlayContext` resolve (the album header's Play) lands afterwards and replaces the table's play, (iii) the saved unshuffle order is not cleared, (iv) no history/row caps (50/500), (v) no immediate publish | `Entities/Track.Table.cs:1274-1297` (`Shuffle()` :1293-1297); `Playback.Host.Context.cs:127-160` (`PlayRows` has no caller outside `PlayContext`) | wiring | DEGRADES | Track.Table.cs (M) | open | One-call swap: `Playback.PlayRows(refs, display, src.Context.Id)` with refs in visible order; update the doc at :259. Table fact: a queued row survives a table play; a pending context resolve is superseded |
| G-254 | Row-overflow pattern (the player-bar seek bug, ch 20): three more Grow boxes in rows with a fixed sibling never set `Shrink 1` — (1) `Sidebar.UI.LibraryV3.cs:842` the V3 destination rail's stack (a `ContentSized` horizontal scroller reports the full width of the words; wider than the pane → nothing scrolls, words + right chevron clipped); (2) `Track.Table.Chrome.cs:441` the table search box beside the fixed 32-DIP filter button inside a 66↔280 animated host (a long query or the expand animation pushes the button out and breaks in-field scrolling); (3) `Lyrics.UI.cs:341` the rail lyrics view placed by `Rail.UI.cs:121`'s `GrowSlot` row (the loading shimmer is ~263 DIP, the rail can be 200; dragging the rail narrower re-triggers). Low: `Shell.PlayerBar.UI.cs:1424/1430` `BarLiveLine` (clipped, invisible). Engine root cause: a ZStack reports its widest layer (`FlexLayout.cs:24-28,1322-1340`), Shrink defaults 0 (`Columns.cs:139-141`); `MinWidth = 0` is a no-op (`Clamp` ignores NaN, :313-318); and a cached row measure re-uses children's LAST laid-out width (:430,473-478,753-765) so a Grow + Shrink-0 child keeps its old wider size after maximize/restore or a rail drag back (children with `Basis = 0` are immune) | as cited; engine `C:\WAVEE\fluent-gpu-pin\src\FluentGpu.Engine\Layout\FlexLayout.cs` | defect | DEGRADES (clipping at narrow widths / after resize) | Sidebar.UI.LibraryV3.cs (J), Track.Table.Chrome.cs (M), Lyrics.UI.cs or Rail.UI.cs `GrowSlot` (K), Shell.PlayerBar.UI.cs (I) | fixed (verified 2026-09-15: all four cited sites carry `Shrink = 1f` with an explicit G-254 comment — `Sidebar.UI.LibraryV3.cs:842`, `Track.Table.Chrome.cs:441`, `Lyrics.UI.cs:341`, `Shell.PlayerBar.UI.cs:1430`) | Was: add `Shrink = 1f` (and `MinWidth = 0f` for intent) to the three; or give `GrowSlot` `Direction = 1`. Check: V3 rail at 200 DIP scrolls; table search at a 66-DIP host keeps the filter button; rail lyrics at 200 DIP after a drag back |
| G-255 | ENGINE: `FluentGpu.Controls\Media\MediaSeekBar.cs:331,380,397` (the video scrub bar `Video.UI.cs` uses) has the exact pre-fix BarSeekRail tree — stack, rail and root all Grow without Shrink; its layers measure ~22 DIP so only the cached-measure trigger applies (PiP/docked-video resize back and forth) | engine file as cited | engine | COSMETIC (until a resize round-trip) | `fluent-gpu` `MediaSeekBar` | open — engine issue to file (with G-135) | `Shrink = 1f, MinWidth = 0f` on all three boxes |
| G-256 | Adjacent layout findings: the Zune deck's title row lets a long artist name push the elapsed clock out of view (`Deck.UI.cs:1132-1140`); an aspect ratio is set on a ZStack (which ignores it) at `Rail.UI.cs:806` (rail video placeholder renders square, not 16:9) and `ReleaseNotes.UI.cs:399` (the band collapses to ~36 DIP with no poster); Grow spacers without Shrink at `Stage.UI.cs:406,596,598` could push the ⌄ exit button and the time label off-screen after maximize → restore if the cached-measure trigger is real (cheapest repro: open immersive lyrics, maximize, restore) | as cited | defect | COSMETIC | Deck.UI.cs, Rail.UI.cs, Stage.UI.cs (K), ReleaseNotes.UI.cs (R) | open | Shrink the title, move the ratio onto an inner box, `Shrink 1` on the spacers; verify the stage after a maximize/restore |

| G-258 | Album gate (a): after a session restore the chrome's route tab still reads "Home" — `RestoreNav` writes the restored route into `s_nav.Current`/`Shell.Current` but never into the `TabWorkspace`, so the tab strip (labels from `WorkspaceTab.Route`) keeps the seeded Home tab; the navigation path does it via `SyncActiveTab → Tabs.SetActiveRoute`. Also: the persisted `activeTabId` is a per-process minted id (`_nextId` restarts at 1) that can match an unrelated tab next launch | `Shell.Host.cs:735-753,627,677-682`; `Shell.Chrome.cs:434,465,582-600`; `Shell.UI.cs:1013-1021`; gate `session.json` (`activeTabId -1`); `album-gate.png` | defect | DEGRADES (every relaunch onto a non-Home page) | Shell.Chrome.cs `TabWorkspace` + Shell.Host.cs `RestoreNav` (I) | open | Pure `TabWorkspace.RestoreActiveRoute(route)` (sets the active tab's route without bumping `PinnedRevision`) called in `RestoreNav` before `TabsVersion++`; restore by pinned index, not id. ShellNavTests: `RestorePinned(empty)` then `RestoreActiveRoute(album)` → `Tabs.Active.Route == album`, `PinnedRevision` unchanged |
| G-259 | Album gate (b): EVERY row of a short list renders blank — the reveal ramp overflows negative. For ≤ 24 visible rows that arm the ramp (every album-family open; every cold playlist/liked open) the table ticker's first advance runs synchronously inside the `Flow.Show` boundary's own run and reaches `Done`; its `_rampActive = false` write is DROPPED (the engine's `MarkDirty` returns early for a computation already Dirty during its run), so the ticker stays mounted and the next frame tick calls `Next(Done, n)`: `int.MaxValue + 12` wraps to −2147483637, `_reveal` goes negative, `RowRevealed` is false for every row → two blank `ShimmerRow`s (what `album-gate.png` shows); the ticker polls every frame so the loop never idles on that page. The seed is NOT the cause (al3 = Single with 2 Complete album-track rows) | `Design.cs:1690-1695` (`Next`: unchecked `reveal + Chunk`, no `Done` guard); `Track.Table.cs:833,905,938-943,2082-2096`; engine `RenderContext.cs:640-648`, `Reconciler.cs:1227,1347-1360`, `ReactiveCore.cs:148-154,218-228`, `AppHost.cs:3620`; `DesignTests.cs:803-827` (no `Next(Done, n)` case); gate log `nav.frames frames=181 hotAllocKB=1098413` | defect (app) + engine | BLOCKS (album/single/EP pages ≤ 24 tracks and short cold playlists show no rows; idle-GPU regression on those pages) | Platform/Design.cs `RevealRamp` (L) + Entities/Track.Table.cs `TableTicker`/`AdvanceReveal` (M — coordinate: WP-5.M does not own Track.Table.cs); engine issue for the lost wakeup | fixed app-side (verified 2026-09-15: `RevealRamp.Next` returns `Done` at `Done` and sums in `long`; `TableHost.AdvanceReveal` early-returns at `Done` and always clears `_rampActive`; `TableTicker` subscribes on its eager mount run and only steps from a later frame) — ONLY decision D42 (the engine's lost wakeup) remains, and that is hardening, not a blocker | (1) `Next`: `if (reveal == Done) return Done;` compute in `long`; tests `Next(Done,2)==Done`, `Next(int.MaxValue-1,2)==Done`. (2) `AdvanceReveal` returns early at `Done` and always clears `_rampActive` when it ends Done. (3) Never advance from the mount run (skip the effect's first run, or drive the ticker from a passive `UseEffect`/frame callback — 0.2.9's `TickerClock.OnFrame` fired only from the frame phase). (4) Engine: `MarkDirty` during a computation's own run re-queues it, or `BackwardsWriteGuard` reports it in Debug; Engine.Tests fact: a Show whose Then-branch mount writes the condition false unmounts in the same flush. Re-shoot the gate on al2/al5 (12/10 rows) |
| G-260 | Album gate: the gate's demand asks unseeded field groups under `--fake` — `DemandAlbum` asks `AlbumFields.Detail` and `DemandRows` asks `Row+Audio+Tags+Video`, which the seed never commits, so the planner routes them to the registered `Spotify.Api` provider in the offline demo (`Spotify.Api.Boot` runs unconditionally; nothing in Spotify/Entities branches on `Args.Fake`) | `Detail.UI.cs:1509,1524-1525`; `App.cs:42`; `Entities.Fake.cs:188-190,208` | data / wiring | DEGRADES (`--fake` gates; possible offline network/backoff noise) | Entities.Fake.cs (Q/WP-5: seed or mark the groups answered-empty) or a providerless `--fake` registration (O*) | open | Commit empty-answered groups in `SeedFake`, or `if (!Args.Fake) Spotify.Api.Boot()` + a fake provider that `MarkUnanswered`s every ask. EntitiesFakeTests: after `SeedFake`, `al3.Knows(AlbumFields.Detail)` and every seeded track `Knows(Row+Audio+Tags+Video)` |
| G-261 | Album gate (c): the fake album titles reuse the track/playlist vocabulary (`s_titles` serves track AND album titles; "Nostalgia 2000s Mix", "Iced Americano" also sit in `s_playlistNames`; "Late Night" ≈ a folder name) so a gate screenshot cannot tell an album from a playlist — the gate read the RIGHT table/slot (al3 = Single, 2008+3 = 2011, title index 3). Ported faithfully from ch 31, which already calls the duplicate-name trap a defect (§0.10f). (d) The command bar has NO structural divergence for a Single (matches 0.2.9 `BuildToolbar` and ch 04 W1 minus Select); the "…" Sort submenu label changed to `detail.sort.menu` (a deliberate fix, unlogged in ch 04) | `Entities.Fake.cs:58-75,191,209`; `Track.Table.Chrome.cs:132-204,842`; ch 31 :105-110,374-375,437 | data (fixture) | COSMETIC | Entities.Fake.cs (Q via WP-5.M's `Entities.Fake.Album.cs`) + ch 31 §3.1 / ch 04 audit log | open — decision D43 (gate subject) | Albums get their own 13-name vocabulary disjoint from playlists and folders; EntitiesFakeTests: "no album title equals a playlist or folder name" + "al3 title/kind/year" pinned; log the Sort label fix in ch 04 §11 |
| G-262 | Album gate (d): the gate profile has no `Drawer`/`TopTrack`, so the header cannot show W2's ⌄ expand lane (`ShowVersions && Profile.Drawer is not null`, and `TableProfile.From(cfg)` has no Drawer) or the ★ top track (`isTop` needs `AlbumFields.TopTrack`, never seeded); no billed artists (no AlbumArtists edge) and no "About this release" panel (the gate passes only Shuffle + GoLibrary) | `Track.Table.cs:545,1777`; `Detail.UI.cs:683,701-709,1500`; `Entities.Fake.cs:208` | deferred / data | COSMETIC (expected until WP-5.M) | Track.Drawer.cs + Album.Page.cs (WP-5.M), Entities.Fake.Album.cs (TopTrack, AlbumArtists) | in flight: WP-5.M | The album page supplies `Profile.Drawer` and the release panel; the seed commits TopTrack (max-PlayCount row) with `AlbumFields.TopTrack` known; EntitiesFakeTests "every album Knows(TopTrack) and TopTrack ∈ its AlbumTracks" |
| G-263 | Album/playlist heart FABs never render: `Controls.SaveButton` returns an empty box while `Controls.Library` is null, and nothing assigns it → every save/follow heart on the detail rail, hero and cards is absent (the gate rail shows Play + share only) | `Controls.cs:63,646-648`; `Detail.UI.cs:1006-1011`; grep `Controls.Library =` → none; `album-gate.png` | wiring | DEGRADES (save/follow from every detail page) | User.cs (WP-5.O) together with `Actions.Services.IsSaved/SetSaved` (G-027 O half) | in flight: WP-5.O | A `LibrarySeam` over `User.Me` edges (IsSaved reads the Saved* edge + subscribes Changed; ToggleSaved = optimistic write + LibraryWrites), assigned in O's install. Pure seam fact over a seeded User: ToggleSaved flips IsSaved and marks `EdgePending.Add` |
| G-264 | Two shuffle implementations on one detail page: the command-bar Shuffle builds the queue from the visible rows (`SetShuffle(true)` + `StartVisible(0)` → `Queue.Replace` + `PlayNow`), the hero satellite goes `FrameActions.Shuffle` → `Services.Play(context)` → `PlayContext`; 0.2.9 had one handler (`DetailShell.cs:328-333`) | `Track.Table.cs:1293-1297`; `Detail.UI.cs:1528-1533`; `Detail.UI.Hero.cs:414-415` | parity / defect | COSMETIC | Track.Table.cs (M, R4-4 with G-253) | open | One shuffle: `TableArgs` gains the frame's shuffle trampoline, or the table's is the only one |
| G-265 | `Wavee.ReleaseTool` NO LONGER COMPILES: R2 moved the PURE region of `ReleaseNotes.Host.cs` into `ReleaseNotes.cs` — one of exactly two files the tool compiles by name — and it references `Platform.Keys`, `IAppSettings` and the generated `Strings.WhatsNew.*`, none of which exist in that project; the "0/0" build did not rebuild the tool (its bin is from 2026-09-12 22:35; `ReleaseNotes.cs` changed 2026-09-14 16:34); `wavee-release.ps1` phase 2 runs the tool, so release-notes validation cannot run | `ReleaseNotes.cs:108-110,167-182`; `Wavee.ReleaseTool.csproj:23-24`; `Platform.cs:46`; `ops/release/wavee-release.ps1:15,240,704` | defect | BLOCKS (release gate E14) | Screens/ReleaseNotes*.cs (R) + the orchestrator's build gate | open | Move `AfterUpdateGate.Consume` (the `IAppSettings`/`Platform.Keys` caller) back to `ReleaseNotes.Host.cs`; `KindPillLocKey`/`SectionTitleLocKey` return literal key strings or move to the Host file; `dotnet build src/apps/Wavee.ReleaseTool` Debug + Release joins every wave gate (G-202's note) |
| G-266 | Optimistic library writes stamp `AddedAt` in APP seconds (`User.Add` writes `new LibraryEdge(Entities.Now, …)`) while decoders and the table's date logic treat it as unix → an optimistic like shows a ~1970 "date added" and sorts wrong until the server echo replaces it | `User.cs:361`; `Track.Table.cs:646`; `Track.UI.cs:148-149` | defect | DEGRADES (Liked/playlist date column, recents after a like) | Entities/User.cs (WP-5.O — the orchestrator says O fixes it) | in flight: WP-5.O | `new LibraryEdge((int)Store.ToUnix(Entities.Now), flags)`; UserTests "Like stamps AddedAt within ±2 s of `Store.ToUnix(Entities.Now)`" |
| G-267 | "Go to podcast" can never be offered: `CanGoToPodcast` reads `t.Album` (an Albums-table handle) and requires `Uri.Kind == Show`, but a show is never a row in the Albums table; the WP-4.5 row works around it locally | `Actions.cs:946-953`; `Track.Menu.cs:160` | defect | COSMETIC | Platform/Actions.cs `ActionRules` (I) + the episode-as-track read model (A) | open | Carry the show slot separately (`TrackFlags.Podcast` + a Show handle) and gate on it; ActionRules fact |
| G-268 | Dead settings keys after WP-4.5/6: `Platform.Keys.DetailSortCol/DetailSortDesc` (default 0 = a real column) superseded by `Detail.SortKeys` (−1 sentinel); `SidebarBootstrapVersion` declared, never read; and WP-6.R's audit left these keys with NO writer: library factory keys, `SavedLibrary`, `LikedCoverStyle`, uniform rail width/collapsed, `PlaylistDepositRecents`, `PrivateSession`, `VideoPreferredPlacement`, `CuratedRailLabels`, legacy sidebar keys; `playback.runtime.*` blocked on D3 (`TempoColumn/PlaysColumn` ARE written by the table's More flyout, `Track.Table.Chrome.cs:849,855`) | `Platform.cs:70-330,86,301-302`; `Detail.cs:1041-1049`; WP-6R report | impl | DEGRADES (settings parity) / COSMETIC (dead keys) | Platform.cs (S: delete the dead three), O (library, liked cover, deposit recents), K (video placement, G-150), J (curated labels; delete the legacy sidebar keys), D3 (runtime) | open | Every key gets a writer or is deleted; `SettingsCatalogTests` enumerates the key table against a writer registry |
| G-269 | Diagnostics report sources never filled: `Diagnostics.RuntimeReportSource/ModulesReportSource/NotesReportSource` declared but unassigned → the runtime/module/notes sections read "not compiled in" or empty | `Diagnostics.cs:235,321,327`; grep `ReportSource =` → none | wiring | DEGRADES (ch 27 parity 57-74) | Setup runtime host (D3, private), Modules.Host.cs (T), ReleaseNotes.Host.cs (R) | open | Each owner assigns its source in its install; `RuntimeReportTests` cover the text |
| G-270 | Small WP-4.5/6 leftovers: the `ConnectDiagnostics` route row keeps the MusicNote placeholder glyph + developer gating (`Shell.cs:161`); `Controls.ExpandChevron` captures `expanded` in a Prop closure at element build so a patched row keeps the old angle (`Controls.cs:835-841`, bind-once semantics not re-verified); pure frame/table decisions still live in UI files with no CORE rule or test (rail widths 224/188, the collapse detent 44/220/96/20, window rungs 900/760, hero presence flags, the drop accept/transparent rule, "this context is playing", the accent order, the filter capability scan, the compact search width, "all liked" — `Detail.UI.cs:313-325,870-922`, `Track.UI.cs`); the thumb-lane under-count in `TableRules.MinWidthFor` was ported instead of fixed (ch 30 §3.2 says fix: the thumb counts as 32 while art is 40/48 at higher densities) | as cited; WP-4.5 report decisions 1–2 | parity / impl (test debt) | COSMETIC | Shell.cs (I), Controls.cs (L), Detail.cs + Track.Rules.cs (M) | open — decisions D44 (# cell), fix the under-count | 0.2.9's Connect glyph + confirm the gating vs ch 27; `IReadSignal<bool>` into `Prop.Of`; move each rule into the CORE partials with facts; `MinWidthFor(art)` + a density-3 relief fact |
| G-271 | `Diagnostics.Headless.cs` 925/700 after the JSON split (> 30 %); lyrics probes P2/P4 not ported (`--lyrics-advance-probe` parity partial) | `wc -l`; WP-6S report; `Diagnostics.Probe.Arms.cs` has no P2/P4 arms | impl (budget / deferred) | COSMETIC | Diagnostics.Headless.cs (X/S), Diagnostics.Probe.Arms.cs (S) | open | Split the grammar or the verdict writer into a named partial; port the two arms with their rule tests |
| G-272 | Unassigned seams left by WP-6.R/S: `Settings.MetadataStats/ClearMetadataCache` (store owner C), `Settings.LicenseKeyCount/ClearLicenseKeys` (D9), `Update.Host.BeforeApply` (T), the video-override toast's "Manage" → `Settings.OpenVideoOverrides()` (no caller; K per D41); `Diagnostics.SidebarPaneFrame` has a consumer (`Diagnostics.Host.cs:373-375`) but no writer (J) | as cited; WP-6R/6S reports | wiring | DEGRADES (Storage stats, licence card, video-override toast, pane-invariant diagnostics) | C, F/D9, T, K, J | open | Each owner assigns its seam in its install; a `SidebarPaneFrameSnapshot` publisher after collapse/expand settles |
| G-273 | The fake seed lacks what the album gate composes: no `AlbumArtists` edge (billed artists absent), no `AlbumFields.Release/Publishing/Availability/TopTrack`, no `TrackArtists` edges (ArtistLine text only), no `TrackFields.Tags/Video`, no gradings (ch 03 item 35: the tone plane cannot pass in `--fake`) | `Entities.Fake.cs:188-214`; `Detail.UI.cs:145`; WP-4.5 report items 67/35 | data | DEGRADES (the Wave 4.5/5 gates cannot show parity items 24/35/W2) | Entities.Fake.Album.cs (WP-5.M) + a palette seed (Q) | in flight: WP-5.M | Seed AlbumArtists/TrackArtists from the artist wrap, TopTrack = max-PlayCount member, Release/Publishing literals, a graded palette entry per bundled cover; EntitiesFakeTests facts per group |
| G-274 | Bookkeeping owed for two deliberate divergences: the after-update plate now opens only after its notes load (differs from ch 28 parity 83 — accepted as a fix of a 0.2.9 bug), and the Settings resident-cache census row was deleted; neither has a CHANGELOG line with an issue number | `ReleaseNotes.cs:88-111`; WP-6R report | decision / release hygiene | COSMETIC | CHANGELOG.md (R) + `github-triage` (D14) | open | File the issue (user approval), add the bullets ending ` (#n)`; ch 28 §11 audit entry |
| G-275 | Autoplay answer's `next_page_url` was decoded and discarded (`RequestAutoplay`, pre-move `Playback.Host.Context.cs:673-675`): a consumed autoplay run re-asked `/autoplay` with the same seeds instead of paging the answer. **Fixed 2026-09-16:** `Playback.Host.Autoplay.cs` keeps the page url; `Refill.Decide` → `PageAutoplay` → `RequestAutoplayPage` (shared `FetchPage`) → `Input.AutoplayPaged`. Tests: `RefillTests`, `PlaybackStepTests` refill region. | | | | | | |
| G-276 | Daylist rollover never happened: both countdowns clamp to 00:00:00 and disarm (Home.Cards.UI.cs HeroCountdown, Playlist.UI.cs DaylistHost), only the toast subscribes to the window end, the planner has no way to re-ask a Known group, the Format commit blanked the masthead, and a Full window/identity rejected the feed's newer edition. **Fixed 2026-09-17:** `Table.Stale` + `Entities.Invalidate`/`InvalidateEdge`; `DaylistRollover`/`DaylistCountdown`/`DaylistEdition` (Entities/Daylist.cs); owner `Home.Host.Feeds` arms a wall-clock timer (30 s grace, 30/60/120/300 s ladder, resume/activation re-arm); edition gate in `CommitPlaylists`. | | | | | | |
| G-277 | **Lossless hung for 57 s instead of degrading.** A FLAC opened fine off its 81,920-byte clear head, then every mirror refused the body: `audio.resolve why=refused` ×10 over 52 s, `audio.underrun` climbing to `stallMs=56000`, an unhandled `InvalidOperationException` out of the decoder seek, then a skip to the next track at Ogg 320. Three causes: (a) `LosslessFallback.Decide` matched only `NoDeriver`/`NoKey`, so a refused body never triggered D7's demotion; (b) `StarvePolicy` applied D5's ~90 s patience to a **refusal**, which is terminal, not slow; (c) `audio.resolve` carried no HTTP status, so the refusal was unattributable. NOT a key-deriver problem (G-014). **Fixed 2026-09-17:** typed `Fault.Refused` carried up from the existing `Refused` outcome; `Decide` widened; `FirstBodyMs = 3_000` deadline demotes a lossless open that lands zero body bytes; `StarvePolicy.RefusedFailMs = 6_000` splits refused from slow while D5's 90 s stands for slow; `audio.mirror` logs the HTTP status at the point a mirror is dropped; the refusal-driven demotion is suppressed for 6 s rather than remembered for 30 min (a refusal is this-minute, a missing key is deterministic). Also fixed en route: the swallowed seek left `s_pendingSeekMs`/`s_pendingSeekQueued` latched, freezing the scrub at the target and blocking every later seek for the rest of the track. | `Spotify.Audio.cs:175-233,570-667,904-943`; `Spotify.Audio.Stream.cs:316-359,1668-1685,2057-2079`; `Playback.Audio.cs:169,2562,4074-4089`; `SpotifyAudioTests.cs:383-425` | defect | BLOCKS (lossless) | F / H (done) | fixed (Debug+Release 0/0, 9,585 pass) — **live re-run owed** | Play a lossless title: the demotion must land in ~1 s with `audio.mirror status=` → `audio.nobody` → `audio.demote why=refused`. Then chase WHY an entitled account's file id is refused |
| G-278 | **Video switched cold every time and leaked a protected session per switch.** Video itself was never broken (it plays end to end: `first.frame sinceSwitchMs=2477`), but `plan=Rebuild warm=false` on every switch with the licence ~80 % of the wait; the prefetch effect subscribed to `State.Surface.Value` and so prefetched the very row about to load, stranding one `[cenc]` session + its segment store each time (2 created, 1 destroyed per session on a `tier=Weak`/`vramMB=128` UMA box); and `Video.Phase`/`FirstFrame`/`Buffered` had **zero consumers** while the surfaces used a `DepKey.Empty` timer armed at the overlay's mount — so the first join got its 400 ms grace and every later one flashed the ring instantly. **Fixed 2026-09-17:** claim-before-work + adopt-in-flight-prepare with three closed no-strand paths; a warm keeper (10 s beat, 30 s shed matching the engine's own `WarmIdleDisposeMs`) pre-acquiring the licence for the playing and next queued row, audio always winning, behind a default-ON `playback.video.prepareAhead` setting; `warm=` now honest; the loading visuals driven by the real phases (§6.2 K, finally built) with `Failed` visually distinct. **Known deviation:** `Acquire`/`Release` are engine-`internal`, so the app cannot pin the runtime — D15's "never idle" is really "never idle longer than one beat". | `Playback.Video.cs`, `Playback.Video.Warm.cs` (new), `Playback.Video.Rules.cs:104-187`; `Video.Host.Wiring.cs:212-279`; `Video.UI.cs`, `Video.cs:1392-1458`; `Settings.UI.Playback.cs:466`; `Settings.cs:138` | defect | DEGRADES | K / B7 (done) | fixed (Debug+Release 0/0) — **live re-run owed** | Switch to video twice: `warm=true` on the second, `session created` ≈ `session destroyed`, `warm.shed afterMs=30000` ~30 s after off, and `first.frame sinceSwitchMs` well under 2477 |
| G-279 | **The album video card showed one video of three, with the album cover as its thumbnail.** `Album.Page.cs:955-956` reduced members to a boolean `any`, so one video and three rendered the same single album-scoped card — which drew `a.ImageId`, the album title and `Detail.Text.AlbumMeta` ("3 songs · 16 min · 1984") and played the ALBUM. The correct per-video still was already resident (`Track.VideoImage`, demanded for every member at `Album.Page.cs:361`) and the drawer already read it (`Track.Drawer.cs:458`). Also: the card never appeared on a full-length album at all (`IsShortRelease` gate), and "Music video · Unavailable" middot-joined an AUDIO verdict onto the video's flag, reading as though the video were unavailable. **Fixed 2026-09-17:** pure `PageRules.SelectVideos`; adaptive hero (1) / wrap grid (2+), each entry its own still, title, duration, clicking through to the VIDEO; the short-release gate lifted; the flag relabelled "Audio unavailable". `05-album.md` amended (wireframe, prose, geometry, parity 22/44/44b/44c/63/63b/63c, ledger row 42) so the next parity pass cannot file it as a regression. | `Album.cs:1114-1138`; `Album.Page.cs:1010-1433`; `Track.Rules.cs:1082-1172`; `en-US.json:270`; `AlbumVideoSectionTests.cs` | defect / parity | DEGRADES | M (done) | fixed (Debug+Release 0/0) | Open a 3-video album: three cards, three distinct real thumbnails; then a 1-video album for the hero arm |

| G-257 | `Shell.History.Store.Clear` deletes `history.json` fire-and-forget on a pool thread; a caller that removes the folder right after (the test teardown did) races the pending delete (`UnauthorizedAccessException`); the test now catches it, production is unchanged | `ShellHistoryStoreTests` teardown (6,634/6,635 → caught); `Shell.Host.cs` History.Store | defect | COSMETIC | Shell.Host.cs (I) | open | Make `Clear` return the delete task (or run it inline — one small file) so a factory reset / sign-out can await it; drop the catch in the test |

## 3. The critical path (pass 4)

Landed and green (uncommitted, HEAD `771fce00` + ~140 files; Debug + Release 0/0, 6,635/6,635): rounds 1–3 — composition, sign-in, the data path (routes, edge door, sync, writes, pins), the reducer/host (hand-off, device reload, Connect inbound, telemetry, play log, deck restore), the audio pump/stream, the queue, Wave 4.5 (Detail frame + track surface + the album gate), Wave 6 screens (Settings, Setup chrome, ReleaseNotes UI, Feedback, updater, Diagnostics, Platform), ops. Launched: `--fake` Home renders with the expanded Classic sidebar and the signed-in chip; the album gate renders its frame (findings G-258–G-261).

1. **In flight now: WP-5.M/N/O/P** (Album/Show/Episode/Track.Drawer · Artist/Concert · Playlist/User · Home/Search/Browse/Recents — each with its own `Entities.Fake.*`, `Spotify.Decode.*`, `Spotify.Api.*` partials; shared-file patches returned to the orchestrator). They close G-191, the G-045 remainder, G-232, G-027's O verbs, G-109's enclosure, G-238, G-213's rail read, and the gate's empty track list (G-259).
2. **Beside them, on disjoint files (§4 round 4):** thin PutState (G-240), the scope-switch orphan (G-241), context paging (G-242), queued-row replay (G-243), the outbound queue forwards (G-248), the rejected-PUT re-announce (G-247), radio parity (G-251, after D34), the `--fake` context fallback (G-249, D33), the table→`PlayRows` swap (G-253), the lyrics waiter (G-252), the row-overflow trio (G-254/256), edge persistence (G-230), the pathfinder wrapper bug (G-231), playlist diff (G-233), owner capture (G-236, D32), the four pin-blind scripts (G-237), the headless library step (G-239), G-077, G-036's effect order, WP-6.T (modules), B9 tests, the budget splits (G-202/NEW-9).
3. **Video:** D30 (combine the engine branches, re-pin) → B7 (the `SetVideoPlacement` caller, `OnVideoDemoted`, headers, memo/prefetch, Phase/FirstFrame, seek index, log lines, csproj native target) → the plan's §4.3 manual gate.
4. **Verification:** the headless smoke (G-018; add `library-sync.wh`), the `--fake` G2 probe over every route once WP-5 lands, parity checklists (G3), the on-box tray/engine checklists, `local-update-e2e.ps1` both scenarios (G-093 landed — run it), `-DryRun` (needs D3 + G-204).
5. **Release:** D3/D9/D14 answered, junction, CHANGELOG issue refs, `_old` deletion.

---

## 4. Fix batches (disjoint file sets; respecting in-flight ownership)

**Pass 4 status.** Landed (uncommitted, green): B0, B0b, B1, B1b, B2, B2b, B3, B3b, B3c, B4, B5, B6, B8, B10, B10b, D-fake, WP-fake, WP-4.5, WP-5.Q, WP-6.R, WP-6.S, R2, tray W-C, the silent-Ended fix, the engine video completion (on its own branch). **In flight (dispatched by the orchestrator, brief `scratchpad\wp5-common.md`):** WP-5.M (`Entities/Album*`, `Show*`, `Episode*`, `Track.Drawer.cs`, new `Entities.Fake.Album.cs`, `Spotify.Decode.Album.cs`, `Spotify.Api.Album.cs`; also the al3 gate's empty track list), WP-5.N (`Artist*`, `Concert*`, `Entities.Fake.Artist.cs`, `Spotify.Decode.Artist.cs`, `Spotify.Api.Artist.cs`), WP-5.O (`Playlist*`, all `User*`, `Entities.Fake.Library.cs`, `Spotify.Decode.Playlist.cs`, `Spotify.Api.Playlist.cs`; fixes the `User.Add` AddedAt app-seconds bug), WP-5.P (`Home*`, `Search*`, `Browse*`, `Recents*`, `Spotify.Decode.Browse.cs`, `Entities.Fake.Home.cs`, `Spotify.Decode.Home.cs`, `Spotify.Api.Home.cs`). No page package edits a shared file; the orchestrator applies their shared-file patches (`Fetch*`, `Spotify.Api.cs`, `Spotify.Decode.cs/Pathfinder.cs`, `Entities.cs`, `Edges.Staging.cs`, `Shell/*`, `Platform/*`, `Detail/Track`, `App.cs`) — so **nothing in round 4 may touch those shared files either** unless the orchestrator sequences it after the patches.

### Round 4 — dispatch next (disjoint from the four page packages and from each other)

| Batch | Owner | Files (exclusive) | Scope | Check |
|---|---|---|---|---|
| **R4-1 Connect wire parity** | G + E (Opus) | `Spotify/Spotify.Decode.Connect.cs`, `Spotify/Spotify.Connect.cs`, `Playback/Playback.cs`, `Playback/Playback.Host.cs`, `Playback/Playback.Host.Context.cs`, `Playback/Playback.Host.Remote.cs`, `Playback/Playback.Transitions.cs`, NEW `Wavee.Tests/PutStateWireTests.cs`, `PlaybackIntakeTests`, `PlaybackStepTests`, `SpotifyConnectTests` | G-240 (thin PutState: prev/next window ≤ 50, queue_revision, ids, restrictions/signals/modes/#38, `is_playing`/speed while paused, per-row metadata), G-241 (rebind after `Entities.Switch` or defer the restore), G-242 (context paging before autoplay; stations page), G-244 (newer inbound load supersedes), G-245 (update_context reshuffles), G-246 (command-id ageing), G-247 (re-announce after 422), G-248 (forward add_to_queue/set_queue while Foreign), G-036 (order `Release(Logout)` before `Connect.Clear()` so BecameInactive leaves), G-250 (comments + the dealer glue test), G-023 (`Telemetry.Boot` on Online) | Port `ConnectStateBuilderTests` over decoded bytes; a warm 50+50 encode allocates nothing; Step facts per row; then the live check: a phone shows the queue, a pause reads as paused, transfer in/out |
| **R4-2 Queue rules + radio** | Q + G (Opus; sequence after R4-1 on `Playback.Host.Context.cs`, or hand R4-1 the radio callback) | `Entities/Queue.Rules.cs`, `Entities/Queue.UI.cs`, `Entities/Queue.cs`, `QueueTests`/`QueueOrderTests`; `Entities/Track.Menu.cs` (M's file — WP-5.M owns `Track.Drawer.cs` only, but confirm with the orchestrator before editing `Track.Menu.cs`) | G-243 (consumed queued rows never rotate back), G-249 (fake-scope `LocalMembers` fallback, D33), G-251 (radio: 0.2.9 seed→playlist, park after current, toasts — D34), G-250 (Queue.UI/Track.Menu comments) | QueueOrderTests: advance past a queued row, Previous twice, not next; fake `PlayContext(album)` posts one Play; the radio verb raises `RadioStarted`/`RadioUnavailable` |
| **R4-3 Library persistence + decode fixes** | B + C + E (Opus) — WAIT for the orchestrator's WP-5 shared-file patches on `Edges.Staging.cs`, `Spotify.Decode.Pathfinder.cs`, `Fetch.cs` | `Entities/Store.cs`, `Entities/Edges.Staging.cs` (+ its budget split `Edges.Staging.Library.cs`), `Entities/Fetch.cs`, `Spotify/Spotify.Decode.Pathfinder.cs`, `Spotify/Spotify.Api.Library.cs`, `StoreTests`, `PathfinderDecodeTests`, `FetchTests`, `EdgesStagingTests` | G-230 (SaveEdges/RegisterEdges appliers for every account relation incl. folder text + pin kinds), G-231 (wrapper pop bug), G-233 (playlist `/diff` via a `PlaylistRevision` column), G-234 (dead route kinds), G-236 (after D32: only `owner_username` when `meta_items` count equals the row count; ignore kind-205 field 7 for capabilities until the capture) | StoreTests round trip with folders + pins; artistOverview fixture asserts `ArtistAlbums`; a `ReadList` verdict fact for playlists |
| **R4-0 Blank rows + ReleaseTool (FIRST — two blockers, tiny)** | L + M + R (Opus, one agent; `Track.Table.cs` is not a WP-5 file — confirm with the orchestrator) | `Platform/Design.cs` (`RevealRamp.Next`), `Entities/Track.Table.cs` (`TableTicker`/`AdvanceReveal`), `Wavee.Tests/DesignTests.cs`; `Screens/ReleaseNotes.cs` + `ReleaseNotes.Host.cs`, `Wavee.ReleaseTool` build | G-259 (Done guard, long arithmetic, no advance in the mount run; file the engine issue D42), G-265 (move the `IAppSettings`/`Platform.Keys`/`Strings.*` code out of the cherry-picked partial) | `Next(Done,n)==Done` facts; the gate re-shot on al2/al5 shows rows and the loop idles; `dotnet build src/apps/Wavee.ReleaseTool` Debug + Release joins the gate |
| **R4-4 Table + lyrics + layout** | M + I + K + J (Sonnet; one agent — `Track.Table.cs`/`Track.Table.Chrome.cs` are M's Wave-4.5 files, not WP-5.M's; sequence after R4-0 on `Track.Table.cs`) | `Entities/Track.Table.cs`, `Entities/Track.Table.Chrome.cs`, `Shell/Shell.Host.cs` (+ split `Shell.Host.Marshal.cs`), `Shell/Shell.Chrome.cs` (`TabWorkspace.RestoreActiveRoute`), `Shell/Sidebar.UI.LibraryV3.cs`, `Shell/Lyrics.UI.cs` or `Shell/Rail.UI.cs` (`GrowSlot`, `RailRow` → `Track.ArtCard`), `Shell/Shell.PlayerBar.UI.cs` (`BarLiveLine`), `Shell/Deck.UI.cs`, `Shell/Stage.UI.cs`, `Screens/ReleaseNotes.UI.cs`, tests | G-258 (restored route onto the active tab), G-253 (`StartVisible` → `PlayRows`), G-264 (one shuffle), G-252 (signal-free lyrics waiter), G-254 (three `Shrink 1` fixes + BarLiveLine), G-256 (Zune title, ZStack aspect ratios, stage spacers), G-257 (awaitable `History.Store.Clear`), G-202/NEW-9 (`Shell.Host.cs` 1,528/1,000 → named partial), G-214 (`RailRow` → `Track.ArtCard`, `Stage.UI.cs:192` pivot constants → `Detail.BandLayout`) | ShellNavTests restore fact; table fact: a queued row survives a table play; lyrics resolve completes with zero probes; V3 rail at 200 DIP scrolls, table search at 66 DIP keeps the filter button, rail lyrics at 200 DIP after a drag back; stage after maximize → restore |
| **R4-7 Screens/platform leftovers** | S + R + I + L (Sonnet) | `Platform/Platform.cs` (dead keys), `Platform/Actions.cs` (`CanGoToPodcast`), `Platform/Controls.cs` (`ExpandChevron` bind), `Shell/Shell.cs` (Connect glyph; tint-owner duplicate), `Screens/Diagnostics.Headless.cs` split, `Screens/Diagnostics.Probe.Arms.cs` (P2/P4), `Screens/ReleaseNotes.Host.cs` (`NotesReportSource`), `Entities/Detail.cs` + `Track.Rules.cs` (UI constants → CORE rules; `MinWidthFor(art)`), tests | G-267, G-268 (delete the three dead keys; the rest per owner), G-269 (R's source), G-270, G-271, G-197 (tint duplicate), G-274 (CHANGELOG lines, after D14) | SettingsCatalogTests key/writer registry; a density-3 relief fact; `Diagnostics.Headless.cs` ≤ 910 |
| **R4-5 Ops + verification plumbing** | O*/S (Sonnet) | `ops/build/generate-third-party-notices.ps1`, `ops/build/pack-wavee-msix.ps1`, `ops/build/publish-wavee-aot.ps1`, `ops/release/wavee-store-submit.ps1`, `ops/tools/perf-tour.ps1`, `ops/release/tests/*`, `Screens/Diagnostics.Probe.cs` (headless composition only), NEW `ops/headless/library-sync.wh`, `Wavee.Tests/HeadlessTests.cs` | G-237 (four scripts → `Resolve-EngineRoot`), G-239 (headless installs `Spotify.Library` + `Lyrics.Boot`; the library script), G-205's regex caveat (parse the props with MSBuild semantics or document the literal-path rule) | Pester green; `library-sync.wh` prints folder/pin counts on the orchestrator's profile |
| **R4-6 Telemetry settings churn** | F + S (Sonnet) | `Spotify/Spotify.Telemetry.cs`, `Platform/Platform.cs` (facade DEBUG assert), `SpotifyTelemetryTests`, `PlatformTests` | G-077 (Interlocked sequence persisted on flush through `Spotify.Post`; no `SettingsChanged` bump per event) | Fact: 10 events → 0 settings writes until flush; a DEBUG assert on off-thread `Settings.Set` |
| **WP-6.T Modules** | T (Opus) | `Platform/Modules.cs`, `Modules.UI.cs`, `Modules.Host.cs`, `_old/Wavee.Tests/Modules/*` port; installs `Playback.Audio.LocalPath/ModuleOpen`, `Shell.LinkModules/MatchLink`, `Modules.IsModulePlayable`, mounts `Video.WatchStage` on the `module:` route | G-021, G-110 (with WP-5.O's local arm), G-128 (AAC + live ring), G-148 (clear video source), G-151, G-212 (`isModulePlayable`) | The 15 ported module test files green; `module:` route renders the watch page under `--fake` |
| **B9 Tests backfill** | Sonnet | `Wavee.Tests/RecentsTests.cs` (after WP-5.P), the remaining dropped sidebar facts, `ShellPlayerBarArtSizeTests` tiers, `SidebarDropTests.cs:163-167` header | G-201 (rest), G-183 (rest), G-223 | — |
| **Engine issues** (in `..\fluent-gpu`, after D30) | engine agent | `PcmAudioSession` Playing → Ended with a buffered sink (G-135); `MediaSeekBar` Shrink (G-255); `WasapiAudioDevice(deviceId)` (G-126); `InterruptPendingRead` for sequential reads (G-121); UIA `AutomationName` + `ProgressBar` reduced motion (G-222) | G-135 first | File the issues |
| **B7 Video host** (after D30) | K + H (Opus) | `Playback/Playback.Video.cs`, `Shell/Video.Host.cs`, `Shell/Video.UI.cs`, `Wavee.csproj` (native target: import `PlayReadyNative.props`, fix `Inputs`), NEW `VideoHostRulesTests` | G-141 (post `Playback.SetVideoPlacement` from `Video.Host.Commit`), G-142 (install `OnVideoDemoted` → unmount + toast), G-143 (signals with R4-1), G-144, G-145, G-146, G-147, G-148 (resolver tiers), G-149, G-150, G-152, G-153, G-056 (resolver), G-058 | Code against G-161's API; the plan's §4.3 manual gate |

The round-1/2/3 batch definitions below are kept as the record of what was dispatched.

| Batch | Model | Files | Gaps | Brief |
|---|---|---|---|---|
| **B2 Library sync + writes host** (now — B1 landed; the write path needs B1b's `CollectionAdd/Remove`, so start with the sync + encoders) | Opus | NEW `Spotify/Spotify.Library.cs` (SHELL: login-time sync of rootlist/collections/recents via `Entities.EnsureEdge`, dealer deltas, `Sidebar.LibraryWrites` implementation incl. `RenameFolder(newName)`, the `SidebarPinSync` bridge), NEW `+Spotify.Encode.cs` (CORE encoders: `CollectionWriteMapper`, `PlaylistWireMapper.Build{Changes,RootlistChanges,CreateChanges}`, `NewGroupId`, folder markers, signals), `Entities/Edges.Staging.cs` (`Relation.Pins` arm, AddRef the folder strings), `Entities/User.cs` (`Dispatch`, pin kind bit), tests `SpotifyLibraryTests`, `EncodeTests` (byte-exact vs 0.2.9 fixtures), the restored `RootlistDropScenarioTests`/`RootlistSlotToOpTests`/`SidebarPinSyncTests` | G-042 (sync), G-043, G-048, G-049, G-062, G-089 (attach drops) | Do NOT touch `Spotify.Api.cs` (B1b) or `Sidebar*.cs` (done; consume `Sidebar.LibraryWrites`/`NewReleasesFetch`/`ConcertsFetch` as they are). App.cs installs the seam (orchestrator follow-up) |
| **B4 Audio pump + stream** (now — coordinate the `HandedOff` input name with B3 before editing the hand-off) | Opus | `Playback/Playback.Audio.cs`, `Spotify/Spotify.Audio.cs`, `Spotify/Spotify.Audio.Stream.cs`, tests `PlaybackAudioTests`, `AudioAdapterTests`, `AudioStreamTests`, `SpotifyAudioTests` | G-100 (pump half), G-101, G-102, G-103, G-104, G-105, G-106, G-107, G-108 (pump), G-109, G-112 (pump), G-113, G-114, G-115, G-116, G-117, G-118, G-119, G-120, G-122, G-124, G-125, G-127, G-129 (after D19), G-134, G-038 | Decisions D4–D9 first; `Playback.cs`/`Playback.Host.cs` are B3's — post the new `AudioSignal` kinds B3 defines |
| **WP-5.Q Queue** (now — `Entities/Queue.cs` and `Queue.UI.cs` are nobody's) | Opus | `Entities/Queue.cs` (`ContextQueue.Build`, `QueueOrder` shuffle, autoplay append), `Entities/Queue.UI.cs` (rail panel + stage pane; `Rail.QueueBody`/`Stage.QueuePaneBody`), `QueueTests`, the `Play/PlayNext/AddToQueue/StartRadio` `ActionServices` verbs + `Shell.OnPlayContext` in Queue.UI's install method (called from `Shell.InstallUi` by the orchestrator) | G-070, G-080 (Q half), G-027 (Q verbs), G-074 (Q half), G-191 (queue) | B3 owns the reducer's autoplay/shuffle effects — consume `Playback.PlayNow` and post inputs only |
| **WP-6.R Screens** (now — skeletons, nobody's; mount K's `Settings.UI.Video.cs`, I's `Setup.UI.Runtime.cs`; B5 wrote `Setup.cs`/`Setup.UI.cs`'s sign-in facet — extend, do not rewrite) | Opus | `Screens/Settings.cs`, `Settings.UI.cs` + the four tab partials, `Settings.Host.cs`, `Setup.UI.cs` (wizard chrome around B5's facet), `Setup.Host.cs` (runtime provisioning), `ReleaseNotes.UI.cs`, `ReleaseNotes.Host.cs`, `Feedback.cs`, `Feedback.UI.cs`, NEW `Platform/Update.Host.cs` (D25: `AppInstallerUpdateService` + scheduler + install-on-quit), tests | G-192, G-093, G-094 (R halves), G-095 (chooser UI), G-122 (Storage card), G-130, G-132, G-182, G-200 (W-D group), G-201 (`ReleaseNotesTests`), G-212 (email/shimmer), G-218 | Every settings key gets a writer or is deleted; keep 0.2.9's log sentences so `local-update-e2e.ps1` passes unchanged |
| **WP-6.S Diagnostics + Platform** (now) | Opus | `Screens/Diagnostics.cs`, `Diagnostics.UI.cs` (incl. the lyrics inspector, A14), `Diagnostics.Host.cs`, `Diagnostics.Probe.cs` (the W6 arms in a named partial: `--perf-bench`, `--startup-bench`, `--crash-probe`, `--lyrics-advance-probe`, `--qr-dump`, `NotificationSimulator`), `Diagnostics.Headless.cs` split (`Headless.Json.cs`), `Platform/Platform.cs` (+`Platform.Settings.cs`: `NetworkPolicy`, ch 28 D3 types, `LogCapturePolicy`, `WaveeLogSessions`, `WaveeVersionInfo`), `Platform/Platform.Host.cs` (ambient power, crash writers), `RouteNoted` consumer, tests | G-086, G-094 (S halves), G-016 (split + `parse-only` section), G-153 (inspector/probe), G-197 (NavigationFrameWatch), G-198, G-199, G-218, G-183 (`SidebarPaneInvariant` consumer) | `Platform.cs` is free; do not touch `App.cs`/`Shell.Host.cs` |
| **B10b Release helpers + slnx** (now, small) | Sonnet | `ops/build/Wavee.Build.psm1`, `ops/release/wavee-release.ps1` (call sites only), `ops/release/tests/*.Tests.ps1` (+ a fixture PE), `Wavee.slnx` (engine projects via the resolved root, or a generated solution filter) | G-205, G-152 (preflight half), G-022 (slnx + script resolution) | Pester green (330 + new) |
| **D-fake small batch** (now) | Sonnet | `Spotify/Spotify.Session.cs` (+ `Spotify.cs` if `Status` needs a fake arm), `Shell/Sidebar.Host.cs` defaults or `Shell/Shell.cs` `FrameRules`, tests | G-065, G-066 | Re-take `--fake --frames 120 --screenshot` |
| **WP-5.M/N/O/P** (after WP-4.5) | Opus ×4 | per plan §5 Wave 5 (M: Album/Show/Episode/Track.Drawer; N: Artist/Discography/Concert; O: Playlist(+local)/User ×8; P: Home ×7/Search/Browse/Recents) + each page's decoders (`Spotify.Decode.<Page>.cs` for the G-045 remainder) + `AppActions.Register` of its verbs + `SetPage` + seams (`Omnibar.Source` P; `IsSaved/SetSaved`, `Controls.Library`/`PreSave`, `Drag.LikedChipArt`, `OnFilesDropped`/`PickAndPlayFile`/`OnNewPlaylist` O; `Daylist.Note` + the feed host for `Notify.Rebuild` P; `ReleaseDrops.Attach` M; the friends host N/K) | G-191, G-045 (rest), G-027 (O verbs), G-055, G-057 (182/212), G-059, G-060 (commit), G-061, G-088, G-089, G-092 (feeds), G-110 (local arm), G-131 (drawer), G-174 (feed seams), G-194, G-213, G-217 (journal) | Each page: chapter §10 parity logged; ReuseGuard silent under `--fake` |
| **WP-6.T Modules** (after WP-5) | Opus | `Platform/Modules.cs`, `Modules.UI.cs`, `Modules.Host.cs`, `_old/Wavee.Tests/Modules/*` port | G-021, G-110 (LocalPath/ModuleOpen), G-128, G-148 (clear video), G-151, G-212 (`isModulePlayable`) | — |
| **B7 Video host** (after D30 + B3) | Opus | `Playback/Playback.Video.cs`, `Shell/Video.Host.cs`, `Shell/Video.UI.cs`, `Wavee.csproj` (native target: import `PlayReadyNative.props`), NEW `VideoHostRulesTests` | G-140 (host side of B3's routing), G-141 (post half), G-142, G-143, G-144, G-145, G-146, G-147, G-148 (resolver tiers), G-149, G-150, G-152, G-153, G-056 (resolver), G-058 (cap reads the columns), G-201 (video tests) | Code against G-161's API list; set `DashSourceDescriptor.SegmentLengthMs/DurationMs`; route `ProtectedVideoRuntime.LogSink` into the log |
| **B9 Tests backfill** | Sonnet | `Wavee.Tests/RecentsTests.cs` (after WP-5.P), remaining dropped sidebar facts (after B2), `ShellPlayerBarArtSizeTests` tiers (G-223) | G-201 (rest), G-183 (rest), G-223 | — |
| **Engine issues** (in `..\fluent-gpu`, after D30) | engine agent | `PcmAudioSession` Playing → Ended with a buffered sink (G-135, + a test with a buffered fake endpoint); `WasapiAudioDevice(deviceId)` + enumeration (G-126); `ICancellableAudioSource.InterruptPendingRead` (G-121); voice total update after open (G-113); UIA `AutomationName` + `ProgressBar` reduced motion (G-222) | G-135 first (likely affects every real end-of-queue) | File issues; G-135 in 0.3, the rest 0.3.x |

The round-1 batch definitions below are kept as the record of what was dispatched.

| Batch | Model | Files | Gaps | Brief |
|---|---|---|---|---|
| **B0 Build + composition** (after W-C) | orchestrator | `App.cs`, `Shell/Shell.Host.cs`, `Directory.Build.props`, `Wavee.slnx` | G-022, G-002, G-003, G-004, G-005, G-007, G-008 (ToUi), G-009, G-012, G-013, G-023, G-024, G-025, G-026, G-027 (Registry.Build) | Worktree-local EngineRoot = the pin; `InstallMarshallers` public and first in Main; composition order per sweep D10: `Platform.Boot → Store.Use → Entities.Boot → Spotify.Boot → Spotify.Api.Boot → Sidebar.Boot → Playback.Boot → Modules.Boot → Sidebar.InstallActionSeams → Actions.Registry.Build → Shell.InstallUi → Shell.Run` (Run: marshallers incl. `Sidebar.Activate`/`Lyrics.Store.ToUi`, `Notify.HostInstall` on attach, auto-login, `Entities.Now` + the idle pump timer, Stopwatch `FrameNowMs`) → tail `Sidebar.Flush; Telemetry.Shutdown; Audio.Shutdown; Video.Shutdown; Store.Shutdown; caches; Platform.Shutdown`. Build Debug + Release, test, hand failures to owners |
| **B1 Fetch routing + edge door + decoders** | Opus | `Entities/Fetch.cs`, `Entities/Entities.cs` (provider routing + scope epoch signal + synthetic-table release), `Entities/Edges.cs` (`FolderId`, failure bit), `Entities/Browse.cs` (Commit hook), `Spotify/Spotify.Decode.cs`, `Spotify/Spotify.Decode.Pathfinder.cs`, `Spotify/Spotify.Decode.Connect.cs`, `Entities/Track.cs` (`VideoGid`, `VideoW/H`), tests `FetchTests`, `EdgesTests`, `DecodeTests`, `PathfinderDecodeTests`, `ClusterDecodeTests` | G-040 (planner half), G-041 (routing), G-042 (edge door), G-044, G-045 (rootlist, home-section, browse, context resolve, autoplay, profile, gander first), G-046, G-047, G-050, G-051, G-052, G-056, G-057, G-058, G-060, G-071 (decode half), G-179 (epoch signal) | Pure folds + fixtures only; no Api calls. Deliver `Api.RoutesFor`'s input contract (kind, wanted → extension kinds) as a CORE table here so B1b can consume it |
| **B1b Api provider routes** (after H-W) | Opus | `Spotify/Spotify.Api.cs` (+ `+Spotify.Api.Library.cs` named partial if over budget), `SpotifyApiTests` | G-040 (provider half), G-041 (pathfinder provider), G-042 (routes), G-043 (`CollectionAdd/Remove`, zstd G-054), G-033 (Profile), G-053, G-055 (filler), G-008 (`GetText`) | One mixed-kind POST per uri; per-subject pathfinder dispatch; sealed groups; edge routes; `Fetch.Answer` through `Spotify.Post` |
| **B2 Library sync + writes host** (after B1, B1b) | Opus | NEW `Spotify/Spotify.Library.cs` (SHELL: login-time sync of rootlist/collections/recents, dealer deltas, `Sidebar.LibraryWrites` implementation, pin bridge), NEW `+Spotify.Encode.cs` (CORE encoders), `Entities/User.cs` (`Dispatch`), tests `SpotifyLibraryTests`, `EncodeTests` (byte-exact vs 0.2.9 fixtures) | G-042 (sync), G-043, G-049, G-048 (caps), G-089 (attach drops) | App.cs installs `Sidebar.LibraryWrites` (B0 follow-up) |
| **B3 Playback reducer + host** (after H-W) | Opus | `Playback/Playback.cs`, `Playback/Playback.Host.cs`, `Playback/Playback.Os.cs`, tests `PlaybackStepTests`, `PlaybackRulesTests`, `PlaybackOwnershipTests`, `PlaybackOsTests` | G-100 (reducer half: `HandedOff`), G-108 (`DeviceReload`), G-112 (`EndingSoon`, re-prepare), G-140, G-141 (fold), G-142, G-143, G-070 (PlayNow from a resolved context), G-071 (host half), G-073 (host), G-074, G-075, G-076, G-078, G-079, G-080, G-081, G-082, G-083, G-084, G-085, G-087, G-036 (Release/epoch) | Every new input gets a Step fact; effects only, no host calls from CORE |
| **B4 Audio pump + stream** (after H-W; after B3's `HandedOff` shape is fixed) | Opus | `Playback/Playback.Audio.cs`, `Spotify/Spotify.Audio.cs`, `Spotify/Spotify.Audio.Stream.cs`, tests `PlaybackAudioTests`, `AudioAdapterTests`, `AudioStreamTests`, `SpotifyAudioTests` | G-100 (pump half), G-101, G-102, G-103, G-104, G-105, G-106, G-107, G-108 (pump), G-109, G-111 (verify), G-112 (pump), G-113, G-114, G-115, G-116, G-117, G-118, G-119, G-120, G-122, G-124, G-125, G-127, G-129 (after D19), G-134, G-038 | Decisions D4–D9 first |
| **B5 Session hygiene + sign-in** (after H-W; D2) | Opus | `Spotify/Spotify.cs`, `Spotify/Spotify.Session.cs`, `Spotify/Spotify.Connect.cs`, `Screens/Setup.cs`, `Screens/Setup.UI.cs`, `Screens/Setup.Host.cs`, tests `SpotifyCoreTests`, `SpotifySessionTests`, `SetupTests` | G-030, G-031, G-032, G-034, G-035, G-036 (session half), G-037, G-039, G-073 (topics), G-095 (SetupBootstrap) | PKCE loopback primary, device code/QR fallback, `SetupGating`; the wizard chrome can wait for W6 (R) |
| **B6 Sidebar wiring** (after J2) | Sonnet (Opus for the binder gate) | `Shell/Sidebar.Host.cs`, `Shell/Sidebar.UI.cs`, `Shell/Sidebar.UI.Drop.cs`, `Shell/Sidebar.UI.Menus.cs`, `Shell/Sidebar.UI.Rail.cs`, `Shell/Sidebar.UI.Rows.cs`, sidebar tests | G-170, G-171, G-172, G-173, G-174, G-175, G-176 (flush half), G-178, G-180, G-183 | Decisions D8 (path), sidebar D5/D8 (freeze API, rebuild gate) |
| **B7 Video host** (after engine G-160–G-163; after B3) | Opus | `Playback/Playback.Video.cs`, `Shell/Video.Host.cs`, `Shell/Video.UI.cs`, `Wavee.csproj` (native target), tests `VideoRulesTests`, NEW `VideoHostRulesTests` | G-141 (post half), G-144 (headers via B1b's `HeaderSet`), G-145, G-146, G-147, G-148, G-149, G-150, G-151 (mount half), G-152, G-153, G-201 (video tests) | Decisions D15 |
| **B8 Notify + OS + shell seams** | Sonnet | `Platform/Notify.cs`, `Platform/Notify.Host.cs`, `Shell/Shell.UI.cs` (`RouteForUri`, `Os.Activate` on first render, duplicate tint owner, the shell `ActionServices` verbs), `Shell/Shell.PlayerBar.UI.cs`/`.PlayerBar.cs` (picker cap, play-next cap/toast, fault titles), `Shell/Shell.Overlays.UI.cs` (theme toggle, `Notify.Say`), `Platform/Design.cs` (`SeedPalette`), `Platform/Actions.UI.cs` (`NowPlayingMenu`), `Platform/Controls*.cs` | G-088 (Rebuild call sites — needs the feed host from W5/W6), G-090, G-091, G-092, G-010, G-193, G-195, G-196, G-197, G-027 (shell verbs), G-210, G-211, G-212 | Decisions D29 first |
| **WP-4.5 Detail + Track surface** | Opus (M) | `Entities/Detail.cs`, `Detail.UI.cs`, `Track.cs` (rules), `Track.UI.cs`, `Track.Table.cs`, tests per plan §6.1 ch 01/03/04 | G-190 | Plan §5 Wave 4.5; gate: the fake album through the real frame in both arms — so `--fake` (G-015) must land first |
| **WP-fake** | Sonnet (Q first cut, orchestrator-owned) | `Entities/Entities.Fake.cs`, the `--fake` arm (in `Diagnostics.Probe.cs` after H-X, or `Platform.cs` after W-B), `Wavee.Tests/EntitiesFakeTests.cs`, `Sidebar.Customizer.UI.cs` samples (after J2) | G-015, G-181 (samples) | Ch 31; pinned `Clock.SeedEpoch`; rootlist with folders, four sets, pins, 166 liked, 16 covers |
| **WP-5.M/N/O/P/Q** (after WP-4.5) | Opus ×5 | per plan §5 Wave 5 (M: Album/Show/Episode/Track.Drawer; N: Artist/Discography/Concert; O: Playlist(+local)/User ×8; P: Home ×7/Search/Browse/Recents; Q: Queue.cs/Queue.UI) | G-191, G-070 (Q's `ContextQueue`), G-080 (Q), G-110 (O local arm), G-131 (M drawer), G-045 (each page's decoder, coordinated with B1's folds), G-088/G-089 (P/M feed hooks), G-194 | Each page: `SetPage` + its seams at install; ch §10 parity logged |
| **WP-6.R/S/T** | Opus ×3 | R: `Settings.*`, `Setup.*` (wizard chrome), `ReleaseNotes.UI/Host`, `Feedback.*`, updater host; S: `Diagnostics.*`, `Platform.cs`/`Platform.Host.cs` (+`Platform.Settings.cs`), probe arms; T: `Modules.*` | G-192, G-093, G-094, G-095, G-017, G-021, G-086, G-110, G-128, G-130, G-132, G-182, G-198, G-199, G-151 | `local-update-e2e.ps1` both scenarios is R's gate |
| **B9 Tests backfill** | Sonnet | `Wavee.Tests/ReleaseNotesTests.cs`, `RecentsTests.cs` (after WP-5.P), sidebar dropped facts (after B2/B6) | G-201, G-183 (tests) | — |
| **B10 Ops** | Sonnet | `Wavee.Version.props`, `launchSettings.json`, `Wavee.csproj` comments, `ops/build/bench-wavee.ps1`, `ops/build/publish-wavee-aot.ps1`, `ops/release/Wavee.Store.psm1`, `ops/release/wavee-release.ps1` (engine-commit + DLL-export asserts) | G-203, G-204 (after D3), G-152 (preflight) | Decisions D1, D16, D17 |
| **Engine issues** (in `..\fluent-gpu`, after the video branch merges) | — | `WasapiAudioDevice(deviceId)` + enumeration; `ICancellableAudioSource.InterruptPendingRead`; voice total update after open; `HeadlessAudioEndpoint(realtime)` | G-126, G-121, G-113, G-111 | File issues; 0.3.x |

---

## 5. Decisions only Christos can make

| # | Decision | Recommended answer |
|---|---|---|
| D1 | Which engine the 0.3 worktree builds against (default resolves to the video-rework branch) | Pin `EngineRoot` to `C:\WAVEE\fluent-gpu-base` via a worktree-local props file + slnx through the property; release preflight asserts the resolved engine commit; switch when the video branch merges |
| D2 | Sign-in before Wave 6 | Yes: pull the login view + PKCE loopback (primary) + device code/QR (fallback) into B5 now, behind the chrome Sign-in button and the `SignInRequired` gate; the wizard chrome stays R's in W6; expose as `login` in the headless arm |
| D3 | Where the audio key deriver is installed | A guarded `#if WAVEE_PLAYPLAY_LOCAL` line in `App.cs` mirroring 0.2.9 (public composition order shows it); confirm with the private repo whether it self-installs; then create the junction |
| D4 | Gapless hand-off model | Keep the pump's butt-join; add `AudioSignal.HandedOff` that advances the cursor without a Load; re-scope the reducer's "Ended advances in the same drain" as the hard-cut fallback |
| D5 | What a starve means | Never end a track on a starve: Buffering/Recovery, wait up to ~90 s, then `Fault.Network`; land bytes per 64 KiB slot |
| D6 | Who owns normalization | The decoders; engine `Normalization = Off` for Spotify voices; track gain only for 0.3, capped by track peak |
| D7 | Lossless fallback and rung gating | Fall back to Ogg 320 on NoDeriver/NoKey (log once); offer the Lossless rung only when `CanDerive`; default 2; metered cap 1 |
| D8 | Cache and document roots | Revert both to 0.2.9's paths: audio cache `%LOCALAPPDATA%\Wavee\Wavee\Cache\audio`, `sidebar-layout.json` under `WaveeMusic\` beside history.json; no migrations |
| D9 | Persistent audio key cache (stores derived keys, DPAPI) | Port behind `AudioKeyCacheEnabled` (zero-network cached replay); if not wanted, delete the orphan key |
| D10 | Folder identity | Add `RootlistEdge.FolderId` (group hex) and 0.2.9's convention (`Id = folder:<hex>`); delete the position-derived id |
| D11 | Who owns library writes and where "group → request" routing lives | A Spotify library host (`Spotify.Library.cs`, B2) implements `Sidebar.LibraryWrites` and the sync; App.cs installs it; one pure routing table in `Spotify.Api` + an edge-planning door in `Fetch.cs`; pages never call `Spotify.Api` directly |
| D12 | Premium gate | Keep 0.2.9: non-Premium welcome → `Failed/NotPremium`, blob cleared, Setup shows PremiumRequired |
| D13 | Suspend/resume semantics | Suspend pauses local playback only, never forwards to a Connect owner; resume re-announces `NewConnection` |
| D14 | GitHub issues to file before their fixes land (CLAUDE.md) | `connect-diagnostics` (plan Q6); ch 14 DATA GAP 1 (concert toast route), 4 (drop toast sound), 8 (sign-out teardown); "switching to video is slow" / "seeking is buggy"; the offline-strip divergence (ch 29); headless host (one issue with the four defects as bullets); Base62 0.2.x rewrite; tray is #152. Approve filing via `github-triage` |
| D15 | Video plan Q1–Q9 | Accept the plan's recommendations: 80 ms cut to the video's own soundtrack; runtime never idle while placement ≠ None, 30 s after off; manifest-only on badge, licence+init+8 s only when video on; 30 s/60 s/32 MiB/2 sessions; segment-granular scrub preview; no Phase C; MSE spike only if warm `attachMs` > 300; release script builds both DLL arches and refuses one lacking `FgPrRuntimeCreate` |
| D16 | Version identity now | `WaveeVersion` → `0.3.0-beta.1` / Crest so dev builds, logs and crash headers stop claiming 0.2.10; `WaveeBuild` stays the release script's |
| D17 | `--fake` and the probe arms before Wave 6 | Land `Diagnostics.Probe.TryRun`'s arm table (with H-X) and the `--fake` scope arm + SEED-CORE now as an orchestrator first cut; other probe arms stay S's |
| D18 | Output-device routing | Engine change (`WasapiAudioDevice` by endpoint id) for 0.3.x; 0.3 ships the enumerated list with a persisted selection and a "follows the Windows default" note (0.2.9 parity) |
| D19 | Spotify MP3 container skip | Sniff decrypted chunk 0 per file instead of deciding by format; confirm on one real MP3 episode first |
| D20 | Headless Q1/Q3/Q5/Q7 | Smoke stays a wave gate + manual rehearsal (not a release-script gate); add `SessionEventKind.Disconnect`; plain `HeadlessLoop`; add the `end-10s` position form |
| D21 | Wave 3 decisions recorded "pending Christos" (handoff d) | Confirm: lossless opt-in via the rung + metered cap; the engine's linear resampler for 0.3 (sinc + exclusive mode post-0.3); no exclusive-mode output |
| D22 | Clock for `Playback.FrameNowMs` (G-024) | A monotonic Stopwatch-ms clock (thread-safe, never frozen, never stepped back by a second host); render sites convert `FrameTime.NowQpc` explicitly at draw |
| D23 | Idle-gated `Fetch.Pump`/`Palette.Tick`/`Entities.Now` (G-025/026) | One named one-shot timer armed to the earliest deadline that posts the pump through `ToUi`; no polling |
| D24 | Credential on `AuthRejected` (G-028) | Clear only on a definitive bad-credentials reject after one retry with a fresh AP; a transient/unknown reject keeps it |
| D25 | Homes for the updater, crash reporter/run marker/crash prompt, factory reset and install-on-quit — plan §2 has no row (G-093/G-094) | `Platform/Platform.Host.cs` (SHELL, S, W6) for deployment/feed/restart/crash-file writers, pure policies in `Platform/Notify.cs` and `Screens/Diagnostics.cs`; keep 0.2.9's log sentences verbatim so `local-update-e2e.ps1` needs no change; add the rows to §2 now |
| D26 | Budget overruns (G-202: `Rail.cs` 418/200, `Video.cs` 1,412/900, `Video.Host.cs` 349/250, `Deck.cs` 1,675/1,500, `Shell.Host.cs` 1,313/1,000, `Diagnostics.Headless.cs` 1,145/700, `Sidebar.cs` 6,397/5,000) | Split `Rail.Hero`/`Rail.Friends` into `Shell/Rail.Surface.cs` and the headless JSON writers into `Diagnostics.Headless.Json.cs`; restate `Video.cs`/`Sidebar.cs` rows at their measured size; accept the ≤ 30 % ones |
| D27 | Winamp marquee pausing when not playing (ch 23 #66 vs the idle-GPU rule) | Accept (idle-GPU rule wins); amend ch 23 §10 |
| D28 | Unported lyrics sources (Musixmatch + DES decrypt, grey CJK) | Drop Musixmatch/DES (decrypting a third-party payload); keep the parsers; port the CJK sources only on demand |
| D29 | Small UI rulings: update toast via `Toast.Show` vs `Notify.Say`; player-bar error title; device picker cap | DONE (B8) — recorded for the audit trail |
| D30 | Combine the two engine branches (G-164): the tray seams (`feat/0.3-tray-seams` @ `eba1285b2`, the pin) and the finished video rework (`feat/0.3-video-runtime` @ `e4a88e3ab`) | Rebase the video branch onto the tray seams (both fork from `869b1c65e`; the video branch touches Media/PlayReady + the gallery csproj, the tray branch Hosting/Win32 — low overlap), run the engine gates once, fast-forward `fluent-gpu-pin`, then dispatch B7. Christos decides because it is a commit on the engine repo |
| D31 | Footprint gate headroom (G-064): Track rows grew to about 1,434 KB of a 1,470 KB budget | Raise the budget to 1,600 KB with the reason in the test (the video/asked columns are real), or pack `Asked` + the flags into one word; do not let the next column fail the gate silently |
| D32 | Grant owner capabilities from rootlist `meta_items` / kind-205 `source` before a capture confirms the wire (G-236)? | Capture first (one headless run with a folder + an owned and a foreign playlist). Until then `StageOwned` only from `owner_username` on a rootlist whose `meta_items` count equals its real-row count; ignore kind-205 field 7 for capabilities |
| D33 | `--fake` and offline context play: WP-5Q removed the local-members fallback so albums never play offline (G-249) | Restore it for FAKE scopes only (`Provider == "fake"`); real offline play stays refused until a cached-context policy exists |
| D34 | Start radio (G-251): keep "play the station now via context-resolve" (B3c) or port 0.2.9's seed → inspiredby-mix playlist, parked after the current track, with the "Radio started → Open playlist" toast? | Port 0.2.9's behaviour: its endpoint/resolver are known-good, station context-resolve is unverified and needs paging (G-242), the toast keys exist. Until then gate the radio rows behind one live resolve check |
| D35 | Next under repeat-one: restart the row (0.3, `Playback.cs:1389-1391`) or move on as Spotify does? | Move on and drop repeat to context (Spotify desktop parity); keep the natural-end reload |
| D36 | Replay offline/failed library writes (0.2.9's durable outbox replayed on next login) or keep "the pending bit is the outbox" (G-043)? | No replay in 0.3: delete the dead `Store.Journal`/`JournalSettle` (no legacy paths), note the divergence in ch 25; revisit a collection-only outbox in 0.3.x |
| D37 | `media.type=video` only while the video host plays (B3b, per the captures) rather than whenever `HasVideo`? | Keep the captures' rule; the offer for a video-capable row played as audio belongs in `associated_video_id` + `signals` (G-240) |
| D38 | Read-ahead tier re-evaluated only at `Promote` (G-114)? | Accept for 0.3 (memory floor); revisit with a measured throughput trigger in 0.3.x |
| D8 / pin authority | — | CLOSED as landed: zstd is decoded (keep the package); `SidebarPinStore` stays authoritative, the bridge converges membership |
| D39 | Classic "#" cell at rest: empty on non-playing rows (0.2.9 code, `Track.UI.cs:338-339`) or the number (ch 01 W8 wireframe)? | Follow the 0.2.9 code; amend ch 01 W8 — a Classic row states only the now-playing mark |
| D40 | Dev builds no longer poll the update feed (WP-6.R) | Accept: the E2E harness installs a packaged Release build (not IsDev); the simulation still walks for developers; note it in the releasing skill's gotchas |
| D41 | Who wires "Manage" on the video-override toast to `Settings.OpenVideoOverrides()` — K or I? | K: the raiser owns the action; I only provides `Notify.Say`'s action slot |
| D42 | Engine half of the reveal-ramp bug (G-259): a computation that writes a signal it read during its own run loses the wakeup (`MarkDirty` returns early while Dirty) | File the engine issue; fix the app half now (the `Done` guard + no advance in the mount run) without waiting for the engine |
| D43 | The Wave 4.5 gate subject: al3 is a 2-track Single (no Select/multi-select) and exercises the least of the surface | Point the gate at an `AlbumKind.Album` with 12 rows (al2) and keep al3 as the Single case; both sit under the 24-row G-259 window, so re-shoot both after the fix |
| D44 | After-update plate waits for its notes (ch 28 §9.4 option a) vs parity 83 | Accept option (a) (an empty plate forever was a 0.2.9 bug); log in ch 28 §11 and the CHANGELOG (G-274) |

---

## 6. Audit log

| # | Date | Pass | What changed |
|---|---|---|---|
| 1 | 2026-09-13 | First pass | Register built from the 93 reports (audio 58, core 60, video/OS 53, sidebar 48, code sweep 60 raw findings), deduplicated; every row verified in code at HEAD `e0cc9710` + the live edits. Closed since the reports were written (verified in code): the four UI marshallers + `Fetch.Pump`/`Palette.Tick` (I1), `Shell.InstallUi`/`Sidebar.InstallActionSeams`/`FrameNowMs` (App.cs), the welcome's market/scope switch and the Connect hello (H-W), the silent sink connect + pacing (H-W), `Sidebar.CustomizerPage` (J2), `Video` partial, `FgPrProbeFile`, arm64 DLL rebuild, ch 14 DATA GAPs 1/3/4/11/12, ch 24 DATA GAP 5, the FLAC three fixes, NVorbis/FlacBox removal, 21 non-public sidebar types, the Wave 2 compile-review items. Nothing was built, run or tested by the auditor |
| 2 | 2026-09-13 (late) | Same pass, second half | Folded in the wave-4 UI/tray/headless miner (82 raw findings) and the I1, J2, tray W-B, tray-engine and H-X reports; re-based on HEAD `201cd247` and the orchestrator's build report. Moved to fixed-unverified: G-020 (40 loc keys merged), G-016 (headless landed; `ProfileRoot`), G-133 (`Stream.Stats`, `Audio.Metrics`), G-178 (rail flyout → `Binder?.Entries`), tray W-B/engine seams inside G-200. New rows: G-024 (`FrameNowMs` on the UI frame clock), G-025 (`Entities.Now` never written), G-026 (idle-gated pump), G-027 (`AppActions.Register`/`Registry.Build`/`ActionServices` never composed), G-028 (`AuthRejected` wipes the credential), G-029, G-184 (J2 leftovers), G-210–G-224 (stage-B leftovers, engine UIA/ProgressBar gaps, stage-A deferrals, chapter rulings). Decisions D22–D29 added. G-019 re-stated as the test-project/Release/test-run gap. Build state recorded in §1 E1. Next pass: after H-W and W-C land and B0 builds + tests run |
| 4 | 2026-09-14 | Pass 4 — after rounds 2–3 (B0b, B1b, B2, B2b, B3, B3b, B3c, B4, B10b, D-fake, WP-4.5, WP-5.Q, WP-6.R, WP-6.S, R2) | Base: HEAD `771fce00` + ~140 uncommitted files (nothing committed by policy); Debug + Release 0/0 against the pin, 6,635/6,635; `--fake` Home rendered with the expanded Classic sidebar and the signed-in chip (G-065/G-066 verified); the album gate rendered its frame. Every report claim re-checked by three verifiers (data/session/ops, playback/audio/queue, UI/screens) plus a layout sweep; the orchestrator's 17 own claims all hold in code. 120 rows restated (≈ 95 to fixed/verified, ≈ 25 to fixed-part); three blockers dropped to DEGRADES (G-027, G-043, G-063); G-077 raised. New rows G-230–G-274 (45): edges never persisted (G-230), the pathfinder wrapper bug (G-231), sealed paging (G-232), playlist diff (G-233), owner-capability assumptions (G-236), pin-blind scripts (G-237), thin PutState (G-240), the scope-switch orphan (G-241), context paging (G-242), queued-row replay (G-243), Connect inbound/outbound leftovers (G-244–248), `--fake` context play (G-249), radio parity (G-251), the lyrics poll (G-252), the table→PlayRows bypass (G-253), the row-overflow pattern (G-254–256), the album-gate findings (G-258–G-264: the tab label, the reveal-ramp overflow that blanks every short list — BLOCKS —, unseeded groups, the fixture vocabulary, the missing heart, two shuffles), the ReleaseTool build break (G-265, BLOCKS), the AddedAt app-seconds bug (G-266), and the WP-4.5/6 leftovers (G-267–G-274). Decisions D32–D44 added; D8 and the pin authority closed. §3/§4 rewritten: WP-5.M/N/O/P in flight; round 4 = R4-0 (the two blockers) · R4-1 Connect wire · R4-2 queue/radio · R4-3 persistence/decode · R4-4 table/lyrics/layout · R4-5 ops/verification · R4-6 telemetry churn · R4-7 leftovers · WP-6.T · B9 · engine issues · B7 after D30. Next pass: after WP-5 lands, R4-0's gate re-shot, the headless smoke |
| 3 | 2026-09-13 (night) | Pass 3 — after gap-fix round 1 and B0 | Re-based on `021c1610` (round 1: Debug + Release, 4,636/4,636) and `771fce00` (B0: Debug, 4,636/4,636; the first `--fake` frame rendered). Every "fixed" claim in the B0, B1, B5, B6, B8, B10, WP-fake, tray W-C, silent-Ended and engine-video reports re-checked by caller grep against the code: 72 rows moved to fixed/verified (marked **pass 3:** / **pass 3b:** inline with the evidence), 13 marked in flight (B1b, B3, WP-4.5). Still open from B0's list: G-008 (needs B1b's `GetText`), G-014 (D3), G-023 (with B3), the slnx/release-script halves of G-022. New rows: G-062 (staging/store follow-ups), G-063 (B1b prerequisites), G-064 (footprint headroom), G-065 (`--fake` chrome says Sign in — no fake session arm), G-066 (sidebar opens collapsed at 1,770 px), G-135 (ENGINE: `Playing → Ended` never fires on a buffered sink — likely the last track of every real queue), G-164 (the two engine branches must be combined before B7), G-205 (release-script helpers untestable → `Wavee.Build.psm1`). Decisions D30, D31 added; D29 recorded done. §3 and §4 rewritten around round 2 (B1b, B3, WP-4.5) with the next round B2 · B4 · WP-5.Q · WP-6.R · WP-6.S · B10b · D-fake · the G-135 engine issue. Next pass: after round 2 lands, the headless smoke and the `--fake` re-shot |
