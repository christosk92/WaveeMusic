# Wavee 0.3 — gap register

The single source of truth for what stands between the worktree and a working end-to-end Wavee 0.3. Written by the gap
auditor from the 93 agent reports, the plans, and the code; every row was checked against the code before it was
listed. The orchestrator dispatches fix batches from §4 and resumes the auditor after each landing.

Baseline of this pass: worktree `C:\WAVEE\wavee-0.3` @ `201cd247` ("wip(0.3): wave 4 UI compiles; composition root,
loc keys, sidebar customizer, tray rules") plus the live edits of H-W (audio/session wiring), tray W-C and the engine
video branch. Stage B (I1–I3, J1–J2, K1–K3), tray W-B, the tray engine seams (`fluent-gpu` `eba1285b2`, Engine.Tests
434/434, Windows.Tests 289/289) and headless H-X are complete. **Build state:** the Wavee app assembly compiles clean in
Debug against the pinned engine `C:\WAVEE\fluent-gpu-pin` @ `eba1285b2` (the orchestrator fixed 23 stage-B errors);
`Wavee.Tests` still fails on 4 errors in files H-W and H-X are editing; no Release build and no test run since stage A
(3,543/3,543).

Conventions. **Category**: defect · wiring (missing wiring) · impl (missing implementation) · deferred (by plan to a
later wave) · data (data gap) · engine (engine gap) · parity (divergence from 0.2.9) · decision. **Severity**: BLOCKS
(blocks e2e) · DEGRADES · COSMETIC. **Owner** letters are plan §2's (A–T; X = headless implementer; U = tray; V =
video rules; O* = orchestrator). **Status**: open · in flight: `<agent>` · fixed-unverified · verified. Line numbers
are as of 2026-09-13 20:30 and drift where a file is in flight. Miner detail lives in the auditor's scratch notes
(`scratchpad\gaps\miner-{audio,core,video,sidebar,ui}.md`, `sweep-code.md`, `auditor-own-findings.md`).

---

## 1. Definition of end to end (plan §8 + G1–G6), with today's state

| # | Step | State today |
|---|---|---|
| E1 | Debug + Release build clean (`TreatWarningsAsErrors`), `Wavee.Tests` green | App assembly: Debug clean (pinned engine). Tests project: 4 errors (AudioStreamTests 436/437, PlaybackAudioTests 493 xUnit1031 → H-W; HeadlessTests 96 → H-X). Release build and the test run not done since stage A |
| E2 | Launch → window paints the shell frame (chrome row, masthead, sidebar in all three designs, rail, player bar, overlays) | App.cs now calls `Shell.InstallUi()`; frame written (I1, 1,814 lines); UNBUILT, never rendered |
| E3 | Sign in: stored 0.2.x credential resumes automatically; a fresh profile can sign in (PKCE/device code) and the profile chip shows name + avatar; Premium gated | No auto-login call; no first-sign-in flow at all (Setup wizard is a skeleton, `Spotify.Pkce` unused); profile never fetched |
| E4 | Welcome → market/scope switch, library sync (rootlist with folders, liked, albums, artists, shows, pins), recents | Welcome sets market/scope (H-W); nothing fetches rootlist/collections/recents; rootlist decoder emits no folders |
| E5 | Every 0.2.9 page renders its loaded state: home, home-section, browse, search, album (+prerelease), playlist (+local), liked, library ×3, artist, discography, show/episode, recents, history, concerts ×3, queue (rail + stage), settings, whatsnew, diagnostics ×2, module | 2 of 30 route kinds have a page (history, sidebar-customize); 58 of 159 app files are 14–16-line skeletons (Entities 38, Screens 17, Modules 3) |
| E6 | Playback: Ogg 96/160/320 and FLAC from the head file; seek inside/outside the head; gapless and crossfade; normalization; next-track prefetch; disk cache; device-format recovery; play a context; shuffle/repeat/autoplay | Single-track play path exists (unbuilt). Gapless hand-off makes every following track play twice; no play-a-context path; FLAC needs a key deriver nobody installs; normalization double-applied; starve ends the track |
| E7 | Connect both ways: device visible, transfer to Wavee resumes the context at position, transfer away, remote volume | Hello now sent (H-W); inbound transfer/play decodes no context/position; inbound volume topic dropped |
| E8 | Video: switch on/off carries position, seek (planner), PiP, fullscreen, pop-out, override manager | Surfaces written (K3); reducer never drives the video host; no manifest id decoded; engine rework in flight and ungated |
| E9 | Lyrics: rail, stage pane, NPV peek, sync, upgrade, inspector | `Lyrics.Boot` never called (no provider); `Lyrics.Store.ToUi` inline |
| E10 | OS surfaces: SMTC, taskbar overlay/thumb buttons, jump list, toasts, tray | `Playback.Os` arms on first publish (verify); `Notify.HostInstall` never called; `Notify.Rebuild` never called (no toast can raise); tray CORE + icons + engine seams landed, `Tray.Host.cs` (W-C) in flight, VerticalSlice + on-box checklist not run |
| E11 | Settings (7 tabs), setup wizard, what's new, feedback, diagnostics + logs | All skeletons except `ReleaseNotes.cs` CORE and the runtime card |
| E12 | Headless smoke (`--headless --script ops/headless/login-smoke.wh`, ogg320, far-seek-disk, flac, gapless) | H-X landed: `Diagnostics.Headless.cs` (1,145), `Diagnostics.Probe.cs` (797), 102 tests, seven `.wh` scripts + `Invoke-WaveeHeadless.ps1`; never run; the scripts' track ids are unverified placeholders |
| E13 | `--fake` renders every route's loaded state (G2), ReuseGuard silent, zero alloc on scroll | `--fake` not parsed; `Entities.Fake.cs` empty |
| E14 | Perf tour vs 0.2.9; `local-update-e2e.ps1` both scenarios; `wavee-release.ps1 -DryRun`; MSIX both arches; signing; notices; CHANGELOG issue refs | No updater/install-on-quit (E2E cannot pass); bench script uses dead flags + an env var; default `EngineRoot` is the video-rework checkout; PlayReady DLL freshness unasserted |
| E15 | `src/apps/_old` deleted, parity checklists (2,650 items) logged | Not started (Wave 6) |

---

## 2. The register

**Totals (pass 2, 2026-09-13 late): 185 rows — 36 BLOCKS · 120 DEGRADES · 29 COSMETIC.** By category: wiring 46 ·
defect 36 · parity 29 · deferred 27 · impl 19 · data 11 · decision 10 · engine 7. By status: open 169 · in flight 7 ·
fixed-unverified 9 · verified 0. The 36 blockers: G-001–005, 014, 018–020, 022, 027 (composition/build) · 030
(sign-in) · 040–043, 046, 047, 056 (data path) · 070–072 (play a context, transfer in, hello) · 100, 101, 109–111
(audio) · 140, 141, 144, 160–162 (video + engine) · 190–192 (Waves 4.5/5/6). Of those, G-001, 020, 072, 111 are
fixed-unverified and G-019, 160–162 are in flight.

### 2.1 Composition root, boot, build (files: `App.cs`, `Shell/Shell.Host.cs`, `Directory.Build.props`, `Wavee.slnx`, loc)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in (owner, wave) | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-001 | `Shell.InstallUi()` was never called; window painted an empty box | `App.cs:34` now calls it; `Shell.UI.cs:49`; `Shell.Host.cs:606` | wiring | BLOCKS | App.cs (O*) | fixed-unverified | Verify with `--frames 60 --screenshot`: frame renders |
| G-002 | No auto-login at launch: nothing calls `Spotify.Login()` except the chip's Sign in/Reconnect buttons; a stored 0.2.x credential is never used | `Spotify.Session.cs:256`; `Shell.UI.cs:838,1130`; `_old/Wavee/Program.cs:437-440` | parity | BLOCKS | App.cs / Shell.Host.cs first-render effect (O*) | open | After the marshallers are attached: `if (Platform.HasStoredCredential()) Spotify.Login();`. Test: pure boot-plan fact + headless `login-smoke.wh` |
| G-003 | `Spotify.Api.Boot()` never called → `Fetch.Register(Transport)` never runs; the planner skips every demand until the first `Api.Run` (a playback/Connect call) | `Spotify.Api.cs:109-128`; `Fetch.cs:559` | wiring | BLOCKS | App.cs (O*) | open | `Spotify.Api.Boot();` after `Spotify.Boot()`. Test: FetchTests fact "a demand planned after composition reaches the provider" |
| G-004 | `Sidebar.Boot()` never called: design/width/collapse/V3 state never restored, `sidebar-layout.json` never loaded, `s_loaded` false so no mutation commits | `Sidebar.Host.cs:80,87-121,774` | wiring | BLOCKS | App.cs (O*) | open | `Sidebar.Boot()` after `Entities.Boot`. Test: change design, restart → persisted |
| G-005 | `Sidebar.Activate(post)` never installed: `Sidebar.ToUi` inline → store `WriteCompleted` and source `Report`s write signals from pool/network threads (C1/C9) | `Sidebar.Host.cs:64,123-133,586-594`; `Shell.Host.cs:241-265` | wiring | BLOCKS | Shell.Host.cs `InstallMarshallers`/`AttachUiPoster` (I/O*) | open | Add `Sidebar.Activate(post)` beside `Store.Post = post` |
| G-006 | `Sidebar.InstallActionSeams()` never called → pin/unpin descriptors permanently disabled | `App.cs:33` now calls it; `Sidebar.UI.Menus.cs:33-45` | wiring | DEGRADES | App.cs | fixed-unverified | Test: a FixedEntity pin binding resolves Available |
| G-007 | `Store.Use(library.db)` / `Store.Shutdown()` never called: graph is memory-only, no `KindShape` registered, no WAL checkpoint | `Store.cs:633-660,771-773` | wiring | DEGRADES | App.cs + kind owners + exit tail (O*) | open | `Store.Use(Path.Combine(Platform.LocalFolder,"library.db"))` before `Entities.Boot` (skip under `--fake`/headless per plan §2.8); `Store.Shutdown()` in the tail. StoreTests cover the round trip |
| G-008 | Lyrics stack never composed: `Lyrics.Boot(...)` has no caller (no provider → every read "unknown"); `Lyrics.Store.ToUi` not installed; the Spotify-native source needs a bearer absolute-URL GET `Spotify.Api` does not expose | `Lyrics.Host.cs:1638,1772-1788`; `Spotify.Api.cs:1207-1211` | wiring | DEGRADES | App.cs (O*), Spotify.Api.cs (F: `Api.GetText(url, ct)`), Shell.Host.cs (ToUi) | open | Add `Api.GetText`; compose `Lyrics.Boot(resolve, (u,ct)=>Task.Run(()=>Api.GetText(u,ct)), Spotify.SpclientBaseUrl)`; install `Lyrics.Store.ToUi = post`. LyricsHostTests fact over a fake GET |
| G-009 | `Notify.HostInstall(post, deepLink, icon)` never called ("from Shell.Run, after the window exists"); Run only calls `HostShutdown` | `Notify.Host.cs:47-52`; `Shell.Host.cs:124-207` | wiring | DEGRADES | Shell.Host.cs `AttachUiPoster` (I/O*) | open | Call with `ApplyDeepLink`; test a toast activation reaches the deep-link intake via the NotifyTests seam |
| G-010 | `Playback.Os.Activate(hwnd, state)` has no explicit caller; `Publish` self-activates with `FluentApp.WindowHandle` on the first reducer effect — SMTC/taskbar/jump list/keep-awake arm only if that happens after the window exists | `Playback.Os.cs:73-90,116-120`; `Shell.Host.cs:589-591` | wiring | DEGRADES | Shell.UI.cs first-render effect (I) | open (verify at runtime) | If the first publish precedes the HWND, call `Playback.Os.Activate(FluentApp.WindowHandle, Playback.Snap())` once on first render |
| G-011 | `Playback.FrameNowMs` left at `TickCount64` | `App.cs:30` now binds `Design.FrameTime.NowMs` | wiring | COSMETIC | App.cs | fixed-unverified | — |
| G-012 | Exit tail incomplete: `Spotify.Telemetry.Shutdown` (last gabo batch lost), `Playback.Audio.Shutdown` (WASAPI), `Playback.Video.Shutdown` (pump/CDM), `Store.Shutdown`, `DiskCache.Shared.Dispose`, `Fetcher.Shared.Dispose`, `Sidebar.Flush`+`Store.FlushNow` never called | `Shell.Host.cs:196-206`; `Spotify.Telemetry.cs:240-250`; `Playback.Audio.cs:345-355`; `Playback.Video.cs:326`; `Sidebar.Host.cs:783-787,1243` | wiring | DEGRADES | Shell.Host.cs finally / App.cs after Run (O*) | open | Order: sidebar flush → telemetry flush → audio → video → store → cache/fetcher dispose → platform. Check: exit log tail shows the drains |
| G-013 | UI marshallers are installed inside `Shell.Run`, after `Entities.Boot`/`Spotify.Boot`/`Playback.Boot`; once `Store.Use` lands, `Store.Warm` answers commit inline on the store thread | `App.cs:28-35`; `Shell.Host.cs:130,241-253`; `Store.cs:908-927` | defect | DEGRADES | App.cs + Shell.Host.cs (O*) | open | Make `Shell.InstallMarshallers()` public and call it first in `Main` (its queue-until-root poster handles pre-window posts). ShellHostTests fact: a post before attach is delivered in order |
| G-014 | `Spotify.Audio.KeyDeriver` never assigned by any public file (0.2.9 bound it under `#if WAVEE_PLAYPLAY_LOCAL`): FLAC and every AP-refused Ogg answer `NoDeriver`/`NoKey` unless the private sources self-install | `Spotify.Audio.cs:136-140`; `Wavee.csproj:160-166`; `_old/Wavee/SpotifyLive/Audio/AudioPlaybackStack.cs:30-67`, `_old/Wavee/Program.cs:333` | decision / wiring | BLOCKS (lossless; refused keys) | App.cs guarded line (O*) + private repo | open — decision D3 | `#if WAVEE_PLAYPLAY_LOCAL` install before `Playback.Boot()`; public build keeps `NoDeriver` + 320 fallback (G-101). Check: `audio.head … key=` log on a deriver build; `CanDerive == false` on public |
| G-015 | `--fake` is not parsed; `Platform.Scope` has no fake arm; `Entities.Fake.cs` is empty (SEED-CORE was due in Wave 4 as an orchestrator cut); bundled covers/fixtures have no consumer → CLAUDE.md's offline demo, the Wave-4/5 gates and the customizer miniature all lack their seed | `Platform.cs:1006`; `Entities/Entities.Fake.cs:1-14`; `Shell.Host.cs:209-224`; `Sidebar.Customizer.UI.cs:73-79` | deferred | DEGRADES (blocks every offline verification gate) | `Screens/Diagnostics.Probe.cs` or Platform.cs (`--fake` arm, O* first cut), `Entities/Entities.Fake.cs` (Q), `Shell/Sidebar.Customizer.UI.cs` samples | open — decision D17 | Parse `--fake` in the probe arm table → `Platform.Scope = Fake`, `Clock.SeedEpoch` pinned, `Entities.SeedFake()` per ch 31 (rootlist with folders, four collection sets, pins, 166 liked, 16 covers). Tests: `EntitiesFakeTests` (determinism) |
| G-016 | Headless arm: `Diagnostics.Headless.cs` (1,145/700 — over 30 %, needs the named partial `Headless.Json.cs`), `Diagnostics.Probe.cs` (797/800 — owner S's Wave-6 arms need a partial), `HeadlessTests.cs` (102 facts, 1 compile error open), seven `.wh` scripts + wrapper; `Platform.ProfileRoot` declared (+ tests); plan §5 and handoff-d lines rewritten. Open: the scripts' track ids are Web-API doc examples (the gapless album id least certain); `parse-only.wh` has no plan section; headless plan still "DRAFT", §8 answers recorded only in a code comment | `App.cs:21-27`; H-X report; `ls ops/headless` | impl | DEGRADES (was BLOCKS) | H-X (done) → O* (split, run) | fixed-unverified | Run `parse-only.wh` offline, then the five smoke scripts on the orchestrator's profile; replace the placeholder ids; split the CORE file; record §8 answers in the plan like the tray plan's §13 |
| G-017 | Localization tables never loaded (`Localization.LoadFolder`/`SetCulture` "left for S"): English resolves through the generator's neutral table, ko-KR/nl and `localization.culture` never apply | `Platform.cs:1007-1008` (LEFT FOR S); csproj:194 comment; `_old/Wavee/App/AppLocale.cs:19-29` | deferred | DEGRADES | Platform.Host.cs `HostOpenLocale` (S; first cut can land now) | open | Load `assets/loc` + `TrySetCulture(Locale.UiCulture)` in `Platform.Boot`. Smoke: a ko-KR label resolves |
| G-018 | Wave 2 and Wave 3 real-traffic gates (login smoke decoding a live `getAlbum`; 10 s through the real pump) were never run — every wiring gap in §2.2–2.5 is invisible to the unit suite | `handoff-20260912d:63-64,89-90` | deferred | BLOCKS (verification) | ops/headless + Probe (H-X, O*) | open | Run `login-smoke.wh`, `ogg320.wh`, `far-seek-disk.wh`, `flac.wh`, `gapless-album.wh` on the orchestrator's profile once G-016 lands |
| G-019 | Build/test verification: the app assembly now compiles in Debug (23 stage-B errors fixed by the orchestrator); `Wavee.Tests` fails on 4 errors in H-W/H-X files (AudioStreamTests 436/437, PlaybackAudioTests 493 xUnit1031; HeadlessTests 96 CS0649 reported fixed, unbuilt); no Release build; no test run since stage A — so none of the ≈ 30k lines written since (stage B, audio swap, FLAC SIMD, headless, tray, App.cs) has run a single fact | orchestrator's build report; `git status` | defect | BLOCKS | H-W, H-X, then O* | in flight: H-W / H-X | After H-W lands: Debug + Release (`-p:EngineRoot=C:\WAVEE\fluent-gpu-pin\`), `dotnet test` (baseline 3,543 + ≈ 700 new); hand failures to owners by file |
| G-020 | 40 pending stage-B/tray loc keys were unmerged (11 referenced: `Strings.Lyrics.*` → CS0117; `runtime.sig.title`, `sidebar.customizer.tooNew/tooNewSub` literal) | orchestrator merged all 40 into `en-US.json` (commit `201cd247`) | wiring | BLOCKS (compile) | `assets/loc/en-US.json` (O*) | fixed-unverified | Release build confirms the generated `Strings.Lyrics.*`/`Strings.Tray.*` members; ko-KR/nl keep falling back (G-017) |
| G-021 | `Modules.Boot()` is `{ }`; `Shell.LinkModules`, `Shell.MatchLink`, `Playback.Audio.ModuleOpen/LocalPath` unassigned; `module:` route has no page; csproj still stages YouTube/Twitch/Radio with nothing to host them | `Modules.Host.cs:15`; `Shell.Overlays.UI.cs:63,71`; `Playback.Audio.cs:2354,2358` | deferred | DEGRADES (modules, local files) | `Platform/Modules*.cs` (T, W6) | open | Port the 0.2.9 module host/router/catalog + `_old/Wavee.Tests/Modules/*` (15 files) |
| G-022 | Default `EngineRoot` = `..\fluent-gpu` = `C:\WAVEE\fluent-gpu` = (case-insensitive) `C:\wavee\fluent-gpu`, the unfinished video-rework branch (`feat/0.3-video-runtime`, 2 wip commits over `869b1c65e`); `Wavee.slnx` hardcodes the same path; builds pass only because the orchestrator passes `-p:EngineRoot=C:\WAVEE\fluent-gpu-pin\` (a second worktree, now @ `eba1285b2` = the tray seams); `wavee-release.ps1` and a plain `dotnet build Wavee.slnx` would build the wrong engine | `Directory.Build.props:36`; `Wavee.slnx:5-9`; `git branch` in each checkout | decision | BLOCKS (reproducibility) | `Directory.Build.props`, `Wavee.slnx`, release script (O*) | open — decision D1 | Worktree-local props override to the pin + slnx via the property; release preflight asserts the resolved engine commit |
| G-023 | `Spotify.Telemetry.Boot()` never called (lazy on first event) and `Shutdown` never called (see G-012); play registration itself unwired (G-076) | `Spotify.Telemetry.cs:209,242` | wiring | DEGRADES | App.cs (O*) | open | `Telemetry.Boot()` on Online (with G-076) |
| G-024 | `Playback.FrameNowMs` now reads `Design.FrameTime.NowMs`, the UI frame clock: it FREEZES while the window is idle, minimized or tray-hidden and is last-writer across hosts (the pop-out window's own `RunFrame` can step it back), yet the audio thread's position throttle, the 1 s ticker, the video watchdog, SMTC rebuild throttles and Connect snapshots all stamp with it off the UI thread | `App.cs:30`; `Design.cs:1500-1506`; engine `Hooks/Context.cs:69-73`; readers in `Playback.Audio.cs` (position throttle), `Playback.Host.cs`, `Playback.Video.cs`, `Playback.Os.cs:593-602` | defect | DEGRADES (background playback position/timeline; tray-hidden playback) | App.cs (O*) + render sites (K/I) | open — decision D22 | Monotonic Stopwatch ms clock for `FrameNowMs`; render sites that want vsync-lattice time convert `FrameTime.NowQpc` explicitly. Test: minimize 30 s while playing → SMTC timeline advances |
| G-025 | `Entities.Now` is never written in the GUI (only the headless tick sets it) → `Fetch`'s backoff `ReadyAt` never expires and `Palette`'s TTL never ages | `Fetch.cs:385,551,682`; `Palette.cs:119,400`; grep `Entities.Now =` in the app → none | wiring | DEGRADES (a 429/5xx backoff never re-sends) | Shell.Host.cs `FrameCompleted` (the orchestrator asked the tray W-C agent to add it) | in flight: W-C | `Entities.Now = unixSeconds` on the frame tick + the idle timer of G-026; FetchTests fact: an expired backoff re-plans |
| G-026 | `Fetch.Pump()`/`Palette.Tick()`/`Entities.Now` ride `FluentApp.FrameCompleted`, which fires only on RENDERED frames → an expired backoff or grading debounce waits while the window is idle, minimized or tray-hidden (exactly when background playback needs the next fetch) | `Shell.Host.cs:255-264`; `Fetch.cs:545-547` | decision / defect | DEGRADES | Shell.Host.cs (I/O*) | open — decision D23 | One named one-shot timer armed to the earliest `ReadyAt` that posts `Fetch.Pump()` through `ToUi`; test by forcing a 429 with the window idle |
| G-027 | The action platform is never composed: `Actions.Registry.Build` has no caller (palette shows only its 19 builtins; bound sidebar actions resolve nothing), `AppActions.Register` has no caller (every `Actions.Menu.Row(id)` returns null → context menus have NO verb rows), and the `ActionServices` verbs other than confirm/clipboard/post/pins are null (`Go`, `CurrentRoute`, `CurrentDestination`, `Play`, `PlayNext`, `AddToQueue`, `StartRadio`, `IsSaved`, `SetSaved`) → bound actions render HostUnavailable, the player-bar play-next drop silently returns | `Actions.cs:239-257,775-781,1033`; `Shell.Palette.cs:307,354`; `Sidebar.UI.cs:524`; `Shell.Overlays.UI.cs:94-104`; `Shell.PlayerBar.UI.cs:645` | wiring | BLOCKS (menus, palette, bound actions) | Shell.UI.cs `InstallUi` (I: Build + Go/CurrentRoute/CurrentDestination), Queue owner Q (Play/PlayNext/AddToQueue/StartRadio), User.cs O (IsSaved/SetSaved), each entity owner (`AppActions.Register` of its verbs) | open | Build once at install; ActionsTests: `Current` holds the 13 built-ins after Build; `Actions.Resolve` becomes Available once the seams are set |
| G-028 | A rejected stored login still wipes the credential: `AuthRejected` → `HasCredential = false`, `ClearCredential + CloseAll` effects — one transient rejection on a live profile loses the sign-in (0.2.9's probes used `clearStoredOnReject:false`); `Disconnect` protects only the exit path | `Spotify.cs:241-246`; `Spotify.Session.cs:219` | decision / defect | DEGRADES | Spotify.cs (D; after H-W) | open — decision D24 | Clear only on a definitive bad-credentials reject after one retry against a fresh AP; a transient/unknown reject keeps it; pure `Spotify.Step` test |
| G-029 | Headless-found tidy-ups: the headless device shows `Environment.MachineName` (needs owner G's name seam); a headless run beside the GUI rewrites the whole shared `store.json` per process; without `--connect` local play still sends the reducer's Connect state updates; stdin/pipe reads can stall the loop | H-X report | defect | COSMETIC | Playback.Host.cs (G), Platform.Host.cs (S), Diagnostics.Probe.cs (X) | open | Name seam on the device identity; per-process store section or read-modify-write with a lock |

### 2.2 Sign-in, session, account

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-030 | A fresh profile cannot sign in: `Shell.Auth` is never written (`FoldAuth` has no caller), nothing routes to Setup, `Setup.UI.cs` is a skeleton, and there is no PKCE/device-code/QR flow (`Spotify.Pkce` unused) — only an already-stored blob logs in | `Shell.cs:1355-1366`; `Shell.Host.cs:86`; `Screens/Setup.UI.cs` (14 lines); `Spotify.cs:800-839`; `Spotify.Session.cs:68-93`; `_old/Wavee/WaveeApp.cs:343-368`, `_old/.../LiveSessionHost.cs:160-164,1110-1117` | impl | BLOCKS (fresh machine) | `Spotify.cs` + `Spotify.Session.cs` (D: pure steps + runner), `Screens/Setup.*` (R, W6), `Shell.UI.cs` gating effect (I) | open — decision D2 | Port both 0.2.9 flows ending in `Platform.SaveCredential(OAuthToken)` → `Spotify.Login()`; a UI-thread effect writes `Shell.Auth = FoldAuth(...)` and the frame mounts the sign-in surface while `SignInRequired`. Facts: device-code poll fold, PKCE loopback parse, `SetupGating` |
| G-031 | `session.lastAccount` never written → `ResolveScope`'s fallback never fires; account change undetectable; the 0.2.9 local-library reset on account change is missing | `Platform.cs:245,954`; `Spotify.Session.cs:229-247` (`AdoptWelcome`) | wiring | DEGRADES | Spotify.Session.cs `AdoptWelcome` (D; after H-W) | open | `Settings.Set(Keys.LastAccount, account)` on change; SpotifySessionTests fact over a memory settings store |
| G-032 | Free/unknown-tier accounts are not gated: `SessionFault.NotPremium` never produced; PutState announces `license=premium` regardless | `Spotify.cs:81,221-230`; `Spotify.Decode.Connect.cs:628-631`; `_old/.../LiveSessionHost.cs:177-188` | parity | DEGRADES | Spotify.cs (D) | open — decision D12 | `Step(Welcome)` with `Tier != Premium` → `Failed/NotPremium` + clear/close; SpotifyCoreTests fact |
| G-033 | The account's own profile (name, avatar) is never fetched: `CatalogKindOf(User)` Unknown, no kind-15 decode arm, `Api.Profile` uncalled, `hm://identity/user-profile-changed` unhandled; the chip reads `User.Me` Identity | `Spotify.Api.cs:392-401,555-561,1264-1268`; `Spotify.Decode.cs:893-919`; `Shell.Overlays.UI.cs:427-432` | data | DEGRADES | Spotify.Api.cs (F), Spotify.Decode.cs (E), Spotify.Connect.cs (topic) | open | Map User→`UserProfile(15)`, `Decode.UserProfile` → `StagedUser`, REST fallback, `Ensure(User.Me, Identity)` on welcome, coalesced push. DecodeTests fixture |
| G-034 | `Audio.ResetKeyLatch()` never called ("Session calls it on a fresh login"): an AP refusal latch and the key cache survive logout/account switch | `Spotify.Audio.cs:460-465` | wiring | DEGRADES | Spotify.Session.cs welcome effect (D) | open | Call inside the Welcome effect; SpotifyAudioTests latch fact |
| G-035 | Client token never refreshed: minted once per AP attempt, expiry stored but unchecked, the 401 retry re-mints login5 only | `Spotify.Session.cs:811-868,1027-1051`; `Spotify.cs:239-244` | defect | DEGRADES | Spotify.Session.cs (D) | open | `ClientToken()` re-mints under `TokenGate` when `Now >= expiry − 120 s`; fact behind an injectable clock |
| G-036 | Logout/drop/epoch bump never reaches Playback or Connect: no `Playback.Release(Logout)` caller, `Connect.Clear()` never called, `DrainConnect` ignores `Item.Epoch` → a dead session's clusters are folded, no BecameInactive | `Playback.cs:87`; `Spotify.Connect.cs:123-128`; `Playback.Host.cs:334-349` | wiring | DEGRADES | Playback.Host.cs (G), Spotify.Session.cs (D) | open | On Logout/Dropped post `Input.Release(Logout)` + `Connect.Clear()`; drop `item.Epoch != Current.Epoch`. PlaybackStepTests fact |
| G-037 | Sign-out teardown never called: `Playback.Os.SignedOut` (jump list) and `Notify.SignedOut` (unschedule drops/daylist, image cache) have no caller (ch 14 DATA GAP 8, decided fix) | `Playback.Os.cs:105-109`; `Notify.Host.cs:89-95`; `Shell.Overlays.UI.cs:602`; `Spotify.cs:275` | wiring | DEGRADES | Shell.Host.cs logout handler or Spotify.cs Logout effect | open — issue owed (D14) | Call both on `SessionEventKind.Logout`; manual parity 43 |
| G-038 | Any TRACK_V4/EPISODE_V4 failure (transport 0, 401-after-retry, 5xx, empty) opens as `Fault.Restricted` — a terminal "unavailable" instead of a retryable network fault | `Spotify.Audio.cs:344-364,673-685` | defect | DEGRADES | Spotify.Audio.cs (F) | open | Return the status; `0/429/≥500` → `Fault.Network`, 200-empty/404 → Restricted. SpotifyAudioTests status-map facts |
| G-039 | Headless `Logout` semantics: today closes sockets AND clears the credential; headless plan wants a `Disconnect` that closes without clearing; the store wrapper is the only protection | `Spotify.cs:275-279`; headless plan §8 Q3 | decision | COSMETIC | Spotify.cs (D) | open — decision D20 | Add `SessionEventKind.Disconnect` |

### 2.3 Data: fetch planner ↔ `Spotify.Api` ↔ decode (the path every page needs)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-040 | The fetch provider ignores `batch.Wanted`: POSTs one extension kind per uri whatever the page asked; `TrackFields.PlayCount/Audio/Tags/Video/Publishing`, `ArtistFields.Overview/Chart`, `AlbumFields.TopTrack`, `PlaylistFields.Saves` never arrive; and because `Table.Applied` clears `Inflight` when any group lands, the next `Ensure(Row)` re-POSTs the same V4 (the sidebar's per-rebuild `Fill` → a request loop) | `Spotify.Api.cs:577-597`; `Entities.cs:1295-1304`; `Spotify.Decode.cs:461`; `Sidebar.Host.cs:2108,2129`; `Rail.UI.cs:455` | impl | BLOCKS | Spotify.Api.cs (F) + Fetch.cs (C) | open — decision D-data (routing table home) | Pure `Api.RoutesFor(EntityKind, wanted)` → extension kinds sent as ONE mixed-kind POST per uri; non-XM groups (overview → pathfinder, chart, popcount) dispatched per subject; groups no route serves are sealed. FetchTests: "a partial answer does not re-ask" |
| G-041 | `wavee:home`, `wavee:search:*`, `wavee:browse` map to `EntityProvider.None` → the planner abandons them; `Api.Home`/`Api.Search` folds have no caller; `HomeSection`/`Browse*` have no decoder | `Entities.cs:741-751`; `Fetch.cs:469-475`; `Home.cs:406-408,519-523`; `Search.cs:148,363-367`; `Spotify.Api.cs:848-853,1074-1082` | impl | BLOCKS | Entities.cs routing (A), Spotify.Api.cs pathfinder provider (F), Spotify.Decode.Pathfinder.cs (E) | open | Route the synthetic prefixes to a pathfinder provider that runs Home/HomeSection/Search/Browse* and answers through `Fetch.Answer`; PathfinderDecodeTests already hold `home.json` |
| G-042 | Relations are never fetched and there is no edge-fetch door: `Api.Playlist/PlaylistDiff/Rootlist/RootlistChanges/Recents/RecentsDiff/CollectionPage/CollectionDelta/Album/ArtistOverview/Discography` have no caller; `Decode.PlaylistV2/PlaylistRevision/CollectionPage/Recents/LibraryV3` have no production caller (`LibraryV3` reachable only from the offline `Export` path) → `Edges.Rootlist/Liked/SavedAlbums/FollowedArtists/SavedShows/Pins/ArtistPopular/FeedSection` never fill; no library sync on login; every library/liked/sidebar surface skeletons forever | `Spotify.Api.cs:1345-1398,1508-1524`; `Spotify.Decode.cs:1349,1503,1728`; `Spotify.Decode.Pathfinder.cs:994-1071`; `User.cs:319` (`ReplaceRootlist` no caller); `Sidebar.Host.cs:2085,2120,2243,2996` | impl | BLOCKS | Fetch.cs + Edges.cs (C/B: `Entities.EnsureEdge(relation, parent, page)`), Spotify.Api.cs (F: provider routes), a login-time library sync host (new SHELL file, see B2) | open — decision D11 | Plan edges like rows (inflight per parent); routes: playlist v2 + revision diff (`%2C`), collection paging 300/set, rootlist `?decorate=revision`, recents diff, pathfinder album/artist/discography; dealer pushes → deltas. EdgesStagingTests-style facts from captured fixtures |
| G-043 | No request-body encoders for any library/playlist write (`Col.PageRequest/WriteRequest`, `Pl.ListChanges`, create, signals, rootlist changes, autoplay), plan's `Api.CollectionAdd/Remove` absent, `Store.Journal` has no network drain; and `Sidebar.LibraryWrites` (every move/deposit/create/rename/delete in the sidebar, tab DnD) is never installed and has no Spotify-side composer (0.2.9's `TryBuildMove`/`PlaylistDiffApplier` dropped) | `Spotify.Api.cs:1298-1302,1362-1402,1508-1524`; `Store.cs:1381`; `Sidebar.Host.cs:3076-3116`; plan §4:953 | impl | BLOCKS (like/save/follow/add/create/move) | `+Spotify.Encode.cs` (E, new named partial: pure encoders), Spotify.Api.cs (F), the library sync host (B2), App.cs installs `Sidebar.LibraryWrites` | open — decision D11 | Port `CollectionWriteMapper`, `PlaylistWireMapper.Build{Changes,RootlistChanges,CreateChanges}`, `SpotifyIds.NewGroupId`, folder markers, signals; optimistic edge write + settle + undo toast; byte-exact facts vs 0.2.9 fixtures; restore `RootlistDropScenarioTests`/`RootlistSlotToOpTests` op-order facts |
| G-044 | Extended-metadata kinds the API asks for are dropped by the decoder: 15 (user profile), 186 (credits), 151 (recommended playlists), 98 (audio associations), 237 (waveforms) → `Api.UserProfiles/TrackCredits/AlbumRecommendations/TrackExpansion` answer with nothing staged | `Spotify.Decode.cs:46-61,893-919`; `Spotify.Api.cs:515-561` | data | DEGRADES | Spotify.Decode.cs (E) | open | Port the projectors (`Backend/Hydration/Projectors/*`): prerelease under 3 keys, waveform 220 columns, recommendations Take(12). DecodeTests from generated protos |
| G-045 | No decoder for: browseAll/browsePage/browseSection, HomeSection, queryNpvArtist, queryAlbumMerch, similarAlbums, whatsNewFeed, userTopContent, artistDiscographyAll, ArtistTopTracksExtended, the 12 concert calls (`StagedConcert` has no producer), content filters, friend feed/presence, gander notifications, popcount, profile REST, ContextResolve, Autoplay | `Spotify.Api.cs:855-1298`; `Entities/Concert.cs:380-407`; grep `Spotify.Decode*.cs` | impl | DEGRADES (BLOCKS the pages that need them) | Spotify.Decode.Pathfinder.cs / Spotify.Decode.cs (E) with each page owner (M/N/O/P/Q, W5) | open | One `Utf8JsonReader` fold per op into `Staging` with a fixture each; wire into the pathfinder provider |
| G-046 | Rootlist decode never emits folder markers or names: every row is `RootlistKind.Item`, `edge.Text` (FolderName) never set, folder nodes have no `EntityKind` so `Stage` drops them; `SidebarProjection.WalkRootlist` expects FolderStart/FolderEnd | `Spotify.Decode.Pathfinder.cs:1031-1038`; `Edges.Staging.cs:446`; `Sidebar.cs:4254-4297` | data | BLOCKS (folders, rail flyout, Move to folder, outdent) | Spotify.Decode*.cs (E) | open | Emit `FolderStart(depth, name, groupId)`/`FolderEnd` from `spotify:start-group:<hex>:<name>`/`end-group`; staging test over a two-level folder fixture |
| G-047 | Folder identity is position-derived and prefix-mismatched: `RootlistEdge` has no `FolderId`, ids are `"folder:"+Position` (0.2.9: `FolderId` = bare group hex, `Id = ForFolder(hex)`); ids move with the folder, persisted `v3.expandedFolders` and 0.2.9 folder pins never match, `PinSyncRules` would sync `spotify:folder:<position>`, `LibraryWrites.MoveRootlist/RenameFolder/DeleteFolder` get ids Spotify cannot resolve; `ResolveUnlistedPin` uses the bare convention while the projection uses the prefixed one | `Edges.cs:551`; `Sidebar.cs:4258,4273-4274,4584,5552,5581-5582,5629-5630`; `_old/.../SidebarProjection.cs:174,182` | data / defect | BLOCKS | Edges.cs (B: `RootlistEdge.FolderId StringId`), Spotify decode (E), Sidebar.cs `WalkRootlist` (J) | open — decision D10 | Add the column, decode the group hex, `Id = SidebarPinId.ForFolder(hex)`, `FolderId = hex`, `ParentFolderId = parent hex`; projection test: a moved folder keeps its id |
| G-048 | Rootlist rows are staged `Authority.Thin` with no capability block → every row reads not-owner/not-editable (deposits refuse "can't edit", Rename/Delete/Access absent, no V3 qualifier chips); nothing Ensures the sidebar's model on mount (ch 25 §7), V3 dropped 0.2.9's seven `LibraryStore.Ensure*` warmers | `Spotify.Decode.Pathfinder.cs:1031`; `Sidebar.cs:4318,4334,4345-4353`; `Playlist.cs:70,401-403`; grep `Entities.Ensure(` in Sidebar*.cs | data / wiring | DEGRADES | rootlist decode (E: caps from `decorate` attributes) + Sidebar.UI.cs PaneHost effect (J) | open | Decode owner/capabilities; one effect Ensures the four relations + `PlaylistFields.Identity|Capabilities` at Visible priority. Projection test: an owned playlist reports ByYou + CanEdit |
| G-049 | Pins never sync with Spotify's ylpin set (read or write): `SidebarPinSync` bridge not ported, `Edges.Pins` never folded into `SidebarPinStore.ApplyRemote`, `OnLocalPinChanged` never becomes a collection write, `PinsMigratedToServer` unread, `User.Dispatch` unimplemented | `Sidebar.Host.cs:1423-1425,1443,1563-1587`; `Platform.cs:100`; `User.cs:328`; `_old/Wavee/App/SidebarPinSync.cs` | parity | DEGRADES | new bridge in Sidebar.Host.cs (J) + collection write (B2) | open — decision (pin authority, recommended: keep `SidebarPinStore` authoritative, port the bridge) | Port SidebarPinSync (settled/delta fold, `PinSyncRules.IsSyncable`, migration latch); restore the 12 dropped `SidebarPinSyncTests` |
| G-050 | `EdgeState` has no Failed member → `PublishState` never reports Failed; V3's error banner, full-page Retry vacancy and parity item 73 unreachable | `Edges.cs:58-66`; `Sidebar.Host.cs:2933-2936`; `Sidebar.UI.LibraryV3.cs:541,567,609-612` | data | DEGRADES | Edges.cs (B) + binder (J) | open | Per-relation failure bit (or Fetch error state) mapped to the binder; V3 test on a failed rootlist with zero rows |
| G-051 | `Browse.Commit` is not in the commit chain → a decoded browse page never lands | `Browse.cs:241-244`; `Entities.cs:1925-1945` | wiring | DEGRADES | Browse.cs + Entities.cs (B/P) | open | `static partial void CommitBrowse(Staging s)` beside `CommitHome`; BrowseTests through `Entities.Commit` |
| G-052 | Retiring a scope leaks the synthetic tables' text: `Scope.Tables` lists the 8 kind tables only, so Homes/Sections/Searches/Browses strings and `Edges.RecentsRevision` are never released on `Entities.Switch` — which the welcome effect now runs on every login | `Entities.cs:1448,1460-1463,1775-1786` | defect | DEGRADES | Entities.cs (A) + Edges.cs (B) | open | Add the synthetic tables to `Tables` / release in `Scope.ReleaseText`; EntitiesTests fact counting StringTable refs across a Switch |
| G-053 | Small spclient semantics dropped: `LikedContentFilters` has no `If-None-Match`/304 path; search has no 8 s deadline; `/permission/base` GET missing | `Spotify.Api.cs:1067-1072,1215-1216,1470-1488` | parity | COSMETIC | Spotify.Api.cs (F) | open | Etag in/out, linked 8 s CTS, `PlaylistPermissionBase(id)`; SpotifyApiTests header facts |
| G-054 | `ZstdSharp.Port` referenced "for zstd /changes + /diff" but nothing decodes zstd and `DecompressionMethods.All` does not cover it → playlist diffs will not decode once wired | `Wavee.csproj:176-177`; `Spotify.Api.cs:148,1353-1366` | impl | DEGRADES (latent) | Spotify.Api.cs (F) | open — decision D8 | Decode zstd bodies on the two playlist routes, or drop the package |
| G-055 | `Palette.Host.Filler` unassigned → cover gradings come only from kind-179 metadata; washes/placeholders neutral otherwise | `Palette.Host.cs:47-51` | wiring | DEGRADES | Spotify.Api.cs + composition (F/L) | open | Filler over the extended-metadata colour endpoint installed after login; PaletteHostTests cover the pump |
| G-056 | No manifest id in the catalogue: TrackV4 `original_video[0].gid` never decoded, no `VideoGid` column, none of 0.2.9's four resolver tiers; `Manifest.Resolve` has no caller → video can never be opened | `Spotify/Protos/metadata.proto:98-104`; `Spotify.Decode.cs:1210-1243`; `Track.cs:170-173` | data | BLOCKS (video) | Spotify.Decode.cs (E), Track.cs (A: `Column<StringId> VideoGid` under `TrackFields.Video`), Playback.Video.cs resolver (H) | open | Decode field 38 gid → hex; resolver: own gid → counterpart's gid → TrackV4 on the counterpart. Decode unit over a captured TrackV4; pure resolver-tier test |
| G-057 | `HasVideo` narrower than 0.2.9 (only when the association carries `associated_uri`; 0.2.9: `files > 0 or counterpart`, aliases via kinds 182/212) | `Spotify.Decode.cs:1242` | parity | DEGRADES | Spotify.Decode.cs (E) | open | Also set on non-empty `files`; port the 182/212 recovery as a follow-up fetch. Decode unit |
| G-058 | No `Track.VideoW/VideoH/LiveVideo` columns (ch 24 DATA GAP 1): the cap seeds 16:9 then refits | `Track.cs:113-127,170-173`; `Video.UI.cs:409-412` | data | COSMETIC | Track.cs (A), Playback.Video.cs, Video.UI.cs | open | Two `ushort` columns + flag under `TrackFields.Video`, read first by the cap/PiP fit |
| G-059 | Cover-less playlist mosaic missing (`MosaicTiles = null` for playlists; only folders) | `Sidebar.cs:4114-4116,4327` | data | COSMETIC | Playlist.cs (Mosaic0..3) + decode + projection | open | Per ch 25 §7 DATA GAPS; projection test with a 4-cover playlist |
| G-060 | Unknown recents content type loses its wire token (`RecentsContentType` is a byte enum; ch 16 §8 wants "the wire token IS the label") | `Recents.cs:42` | decision | COSMETIC | Recents.cs + Decode (P/E) | open | Keep the byte, add `StringId RawType` written only when `None` |
| G-061 | Dead staging fields: `SectionTable.Duplicates` has no producer; `StagedSection.ParentUri` unused | `Home.cs:228,540` | defect | COSMETIC | Home.cs (P) | open | Dedupe at stage time or delete both |

### 2.4 Playback reducer/host, Connect, telemetry, OS bridges, notifications

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-070 | No play-a-context path: `PlayNow` needs a row + `QueueCursor`; `Queue.Replace` and `Api.ContextResolve` have no caller, no context-resolve or autoplay decoder, `Shell.OnPlayContext` null (deep link `wavee://play?ctx=`, omnibar ▶, every "play this" intake inert) | `Queue.cs:344-355`; `Spotify.Api.cs:1291-1298`; `Shell.Host.cs:400-421` | deferred | BLOCKS (plan DoD 4: play/next) | Queue.cs (Q: `ContextQueue.Build`, headless plan §3.1), Spotify.Decode.cs (E), Shell.Host.cs install (I) | open | ContextResolve decode → `Queue.Replace` → `PlayNow(first, context, CursorOf(first))`; install `OnPlayContext`; ShellRoutesTests deep-link fact + queue seed test |
| G-071 | An inbound Connect transfer or play claims ownership and announces but plays nothing: `RemoteCommand` carries no context uri, `skip_to`, `play_origin`, position or the transfer's `TransferState`; no host code resolves anything (the phone shows Wavee active and silent) | `Playback.cs:1810-1819`; `Spotify.Decode.Connect.cs:395-397,425-467`; `Playback.Host.cs:341-345` | impl | BLOCKS (DoD 4: transfer to Wavee) | Spotify.Decode.Connect.cs (E), Playback.Host.cs (G), the context resolver (G-070) | open | Decode `command.context.uri`, `options.skip_to{track_uri,track_index,track_uid}`, `seek_to`, `transfer.data` into a pooled value; the host builds the queue and posts `Input.Play(…, fromMs)` under the claim epoch. ClusterDecodeTests from captured request bodies |
| G-072 | Hello PUT (`PublishReason.NewDevice/NewConnection`) never sent → device invisible, roster empty | now `SessionEffects.AnnounceDevice` → `Connect.AnnounceDevice()` → `Playback.HelloReason` (`Spotify.Session.cs:226`, `Spotify.Connect.cs:256`, `Playback.Host.cs:282-293,451-453`) | wiring | BLOCKS | H-W G1 | fixed-unverified | Smoke: another client's picker lists the device |
| G-073 | Inbound Connect volume and logout topics dropped: only `cluster` pushes and REQUESTs handled; 0.2.9 subscribed `hm://connect-state/v1/connect/volume`; PutState advertises `supports_logout` | `Spotify.cs:915-917`; `Spotify.Connect.cs:190-191`; `Spotify.Decode.Connect.cs:710`; `_old/Wavee/Backend/ConnectCommand.cs:197-217` | parity | DEGRADES | Spotify.cs (topic), Spotify.Connect.cs (F), Playback.Host.cs (G) | open | Classify the volume topic, decode varint field 1 → `Input.WireVolume`; logout topic → `Spotify.Logout()`. DealerFrameTests + PlaybackStepTests |
| G-074 | Controller queue/context writes not applied: AddToQueue/SetQueue only claim; UpdateContext/SetOptions ignored | `Playback.cs:1870-1879` | deferred | DEGRADES | Playback.Host.cs (G) + Queue.cs (Q, W5) | open | Decode and apply through Queue in the drain; facts over captured commands |
| G-075 | PutState never carries the current item's queue uid (`CurrentUid()` = "") | `Playback.Host.cs:456-458` | impl | COSMETIC | Playback.Host.cs (G) | open | Carry the server-minted uid on `QueueEdge`; PutStateEncode fact |
| G-076 | Play registration (gabo) and resume points (herodotus) entirely unwired: no caller of `Telemetry.Boot/Started/Paused/Resumed/Seeked/Ended/PlayHistory/ResumePoint/ResumeMs/Shutdown` → no royalties, no Recently Played, no Wrapped, no episode resume | `Spotify.Telemetry.cs:209,334-438,608-696`; grep | wiring | DEGRADES (must ship) | Playback.Host.cs (G) + App.cs | open — decision (G emits from `Execute` on epoch-matched facts) | A `Registration` per load epoch; epoch-matched Started/Pause/Resume/Seek/Ended/Transfer/Stop → Telemetry; `ResumeMs` before an episode load. Pure facts over a recorded Step trace |
| G-077 | `Telemetry.Enqueue` (any thread) writes `Settings.Set(GaboGlobalSequence)` whose facade bumps `SettingsEpoch` and a Signal (C1) | `Spotify.Telemetry.cs:227-235`; `Platform.cs:828-833` | defect | DEGRADES (latent) | Spotify.Telemetry.cs (F), Platform.cs (S) | open | Interlocked sequence, persisted on flush through `Spotify.Post`; DEBUG UI-thread assert in the facade |
| G-078 | Launch restore of the deck missing: `SnapshotSession` is an unimplemented partial, no restore input (0.2.9 restored a paused session) | `Playback.Host.cs:518-521`; `_old/Wavee/App/PlaybackBridge.cs:1688`; `Playback.cs:89-90` | parity | DEGRADES | Playback.Host.cs (G) + Shell.Host.cs session doc (I) | open — recommended: restore paused, audio-first, never claiming | `Input.Restore(row, context, cursor, posMs)`; PlaybackOwnershipTests pin `AllowsLoad` |
| G-079 | `Shell.PlayLog.Append` has no caller → jump list recents, History, recents recency, sidebar "played" feeds never fill | `Shell.Host.cs:1040` | wiring | DEGRADES | Playback.Host.cs (G) via an `Action` seam set by Shell.Host.cs (I) | open | On epoch-matched Started post `PlayLog.Append(track, context, UnixNowMs(), title)`; ShellHostTests fact |
| G-080 | Local shuffle flips a flag only (no `QueueOrder` reorder); autoplay never runs (`EndOfQueue` ends the phase although `AutoplayEnabled` defaults true and `Api.Autoplay` exists) | `Playback.cs:1496-1507,1578-1587`; `Spotify.Api.cs:1298-1302`; `Platform.cs:205` | parity | DEGRADES | Queue.cs (Q), Playback.cs + Host (G) | open | Shuffle applies `QueueOrder` to NextUp; `Effects.Autoplay` at end-of-context → Api.Autoplay (+ encoder G-043) → rows appended as `QueueProvider.Autoplay`; PlaybackStepTests |
| G-081 | Suspend pauses a foreign Connect device: `PowerPolicy.CanPauseOnSuspend` never set (default true), `OnSuspendUi` posts `Input.Pause` which `DoPause` FORWARDS as `RemoteCmd.Pause`; the local-only `DoSuspend`/`ReportSuspend` is unreachable | `Playback.Os.cs:741,791-796`; `Playback.cs:1509-1511,1972-1983`; `Playback.Host.cs:636` | defect | DEGRADES | Playback.Os.cs (H), Playback.cs (G) | open — decision D13 | `OnSuspendUi` → `Playback.ReportSuspend()`; `DoSuspend` also sets `fx.Smtc`; delete `CanPauseOnSuspend`. Step fact: Suspend with Foreign owner emits no `SendRemote` |
| G-082 | Resume does not re-announce to Connect (0.2.9 `AnnounceNewConnection` after `PBT_APMRESUMEAUTOMATIC`); `Resume_` is a no-op | `Playback.Os.cs:798-804`; `Playback.cs:1371,1987-1990` | parity | DEGRADES | Playback.Os.cs, Playback.cs | open | `OnResumeUi` → `ReportWake()`; `Resume_` → `Announce(NewConnection)`. Step fact |
| G-083 | SMTC reads the Track table for episodes (`new Track(s.Current.Slot)` without checking `Kind`) | `Playback.Os.cs:197-205`; `Playback.Host.cs:423-424` | defect | DEGRADES | Playback.Os.cs (H) | open | Switch on `Current.Kind`; pure `SmtcDisplayFor(kind, …)` facts in PlaybackOsTests |
| G-084 | SMTC metadata never refreshes after late hydration (display pushed only on identity change; a remote/Connect track with unknown Identity keeps an empty card) | `Playback.Os.cs:189-208`; `Playback.Host.cs:415-425` | defect | DEGRADES | Playback.Os.cs + Host | open | Dedupe key = (id, knowsIdentity, imageId); re-publish when the row's Identity group commits |
| G-085 | Display keep-awake during video reads `Playback.VideoActive` (never true; one drain stale) | `Playback.Os.cs:737,763-776`; `Playback.Host.cs:408,550` | wiring | DEGRADES | Playback.Os.cs | open | Decide from `in State` (`s.Kind == Video`) in `OnStateChanged`; table fact |
| G-086 | Ambient power cadence (2 s `GetSystemPowerStatus` poll, debounce, 30/24 Hz, energy saver) exists only as constants | `Design.cs:1524-1536`; grep | parity | DEGRADES | Platform.Host.cs (S, W6) | open | Port poll + debounce + `DefaultLoopHz` apply; pure `Plugged(status)` facts |
| G-087 | Jump list may publish before `JumpList.Aumid` is known (set later by `Notify.HostInstall`, no rebuild) | `Playback.Os.cs:81,550-555`; `Notify.Host.cs:69`; `Shell.UI.cs:283` | defect | COSMETIC | Playback.Os.cs | open | `Aumid` setter calls `Attach()` when active |
| G-088 | `Notify.Rebuild` has no external caller → `HostConsider`/escalation never runs; no Windows banner can ever raise (the panel shows local toasts only); the feed sources (gander, what's-new, update) have no decoder/host | `Notify.cs:1005-1026,1080`; `Spotify.Api.cs:1220-1250` (raw, no decoder) | wiring / impl | DEGRADES (toasts BLOCKED) | a feed host (W5/W6) + Notify.Host.cs; Spotify.Decode.Pathfinder.cs (gander/whatsNew folds) | open | Each source fetch calls `Notify.Rebuild(update, social, releases, activity)`; assign `RefreshFeeds/ActivityMarkAllRead/ClearActivity/UndoActivity/UpdateCommand` (G-092). NotifyTests escalation facts exist; manual [SIM] parity 27 |
| G-089 | Pre-save drop and daylist schedulers never attached (`ReleaseDrops.Attach/OnSavedChanged/SimulateSchedule`, `Daylist.Note`) — Settings "Send event" cannot reach them | `Notify.Host.cs:333,379,458,471,593` | wiring | DEGRADES | prerelease owner (M), home daylist hydrator (P), Settings notifications page (R) | open | Attach `Resolve` at login; `Daylist.Note` on card hydration; Send event → `SimulateSchedule` |
| G-090 | `Notify.Dismiss`/`ClearLive` have no caller: in-app dismiss / Mark all read leave Action Center banners | `Notify.Host.cs:612-623`; `Shell.UI.cs:1429` | wiring | COSMETIC | Notify.cs | open | MarkAllRead → `ClearLive()`; row dismiss → `Dismiss(id)` |
| G-091 | Toast image cache rooted at `%LOCALAPPDATA%\FluentGpu\toastimg`, unbounded, cleared only by the never-called `SignedOut`; `TagFor` uncapped at 64 chars (over-long tag → silently dropped banner) | `Notify.Host.cs:94,175,300,570`; engine `ToastImageCache.cs:48-57` | parity | COSMETIC | Notify.Host.cs | open — decision (cache location: `%LOCALAPPDATA%\Wavee\toastimg`, 50 MB) | `new ToastImageCache("Wavee")` + cap; tag = hash when > 64; pure `TagFor` length fact |
| G-092 | Notification panel verbs unassigned (`RefreshFeeds`, `ActivityMarkAllRead`, `ClearActivity`, `UndoActivity`, `UpdateCommand`) and two updater seams exist (`Shell.UpdateVerb` from I3, `Notify.UpdateCommand` from I1) with nothing behind either; activity rows have no journal (ch 19 DATA GAP 3) | `Notify.cs:1086-1100`; `Shell.Overlays.UI.cs:77`; I1 report | wiring | DEGRADES | feed host + update host (W6) | open | Route one seam into the other via `Notify.VerbFor`; assign the five seams from the feed/update hosts |
| G-093 | No app updater at all: no AppInstaller/package update service, scheduler or check; the `wavee-stable` feed would never be polled; `ShutdownUpdatePolicy`/install-on-quit absent → `local-update-e2e.ps1 -Scenario inapp` waits for 0.2.9 log lines that 0.3 never writes | `Notify.cs:323-363,772,1100`; `Shell.Overlays.UI.cs:77`; `_old/Wavee/App/AppInstallerUpdateService.cs`; `ops/release/tests/local-update-e2e.ps1:1896-1910` | parity | DEGRADES (BLOCKS release gate E14) | a W6 update host (R/S) + App.cs; harness wording | open | Port `AppInstallerUpdateService` + scheduler + install-on-quit; install both seams; keep the log wording or update the harness in the same change; run both E2E scenarios |
| G-094 | 0.2.9 startup services not ported: factory-reset apply, relaunch broker (`--relaunch-after`), crash report writer + pending-report arm, RunMarker/CrashPromptPolicy, WER dump probe, StartupPreflight notice, preferred-GPU LUID seed, `FluentApp.DiagnosticRun` (census hook, benches), NavigationFrameWatch/MemorySampler, DealerArchive, AttachParentConsole | `_old/Wavee/Program.cs:51-74,78,82,130,153-195,365-401,418-433,499-514,532,566,629-697`; grep → key declarations only | parity | DEGRADES | Screens/Diagnostics*.cs, Screens/Feedback*.cs, Platform.Host.cs, App.cs (R/S, W6) | open | Port per plan §2 rows; port `CrashPromptPolicyTests`, `RunMarkerTests`, `FactoryResetTests`, `ShutdownUpdatePolicyTests` |
| G-095 | Setup wizard never armed: `SetupBootstrap`/`SidebarBootstrap` not ported, `setup.pending`, `sidebar.onboarding.seen`, `*.bootstrap.version` never written → first-run wizard and design chooser can never appear; `SidebarDesignGating` has no consumer | `Platform.cs:82-110`; `Sidebar.Modes.cs:178-187`; `SidebarDesignTests.cs:408-411` | parity | DEGRADES | Screens/Setup.cs (R), Sidebar.Host.cs (J), App.cs | open — decision (sidebar miner D6: fold fresh-install into Setup, drop the v0 key migration) | Port SetupBootstrap; run in `Platform.Boot`; port the two dropped SidebarDesign facts |

### 2.5 Audio: decoders, stream, pump, gain (files: `Playback.Audio*.cs`, `Spotify.Audio*.cs`, `Playback.cs`)

Note: the audio swap and the FLAC SIMD pass were never built; H-W is editing `Playback.Audio.cs`, `Spotify.Audio.Stream.cs`, `Playback.Host.cs`. Every row below is verified in code but not at runtime.

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-100 | A committed gapless join or crossfade never advances the reducer: the pump posts `Started` stamped with A's epoch at position 0, the reducer's `Started` arm does not move the cursor, no hand-off input exists → the UI keeps showing A while B plays; when B ends, `Ended → Advance` loads B AGAIN, and so on for every track. Default settings take this path (crossfade off = gapless; B prepared at every load). The reducer's own model ("Ended advances in the same drain") contradicts the pump's join | `Playback.Audio.cs:1795-1825,1871,2021-2044`; `Playback.cs:129-152,1612-1623,1705-1709`; `PlaybackStepTests.cs:269` | wiring | BLOCKS | Playback.cs (G), Playback.Audio.cs (H) | open — decision D4 | `AudioSignal.HandedOff(preparedId)`: reducer advances the cursor, bumps the epoch without `fx.Load`, returns the new epoch through an effect the pump adopts. Step fact "a hand-off advances the cursor and emits no Load"; pump fact for the posted input |
| G-101 | A refused FLAC key (`NoDeriver`/`NoKey`) fails the whole track instead of re-running the ladder at VeryHigh320 (plan gate: public-only build plays the 320 rung) | `Spotify.Audio.Stream.cs:1207-1211`; `Spotify.Audio.cs:653-663`; `Playback.Audio.cs:2272-2277,2291`; flac plan :2006-2009 | defect | BLOCKS (lossless) | Spotify.Audio.cs (F) | open — decision D7 | In `Open`, on Flac + NoDeriver/NoKey re-`Choose(track, lossless:null, VeryHigh320)`; pure `LosslessFallback.Decide(fmt, fault)` + test |
| G-102 | An 8 s wait for bytes ENDS THE TRACK: `Ring.ReadAt` returns −1 at its bound, both adapters treat `n <= 0` as EOF, the engine goes Ended, the reducer advances; and ranges are published only after the whole 512 KiB arrives, so at ≲64 KB/s the first fill after the 80 KiB head exceeds 8 s (≈2 s into a cold start). 0.2.9 degraded to a blocking read with a ~90 s budget | `Spotify.Audio.Stream.cs:546-551,903-936,380-390,720-726`; `Playback.Audio.cs:1179-1180,1258,2853-2857,2917`; `_old/.../PrefetchingReadStream.cs:56-73` | parity / defect | DEGRADES (skips tracks on slow links) | Playback.Audio.cs adapters + `RingSource` (H), Spotify.Audio.Stream.cs (F) | open — decision D5 | Return 0 only when `ReadAt` returns 0; on a starve post Buffering/`RecoveryKind.Network`, wait up to ~90 s, then `<0` → `Fault.Network`; `Land` each completed 64 KiB slot inside the read loop. Tests: a body stalled 10 s then resumed must not end; a throttled source records `Starves == 0` |
| G-103 | `Prefetching.Read` (module byte seam) returns 0 at its 8 s deadline although its doc says it degrades to the blocking read → slow module/radio bodies truncate | `Playback.Audio.cs:2366-2368` vs `:2409` | defect | DEGRADES | Playback.Audio.cs | open | Fall back to a blocking `_inner.Read`; extend the transient-miss fact |
| G-104 | Every track plays ≈ +4 dB too loud: `SeedFromSettings` sets engine `Normalization = Track` with `default` ReplayGainInfo → `ScalarLinear(default, Track, −14) ≈ 1.585` applied to every voice on top of the decoders' Spotify gain | `Playback.Audio.cs:336-337,1770,1840`; engine `AudioSources.cs:19-24`, `MediaEffects.cs:177`, `PcmAudioPlayer.cs:241-245,598` | defect | DEGRADES | Playback.Audio.cs (H) | open — decision D6 | Decoders own the gain: seed `Normalization = Off` (or `ReferenceLufs = TagReferenceLufs`); pure fact `ScalarLinear(default, seeded, ref) == 1` |
| G-105 | A FLAC can play at up to +30 dB: with catalogue gain 0, `OpenBody` reads the Ogg-only gain at byte 144 of the head (STREAMINFO/SEEKTABLE bytes in a FLAC); `NormalizationFactor` clamps only to ±30 dB | `Spotify.Audio.Stream.cs:1214-1216`; `Playback.Audio.cs:790-801`; `Spotify.Audio.cs:267-268` | defect | DEGRADES | Spotify.Audio.Stream.cs (F) | open | Read the head gain only for Ogg; pure `GainFor(fmt, catalogueGain, head)` with a FLAC head fixture (0) and an Ogg one (−3.5) |
| G-106 | Ogg peak cap not applied (`GainLinear` passes no peak; the float at head byte 148 never read) | `Playback.Audio.cs:784-785,796-801`; `Spotify.Audio.cs:751-754`; `Spotify.Audio.Stream.cs:813` | wiring | DEGRADES | Spotify.Audio.cs, Stream, Playback.Audio.cs | open | `HeadGain` → (gainDb, peak) carried on `Body`/`Opened`; `GainLinear(gain, peak)`; AudioAdapterTests with a real header |
| G-107 | An Ogg track is un-normalized when the head GET fails on an uncached file (gain baked at `TryOpen`) | `Spotify.Audio.Stream.cs:1184-1216`; `Playback.Audio.cs:1030-1034` | defect | DEGRADES | Spotify.Audio.Stream.cs | open | Read gain from the first decrypted range before returning `Opened` (bounded), or accept a late gain; `GainFor` empty-head fact |
| G-108 | A device format change leaves the session silent: the pump posts `DeviceLost`, `DoDeviceLost` returns unless the owner is Foreign, nothing reloads; deck stays "Playing" in silence (0.2.9 soft-reloaded at the saved position) | `Playback.Audio.cs:2142-2166`; `Playback.cs:1926-1928`; engine `PcmAudioPlayer.cs:1402,1637` | parity | DEGRADES | Playback.cs (G), Playback.Audio.cs (H) | open — decision (reducer reloads at `Position(now)`) | `AudioSignal.DeviceReload` → `fx.Load` current row with `LoadFromMs = Position(now)`, intent kept; Step fact + `DeviceRecoveryPlan` table facts |
| G-109 | `wavee:episode:` (WaveePodcast provider) routes into the Spotify track ladder → TRACK_V4 fails → `Restricted/Unavailable`; nothing maps an episode enclosure to an `ExternalUrl` FileChoice | `Playback.Audio.cs:2241-2243,2259-2285`; `Spotify.Audio.cs:655-663`; `Entities.cs:747-748` | wiring | BLOCKS (Wavee podcasts) | Playback.Audio.cs (H) + the podcast owner's episode store (M) | open | Resolve enclosure URL → `FileChoice(ExternalUrl, Mp3)` → `Open(in choice)`; routing switch test |
| G-110 | Local files cannot play: `LocalPath`/`ModuleOpen` seams never assigned; `PlayableLinks.ProbeFlac`, `Keys.LocalFileTags`, `wavee:local:art:` not written; `Shell.OnFilesDropped`/`PickAndPlayFile` unassigned | `Playback.Audio.cs:2226-2230,2301-2302`; `Shell.Host.cs:103`; `Shell.Overlays.UI.cs:56` | deferred | BLOCKS (local files) | Modules.cs/Modules.Host.cs (T, W6), Playlist.Page.cs local arm (O, W5), Drag.cs rules (L) | open | Set `Playback.Audio.LocalPath` at boot from `PlayableLinks`; pure `ProbeFlac` (flac plan §6) + probe fact over `Fixtures/flac`; install the drop/pick verbs |
| G-111 | Silent sink (`--fake`/`--silent`): session was never connected, unpaced, transport routed to `s_player`, seek zeroed the start offset, fake load probed WASAPI | now: `OpenSilentSession` connects (`Playback.Audio.cs:677-688`), `PacedSilentEndpoint` (`:307,321,347-366,713-714`) | defect | BLOCKS (the `--fake` playing-bar gate) | H-W H2 | fixed-unverified (verify pause/resume routing, `ApplySeekAsync` start offset, no WASAPI probe on a fake load) | `PlaybackSilentSinkTests`: a 3 s fake track ends in ~3 s; pause/resume reach the session |
| G-112 | Next track is fully opened at the current track's LOAD (head, resolve, key, range 1, tail, 30 s ring) and never re-armed: the "ending soon, unprepared" nudge posts a plain `Position`; a failed `TryPeek` at load never retries; queue/shuffle/repeat changes neither re-prepare nor invalidate the slot; `Spotify.Audio.Prefetch` (warm-only) has no caller | `Playback.cs:1417,1474,2055-2061,1625-1631`; `Playback.Audio.cs:2011-2037,374-379,1721-1753`; `Spotify.Audio.Stream.cs:1231-1249` | wiring | DEGRADES (memory, wrong-track joins) | Playback.cs (G), Playback.Audio.cs (H) | open — decision (prepare in the endgame: fade + 8 s; prefetch at load) | `AudioSignal.EndingSoon`; reducer prepares when `dur − pos ≤ EndingSoonMs`; re-emit `PrepareNext`/`CancelPrepared` on next-row change; pump replaces the slot when the id differs; at load only `Prefetch`. Step facts |
| G-113 | The gapless join frame comes from catalogue `duration_ms`, not the voice's exact end (`ExactVoiceEndFrame`); Ogg `ExactFrames` is almost never known at open (tail queued after range 1's Content-Range; `_tail` picked up only by `Seek`) → a gap/overlap of (duration_ms − true length) | `Playback.Audio.cs:604,1078-1080,1289-1290,1768-1769,1812,1857,1937-1938`; `Spotify.Audio.Stream.cs:830-833,1033-1040`; engine `VoiceLengthIsExact/ExactVoiceEndFrame` | defect / deferred | DEGRADES | Playback.cs `GaplessJoinClock`, Playback.Audio.cs; engine (no API to update a voice's total after open) | open | `JoinFrameExact` overload when `pcm.VoiceLengthIsExact`; wait for `TailGranule` (≤ 2 s) before `PrepareAsync` |
| G-114 | No shared read-ahead budget: each `Body` pins its whole slot ring at construction (up to 16 MiB + 256 KiB POH for the 600 s tier / FLAC24), and B's body is built at A's start → ≈33 MiB pinned per lossless session (0.2.9: 24 MiB total / 12 per stream); rings not pooled; read-ahead tier fixed at construction, `metered` never passed, throughput sampled once | `Spotify.Audio.Stream.cs:451,477-493,513-522,783-801,1222-1223`; `Playback.Audio.cs:1730` | deferred | DEGRADES | Spotify.Audio.Stream.cs (F), Playback.Audio.cs `Prepare` (H) | open | Process-wide slot budget shared by active + prepared, slots pooled; pass metered; re-evaluate the tier in `TryPlan`. Pure `SlotCount` under a budget |
| G-115 | Expired CDN URLs never re-resolved inside a `Body` (`_mirrors` fixed at open; all-refused only drops the cache entry + 250 ms backoff) → after a long pause / for a prepared track / a seek past the TTL, dead URLs retry until the 8 s starve | `Spotify.Audio.Stream.cs:765,783-788,938-941`; `Spotify.Audio.cs:404-445` | impl | DEGRADES | Spotify.Audio.Stream.cs (F) | open | On all-mirrors-refused run `Resolve(fileIdHex)` on an api thread, swap `_mirrors`, retry; fake source refusing the first URL set |
| G-116 | Seek inside the clear head still cancels the fill and issues a probe (`Body.IsResident` unused); every non-final probe starts a 512 KiB fill the next probe cancels; `Ring.Retarget` sizes the probe from its aligned start (RingSource compensates) | `Spotify.Audio.Stream.cs:563-580,640-652,684-694,861-872`; `Playback.Audio.cs:1400-1409` | defect | DEGRADES | Spotify.Audio.Stream.cs (F) | open | Head-resident probe → epoch bump only; `_seeking` flag gates `TryPlan`; `end = AlignUp(probe + bytes)`. AudioStreamTests: seek inside head = 0 requests; far seek with 2 probes → requests == 3, cancels == 0 |
| G-117 | Superseded loads are never cancelled (`LoadCoreAsync`/`PrepareCoreAsync` CTS never cancelled; up to 4×20 s deadlines run to completion, start range 1 + tail, then close); the Host header claims `Audio.Load` cancels the previous epoch | `Playback.Audio.cs:541-543,1729`; `Playback.Host.cs:36-37` | defect | DEGRADES | Playback.Audio.cs (H) | open | Keep `s_loadCts`; `Load()` cancels + replaces synchronously; pass the token into `Open`. Holder fact: ten loads → nine cancelled |
| G-118 | A 200 reply to a non-zero `Range` is accepted as that range (external podcast hosts ignoring Range corrupt every range after the first) | `Spotify.Audio.Stream.cs:97-100,907-918` | defect | DEGRADES | Spotify.Audio.Stream.cs | open | Accept 200 only at `start == 0`, else skip or refuse; fake 200-only source |
| G-119 | External episode needs a HEAD with Content-Length (`HeadLength` → 0 → `Fault.Network`) | `Spotify.Audio.Stream.cs:1162-1165`; `Spotify.Audio.cs:697-711` | defect | DEGRADES | Spotify.Audio.Stream.cs | open | Fall back to `Range: bytes=0-0` + `Content-Range` |
| G-120 | A fully cached file still needs storage-resolve (`!mirrors.Ok` fails the open even when `disk.KnownSize` covers the file; plan §5.4: 0 requests) | `Spotify.Audio.Stream.cs:1173-1212` | defect | DEGRADES | Spotify.Audio.Stream.cs | open | Lazily resolved mirrors on the first miss; extend `Cached_file_needs_no_request` to the open |
| G-121 | The engine's seek does not cancel a read blocked in the ring (`SeekFrameAsync` applied after the current decoder read returns → a seek during a stall waits the 8 s bound); `RingSource.Cancel` DISPOSES the body | engine `PcmAudioPlayer.cs:1136-1165`; `Playback.Audio.cs:1417-1421` | engine | DEGRADES | `..\fluent-gpu` (`ICancellableAudioSource.InterruptPendingRead()`), Playback.Audio.cs | open — engine issue to file | Engine calls the interrupt before `SeekFrameAsync`; app maps it to an epoch bump |
| G-122 | Audio cache root moved without migrating 0.2.9's (`%LOCALAPPDATA%\Wavee\cache\audio` vs `…\Wavee\Wavee\Cache\audio`, auto budget ≤ 128 GiB): the old root is orphaned, never trimmed; no Storage tab to see or clear it | `Spotify.Audio.Stream.cs:155`; `_old/.../AudioBodyDiskCache.cs:30-34`; `Platform.cs:232-237`; `Settings.UI.Storage.cs` (14 lines) | parity | DEGRADES | Spotify.Audio.Stream.cs `DiskCache.DefaultDirectory` (F); Settings Storage (R, W6) | open — decision D8 | Reuse the 0.2.9 root (keys already identical) or delete once; pure path test |
| G-123 | The persistent audio key cache (0.2.9 DPAPI `LicenseKeyDiskCache`, 30 d, 4096) became a 256-entry in-memory cache; `audio.cache.keys.enabled` orphaned → every launch re-requests/re-derives every key; a disk-cached replay still pays the key RTT | `Spotify.Audio.cs:449-519`; `Platform.cs:233`; `_old/.../LicenseKeyDiskCache.cs:14-50` | parity | DEGRADES | Spotify.Audio.cs (F) + Platform protector | open — decision D9 (stores derived keys) | Port behind `AudioKeyCacheEnabled` or delete the key; pure round trip with a no-op protector |
| G-124 | The audio backend boots ON THE UI THREAD: `Load`/`SetVolume` call `Boot()` from `Execute`, and `WasapiPcm.CreateBackend → ProbeFormat` opens an endpoint synchronously (seconds on a cold Bluetooth/USB device) | `Playback.Audio.cs:302-315,363-366,420-422`; `Playback.Host.cs:392,399`; engine `WasapiPcm.cs:32-35` | parity | DEGRADES | Playback.Audio.cs (H) | open | Boot on the pump chain; warm after the startup quiet period; `SetVolume` stores intent before boot |
| G-125 | The format label is interned off the UI thread (`OpenSessionAsync` on the pump's pool thread calls `Entities.Strings.Intern`; the `StringTable` is single-writer, C1) | `Playback.Audio.cs:614`; engine `StringTable.cs:38` | defect | DEGRADES | Playback.Audio.cs, Playback.cs | open | Post a label code and intern in `DoFormat`, or pre-intern the fixed label set at boot |
| G-126 | No local output-endpoint enumeration or routing: `EnumerateEndpoints` never assigned (0.2.9 enumerated via COM), `Select` only persists, the engine's WASAPI leaf opens only the default endpoint; `FilterForPicker`/`Fold(FormFactor)` not ported | `Playback.Audio.cs:140-203`; engine `WasapiAudioDevice.cs:405-475`; `Stage.UI.cs:758,809` | engine / parity | DEGRADES | `..\fluent-gpu` (`WasapiAudioDevice(deviceId)` + MMDevice enumeration), Platform/Playback.Audio.cs (H) | open — decision D18 | Engine issue for 0.3.x; ship the enumerated list + persisted selection with a "follows the Windows default" note (0.2.9 parity); `DevicePickerItemsTests` |
| G-127 | WASAPI `FormatSink`/`DiagSink` unwired → device-open failures and the negotiated format silently dropped | grep; `Platform.Host.cs:88` | wiring | DEGRADES | Platform.Host.cs (S) or Playback.Audio.cs Boot (H) | open | Wire both to `Log` before the first open |
| G-128 | Live radio: ADTS sniffs to `Unknown` → Vorbis adapter; no AAC decoder (0.2.9 `AacSampleSource` over `MfAacDecoder`), no reconnecting live HTTP ring; a dropped stream reads as Ended (skips the station) | `Playback.Audio.cs:2463-2468,774-779,2640-2735` | parity | DEGRADES | Playback.Audio.cs (H) + module host (T, W6) | open | `AacAudioDecoder` over `FluentGpu.Media.Windows.MfAacDecoder` with 8-frame priming; live wrapper with reconnect budget; drop → `Fault.Network` |
| G-129 | Spotify MP3 skips 167 bytes on the format's say-so (`HeaderBytesFor(Mp3) = 0xa7`); librespot applies the header only to Ogg, go-librespot opens MP3 at 0 — would lose ID3/Xing/LAME | `Spotify.Audio.cs:158-163`; SYMPH/WEBFLAC reports | decision | DEGRADES (MP3 episodes) | Spotify.Audio.cs, Stream | open — decision D19 | Sniff decrypted chunk 0 (ID3/0xFFEx at 0 → 0; OggS at 0xa7 → 0xa7); verify on one real Spotify MP3 episode |
| G-130 | Lossless rung unreachable and metered cap ignored: Settings ▸ Playback is a stub (nothing sets `playback.quality = 3`), `PreferredQuality()` never applies `MeteredQualityCap`, no `NetworkPolicy`/metered detection | `Settings.UI.Playback.cs` (14 lines); `Spotify.Audio.cs:643-647`; `Platform.cs:193` | deferred | DEGRADES | Settings.cs/Settings.UI.Playback.cs (R, W6), Platform.cs `NetworkPolicy.EffectiveQuality` (S), Spotify.Audio.cs | open | Fourth rung offered only when `CanDerive`; `EffectiveQuality(user, cap, cost)` table + test |
| G-131 | Per-track format override and the drawer's format ladder not wired (no override map on the Host; `SpotifySource` always uses the global quality; `Track.Drawer.cs` empty); FLAC badge shows "FLAC 24-bit" not "FLAC 24/44.1", bitrate never published | `Playback.Audio.cs:2268,2791,2827,2281,614,2220-2221` | deferred | COSMETIC | Playback.Host.cs (G), Playback.Audio.cs (H), Track.Drawer.cs (M, W5) | open | uri→format map on the Host; `Open(uri, quality, …)` with the override; publish the opened decoder's label + kbps |
| G-132 | Crossfade, EQ, normalization toggles have no settings UI (applied only from `SeedFromSettings` at boot; crossfade only from the palette; a normalization change applies from the next track at best) | `Playback.Audio.cs:330-337,784-785`; `Shell.Palette.cs:288`; Settings stubs | deferred | DEGRADES | Screens/Settings*.cs (R, W6) + `Playback.Audio.SetNormalization(bool)` | open | Bind rows to `SetCrossfade`/`SetEqualizer`/`SetNormalization` |
| G-133 | Stream `Stats` and pump `Metrics` doors (heads, resolves, cache hits, first-audio ms, seek latency by kind, xruns, gapless exact/degraded, decode ×realtime) | `Stream.Stats` landed (`Spotify.Audio.Stream.cs:96-109`); `Playback.Audio.Metrics.Read()` is read by `Diagnostics.Probe.cs:463` and the app assembly compiles, so H1 is in the uncommitted `Playback.Audio.cs` | impl | COSMETIC (blocks measurement) | H-W F1/H1 | fixed-unverified | The `ogg320.wh`/`gapless-album.wh` runs are the check (`xruns==0`, `exact>=1`) |
| G-134 | Audio tidy-ups: prepare overlap hard-coded Audio→Audio (`AllowCrossfade(Audio, Audio)`); decoder buffers allocated per track (192 KiB pinned window, 256 KiB Ogg buffer, 0.3–0.7 MB Vorbis tables → LOH/POH churn); refused head splice re-reads the same offset instead of restarting at 0; `NoPage` probe window lands without reading one more window; `ApplySkip`/`SetSkip` legacy path kept; `NU1701` NoWarn outlived FlacBox; `VorbisComment` reader not shared; `OpenBody` parallel head/resolve/key and `Prefetch` untested; `--flac-probe`/`--vorbis-probe` absent; empty `Cancel()` on the file and ICY sources | `Playback.Audio.cs:1748,1047-1049,978-989,1303-1307,553-571,2421-2423,2550,2793`; `Wavee.csproj:14`; `Playback.Audio.Flac.cs:196,251`; `AudioStreamTests.cs:123-596` | defect / deferred | COSMETIC | Playback.Audio.cs, Flac/Vorbis, csproj, tests (H/F/S) | open | Pass `PlayableKind` with `PrepareNext`; pool two adapters' buffers; splice restart; `NoPage` read-ahead; delete `SetSkip`; drop NU1701 if clean; shared comment reader; inject seams into `OpenBody` + the cold-start fact; probe arms in W6 |

### 2.6 Video (Wavee host + surfaces) and the engine rework

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-140 | The reducer never drives the video host: `Execute()` calls only `Audio.*`; `Audio.Load` opens an audio session whatever the `LoadKind`; `Playback.Video.Load/Stop/Play/Pause/SetVolume` have no caller; the video element's play/pause/seek, SMTC, thumb buttons and media keys all reach the AUDIO host | `Playback.Host.cs:383-411`; `Playback.Audio.cs:363-369,515-557`; `Video.UI.cs:175-188`; `Playback.Os.cs:274-290,501-507` | wiring | BLOCKS (video) | Playback.Host.cs + Playback.cs (G) | open | Route `Load` by `LoadKind` (Video → resolve source then `Video.Load(src, epoch, fromMs)`), and `Stop/PauseHost/ResumeHost/Seek/Volume` by `s_state.Kind`. Step+Execute facts with fake audio/video seams |
| G-141 | Nothing ever produces `PlayableKind.Video`: `MediaSwitch.KindOf` has no caller; the placement toggle writes only `Video.State.Surface` and posts nothing to Playback; no `Effects` video slot; a null manifest resolve has no failure path → every surface sits on poster + "Loading…" forever | `Playback.cs:472-475`; `Video.Host.cs:87-105`; `Playback.Host.cs:592`; `Video.UI.cs:282-311,480-481,656,845,992` | wiring | BLOCKS (video) | Video.Host.cs (K: post `Input.VideoPlacement(active)` on the `IsActive` edge), Playback.cs (G: fold `Kind = KindOf(active && HasVideo, isLocal)`, emit `Load` at `Position(now)`), Playback.Host.cs | open | Plan §6.2 G1–G3; on null resolve `FoldAvailability(false)` + `Notify.Say`. Step facts: placement on for a HasVideo track emits `fx.Load` kind Video with `fromMs = pos`; off emits Audio |
| G-142 | Video faults never recover or fall back (0.2.9: one retry per uri, then audio fallback when Connect-originated, else toast + `NotifyVideoUnavailable`); 0.3 folds `Failed` generically, the watchdog just posts a fault | `Playback.Video.cs:653-657,737-744` | parity | DEGRADES | Playback.cs (G), Video.Host.cs (K) | open | One re-resolve + reload; on a second fault demote placement, `Load` Audio at the carried position, `Notify.Say`. Step sequence fact |
| G-143 | `ConnectVideoFacts` never published (one extra PutState when a badge lands); `track_player` derives from `snap.Kind`, never Video → remote controllers never see the video offer | `Video.cs:675-700`; `Spotify.Decode.Connect.cs:670` | parity | DEGRADES | Playback.cs (G), Video.cs (K) | open | Observe `(uri, HasVideo, gid)` on the Video group landing; on a real gain `Announce(PlayerStateChanged)`; unit on `Observe` + Step fact |
| G-144 | Manifest/licence request headers diverge from 0.2.9: `Origin` = spclient base, no `Referer`, no `Accept: */*`; licence POST lacks `Content-Type: text/xml; charset=utf-8` and `SOAPAction` → server rejection likely (unverified) | `Playback.Video.cs:792-801,1112-1120`; `Spotify.Api.cs:329-345`; `_old/.../SpotifyLicenseRelay.cs:32-35`, `SpotifyVideoResolver.cs:39-41` | parity | BLOCKS (likely) | Spotify.cs `HeaderSet` bits, Spotify.Api.cs (F), Playback.Video.cs (H) | open | `XpuiOrigin` (Origin + Referer xpui) and `ContentSoapXml` header bits on both routes; pure header-fold unit |
| G-145 | Licence relay blocks the CDM thread (synchronous `Api.Send` inside the delegate, completed `ValueTask`; the branch runtime calls it synchronously on the KeyMessage thread); and the relay is per-load (`RelayForward` reads `s_activeRelay` set in `ResetPerLoad`) so a prefetched/next-track licence sees null or the wrong relay | `Playback.Video.cs:544,563-574,1103-1107,1121`; engine `ProtectedVideoRuntime.cs:612-648` | defect | DEGRADES | Playback.Video.cs (H) | open | `new ValueTask<LicenseResponse>(Task.Run(...))`; pass the source's relay per open (`MediaOpenOptions.LicenseRelay`), delete `RelayForward`; fact with a blocking fake sender |
| G-146 | No manifest memo/single-flight/prefetch: every resolve refetches; `PrefetchSchedule` unused; 0.2.9's 10-min `SingleFlightMemo` + `VideoCdnWarm` absent (warm ≤ 300 ms target unreachable) | `Playback.Video.cs:786-819`; grep | impl | DEGRADES | Playback.Video.cs `Prefetch` (H), Playback.cs `Effects.PrefetchVideo` (G) | open — decisions D15 (Q3 prefetch policy) | Plan §6.2 H2/G1: memo keyed by manifest id, `PrepareAsync` on the protected backend, emitted from Step per `Decide`; Step fact "ending-soon emits PrefetchVideo why=next" |
| G-147 | Manifest duration fallback stricter than 0.2.9 (both `*_time_millis` required on the content object; 0.2.9 read the root, start default 0) → zero duration → `ToDescriptor` null ("no video") | `Playback.Video.cs:855-857,944-947`; `_old/.../SpotifyVideoManifest.cs:91-97` | parity | DEGRADES | Playback.Video.cs | open | Port the fallback; `Manifest.Parse` fixture with end-only on the root |
| G-148 | Local video overrides and module clear video never become a source (`VideoSource.LocalFile/Clear` have no callers); quality setting not applied live (`SetPreferredHeight` uncalled), metered cap absent | `Video.cs:844-1093`; `Settings.UI.Video.cs:55-285`; `Playback.Video.cs:306-323,556-559` | wiring | DEGRADES | Playback.Video.cs resolver (H), module owner (T), Settings.UI.Video.cs, Platform.cs | open | Resolver tier 1 `Overrides.Decide(uri)` → `LocalFile`; module `form: video` → `Clear(url, isLive)`; settings write → `SetPreferredHeight` |
| G-149 | Host behind the video plan: carried position applied after start (no `OpenAsync(source, MediaOpenOptions)` — exists only in the video branch); no `Phase/FirstFrame/Buffered` signals (poster drops on player presence); seek planner fed an empty index, `KeyframeMs` never passed, no `seek.plan/seek.done`; audio hand-off (`AudioHandoff`, `Effects.AudioCut/VideoGo`, `FadeOutAndPark`) unimplemented (silence across the switch); always-on `VideoLog` lines and the native `LogSink` not routed; descriptor misses `SegmentLengthMs/DurationMs/SegmentsStartWithKeyframe`; `PlayReassertBudget` arm still present | `Playback.Video.cs:93-102,179,277-288,388,431,463,464,638-647,669-680,723-724,1002-1022`; `Video.UI.cs:172-193`; `Playback.Video.Rules.cs`; `Playback.cs:501` | deferred | DEGRADES | Playback.Video.cs (H), Playback.cs/Host (G), Playback.Audio.cs (H), Video.UI.cs (K) — after the engine (G-162) | open | Plan §6.2 H1–H6, G1–G4, K; plan §4.1 tests |
| G-150 | Placement preference never persisted/restored (`video.placement` key, `PlacementPersistence.Save/Load` uncalled; boots `Music`); availability not re-folded at track boundaries (`FoldAvailability` only from the rail's OpenAt) | `Platform.cs:178`; `Video.cs:634-665,732-754`; `Rail.UI.cs:214`; `Video.Host.cs:141-142` | parity / wiring | DEGRADES | Video.Host.cs, Shell.Host.cs, Shell.UI.cs effect (K/I) | open | Load at shell boot into `Preferred`, save in `Commit`; effect on `Playback.Current` + the row's Video group → `FoldAvailability(hasVideo)` with `commitUpgrade` at boundaries; `BoundaryFold` pure helper + facts |
| G-151 | Module watch-page stage never mounts (`Video.WatchStage` no caller; `ActiveStagePlayable` only cleared) | `Video.UI.cs:69-99`; `Shell.UI.cs:736` | wiring | DEGRADES | Modules.UI.cs (T, W6), Shell.UI.cs | open | The watch page mounts `WatchStage(uri, poster, onPlay)` and writes `ActiveStagePlayable` while active |
| G-152 | `Wavee.csproj`'s native build target is stale against the video branch: `Inputs` name the deleted `PlayReadyNative.cpp`, omit `PrRuntime/PrLicense/PrSession.cpp`; arch rule duplicated instead of importing the branch's `PlayReadyNative.props`; the shipped DLL follows whichever engine root resolved (base has only `out\arm64`) | `Wavee.csproj:75-96`; engine `ops/tools/playready-native` listing; `FluentGpu.WindowsApp.csproj:29-40` | defect / decision | DEGRADES (x64 DRM; rebuilds every build) | Wavee.csproj (O*), release preflight | open — decision D15 (Q8) | Import `PlayReadyNative.props` when present; `Inputs = *.cpp;*.h;build.cmd`; release script asserts both arch DLLs export `FgPrRuntimeCreate` |
| G-153 | K3 leftovers: fullscreen exit button duplicated locally instead of K1's chrome; keyframe-accurate scrub preview impossible (no index/phase from the host); pop-out and fullscreen titles do not update when metadata arrives late; the lyrics store sets `Prefs.Lyrics.Available` for untimed docs (stale "second line" toggle); stage-rect probe, lyrics inspector, `--lyrics-advance-probe` → W6 | K3 report; `Lyrics.Host.cs:1729` | deferred | COSMETIC | Video.UI.cs, Lyrics.UI.cs (K), Diagnostics (S, W6) | open | Title signals from the row's Identity; `Available` only for timed docs |
| G-160 | ENGINE (in flight): x64 native DLL is the old ABI (2026-08-29 `FgPlayReady*` exports) while the managed side P/Invokes `FgPr*`; only arm64 rebuilt | `C:\wavee\fluent-gpu\ops\tools\playready-native\out\x64\` | engine | BLOCKS (x64 video) | engine video agent + release script | in flight: engine video | `build.cmd x64`; release gate on the export |
| G-161 | ENGINE (in flight): protected-session seams (`Phase`, `FirstFrameEpoch`, `GetKeyframes/GetBuffered`, `SeekAsync(pos, mode, hint)`, `PrefetchAsync`) live on `IProtectedVideoPlayer` inside `ProtectedMediaSession`; `IMediaPlayer` only gained `OpenAsync(source, options)` → Wavee cannot reach any of them | engine `IProtectedVideoPlayer.cs:42-93`; `IMediaPlayer.cs:138-144` | engine | BLOCKS (plan targets) | `FluentGpu.Engine/Media/Playback/{IMediaPlayer,MediaPlayer,IVideoSurfaceSession}.cs`, `ProtectedMediaSession.cs` | in flight: engine video | Optional session interface (phase/first-frame signals, index epoch, `CopyKeyframes/CopyBuffered`, `SeekAsync(to, mode, hint)`) surfaced by `MediaPlayer` + a public prepare entry; facade fact over `FakeProtectedVideoPlayer` |
| G-162 | ENGINE (in flight): the branch is two wip commits + uncommitted edits (`PrInternal.h`, `PrLicense.cpp`, `build.cmd`, two csprojs, untracked `PlayReadyNative.props`), never built or run; no VerticalSlice protected gate; the real-DLL runtime test (plan §4.2) absent; native behaviours (attach while licence pending, `MESourceStarted` start time, swapchain handle, `SetSource("")` detach, WinRT HTTP `Completed`) unproven; KeyMessage HttpHeaders dropped before the relay | `git status` in the engine; reports a2af9ef, a551b61 | engine | BLOCKS | `C:\wavee\fluent-gpu` | in flight: engine video | Plan §6.3: Debug + Release, `FluentGpu.Windows.Tests`, VerticalSlice (+ the protected gate), `check-canon.ps1`, both DLL arches, then §4.3 manual gate with `[video.native]` lines |
| G-163 | ENGINE defects found by the engine tests (in flight): PSSH parse overflows on a hostile KID count (`LicenseCachePolicy.cs:145-149`); Rebuffer/Opening publish `BufferingInfo.None` (`ProtectedMediaSession.cs:432-445,624`); demuxer ignores `elst`, merges traks, reads the first `traf` only (`CencMediaSource.h:287-293,384-385`); licence events match handle 0 loosely and Expired skips `AcceptCompletion` (`ProtectedVideoRuntime.cs:530-556`); `WaitBufferedAsync` cancel leaks waiters (`:453-461`); `IsAvailable` checks load not exports (`:109,774-790`) | as cited | engine | DEGRADES | engine files | in flight: engine video | The written tests name each fix |

### 2.7 Sidebar platform and pane (files: `Shell/Sidebar*.cs`)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-170 | Several "+"/drop commits are silent no-ops without the `LibraryWrites` seam (header "+" drop, folder "+" drop, `CreatePlaylistFlow`, `NewPlaylistInFolder` just `?.Invoke`; `accepts:` still arms the cue) — contract says a null seam refuses with a toast | `Sidebar.UI.Drop.cs:421,432-433,443,452`; `Sidebar.UI.cs:1968`; `Sidebar.UI.Menus.cs:573` | defect | DEGRADES | Sidebar.UI.Drop.cs, Sidebar.UI.cs, Sidebar.UI.Menus.cs (J) | open | Gate `accepts` on the seam member; route the null arm through `RefuseDrop(Unavailable)`; extract `SidebarCreateDropRules.Accepts(payload, seams)` + test |
| G-171 | Rename folder passes the OLD name (no dialog/inline editor); playlist row menu has no "Access ▸" (builder exists) | `Sidebar.UI.Menus.cs:236-263,282-283,591-598`; `Actions.UI.cs:339-353` | impl / deferred | DEGRADES / COSMETIC | Sidebar.UI.Menus.cs (J); Access after owner O ships set-public/collaborative | open | Shared text-input dialog (0.2.9 `FolderActions.Rename`); insert `Actions.Menu.Access` for owner rows |
| G-172 | Recents / "Jump back in" feeds hard-wired empty although `Shell.History.Store` and `Shell.PlayLog.Recency` exist (binder returns `Array.Empty`, `SidebarRecency.Empty`, no `lastPlayed`, versions pinned 0) | `Sidebar.Host.cs:2631-2632,2679,2687-2691,2975-2976`; `Shell.Host.cs:612-634,967-997` | wiring | DEGRADES | Sidebar.Host.cs binder + Sidebar.UI.cs `PumpBinder` (J) | open | Build `SidebarRecency` from History entries, played contexts from PlayLog, pass `Recency` as `lastPlayed`; fold both Version signals into triggers |
| G-173 | Queue / Now Playing sections never refresh (sources read playback only inside `Fill`, never raise `Changed`; `PlaybackEpoch` 0; pump reads no playback/queue signal); queued episodes skipped; top-tracks/concert rows never re-plan when their fetch lands | `Sidebar.Host.cs:2020-2081,2119-2152,2312-2347,2986`; `Sidebar.UI.cs:117-143` | wiring | DEGRADES | Sidebar.UI.cs `PumpBinder`, Sidebar.Host.cs (J) | open | Read `Playback` current/queue version signals; add `Tracks.Changed`/`ArtistPopular`/`FeedSection`/`Concerts` reads; `FromEpisode` mapper |
| G-174 | New releases and Concerts have no fetcher (`RegisterAll` called with null `SidebarFeedFetch`/`SidebarConcertsFetch`); unresolved releases dropped instead of stubbed; concert rows navigate to NotFound (bare `spotify:concert:` uri as route key; table parses `concert:<bareId>`) | `Sidebar.UI.cs:107`; `Sidebar.Host.cs:2203-2377,2343-2345`; `Shell.cs:159,276` | wiring / defect | DEGRADES | Sidebar.Host.cs (J) + Concert/WhatsNew owners (N/R) | open | Key rows `Shell.NameOf(Shell.For(uri))`; wire both seams when the feeds land; dimmed stub row for an unresolved release |
| G-175 | `sidebar-layout.json` moved from `%LOCALAPPDATA%\Wavee\WaveeMusic\` (0.2.9 and 0.3's other shell documents) to `%LOCALAPPDATA%\Wavee\` with no migration → upgraders lose layout, pins, V3 order, expanded folders; `SidebarStoreTests.cs:429` pins the new path | `Sidebar.Host.cs:1070`; `_old/.../SidebarLayoutStore.cs:110-114`; `Shell.Host.cs:634,798,999` | parity (data loss) | DEGRADES | Sidebar.Host.cs, SidebarStoreTests (J) | open — decision D8 | `Path.Combine(Platform.LocalFolder, "WaveeMusic", "sidebar-layout.json")` |
| G-176 | Persisted pane scalars written but never read back (consequence of G-004); no sidebar flush on shutdown (last edit inside the 300 ms debounce lost; `Sidebar.Flush`/`FlushNow` have no caller) | `Sidebar.Host.cs:89-111,256-282,335-338,783-787,1243,1369`; `Shell.UI.cs:222-238,505` | wiring | DEGRADES | App.cs (G-004), Shell.Host.cs finally (G-012) | open | Covered by G-004/G-012; store fact Commit → FlushNow → WaitForWrites → file on disk |
| G-177 | `Controls.NowPlaying` seam never installed (sidebar ♪ and the now-playing card overlays never light; `RefreshPlayState` returns before subscribing when null); unresolved bound-action label falls back to "Manage extension" not the raw `providerId.actionId` (ch 26 §7.2); the registry itself is G-027 | `Controls.Art.cs:70`; `Sidebar.UI.cs:1200-1216`; `Sidebar.UI.Menus.cs:687-715` | wiring | DEGRADES | Playback/Controls owner (G/L: NowPlaying), Sidebar.UI.Menus.cs (label) | open | Install at boot; `label = binding.ActionKey` when unresolved; W22 fact |
| G-178 | Rail folder flyout subscribed to the never-published static `Sidebar.Entries` cell → an open flyout did not refresh | orchestrator changed `Sidebar.UI.Rail.cs:302` to `Binder?.Entries` (commit `201cd247`) | defect | DEGRADES | Sidebar.UI.Rail.cs (J) | fixed-unverified | Consider deleting the static `Sidebar.Entries` (no legacy paths); manual: create a playlist with the flyout open |
| G-184 | J2 leftovers: after "Remove section" inside the options popover the subject clears but the popover stays open ("Select a section…"); palette drops and card-menu moves go straight to `Sidebar.Dispatch` so a refused one is silent; three chapter-vs-code conflicts followed 0.2.9 (Divider Behavior group, 280-wide palette search, design labels) need ch 26 amended; the item picker's Library tab reads `Binder.Entries` rather than ch 26 §7.2's `User.Me` edge scans; Esc inside an unhandled text box also leaves the page | J2 report items 2–3, "Left out" | defect / parity | COSMETIC | Sidebar.Customizer.UI.cs (J), ch 26 §10 | open | Close the popover when `Edit.OptionsSection` goes null; route refused palette drops through the refusal caption; strike/amend the three parity items |
| G-179 | Binder subscription goes stale on `Entities.Switch` (signals captured at effect time from the replaced scope) — latent until login switches scope (now does) | `Sidebar.UI.cs:120-132`; `Entities.cs:1751,1775-1786` | defect | DEGRADES → BLOCKS after login | Entities.cs (A: a scope-epoch signal), Sidebar.UI.cs (J) | open | Read a `Signal<uint>` scope epoch first in `PumpBinder`; test: switch scope, stage a rootlist edge → Revision advances |
| G-180 | Every table change anywhere forces a full three-pass library rebuild (unconditional `Invalidate()+Sync()` on `Playlists/Albums/Artists/Shows.Changed`, materialising `EntityUri.Text` per row) | `Sidebar.UI.cs:128-131,146-151`; `Sidebar.Host.cs:2687-2713` | defect (perf) | DEGRADES | Sidebar.UI.cs, Sidebar.Host.cs (J) | open — decision (sidebar D8) | Fold per-table publication counters into `SidebarBinderTriggers`; drop the unconditional Invalidate; render-census check |
| G-181 | Customizer leftovers after J2 (done, 2,612 lines; Ctrl+Z/Y and Esc landed; `PropertyPanel` compiles): template miniature has no deterministic sample (`MiniaturePlaylist/Artist/ShortcutCount` unassigned — needs `SeedFake`, G-015); palette extension group reads the local `SidebarDataSourceTable`, no `WaveeExtensionRegistry` (the nine sources are not in `Actions.Registry`) | `Sidebar.Customizer.UI.cs:73-79`; `Sidebar.Host.cs:2362-2407`; `Actions.cs:727` | deferred / impl | COSMETIC | Entities.Fake.cs (Q), Actions.cs + Sidebar.Host.cs (I/J) | open | Register the nine sources into `Actions.Registry` (first wins); `SidebarSourcesTests` `RegisterAll` fact |
| G-182 | Design chooser / Settings design picker / Setup page absent (`SidebarDesignGating` no consumer; no Appearance row; W21, parity 80) | `Sidebar.Modes.cs:178-187`; grep `sidebar` in Screens → none | deferred | DEGRADES | Screens/Settings*.cs, Setup*.cs (R, W6) | open | Port the three-card picker + row form |
| G-183 | Sidebar test debt: no store test seam (`Sidebar.Store` bound to the real profile → binder integration facts unported, tests create `%LOCALAPPDATA%\Wavee`); dropped 0.2.9 facts (SidebarPinSyncTests 12, SidebarBootstrapTests ~20, `RootlistDropScenarioTests`, op-order half of `RootlistSlotToOpTests`, populated now-playing, `PinnableRoutes`); no pure rule for "missing seam refuses"; `SidebarPaneInvariant` has no consumer; binder drag-freeze API (`BeginDragSession/…`) uncalled; Liked cover ignores the treatment pref; stale test headers | `Sidebar.Host.cs:69,1066-1070,2589-2604,2738-2741`; `SidebarProjectionTests.cs:36,44-54`; `SidebarDocTests.cs:18-21`; `Sidebar.UI.Rows.cs:492-505` | impl / decision | COSMETIC | Sidebar.Host.cs (`Sidebar.UseStore`), tests (J/Sonnet) | open — decision (delete the freeze API) | `UseStore` with a lazy default; port the facts as their production halves land |

### 2.8 Shell, platform, screens, tests, ops

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-190 | Wave 4.5 not started: `Detail.cs`, `Detail.UI.cs`, `Track.UI.cs`, `Track.Table.cs` are skeletons (`Track.cs` has columns only); the ten `detail.rail.*` keys orphaned; the rail/page convergence facts absent | file sizes; `Platform.cs:154-168`; `Prefs.cs:249` | deferred | BLOCKS (every detail page) | M, Wave 4.5 | open | Work package WP-4.5 (§4) |
| G-191 | Wave 5 not started: 34 Entities page/UI files are skeletons (Album, Artist/Discography, Browse, Concert, Episode.UI, Home ×6, Playlist, Queue.UI, Recents, Search, Show, Track.Drawer, User ×7, Entities.Fake) → 28 of 30 route kinds paint not-found; `Rail.QueueBody`/`Stage.QueuePaneBody`, `Omnibar.Source`, `Shell.OnNewPlaylist/OnNewFolder`, `Drag.LikedChipArt`, `Controls.PreSave`, `Controls.Library` unassigned | file sizes; `Shell.cs:123-162`; `Rail.UI.cs:46`; `Stage.UI.cs:60`; `Shell.cs:1910`; `Shell.Palette.cs:239,242`; `Drag.cs:500-505`; `Controls.cs:58-68` | deferred | BLOCKS | M/N/O/P/Q, Wave 5 | open | Work packages WP-5.* (§4) |
| G-192 | Wave 6 not started: Settings ×7, Setup.UI/Host, Diagnostics ×4, Feedback ×2, ReleaseNotes.UI/Host, Modules ×3 are skeletons; `Shell.ReportDialogOpener`, `Setup.Runtime.Host` unassigned; 55 settings keys never referenced, 36 read with no writer; what's new, after-update dialog, logs panel, FPS overlay, lyrics inspector, crash-reports card absent | file sizes; `Shell.Overlays.UI.cs:80,104,201`; `Setup.UI.Runtime.cs:100,206,249`; `Platform.cs:70-330` | deferred | BLOCKS (Settings, sign-in) / DEGRADES | R/S/T, Wave 6 | open | Work packages WP-6.* (§4); every key gets a writer or is deleted; `SettingsCatalogTests` |
| G-193 | `Controls.RouteForUri` unassigned → every rich-text anchor is styled but does not navigate (needs only the existing route codec) | `Controls.cs:63` | wiring | DEGRADES | Shell.UI.cs `InstallUi` (I) | open | `RouteForUri = uri => Shell.NameOf(Shell.For(uri))`; ControlsTests fact |
| G-194 | Friends panel host absent: `Rail.Friends.SetActive/Refresh` unassigned, state starts Offline, Retry inert; `FriendFeed/FriendPresence` have no caller or decoder | `Rail.cs:374-380`; `Spotify.Api.cs:1220,1231` | impl | DEGRADES | a friends host (SHELL, K) + Spotify.Decode.Pathfinder.cs (E) | open | Host polls the feed while active, stages friend edges, assigns both seams; RailTests pin `SurfaceFor` |
| G-195 | `Stage.NowPlayingMenu` unassigned (right-click on the stage identity raises nothing) | `Stage.UI.cs:64` | wiring | COSMETIC | Actions.UI.cs install (I) | open | Build from the action platform at install |
| G-196 | `Shell.SeedPalette` unset (falls back to `Tok.Use(kind)`; 0.2.9 `Tok.Use(Tok.NeutralPalette, kind)`) | `Shell.Host.cs:107,316-317`; `_old/Wavee/Design/WaveeTheme.cs:15,28` | wiring | COSMETIC | Design.cs (L) | open (verify engine default) | Install `SeedPalette = k => Tok.Use(Tok.NeutralPalette, k)` |
| G-197 | I1 leftovers: `RouteNoted` seam stands in for `NavigationFrameWatch` (no 0.3 home); activity rows have no kind glyph/status chip/track list (no journal, ch 19 DATA GAP 3); omnibar rows have no "⋯"/context menu; tab DnD depends on `LibraryWrites.ResolveTracks/DepositTracks` (G-043); the offline strip is a deliberate divergence from ch 29 that still needs an issue; masthead insets hard-coded 36/32; duplicate tint owner (`Shell.TintOwner`/`Claim` vs L's `TintOwnership`); stale comments (`Video.State.HostCapability` doc; the seam comments in Playback/Spotify/Store/Palette still say App.cs sets the marshaller) | I1 report; `Shell.UI.cs:70-72` | deferred / defect | COSMETIC | Shell.UI.cs, Shell.cs (I), Diagnostics (S) | open — issue owed (D14) | Delete the duplicate tint owner; port NavigationFrameWatch in W6; fix comments |
| G-198 | `WaveeVersionInfo` not ported: the csproj stamps Codename/Channel/PackageVersion/Commit/FeedRelease/UpdateBaseUrl/StoreId "read back through `WaveeVersionInfo.Parse`" but the type does not exist → About, crash headers, update checker, what's-new gating have no version source (sidebar uses `AssemblyName.Version`) | `Wavee.csproj:22-53`; `Sidebar.Host.cs:1407-1415` | impl | DEGRADES | Platform.cs (S) or ReleaseNotes.Model.cs (R; keep in a named partial so `Wavee.ReleaseTool` can include it) | open | Port `_old/Wavee.Core` `WaveeVersionInfo` (+`NormalizeUpdateBaseUrl`) with tests |
| G-199 | Platform (S, W6) not written: `NetworkPolicy` + 3 composition sites, ambient power policy (G-086), `Clock.SeedEpoch`, `AppLocaleBootstrap` engine half (G-017), ch 28 D3 `RuntimeStatus/ProvisioningOutcome/ProgressFraction/ShortHash`, `LogCapturePolicy`, `WaveeLogSessions`; the log facade builds strings on filtered hot paths (no interpolated-handler overloads); `Platform.cs` at budget → needs `+Platform.Settings.cs` | `Platform.cs:1003-1013,653` | deferred | DEGRADES | Platform.cs / Platform.Host.cs (S) | open | Per ch 29 §9.10 / ch 28 D3 with ported tests |
| G-200 | Tray: W-B done (`Platform/Tray.cs` 380, `TrayTests` 58, four keys default `WhileHidden`, six `.ico` + generator, 29 loc keys merged); engine seams E1–E8 committed `eba1285b2` (Engine.Tests 434/434, Windows.Tests 289/289) but VerticalSlice and the 12-step engine on-box list NOT run (`RunFrame`'s gate changed; `AppInstance.GetActivatedEventArgs` for `StartupTask` needs live validation). Absent: `Platform/Tray.Host.cs` (W-C), the `Playback.Os.Publish` hook, `Shell.Host.cs` edits (`--tray` in `ParseArgs`, `StartHidden`, `CloseRequested`, `Tray.Host.Shutdown()`, `RegisterStartup(args)`, `WakeFor` before showing on a redirect), `DeepLinkKind.Quit` / `wavee://quit`, the Settings ▸ General group (W-D), the `(#152)` CHANGELOG bullet, the 13-step app on-box checklist; the tray plan's §4.1/§8 still say the default is Always (code follows §13) | `ls`; tray reports; `Shell.Host.cs:163-224,340,355-359`; `Shell.cs:439`; tray plan :233,:1248 vs :1567 | deferred | DEGRADES (tray) | W-C (U + I/H edits), W-D (R, W6), plan text | in flight: tray W-C | Tray plan §7.4–7.6, §12.2; then VerticalSlice + both checklists |
| G-201 | Tests missing for real CORE: `Screens/ReleaseNotes.cs` (1,912 lines, compiled by `Wavee.ReleaseTool`, 0.2.9 had `ChangelogParserTests` + `ReleaseNotesStoreTests`), `Entities/Recents.cs` (five 0.2.9 files), video host rules (`Video.Plan`, `Manifest.Parse/ToDescriptor/Address`, `StartWatchdog`; 0.2.9 `VideoSwitchPolicyTests`, `SpotifyVideoManifestTests`), `Playback.Video.cs` host, `Notify.Host.cs`, `Home.Host.cs`, `Entities.Fake` | grep `Wavee.Tests` | impl | DEGRADES (release gate runs untested rules) | `Wavee.Tests/ReleaseNotesTests.cs`, `RecentsTests.cs`, `VideoHostRulesTests.cs` (Sonnet) | open | Port the 0.2.9 files against the 0.3 names |
| G-202 | Over-budget files not split (`Playback.cs` 2,221/1,790; `Playback.Audio.cs` 3,010/2,470; `Spotify.Decode.cs` 1,811/1,600; `Spotify.Decode.Connect.cs` 737/520; `Playback.Video.cs` 1,137/1,100; `Sidebar.cs` 6,397/5,000; `Sidebar.Host.cs` 3,116/2,500; `Shell.Host.cs` 1,313/1,000; `Video.cs` 1,412/900); `ReleaseNotes.cs` 1,579/1,000 while the ReleaseTool compiles exactly two partials by name (a third breaks it) | `wc -l`; `Wavee.ReleaseTool.csproj:23-24` | deferred | COSMETIC | owners | open | Name partials on the next edit that crosses 30 %; add a ReleaseTool build to the wave gate |
| G-203 | Release/ops: `Wavee.Version.props` still 0.2.10 "Breaker" build 11; `launchSettings.json` carries `--real-backend` and no `--fake`; csproj comments claim loc/cover loading; `publish-wavee-aot.ps1`, `Wavee.Store.psm1`, `pack-wavee-msix.ps1` reference `src/apps/Directory.Build.props`, `ops/diag/README.md`, `Wavee.Core\Versioning\StoreVersion.Quad` (none exist); `bench-wavee.ps1` launches `--fake --perf-bench` (neither parsed) and passes its output dir through `$env:WAVEE_BENCH_OUT` (banned env-var switch); no `ops/headless/**` | `Wavee.Version.props:5-9`; `launchSettings.json:5,12`; `Wavee.csproj:193-195`; `publish-wavee-aot.ps1:12-15`; `Wavee.Store.psm1:38`; `pack-wavee-msix.ps1:78`; `bench-wavee.ps1:6,40,47` | defect / parity | DEGRADES (perf gate) / COSMETIC | ops scripts, props (O*/S) | open — decisions D16, D17 | `0.3.0-beta.1`/Crest; `--perf-bench --probe-out <dir>` with the probe arm; repoint the comments; `ops/headless` with H-X |
| G-204 | Release pipeline gates the PlayPlay junction (`wavee-release.ps1:241,541`) and the worktree has none; the junction can be created now that Wave 2 re-homed the audio stack, but G-014 must be answered first | `wavee-release.ps1:241,541`; handoff d :23-24 | decision | DEGRADES (release rehearsal) | O* (`.\link-playplay.ps1`) | open — decision D3 | Create the junction after D3; `-DryRun -SkipTests` |

### 2.9 Stage-B leftovers, engine UI gaps, stage-A deferrals (from the wave-4 reports, verified 20:35)

| ID | Gap | Evidence | Cat | Sev | Fix belongs in | Status | Fix spec |
|---|---|---|---|---|---|---|---|
| G-210 | Device picker has no height cap: a dozen Connect devices overrun the window (ch 20 §9 trap, parity 81 "0.3 must") | `Shell.PlayerBar.UI.cs:1050-1090` | defect | DEGRADES | Shell.PlayerBar.UI.cs (I) | open | Max-height scroller (window height − 96 DIP) |
| G-211 | Play-next drop has no batch cap, no "Added the first N" truncation toast, and is silent on a null seam (ch 19 #87 "nothing answers with silence") | `Shell.PlayerBar.UI.cs:633-645` | parity | DEGRADES | Shell.PlayerBar.UI.cs + Shell.PlayerBar.cs (I) | open | 0.2.9's cap + toast; a toast when the seam is null |
| G-212 | Player-bar/overlay parity: the error title is always "Can't play this track" (`Fault` carries no message); no mosaic cover on the bar's art tile (parity 85); module playables never detected (`isModulePlayable = false`); the theme toggle is duplicated (`Shell.Overlays.UI.cs:563-566` vs `Shell.Palette.cs:249`); the update toast calls `Toast.Show` directly (bypasses the one `Notify.Say` announce rule); profile header has no email line (no `User` contact column); chip skeleton is a static fill, not a shimmer; runtime folder-picker filter names hard-coded English (`Setup.UI.Runtime.cs:358`) | `Shell.cs:1265-1268`; `Shell.PlayerBar.UI.cs:599,607`; `Shell.Overlays.UI.cs:316,430`; I2/I3 reports | parity / defect | COSMETIC | Shell.PlayerBar.cs (`Fault` → loc-key map + test), Shell.Overlays.UI.cs, Modules.cs (T), User.cs + decode, Notify.cs (`Say` with title/content) | open — decisions D29 | — |
| G-213 | NPV credits section has no data: no credits edge (`EdgeTable<CreditEdge> TrackCredits`, `TrackFields.Credits`; kind 186 also dropped by the decoder, G-044) | `Edges.cs:610-612` (doc comment only); `Rail.UI.cs:454-455` | data (ch 21 G5) | DEGRADES | Edges.cs (B), Spotify.Decode.cs (E), Rail.UI.cs (K) | open | Add the edge + field group; parity 16 |
| G-214 | Rail/stage rows use a local compact row instead of `TrackRow.ArtCard` and restate the pivot constants (until Wave 4.5 lands `Track.UI.cs`/`Detail.cs`); device-kind glyph order and `FriendEdge.ContextSlot` (assumed playlist) are assumptions | `Rail.UI.cs:922,956`; `Stage.UI.cs:191`; K1 report | deferred | COSMETIC | Rail.UI.cs, Stage.UI.cs, Rail.cs (K) after WP-4.5 | open | Swap to the shared row/band constants; key the glyph off the device-kind enum |
| G-215 | Deck: no stream bitrate/sample rate published (kbps "---", kHz shows the codec badge; ch 23 GAP 4); no blur-hash first paint (no `ImageHash` column); reduced motion does not stop tape/disc/meter/level motion in a mounted deck (parity 55); the Winamp marquee pauses when not playing (ch 23 #66 vs the idle-GPU rule) | `Deck.Faces.cs:1035-1039,1197-1201`; `Deck.UI.cs:390`; `Deck.cs:1574`; `Playback.Host.cs:140` | data / parity | COSMETIC | Playback.Host.cs + Playback.Audio.cs (H), Track.cs + decode (A/E), Deck.Faces.cs (K), ch 23 §10 | open — decision D27 (marquee) | Publish bitrate + rate with the format; `Column<StringId> ImageHash` → `Controls.Artwork(blurHash:)`; snap poses under `Design.Reduced` |
| G-216 | Immersive stage host lives in `Stage.UI.cs` (K1/K3) while ch 22 puts it in `Lyrics.UI.cs` — the coordinator settled it as K1's; the chapter is not amended | `Stage.UI.cs` (backdrop/drift/Escape/shield); ch 22 §9 | decision (settled) | COSMETIC | ch 22 §9 + plan §2 A12 note | open (doc only) | Record `Stage.UI.cs` as the host |
| G-217 | Session document has no playback section (`SessionDto` = Nav + Shell) — pairs with G-078; recent-surface pin reason and the activity/undo journal tables absent from `Store.cs` (the notification panel's Undo/Clear have no journal) | `Shell.Host.cs:747-752`; grep `pin_reason` / `activity` in Store.cs → none | deferred | DEGRADES | Shell.Host.cs (I) + Playback (G); Store.cs (C) | open | `SessionPlaybackDto`; journal tables + the Notify journal seams (G-092) |
| G-218 | `NotificationSimulator` ("Send event") absent → ch 27 parity 40b-c, ch 19 36-37/71-74 cannot be driven | plan §2 `Diagnostics.Probe.cs` row; no code | deferred | DEGRADES (verification) | Diagnostics.Probe.cs (needs the named partial, G-016) or Diagnostics.Host.cs (S, W6) | open | Port `_old/Wavee/App/NotificationSimulator.cs` over `Notify.Rebuild` |
| G-219 | Lyrics: the CJK and Musixmatch network sources and the DES decrypt were not ported (only Amll, LrcLib, SpotifyNative); a pre-save revoked while Wavee was closed is never unscheduled (ch 14 gap 10) | `Lyrics.Host.cs:1780-1782`; Wave-4-owner-I/K reports | parity | DEGRADES / COSMETIC | Lyrics.Host.cs (K), Notify.Host.cs (I) | open — decision D28 | Drop Musixmatch/DES, keep the parsers, CJK on demand; `RemoveGroup("wavee.release-drops")` before the launch re-derive |
| G-220 | Video overrides are not mirrored into `Track.LocalVideo`/`VideoOverride` columns (the roster changes never commit a Staging); Storage-tab sources absent (`Audio.CacheStatus()`, `Audio.Relocate(...)`, licence-key count; the resident-census row undecided) | Wave-4-owner-K report; grep `CacheStatus` / `Relocate` / `Census(` → none; `Store.cs:612-616` (`Store.Stats` exists) | deferred / data | DEGRADES | Entities.cs commit path + Video.Overrides.cs (A/K); Spotify.Audio*.cs (F) for the Storage sources (R mounts) | open | Commit a Staging on `Overrides.Changed`; add the two audio calls; re-point the census row at `Entities.Census()` |
| G-221 | Owner L deferrals: the drag COMMIT half (deposit/reorder execution), `SelectionBar` commands, the `RowSwipe` wrapper + digitizer probe (`TouchCapable` defaults `true`), palette persistence (no palette table in `Store.cs`) | `Controls.cs:855-859`; Wave-4-owner-L report | deferred | DEGRADES | User.cs / Sidebar.Host.cs (O/J), Track.UI.cs (M), Platform.Host.cs (S), Store.cs (C) | open | Each owner's wave; `Palette.Filler` + a palette table (G-055) |
| G-222 | Engine UI gaps: engine boxes have no accessible-name property (glyph-only buttons carry names only as tooltips; UIA `AutomationName` absent); `ProgressBar` ignores reduced motion (the player-bar top-edge sweep keeps sweeping, ch 20 parity 70) | I2/K2/L reports (UIA not inspected by the miner) | engine | DEGRADES / COSMETIC | `fluent-gpu` UIA provider, `ProgressBar` | open — engine issues to file | `AutomationName` mapped in the UIA provider; honour `Motion.Reduced` |
| G-223 | No test pins the player-bar art size (48 above Medium, 40 below) — the 56 bug I2 found was invisible to the suite (I1 added `ShellPlayerBarArtSizeTests`; verify it covers all six tiers) | `Shell.cs:1139`; `ShellTabStripRulesTests.cs` | impl | COSMETIC | Wavee.Tests | open (verify) | `[Theory]` over the six tiers |
| G-224 | Chapter rulings owed (doc-only): window placement not persisted (ch 18 #83 — keep 0.2.9's 1180×760); first-frame accent = OS ramp on frame 0 (ch 30 #75); `PlayLinkRules` lives in `Actions.Rules.cs` though ch 19 files it under `Modules.cs`; ch 26's three J2 conflicts (G-184); ch 23 #66 (G-215); ch 22 stage host (G-216) | miner-ui decisions 8–9, 11–13, 17 | decision | COSMETIC | the chapters' §10/§11 | open | Record each ruling in the chapter's audit log |

---

## 3. The critical path

Everything below is ordered; items on one line can run in parallel (disjoint files).

1. **Build green** (blocks everything): loc merged (done) · ProfileRoot + headless landed (done) · pin the engine for every build, not just the orchestrator's command line (G-022) · wait for H-W and tray W-C to leave their files · `dotnet build` Debug + Release, `dotnet test` (G-019).
2. **Composition** (one orchestrator batch, App.cs + Shell.Host.cs only): G-002, G-003, G-004, G-005, G-007, G-008 (ToUi half), G-009, G-012, G-013, G-014 (after D3), G-023, G-024 (clock), G-025/G-026 (Now + idle timer), G-027 (Registry.Build + the shell verbs) — then the headless smoke (G-018) is the first real-traffic run since Wave 1.
3. **Data path** — in parallel with 2: G-040 + G-041 + G-042 (fetch routing, the edge door, the pathfinder provider) · G-046 + G-047 (rootlist folders, `FolderId`) · G-043 (encoders + library writes host) · G-056 (video gid). Nothing in Wave 4.5/5 renders loaded state without these.
4. **Playback e2e** — after H-W lands, in parallel with 3 but sequenced inside `Playback.cs`: G-100 (double play), G-070 + G-071 (play a context / transfer in), G-101, G-102, G-108, G-112, G-076, G-081/082 — reducer/host batch first (Playback.cs, Playback.Host.cs), then the pump/stream batch (Playback.Audio.cs, Spotify.Audio*.cs).
5. **Sign-in** (D2): pull the login view + PKCE/device-code forward (G-030) so a fresh profile can be verified before Wave 6; session hygiene (G-031–G-036) with it.
6. **Wave 4.5** (Detail + Track surface, G-190) → **Wave 5** pages (G-191) → **Wave 6** screens (G-192), each page registering its `SetPage` and its seams. `--fake` seed (G-015) lands at the START of 4.5 so every gate can run offline.
7. **Video**: engine gates (G-160–G-163) → G-140/G-141/G-144/G-145 → G-149/G-150 → the plan's §4.3 manual gate.
8. **OS/notifications/tray**: G-088/G-089/G-092 with the feed hosts; tray W-C after W-A; G-093 (updater) before any release rehearsal.
9. **Release**: G-203/G-204, `local-update-e2e.ps1` both scenarios, `-DryRun`, parity checklists, `_old` deletion.

---

## 4. Fix batches (disjoint file sets; respecting in-flight ownership)

In flight now and NOT in any batch below: H-W (`Spotify.Audio.Stream.cs`, `Spotify.Api.cs`, `Spotify.Session.cs`, `Spotify.cs`, `Spotify.Connect.cs`, `Playback.Host.cs`, `Playback.Audio.cs` + their tests), tray W-C (`Platform/Tray.Host.cs`; edits to `Shell/Shell.Host.cs`, `Shell/Shell.cs`, `Playback/Playback.Os.cs`, maybe `App.cs`), engine video (`C:\wavee\fluent-gpu`). Batches marked "after H-W" / "after W-C" open when those report. B0 must wait for W-C on `Shell.Host.cs`/`App.cs`.

| Batch | Model | Files | Gaps | Brief |
|---|---|---|---|---|
| **B0 Build + composition** (after W-C) | orchestrator | `App.cs`, `Shell/Shell.Host.cs`, `Directory.Build.props`, `Wavee.slnx` | G-022, G-002, G-003, G-004, G-005, G-007, G-008 (ToUi), G-009, G-012, G-013, G-023, G-024, G-025, G-026, G-027 (Registry.Build) | Worktree-local EngineRoot = the pin; `InstallMarshallers` public and first in Main; composition order per sweep D10: `Platform.Boot → Store.Use → Entities.Boot → Spotify.Boot → Spotify.Api.Boot → Sidebar.Boot → Playback.Boot → Modules.Boot → Sidebar.InstallActionSeams → Actions.Registry.Build → Shell.InstallUi → Shell.Run` (Run: marshallers incl. `Sidebar.Activate`/`Lyrics.Store.ToUi`, `Notify.HostInstall` on attach, auto-login, `Entities.Now` + the idle pump timer, Stopwatch `FrameNowMs`) → tail `Sidebar.Flush; Telemetry.Shutdown; Audio.Shutdown; Video.Shutdown; Store.Shutdown; caches; Platform.Shutdown`. Build Debug + Release, test, hand failures to owners |
| **B1 Fetch routing + edge door + decoders** | Opus | `Entities/Fetch.cs`, `Entities/Entities.cs` (provider routing + scope epoch signal + synthetic-table release), `Entities/Edges.cs` (`FolderId`, failure bit), `Entities/Browse.cs` (Commit hook), `Spotify/Spotify.Decode.cs`, `Spotify/Spotify.Decode.Pathfinder.cs`, `Spotify/Spotify.Decode.Connect.cs`, `Entities/Track.cs` (`VideoGid`, `VideoW/H`), tests `FetchTests`, `EdgesTests`, `DecodeTests`, `PathfinderDecodeTests`, `ClusterDecodeTests` | G-040 (planner half), G-041 (routing), G-042 (edge door), G-044, G-045 (rootlist, home-section, browse, context resolve, autoplay, profile, gander first), G-046, G-047, G-050, G-051, G-052, G-056, G-057, G-058, G-060, G-071 (decode half), G-179 (epoch signal) | Pure folds + fixtures only; no Api calls. Deliver `Api.RoutesFor`'s input contract (kind, wanted → extension kinds) as a CORE table here so B1b can consume it |
| **B1b Api provider routes** (after H-W) | Opus | `Spotify/Spotify.Api.cs` (+ `+Spotify.Api.Library.cs` named partial if over budget), `SpotifyApiTests` | G-040 (provider half), G-041 (pathfinder provider), G-042 (routes), G-043 (`CollectionAdd/Remove`, zstd G-054), G-033 (Profile), G-053, G-055 (filler), G-008 (`GetText`) | One mixed-kind POST per uri; per-subject pathfinder dispatch; sealed groups; edge routes; `Fetch.Answer` through `Spotify.Post` |
| **B2 Library sync + writes host** (after B1, B1b) | Opus | NEW `Spotify/Spotify.Library.cs` (SHELL: login-time sync of rootlist/collections/recents, dealer deltas, `Sidebar.LibraryWrites` implementation, pin bridge), NEW `+Spotify.Encode.cs` (CORE encoders), `Entities/User.cs` (`Dispatch`), tests `SpotifyLibraryTests`, `EncodeTests` (byte-exact vs 0.2.9 fixtures) | G-042 (sync), G-043, G-049, G-048 (caps), G-089 (attach drops) | App.cs installs `Sidebar.LibraryWrites` (B0 follow-up) |
| **B3 Playback reducer + host** (after H-W) | Opus | `Playback/Playback.cs`, `Playback/Playback.Host.cs`, `Playback/Playback.Os.cs`, tests `PlaybackStepTests`, `PlaybackRulesTests`, `PlaybackOwnershipTests`, `PlaybackOsTests` | G-100 (reducer half: `HandedOff`), G-108 (`DeviceReload`), G-112 (`EndingSoon`, re-prepare), G-140, G-141 (fold), G-142, G-143, G-070 (PlayNow from a resolved context), G-071 (host half), G-073 (host), G-074, G-075, G-076, G-078, G-079, G-080, G-081, G-082, G-083, G-084, G-085, G-087, G-036 (Release/epoch) | Every new input gets a Step fact; effects only, no host calls from CORE |
| **B4 Audio pump + stream** (after H-W; after B3's `HandedOff` shape is fixed) | Opus | `Playback/Playback.Audio.cs`, `Spotify/Spotify.Audio.cs`, `Spotify/Spotify.Audio.Stream.cs`, tests `PlaybackAudioTests`, `AudioAdapterTests`, `AudioStreamTests`, `SpotifyAudioTests` | G-100 (pump half), G-101, G-102, G-103, G-104, G-105, G-106, G-107, G-108 (pump), G-109, G-111 (verify), G-112 (pump), G-113, G-114, G-115, G-116, G-117, G-118, G-119, G-120, G-122, G-124, G-125, G-127, G-129 (after D19), G-134, G-038 | Decisions D4–D9 first |
| **B5 Session hygiene + sign-in** (after H-W; D2) | Opus | `Spotify/Spotify.cs`, `Spotify/Spotify.Session.cs`, `Spotify/Spotify.Connect.cs`, `Screens/Setup.cs`, `Screens/Setup.UI.cs`, `Screens/Setup.Host.cs`, tests `SpotifyCoreTests`, `SpotifySessionTests`, `SetupTests` | G-030, G-031, G-032, G-034, G-035, G-036 (session half), G-037, G-039, G-073 (topics), G-095 (SetupBootstrap) | PKCE loopback primary, device code/QR fallback, `SetupGating`; the wizard chrome can wait for W6 (R) |
| **B6 Sidebar wiring** (after J2) | Sonnet (Opus for the binder gate) | `Shell/Sidebar.Host.cs`, `Shell/Sidebar.UI.cs`, `Shell/Sidebar.UI.Drop.cs`, `Shell/Sidebar.UI.Menus.cs`, `Shell/Sidebar.UI.Rail.cs`, `Shell/Sidebar.UI.Rows.cs`, sidebar tests | G-170, G-171, G-172, G-173, G-174, G-175, G-176 (flush half), G-178, G-180, G-183 | Decisions D8 (path), sidebar D5/D8 (freeze API, rebuild gate) |
| **B7 Video host** (after engine G-160–G-163; after B3) | Opus | `Playback/Playback.Video.cs`, `Shell/Video.Host.cs`, `Shell/Video.UI.cs`, `Wavee.csproj` (native target), tests `VideoRulesTests`, NEW `VideoHostRulesTests` | G-141 (post half), G-144 (headers via B1b's `HeaderSet`), G-145, G-146, G-147, G-148, G-149, G-150, G-151 (mount half), G-152, G-153, G-201 (video tests) | Decisions D15 |
| **B8 Notify + OS + shell seams** | Sonnet | `Platform/Notify.cs`, `Platform/Notify.Host.cs`, `Shell/Shell.UI.cs` (`RouteForUri`, `Os.Activate` on first render, duplicate tint owner, the shell `ActionServices` verbs), `Shell/Shell.PlayerBar.UI.cs`/`.PlayerBar.cs` (picker cap, play-next cap/toast, fault titles), `Shell/Shell.Overlays.UI.cs` (theme toggle, `Notify.Say`), `Platform/Design.cs` (`SeedPalette`), `Platform/Actions.UI.cs` (`NowPlayingMenu`), `Platform/Controls*.cs` | G-088 (Rebuild call sites — needs the feed host from W5/W6), G-090, G-091, G-092, G-010, G-193, G-195, G-196, G-197, G-027 (shell verbs), G-210, G-211, G-212 | Decisions D29 first |
| **WP-4.5 Detail + Track surface** | Opus (M) | `Entities/Detail.cs`, `Detail.UI.cs`, `Track.cs` (rules), `Track.UI.cs`, `Track.Table.cs`, tests per plan §6.1 ch 01/03/04 | G-190 | Plan §5 Wave 4.5; gate: the fake album through the real frame in both arms — so `--fake` (G-015) must land first |
| **WP-fake** | Sonnet (Q first cut, orchestrator-owned) | `Entities/Entities.Fake.cs`, the `--fake` arm (in `Diagnostics.Probe.cs` after H-X, or `Platform.cs` after W-B), `Wavee.Tests/EntitiesFakeTests.cs`, `Sidebar.Customizer.UI.cs` samples (after J2) | G-015, G-181 (samples) | Ch 31; pinned `Clock.SeedEpoch`; rootlist with folders, four sets, pins, 166 liked, 16 covers |
| **WP-5.M/N/O/P/Q** (after WP-4.5) | Opus ×5 | per plan §5 Wave 5 (M: Album/Show/Episode/Track.Drawer; N: Artist/Discography/Concert; O: Playlist(+local)/User ×8; P: Home ×7/Search/Browse/Recents; Q: Queue.cs/Queue.UI) | G-191, G-070 (Q's `ContextQueue`), G-080 (Q), G-110 (O local arm), G-131 (M drawer), G-045 (each page's decoder, coordinated with B1's folds), G-088/G-089 (P/M feed hooks), G-194 | Each page: `SetPage` + its seams at install; ch §10 parity logged |
| **WP-6.R/S/T** | Opus ×3 | R: `Settings.*`, `Setup.*` (wizard chrome), `ReleaseNotes.UI/Host`, `Feedback.*`, updater host; S: `Diagnostics.*`, `Platform.cs`/`Platform.Host.cs` (+`Platform.Settings.cs`), probe arms; T: `Modules.*` | G-192, G-093, G-094, G-095, G-017, G-021, G-086, G-110, G-128, G-130, G-132, G-182, G-198, G-199, G-151 | `local-update-e2e.ps1` both scenarios is R's gate |
| **B9 Tests backfill** | Sonnet | `Wavee.Tests/ReleaseNotesTests.cs`, `RecentsTests.cs` (after WP-5.P), sidebar dropped facts (after B2/B6) | G-201, G-183 (tests) | — |
| **B10 Ops** | Sonnet | `Wavee.Version.props`, `launchSettings.json`, `Wavee.csproj` comments, `ops/build/bench-wavee.ps1`, `ops/build/publish-wavee-aot.ps1`, `ops/release/Wavee.Store.psm1`, `ops/release/wavee-release.ps1` (engine-commit + DLL-export asserts) | G-203, G-204 (after D3), G-152 (preflight) | Decisions D1, D16, D17 |
| **Engine issues** (in `..\fluent-gpu`, after the video branch merges) | — | `WasapiAudioDevice(deviceId)` + enumeration; `ICancellableAudioSource.InterruptPendingRead`; voice total update after open; `HeadlessAudioEndpoint(realtime)` | G-126, G-121, G-113, G-111 | File issues; 0.3.x |

---

## 5. Decisions only Christos can make

| # | Decision | Recommended answer |
|---|---|---|
| D1 | Which engine the 0.3 worktree builds against (default resolves to the video-rework branch) | Pin `EngineRoot` to `C:\WAVEE\fluent-gpu-base` via a worktree-local props file + slnx through the property; release preflight asserts the resolved engine commit; switch when the video branch merges |
| D2 | Sign-in before Wave 6 | Yes: pull the login view + PKCE loopback (primary) + device code/QR (fallback) into B5 now, behind the chrome Sign-in button and the `SignInRequired` gate; the wizard chrome stays R's in W6; expose as `login` in the headless arm |
| D3 | Where the audio key deriver is installed | A guarded `#if WAVEE_PLAYPLAY_LOCAL` line in `App.cs` mirroring 0.2.9 (public composition order shows it); confirm with the private repo whether it self-installs; then create the junction |
| D4 | Gapless hand-off model | Keep the pump's butt-join; add `AudioSignal.HandedOff` that advances the cursor without a Load; re-scope the reducer's "Ended advances in the same drain" as the hard-cut fallback |
| D5 | What a starve means | Never end a track on a starve: Buffering/Recovery, wait up to ~90 s, then `Fault.Network`; land bytes per 64 KiB slot |
| D6 | Who owns normalization | The decoders; engine `Normalization = Off` for Spotify voices; track gain only for 0.3, capped by track peak |
| D7 | Lossless fallback and rung gating | Fall back to Ogg 320 on NoDeriver/NoKey (log once); offer the Lossless rung only when `CanDerive`; default 2; metered cap 1 |
| D8 | Cache and document roots | Revert both to 0.2.9's paths: audio cache `%LOCALAPPDATA%\Wavee\Wavee\Cache\audio`, `sidebar-layout.json` under `WaveeMusic\` beside history.json; no migrations |
| D9 | Persistent audio key cache (stores derived keys, DPAPI) | Port behind `AudioKeyCacheEnabled` (zero-network cached replay); if not wanted, delete the orphan key |
| D10 | Folder identity | Add `RootlistEdge.FolderId` (group hex) and 0.2.9's convention (`Id = folder:<hex>`); delete the position-derived id |
| D11 | Who owns library writes and where "group → request" routing lives | A Spotify library host (`Spotify.Library.cs`, B2) implements `Sidebar.LibraryWrites` and the sync; App.cs installs it; one pure routing table in `Spotify.Api` + an edge-planning door in `Fetch.cs`; pages never call `Spotify.Api` directly |
| D12 | Premium gate | Keep 0.2.9: non-Premium welcome → `Failed/NotPremium`, blob cleared, Setup shows PremiumRequired |
| D13 | Suspend/resume semantics | Suspend pauses local playback only, never forwards to a Connect owner; resume re-announces `NewConnection` |
| D14 | GitHub issues to file before their fixes land (CLAUDE.md) | `connect-diagnostics` (plan Q6); ch 14 DATA GAP 1 (concert toast route), 4 (drop toast sound), 8 (sign-out teardown); "switching to video is slow" / "seeking is buggy"; the offline-strip divergence (ch 29); headless host (one issue with the four defects as bullets); Base62 0.2.x rewrite; tray is #152. Approve filing via `github-triage` |
| D15 | Video plan Q1–Q9 | Accept the plan's recommendations: 80 ms cut to the video's own soundtrack; runtime never idle while placement ≠ None, 30 s after off; manifest-only on badge, licence+init+8 s only when video on; 30 s/60 s/32 MiB/2 sessions; segment-granular scrub preview; no Phase C; MSE spike only if warm `attachMs` > 300; release script builds both DLL arches and refuses one lacking `FgPrRuntimeCreate` |
| D16 | Version identity now | `WaveeVersion` → `0.3.0-beta.1` / Crest so dev builds, logs and crash headers stop claiming 0.2.10; `WaveeBuild` stays the release script's |
| D17 | `--fake` and the probe arms before Wave 6 | Land `Diagnostics.Probe.TryRun`'s arm table (with H-X) and the `--fake` scope arm + SEED-CORE now as an orchestrator first cut; other probe arms stay S's |
| D18 | Output-device routing | Engine change (`WasapiAudioDevice` by endpoint id) for 0.3.x; 0.3 ships the enumerated list with a persisted selection and a "follows the Windows default" note (0.2.9 parity) |
| D19 | Spotify MP3 container skip | Sniff decrypted chunk 0 per file instead of deciding by format; confirm on one real MP3 episode first |
| D20 | Headless Q1/Q3/Q5/Q7 | Smoke stays a wave gate + manual rehearsal (not a release-script gate); add `SessionEventKind.Disconnect`; plain `HeadlessLoop`; add the `end-10s` position form |
| D21 | Wave 3 decisions recorded "pending Christos" (handoff d) | Confirm: lossless opt-in via the rung + metered cap; the engine's linear resampler for 0.3 (sinc + exclusive mode post-0.3); no exclusive-mode output |
| D22 | Clock for `Playback.FrameNowMs` (G-024) | A monotonic Stopwatch-ms clock (thread-safe, never frozen, never stepped back by a second host); render sites convert `FrameTime.NowQpc` explicitly at draw |
| D23 | Idle-gated `Fetch.Pump`/`Palette.Tick`/`Entities.Now` (G-025/026) | One named one-shot timer armed to the earliest deadline that posts the pump through `ToUi`; no polling |
| D24 | Credential on `AuthRejected` (G-028) | Clear only on a definitive bad-credentials reject after one retry with a fresh AP; a transient/unknown reject keeps it |
| D25 | Homes for the updater, crash reporter/run marker/crash prompt, factory reset and install-on-quit — plan §2 has no row (G-093/G-094) | `Platform/Platform.Host.cs` (SHELL, S, W6) for deployment/feed/restart/crash-file writers, pure policies in `Platform/Notify.cs` and `Screens/Diagnostics.cs`; keep 0.2.9's log sentences verbatim so `local-update-e2e.ps1` needs no change; add the rows to §2 now |
| D26 | Budget overruns (G-202: `Rail.cs` 418/200, `Video.cs` 1,412/900, `Video.Host.cs` 349/250, `Deck.cs` 1,675/1,500, `Shell.Host.cs` 1,313/1,000, `Diagnostics.Headless.cs` 1,145/700, `Sidebar.cs` 6,397/5,000) | Split `Rail.Hero`/`Rail.Friends` into `Shell/Rail.Surface.cs` and the headless JSON writers into `Diagnostics.Headless.Json.cs`; restate `Video.cs`/`Sidebar.cs` rows at their measured size; accept the ≤ 30 % ones |
| D27 | Winamp marquee pausing when not playing (ch 23 #66 vs the idle-GPU rule) | Accept (idle-GPU rule wins); amend ch 23 §10 |
| D28 | Unported lyrics sources (Musixmatch + DES decrypt, grey CJK) | Drop Musixmatch/DES (decrypting a third-party payload); keep the parsers; port the CJK sources only on demand |
| D29 | Small UI rulings: update toast via `Toast.Show` vs `Notify.Say`; player-bar error title; device picker cap | Extend `Notify.Say` with title/custom content; a `Fault` → loc-key map with a test; a scroller capped at window height − 96 DIP |

---

## 6. Audit log

| # | Date | Pass | What changed |
|---|---|---|---|
| 1 | 2026-09-13 | First pass | Register built from the 93 reports (audio 58, core 60, video/OS 53, sidebar 48, code sweep 60 raw findings), deduplicated; every row verified in code at HEAD `e0cc9710` + the live edits. Closed since the reports were written (verified in code): the four UI marshallers + `Fetch.Pump`/`Palette.Tick` (I1), `Shell.InstallUi`/`Sidebar.InstallActionSeams`/`FrameNowMs` (App.cs), the welcome's market/scope switch and the Connect hello (H-W), the silent sink connect + pacing (H-W), `Sidebar.CustomizerPage` (J2), `Video` partial, `FgPrProbeFile`, arm64 DLL rebuild, ch 14 DATA GAPs 1/3/4/11/12, ch 24 DATA GAP 5, the FLAC three fixes, NVorbis/FlacBox removal, 21 non-public sidebar types, the Wave 2 compile-review items. Nothing was built, run or tested by the auditor |
| 2 | 2026-09-13 (late) | Same pass, second half | Folded in the wave-4 UI/tray/headless miner (82 raw findings) and the I1, J2, tray W-B, tray-engine and H-X reports; re-based on HEAD `201cd247` and the orchestrator's build report. Moved to fixed-unverified: G-020 (40 loc keys merged), G-016 (headless landed; `ProfileRoot`), G-133 (`Stream.Stats`, `Audio.Metrics`), G-178 (rail flyout → `Binder?.Entries`), tray W-B/engine seams inside G-200. New rows: G-024 (`FrameNowMs` on the UI frame clock), G-025 (`Entities.Now` never written), G-026 (idle-gated pump), G-027 (`AppActions.Register`/`Registry.Build`/`ActionServices` never composed), G-028 (`AuthRejected` wipes the credential), G-029, G-184 (J2 leftovers), G-210–G-224 (stage-B leftovers, engine UIA/ProgressBar gaps, stage-A deferrals, chapter rulings). Decisions D22–D29 added. G-019 re-stated as the test-project/Release/test-run gap. Build state recorded in §1 E1. Next pass: after H-W and W-C land and B0 builds + tests run |
