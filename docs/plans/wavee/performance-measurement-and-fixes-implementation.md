# Wavee performance: reliable measurements, then targeted fixes

Date: 2026-09-09. Status: measurement implementation in progress; performance gates not satisfied.

## Execution evidence (continuation)

- Added completed-UI-frame lifetime totals, exact 8.3 ms threshold, final shutdown flush, exact process memory bytes and OS lifetime peak. Attached frame observation before the application loop. Corrected `unaccounted` subtraction: fence/present are already included in SubmitMs.
- Added route-owned primary-content frame reveal observations with navigation identity, including retained-page activation. This is deliberately labeled `boundary=ui-frame`; existing primary readiness permits unresolved detail rows and excludes some secondary widgets/images. Full visible-content readiness and actual presentation remain unproven and required for the user's complete objective.
- Tour now uses whole-session counters, all route rollups, cold AND warm gates, exact byte thresholds, final memory sample and INCOMPLETE for missing observations. Added fixture tests, verified passing. First route reordered to album-A so returning Home is an actual activation.
- Profiler now accepts existing artifacts, preserves raw dump output, verifies object type/size and target root chain, and identifies threads from Speedscope. Fixture tests passed. Full trace analysis exposed excessive PowerShell analysis overhead; analyzer optimization is in progress before repeating analysis.
- Verified existing dump targets via `dotnet-dump analyze -c`: 16 MiB byte array `01d538882bb8` belongs to GlyphRenderer; 2 MiB `01d53c240240` to PixelBufferPool; 4 MiB `01d53d3685b8` to the completed decode queue. Evidence: `%TEMP%/wavee-heap-raw-verified.txt`, `%TEMP%/wavee-roots-verified.txt`. Shared LiveDealer root prefixes were real; truncating the first 30 lines hid the actual owners. Column parsing was correct.
- Engine Debug and Release baseline builds succeeded, each reporting 156 warnings and zero errors. Full VerticalSlice initially failed one progress.3 state-resume assertion. Investigation found the test assumed one hosted deadline flush completed both parent and child updates; its component semantics check now explicitly drains reactive work and preserves all assertions. Full rerun pending.
- App solution Release initially failed nine backend-to-UI diagnostic dependency errors from the handoff changes. Extracted shared diagnostic clock and token-owned registry into Wavee.Core, added behavior tests and rewired backend/UI adapters. Rebuild and full app tests pending.
- Native ARM64 AOT baseline remains pending. No performance behavior fix has been made during measurement repair.

Native attempt update: ordinary ARM64 AOT publish succeeded (46.38 MiB exe, native PDB available). First full tour attempt exposed a real catalog/query deadlock after four seconds, so the tour was stopped and is INVALID as a complete baseline. Startup primary reveal was 266.4914 ms; observed working set later about 628 MiB. Evidence saved to `%TEMP%/wavee-arm64-aborted.log` and `%TEMP%/wavee-arm64-hang-stacks.txt`.

The ARM64 CDB at `C:/Program Files (x86)/Windows Kits/10/Debuggers/arm64/cdb.exe` captured the actual opposing stacks noninvasively: `RetryUnservedDemand -> CatalogRepository.Peek -> Monitor.Enter` holds the query gate; `Project`'s lambda waits for that gate inside `CatalogRepository.ReadConsistent -> DataCommitQueue.ReadConsistent`; UI is blocked in `MemorySampler.Sample -> CatalogRepository.ResidentCount`. `TryReplan` has the same reverse lock order by inspection. Fixing this production deadlock is a prerequisite to further baseline runs, not a sampling workaround. The hung test process PID 24024 was intentionally terminated after saving evidence; its unclean run marker is expected.

Verification update: app Debug/Release builds passed (28 warnings, no errors). App tests: 7616 passed, 2 failed, 1 skipped. Connect masking harness race repaired to await metadata ingress like production; lyrics cache write/read replacement race remains under investigation. Release Pester: 304 passed, 0 failed, 1 skipped. Frame/reveal/owner/parser fixtures passed. The optimized thread analyzer completed existing-trace analysis; corrected synthetic Speedscope marker handling preserves category totals and resolves actual exclusive method stacks. Full engine harness rerun after deterministic progress.3 correction remains pending.

Concrete memory candidates from source inspection, awaiting native measurements: glyph atlas backing is 16 MiB CPU + 16 MiB texture + three 16 MiB upload banks (80 MiB requested backing); sparse compositor overlay values can avoid full-high-water NodePaint/interaction/brush copies per snapshot; opacity scratch surfaces currently allocate full-window targets. These are allocation accounting, not measured WS savings. Preserve text subpixel phases, fence lifetime and visuals.

Latest verification: full VerticalSlice passes all 1396 checks, with native exit code 0 captured separately from its intentional stderr diagnostics (`%TEMP%/wavee-verticalslice-confirmed.log` and `.stderr.log`). The earlier PowerShell wrapper exit 1 came from stderr handling, not a failed assertion. The lyrics cache repair uses `FileShare.Read | FileShare.Delete` and streamed source-generated deserialization; a deterministic test holds the production reader across replacement and verifies both old and new documents. Query lock-order repair and its account-switch race tests are undergoing review before the next full app test run.

Resume 2026-09-09 (after the credit pause): the lock-order test's `handle.Subscribe` compile error is fixed (`handle.Changes.Subscribe`). Query patch reviewed: `CatalogRepository.Epoch` moves only in `SetSessionCore` and `SetOfflineCore`, so PublishJoin's epoch recheck rejects a join only across a real scope/online change and cannot livelock under ordinary commits; a rejected join sets `_joinSuperseded` and queues one full pass (coalesced with the Session fan-out's own). `TryReplan` now plans under the node gate alone when every required key was observed by the last join (the per-row scroll case never queues behind a commit's publication) and re-runs the whole plan under catalog -> node only when a never-observed key needs the repository's knowledge; nothing is written before that decision. Tour timeout handling finished: coverage completeness and the KeepOpen verdict are conditioned on the actual shutdown attempt, the manifest records `StartupFailure`/`ShutdownAttempted`/`ShutdownCompleted`, and startup-policy fixtures were added (analysis and profile fixture checks pass, tour script parses clean). Full app test run and Release build are the next serial gates; the native binary still predates these repairs.

Verification 2026-09-09 (resume): full app tests 7622 passed / 1 failed / 1 skipped (`%TEMP%/wavee-tests-resume.log`); the one failure was the new lyrics upgrade test, root-caused by a live probe: on Windows `MoveFileEx(REPLACE_EXISTING)` returns ERROR_ACCESS_DENIED against a reader that holds the target with `FileShare.Delete`, while delete (POSIX semantics) + plain rename installs the replacement and the open handle keeps its complete old bytes. `LyricsDiskCache.SaveAsync` now deletes then renames (one retry if a concurrent writer re-created the name); the 22 LyricsDiskCache tests pass (`%TEMP%/wavee-tests-lyrics.log`). App Release build: 28 warnings, 0 errors (`%TEMP%/wavee-app-release-resume.log`). Query lock-order probes and the scope-race test pass inside the full run. Tour manifest additionally records dirty-tree fingerprints (status count + diff SHA256 for app and engine) and a playback classification from the always-on `put-state playing=` lines; fixtures pass.

Memory slice 1 (engine, 2026-09-09): glyph atlas dirty-row upload staging. `FluentGpu.Engine/Seams/Text/GlyphAtlasStore.cs` (new, pure: R8 mirror, append-only shelf packer, generational reset, dirty-row tracker + staging placement) and `FluentGpu.Windows/D3D12/GlyphRenderer.cs` (`UploadIfDirty` stages and copies only dirty rows plus a one-row apron, one fixed band mapping per frame so every dirty row is copied exactly once across multiple flushes per submit; banks resized to 1024 rows and growable to a 2048-row cap; first-frame zero upload replaced by the barrier alone since committed D3D12 resources start zeroed). Backing: upload banks 3 x 16 MiB -> 3 x 4 MiB (cap 3 x 8 MiB), total atlas backing 80 -> 44 MiB (worst 56); per-flush copy ~16 MiB -> ~200-320 KiB. `AtlasResetPending` now also covers a staging shortfall (Diag `text.atlas/stagingShort`). Gates: engine Debug + Release 156 warnings / 0 errors (`%TEMP%/wavee-engine-{debug,release}-resume.log`), VerticalSlice ALL CHECKS PASSED 1409 checks incl. new `gate.atlas.upload.*` (`%TEMP%/wavee-verticalslice-resume.log`), WindowsApp `--screenshot` on the Adreno X1-85 renders text/icon masks intact. These are allocation-accounting savings; the native WS effect is measured by the next tour.

Native ARM64 AOT republished 2026-09-09 12:28 with the query deadlock, lyrics and glyph atlas changes: `src/apps/Wavee/bin/Release/net10.0/win-arm64/publish/Wavee.exe` (46.4 MiB, `%TEMP%/wavee-arm64-aot-resume-build.log`). This is the binary the next coordinated tour must measure: `powershell -NoProfile -File ops/tools/perf-tour.ps1 -Exe <that path>`; stop at failed startup readiness and keep the incomplete report/log/manifest.

Native tour 1 (2026-09-09 12:29-12:33, ARM64 AOT, `%TEMP%/wavee-perf-tour-native-1.{md,log,manifest.json}`, pid 4468): the deadlock is gone — the tour ran the whole cold lap and into the warm lap. Verdicts: frames **FAIL** (session over83=51, worst 143.1 ms — a startup frame with unaccounted=134.4 ms under TitleBar, i.e. a native wait, plus 29.7/16.1 ms first-paint frames dominated by PagedShelfCore/ShelfCard mounts and the SidebarPaneSlot ItemsView), memory **FAIL** (first sample 252 MB, 620 MB after the first route, OS lifetime peak 1000.1 MB; managed heap ~350 MB of which LOH 247 MB, gpu ~190 MB, the remainder unmanaged/scene), reveals **FAIL** (cold artist-A 1483.5 ms, artist BLACKPINK 503.7 ms; most other cold reveals 55-65 ms; warm album-A 18.7, home 58.7). Readiness 1.5 s. The warm lap is INCOMPLETE from playlist-A on: the app crashed at t=1788949963547 with `ObjectDisposedException: CancellationTokenSource_Disposed` on the UI thread inside `AppHost.FlushHosted -> TreeReconciler.BeginKeepAliveExit/FreeKeepAliveEntry -> HomeSectionPage` effect cleanup (crash report `%LOCALAPPDATA%/Wavee/logs/crash-report-20260909-123243-547.txt`, RVAs resolved with cdb `ln` against the publish PDB). Root cause: HomeSectionPage registers an unmount cleanup that cancels+disposes `walkCts` AND the walk effect's own cleanup cancels the same source again; at KeepAlive eviction the unmount cleanup runs first. Fix: the walk effect's cleanup only acts while it still owns `walkCts` (`Features/Home/HomeSectionPage.cs`). Detailed attribution of the 51 frames, reveal windows and memory composition is being extracted from the raw log next.

Native tour 1 attribution (from the raw log, seq numbers cited): 51 over-budget frames = 34 flush/reactive, 11 submit/fenceWait (GPU fence stalls under the synthetic wheel, comps=0), 4 layout, 1 record, 1 unaccounted. Flush buckets: PagedShelfCore/ShelfCard shelf mounts on artist pages (13 frames; worst seq=57 flush 19.2 ms with 2326 mounts), ItemsView/sidebar (8; seq=173 flush 15.6 ms ResponsiveBox×13), ExpandableRowSlot/BoundRowContent (4; seq=359 liked flush 64.9 ms, frame 70.7 ms — the largest non-startup frame), layout on shelf grids (4; seq=466 13.7 ms). Startup frame seq=31 (143.1 ms) has nodes=0 draw=0 cmds=0, census ≈0.2 ms, unaccounted=134.4: a native present/composition wait right after adapter negotiation on the Weak UMA tier, not engine CPU work. Reveal breaches: navId=4 artist-A 1483.5 ms is gated by `commit.slow SweepAsync runMs=3429.0 pendingAfter=3` (seq=195) with RecordRecentAsync/PreloadAsync queued ≈1405-1445 ms behind it; navIds 13/14/23 (490/725/504 ms) are cold `envelope:ArtistOverview` fetches of 278-457 ms plus AcceptAsync commits of 88-147 ms inside the window; navId=1 startup 446.9 ms spans login/AP resolve. The Sweep alone is 76% of the 4481.6 ms commit.slow total. Memory: launch 252 MB = 39 MB managed committed + 87 MB gpu + 126 MB other; first navigation +368 MB (scene 688→5892, components 96→704); afterwards the non-managed non-gpu remainder is flat at ~370-420 MB and growth is managed committed (LOH 20.7→256.9 MB) + gpu (87→190 MB) with retained routes (KeepAlive never returns components toward baseline across 24 anchors). Warm lap was also contaminated by unplanned navigations (Porter Robinson / Roy Kim / Sung Si Kyung / browse, navIds 12-16) between warm album-A and home — human interaction during the timed run; the next tour must run untouched. Slices started from this evidence: SweepAsync slicing/deferral off the foreground commit path (Persistence/CatalogCacheMaintenance.cs) and sparse compositor animation overlays (engine SceneRecordingSnapshot.Animation.cs).

Slices written after tour 1 (pending gates):
- **Catalog sweep off the commit owner** (`Wavee.Core/Catalog/CatalogSweepSchedule.cs` new pure schedule; `Backend/Persistence/CatalogCacheMaintenance.cs`, `SqliteCatalogMaintenance.cs`, one index in `SqliteCatalogPersistence.cs`; `DataCommitQueue.Pending`/`OwnerIdleFor`). Measured on a copy of the live 124 MB library.db: the old batch cost ≈2456 ms warm (1079 ms pinned-bytes SUM scans, 817 ms extension candidate scan, 221 ms resource scan, 209 ms day-roll, 103 ms orphan anti-join) and deleted nothing (58.5 MB < 64 MB budget, no TTL due). Now: an 8 ms index-only `CatalogGcHasWork` probe on the read connection, batches run off the owner with evictions published via the publication gate, deferral around pending commands / a 1500 ms reveal window / owner idle < 120 ms with a 40×250 ms cap, pinned-bytes short-circuit when under budget, orphan anti-join only after deletes, `ix_extension_cache_gc`. Always-on `catalog.sweep` line replaces the lost visibility. Tests: `CatalogSweepScheduleTests` (12) + `CatalogRetentionTests` (+10).
- **Sparse compositor animation overlays** (engine `Scene/SceneRecordingSnapshot.Animation.cs` rewritten; `SceneRecordingSnapshot.cs`, `Animation/AnimScheduler.Compositor.cs` DistinctNodeCount, `Hosting/Threading/SceneRenderFrame.cs` publisher-side `ReserveCompositorRows` after animation capture; canon `docs/design/subsystems/threading-render-seam.md` §3.5). Backing at 32768 nodes × 3 snapshots: dense 49,938,432 B (47.6 MiB) → 1,171,464 B (1.1 MiB) with 256 rows of 500 B each + 4 B/node lookup + 4 B/node dirty epoch; ceiling 4096 rows. Overflow never grows in Tick: spill row + dirty chain + remembered demand reserved at the next capture. Gates added: `gate.compositor-row-sparse/-overflow/-alloc`; check-canon OK.
- **Account flyout opening empty** (engine): OS-windowed popups are created hidden and revealed after their "first content frame", but `D3D12Device.Present` stood down any present whose HWND is not visible — including the popup's composition swapchain — so the first present was dropped while both reveal paths (sync `Show()+AnimatePopupOpen`, async `PopupPresented` latch off the publication sequence) went ahead: frost with nothing inside. Intermittent because a later motion-driven frame usually repainted it, except when `CompositionBackdrop.AnimateOpen` early-returned without `_opened` and the loop idled (plus 1000 ms frame-latency waits from the un-replenished semaphore). Fix: `ISwapchain.HasPresentedContent`; composition targets that never presented are exempt from the stand-down so the first frame lands before the window shows; both reveal gates require the real paint; unplaced popups are not recorded; `WakeReasons.PopupAnim` also means "owes first paint"; `AnimateOpen` latches `_opened` first. Files: `Seams/Rhi/Rhi.cs`, `D3D12Device.cs`, `AppHost.cs`, `SceneRenderFrame.cs`, `CompositionBackdrop.cs`, `WakeDiagnostics.cs`, headless RHI (`PresentStandDown` model), gate `e4popup.3b`. Adjacent finding not changed: the `ClosedAction = () => handle.Value = null` idiom (~20 sites) can null a NEW handle on close-then-reopen within the 83 ms fade.

- **Track-row expander overlap / empty gap** (user report 2026-09-09): the drawer is a `SizeMode.Reflow` child; closing it seeds an exit-reflow pin on the slot root (the reserved extent in the Measured list), which by design must never carry `AnimFlags.NaturalTarget` (`AnimScheduler.Reflow.cs:72-74`) — but `RecordDeclaredSize` granted the bit on every declared NaN and `WriteColumns` calls it on every re-render, so `RunReflowLayout` re-aimed the closing pin from a child extent that still included the fading exit orphan: either a band nothing paints in (empty gap) or content arranged past the reservation (overlap; the slot root has no clip). Engine fix: `RecordDeclaredSize` only preserves `NaturalTarget` (`AnimSuite` 5242 already asserts enter-reflow is the only shape carrying it). App: `TrackRowTemplate` measured rows at the 48 DIP constant while the list reserved the density height — now `shape.RowH` (`RowShape.RowH` from `DetailTrackTableRules.RowHeightFor`; `BoundRow` had silently dropped its `rowH` parameter); `ToggleExpanded` corrects off-window collapses to the live shape height; drawer version rows extracted into pure `TrackDrawerRows` (at most one video row, stable keys, reservation dropped once answered) with `TrackDrawerRowsTests` (9). Title→subtitle gap 6 → 2 DIP: two `Flow.Show` boundaries in the title column each consumed a 2 DIP gap slot while collapsed; the three exclusive title variants now share one `Gap = 0` slot. Live checks still owed: double-toggle a row while the first is closing; density/style ladders 40/48/56/64 and 36/40/44/48.

Gates for the post-tour-1 slices: engine Debug/Release 156 warnings / 0 errors, VerticalSlice ALL CHECKS PASSED 1413 (`%TEMP%/wavee-*-resume2.log`); app full tests 7657 passed / 2 failed → both resolved (ConnectControllerTests.InboundSkipPrev used a fixed 20 ms sleep and flaked in isolation on the untouched tree, now waits for the host to settle; the new ClearAsync test asserted pinned rows are cleared, but the GC batch has always excluded `temp.catalog_pins` even on a clear — test corrected to the real semantics), rerun 114/114 (`%TEMP%/wavee-tests-resume3.log`); app Release 0 errors; native republished 13:21 (`%TEMP%/wavee-arm64-aot-resume3-build.log`).

Native tour 2 (13:21-13:26, `%TEMP%/wavee-perf-tour-native-2.{md,log,manifest.json}`): both laps complete, no crash. Frames **FAIL** over83=60 of 23665, worst 109.3 ms (tour 1: 143.1); startup first route firstFrameMs 128.3 (169.4). Memory **FAIL**: launch 203.8 MB (252.2), after album-A 400.7, end of cold lap 811, OS peak 896.0 (1000.1). Reveals **FAIL**: startup 149.2 (446.9), cold artist-A 156.5 (1483.5 — the sweep no longer gates it: `catalog.sweep periodic outcome=nothing evictable probeMs=162.1 batches=0`), plus navIds 24/26/27 at 746/519/544 ms which belong to a second burst of unplanned navigations (navIds 19-27, warm liked/albums) — provenance being checked against the handoff timestamps. Every per-route "navigation frame coverage missing / scroll movement not observed" line in that report is a tour parser defect (the nested `worst=frameMs=` census was invisible to the whitespace-anchored field reader), fixed in `Get-PerfRollup` with a fixture; the raw log is unaffected.

Managed-heap attribution (JIT ARM64 heap run 13:29-13:33, `%TEMP%/wavee-perf-tour-jit-heap-1.*`, `wavee-tour-13676.gcdump`, plus `dumpheap -stat` from the pre-change 10:42 dump in `%TEMP%/wavee-dumpheap-stat-1042.txt`): heap ≈ 257-293 MB. Composition (10:42 dump, 280 MiB): `Byte[]` 59.9 MiB (glyph mirror 16 MiB, pixel pool ~32 MiB, decode queue 4 MiB, misc), LOH **Free 45.3 MiB** (fragmentation; mem.sample `fragmented` reached 106 MB), dense scene columns ≈80 MiB (`NodePaint[]` 44.6 → 17.75 MiB after the sparse overlays, `LayoutInput[]` 8.4, `TextMeasureCache[]` 8.3, `InteractionAnim[]` 5.1 → sparse, `InteractionInfo[]` 3.5, `BrushAnim[]` 3.1 → sparse, `ScrollBody[]` 5.9-12.3 dense by node), retained DSL element records ≈25 MiB (`BoxEl` 19.5 MiB for 11,410 instances ≈1.8 KB each, `TextEl` 2.5, `ComponentEl` 1.1, 12.5k `Action` closures), catalog/query bookkeeping ≈17 MiB (`HashSet<ResourceKey>` entries 6.3, `ResourceKey[]` 4.6, `ResourceSnapshot` 2.2, `Track` 1.65), strings 6.6 MiB, `DrawListOpcodeStats[]` 2.9 MiB (diagnostics), `RunGlyph[]` 3.4 MiB. The fresh gcdump's large-object table (77.6 MiB of 256.7 MB) confirms the post-change shape: `NodePaint[]` 17.75 MiB ×5 scenes, `Byte[]` 17.3, `ScrollBody[]` 11.75 (single dense array, slice started), `OverlayRow[]` 0.12 MiB (sparse overlays live). At the 896-1000 MB peaks the managed committed heap is ~350 MB and engine-accounted GPU bytes ~170 MB; the remaining ~380 MB is non-managed and unattributed — next measurement is `!address -summary` / `!heap -s` on the live native process.

Tour 2 attribution (raw log, pid 36952): of the 60 over-budget frames, 24 (41%) belong to navIds 19-27 — the user's own clicks into never-warmed artists during the warm liked/albums steps (user confirmed), all flush-dominated PagedShelfCore/ShelfCard/LazyNowPlayingOverlay mounts; 6 are startup (seq=30 worst 109.3 ms: nodes=0 draw=0 cmds=0 unaccounted=106.4 right after adapter negotiation — first-present warmup, same signature as tour 1's 143 ms); cold artist-B 4, cold home 4, warm artist-A/B 3 each. Scripted reveal breaches: navId=1 startup 149.2 ms (compounding first-pass query joins, no fetch in the window) and navId=4 cold artist-A 156.5 ms (`catalog.gate.wait 13.1 ms PublishJoin pendingCommits=1` at nav start); the 746/519/545 ms reveals are the clicked cold artists (ArtistOverview/identity fetch batches, `commit.slow AcceptAsync 207.2 ms` inside navId=24). Memory: launch 203.8 MB = 34.3 managed committed + 87.1 gpu + 82.4 other; peak 877.4 = 347.3 + 189.1 + 341.0 other (39%, scaling with ws); steps +285.8 MB at the first navigation (catalog resident 142→1234, deps 380→2966), +113.7 MB cold artist-B (resident 2584→9363), +90.7 MB playlist-B. `catalog.sweep` ran once (nothing evictable, probe 162 ms off-owner); commit.slow 32 lines / 1144 ms logged, worst 207.2 ms AcceptAsync; gate.wait 16 lines / 154.7 ms, worst 23.9 ms. Live native attach (`%TEMP%/wavee-native-mem-{launch,loaded}.txt`, `!address -summary`): reserved VA — Image 377.6 MB (1030 regions), Stack 116.5 MB, MappedFile 68-82 MB, NT Heap 34.6-38.6 MB, private <unknown> ≈68 GB (GC/RHI reservations); Get-Process private bytes 388 MB at 20 s (home hydrated) and 547 MB after four routes. Reserved sizes do not answer which pages are resident, so a per-page working-set classifier (QueryWorkingSetEx over VirtualQueryEx regions, by type and module) is the next measurement.

Working-set attribution (new tool `ops/tools/perf-ws-attribution.ps1`: QueryWorkingSetEx over VirtualQueryEx regions, resident private/shared per type, per image module, per private allocation base; outputs `%TEMP%/wavee-ws-attrib-{launch,loaded}.md`, UTF-16). Native process 20 s after launch (home hydrated, ws 492.6 MB, private 444): Private resident 395.7 MB, Image resident 88.4 MB shared + 5.7 private (Wavee.exe 24.4 MB, OneCoreUAPCommonProxyStub 6.3, Spotify.dll 4.0, ntdll 3.5, qcgpuarm64xcompilercore 2.8 of 49.3 committed, windows.storage 2.6, combase 2.3, D3D12Core 1.7, qcdx12arm64xum 1.7), Mapped 2.6. Private by allocation base: GC reservation 0x1ce107f0000 193.5 committed / 173.6 resident; **nine PAGE_WRITECOMBINE (0x404) blocks, eight of exactly 16.0 MB + one 19.7 MB = 147.7 MB resident** (D3D12 upload-heap signature; engine `gpu bytes` census said 87 MB) → ten blocks / 163.7 MB after four routes; RW blocks of 32 + 32 (24.1→32) MB and 8.4→31.7 MB near the GC range (unidentified: GC bookkeeping or CPU-mapped default/readback heaps); NT heaps 144 regions 7.7 MB + 59 regions 6.2 MB. So at launch the non-managed, non-image working set is dominated by upload heaps, not by native heaps. Slices started: D3D12 upload arena right-sizing + census honesty and first-frame PSO/compile investigation (D3D12Device + pipelines); artist-page shelf mount storm (PagedShelf + artist shelves); ScrollBody sparse pool (resumed after a usage-limit interruption mid-edit of ScrollKernel.cs).

- **Sparse ScrollBody pool** (engine `Scroll/ScrollKernel.cs`, `ScrollBody.cs` doc, `VerticalSlice/Suites/ScrollKernelSuite.cs`, canon `docs/design/subsystems/scene-memory.md` §2.6a "node-indexed dense arrays are for the spine only"): the five node-indexed arrays (12,746,752 B at 32768 nodes, `sizeof(ScrollBody)`=376) become a slot pool + 4 B/node lookup (143,776 B at 32 viewports; −12.02 MiB). Growth only in `BindNode`; `Unbind` parks the slot on a deferred free list flushed after `EmitTouched`/`CompactActiveList`; `ScrollBody` never reaches the compositor (projected into `ColdSlab<ScrollState>`, already pooled). Behaviour changes to review: `ResolveRestores` iterates in bind order; `Unbind` now clears a pending restore (old code leaked `_restorePendingCount`); `ApplyContactEnd` falls back to self when `LastAbsorbed` unbound; the old rebind-in-same-pass duplicate-active bug disappears. Gates: `gate.kernel.body-sparse`, `gate.kernel.slot-reuse` (asserts cap 32 from `initialCapacity` 16); check-canon OK.

- **D3D12 CPU-visible memory, reconciled** (engine): the 147.7 MB of write-combined pages is NOT upload heaps. Full census of every UPLOAD/readback resource at launch = 19.66 MiB (7 pipeline rings 4.45 MiB, glyph instances 1.88, glyph staging 12.0, readback ~0); on the UMA non-cache-coherent Adreno, DEFAULT-heap resources are mapped L0 write-combine too, and the driver places all GPU allocations in pinned 16 MiB WC segments, so the 87.1 MB of tracked resources (≈40 MB compositor RT pool — canvas-sized slots, `OpacityLayerCompositor` MaxPool 32 + Acrylic 12 + BakedBlur, never idle-trimmed; 16.78 MB glyph atlas; 19.7 upload; 11.0 back buffers) fill nine segments at 59% occupancy. All 33 `Track` sites audited: census is complete. Change made: one persistently mapped `UploadArena` per frame-in-flight replaces the 7 pipelines' 27 private UPLOAD rings (`Seams/Rhi/UploadArenaPolicy.cs` pure sizing: 384 KiB initial, 2 MiB cap ≥ documented worst case 1,556,480 B, grow only at BeginFrame on the fenced bank, refusal arms the un-skippable repaint); pipelines 4,669,440 → 1,179,648 B, engine-wide upload 19.66 → 15.33 MiB; census gains `upload: arena=… peak=… refused=…`, plus always-on one-shot `[d3d12.boot] initDevice/pipelines ms` and `[d3d12.mem] first-present` per-class breakdown; pal-rhi.md §3.2; 8 `arena.*` gates. Real ≥100 MB levers named: compositor RT pool idle trim + damage-bounded transient RTs (~40 MB), text 45.6 MB (2048² atlas / fewer phases), thumbnails as separate 64 KiB-granule textures on UMA (atlas packing disabled there for the old barrier hang; a COMMON CPU-writable atlas page needs no barrier). PSO library plan written (`PsoCache` with `ID3D12PipelineLibrary`, cache dir via `FluentAppOptions`), not implemented: device+pipeline bring-up is ~56 ms BEFORE the frame loop, not the first frame.
- **Startup first frame is app-side**: the 106-134 ms `unaccounted` sits after submit in DrainPassiveEffects; the UI thread logs `[playback.buckets] startup - bridge - activated` (+73 ms) and `[app] Online; shell up from cache` (+97 ms) inside that window — Wavee's first-frame mount effects (playback bridge activation, shell-from-cache resume) run synchronously. Slice started: move activation/restore off the UI thread with ordering guarantees and a pure gating class.
- **Artist shelf mount storm** (engine `Controls/PagedShelf.cs`, new `Controls/ShelfProbeMath.cs` source-included by Wavee.Tests, `ShelfMountBudgetTests` 26 facts): the measured shelf's height probe set `_probeSample = min(count, 24)` and never cleared it, so `sampleCells = new Element[_probeSample]` rebuilt up to 24 full `MediaCard.Shelf` subtrees per shelf on EVERY render and kept them mounted for the shelf's life — 8 measured artist shelves ⇒ ~190 phantom cards remeasured every layout pass (explains `PagedShelfCore×6-11 a=3.2-4.7 MB`, `measures=3.6-8.8k`, `ShelfCard×46-100` vs `ShelfCardSlot×12-66`). Also nothing was viewport-gated (8 below-the-fold strips mounted ~12 cards each on the publication frame) and the propless `ItemsView` element was rebuilt per render. Now: transient probe cells in chunks of 4 keyed by index (≤6 frames, identical final lock), `LazyScroll.Slot`-gated one-way strip mounting with mount-erring safety nets (pages without a provider unchanged), `_liveStrip` built once, hoisted delegates. Reported, not changed: `ShelfCard.Props`/`LazyNowPlayingOverlay.Props` default record equality with fresh delegates never gates; `Surfaces.Shimmer` swaps element type+key on cover arrival (re-realizes `CoverShimmer`); `AccentHeader` reference-compared re-renders all shelf chrome per page render.

Engine gates on the batch (glyph, overlays, flyout, reflow, scroll pool, upload arena, shelf): Debug/Release 156 warnings / 0 errors, canon OK, VerticalSlice `%TEMP%/wavee-verticalslice-resume3.log` — every check passed except `gate.path.fill.winding [ring-nonzero] seed=0x68196636 mismatches=1/1000`. Root cause of the flake: the sampler seeded from `string.GetHashCode`, which .NET randomizes per process, so every run sampled different points; no path-fill code changed today (only Controls/PagedShelf+ShelfProbeMath, Scroll, Seams/Rhi/UploadArenaPolicy, DiagnosticsSuite/ScrollKernelSuite and Windows/D3D12 files, which the headless slice does not compile). `PathSuite.WindingSampleCheck` now seeds with FNV-1a over the tag; `--suite path` passes 70/70 deterministically. The one sample under the old random seed that disagreed between the reference point-in-contours test and the triangle soup for ring-nonzero remains a latent boundary-tolerance discrepancy (1 in 1000 uniformly padded samples), now reproducible on demand rather than a coin flip; not investigated further.

- **Startup first-frame activation** (app): the 106-143 ms `unaccounted` was ONE mount effect in `WaveeApp.Render` activating everything inline: window A (+73/+98 ms) = the tail of `PlaybackBridge.Activate` — SMTC (first WinRT activation, cross-process GetForWindow, per-call RoActivateInstance in UpdateTimeline), taskbar (CoCreateInstance TaskbarList, LoadImageW per .ico), Jump List (one ShellLink CoCreate + IPropertyStore commit per item, `.customDestinations-ms` disk write), toast registration (HKCU writes, CoRegisterClassObject possibly twice); window B (+24/+17) = PowerBridge `NetworkPolicy.Install` (NetworkListManager CoCreate + two Advise) and `HasStoredCredential` DPAPI; window C = shell leaf effects. `HostDispatch.Post` drains before frameStart and `DrainUiPosts` snapshots the queue length, so a self-re-posting ladder lands one step per drain outside the timed frame. Now: `App/StartupActivation.cs` (pure: `StartupAffinity` Core/Worker/Window, `ActivationCommandQueue`, `StartupActivation.Run`), `PlaybackBridge.Activate` reduced to subscriptions + `Release()`, new `ActivateMediaControls/ActivateTaskbar/ActivateJumpList` window steps, `PostOrQueue` replaces seven silent pre-activation drops (and `CommitSeek/PreviewSeek` queue instead of throwing), `WaveeNativeBoot.InstallDispatcher`/`Register` split, `PowerBridge.InstallOsHooks` as a Window step (NetworkStatus.Subscribe is inert off the UI thread), worker steps `session-resume` (DPAPI off-thread, decision posted back), `cache-gc`, `update-poll`. Always-on `[playback.buckets] startup.step/startup.phase` (Warning when a Core step exceeds 8.3 ms). `StartupActivationTests` (15). Behaviour delta: OS surfaces appear a few drains after first paint. Not done: caching `HasStoredCredential` (five invalidation sites via Credentials.cs).

- **Compositor render-target pool** (engine): inventory at 1195×767 (canvas slot 3,666,260 B): `OpacityLayer.Pool` 4 live canvas slots at launch (14.7 MiB, cap 32), `Acrylic.Canvas` 3.50 MiB unconditional, `Acrylic.Pool/Cache` po2 buckets (a full-width band ⇒ 2048×1024 = 8 MiB, trimmed only after 600 frames on every tier), `BakedBlur.ScratchA/B×3` 6.00 MiB created eagerly at device init and never released; `D3D12MemoryDiagnostics.BreakdownLine`'s worked example `total=87.1MiB n=64 | Glyph.AtlasTexture=16.0MiB×1 | OpacityLayer.Pool=14.7MiB×4 | …` matches the measured launch total byte-for-byte. Changes: new portable `Render/LayerTargetPool.cs` (bucketing, trim ladder `LayerTargetTrim` — weak tier warm reserve 2 canvas slots, hard cap 4, cold release after 900 idle frames — census, subtree probe, `BoundedGroupRegion`); one `Retire` chokepoint in OpacityLayer (7 eviction sites) and Acrylic; `TryBoundedExactBlur` replaces the exact-blur arm's second canvas lease with a guarded bucketed region scratch (σ=4 strip 340×120 → 384×192 bucket, 0.28 vs 3.50 MiB); `TryAcquireBoundedGroup`/`CompositeBounded` for plain opacity bands (1195×300 → 1216×320, 1.48 vs 3.50 MiB); `DsBlurHlsl` gains a source-uv offset (24→28 root constants, existing callers byte-identical); Acrylic weak-tier trim 600→120 frames; BakedBlur banks lazy on first job (launch −6.00 MiB deterministic); `GpuProfile.IsWeak` read per tick instead of static-init capture; `gpu` census line gains `| rt: inuse= free= pin= retire=`. Expected ~40 → ~20-24 MB steady state. Gates: `LayerPoolSuite` 22 `gate.layerpool.*`; canon gpu-renderer.md §7.1 + SPEC-INDEX row; check-canon OK. GPU-run claims to confirm: bounded exact-blur pixel identity, bounded group placement under damage-scissored replay (`--repaint-identity`), first image bake after lazy banks, weak-tier churn with reserve 2 / cap 4.

Engine gates on the full batch incl. the compositor pool, upload arena, startup-agnostic Windows changes (`%TEMP%/wavee-*-resume4.log`): Debug/Release 156 warnings / 0 errors; VerticalSlice ALL CHECKS PASSED **1457** checks (1396 at the start of the day); FluentGpu.Windows.Tests 169/169; WindowsApp `--screenshot` on the Adreno renders byte-identical to the pre-compositor-change capture and prints the new always-on lines: `[d3d12.boot] initDevice=34.7ms pipelines=98.2ms (opacity=93.2 acrylic=48.9 glyphs=37.9 …)` and `[d3d12.mem] first-present total=52.71MiB n=37 | Glyph.AtlasTexture=16.00 | Swapchain.BackBuffer=14.83×3 | Glyph.AtlasUpload=12.00×3 | Acrylic.Canvas=4.94 | Glyph.InstanceUpload=1.88×3 | Upload.Arena=1.x` — no OpacityLayer.Pool or BakedBlur entries at first present any more. A first full app chain was killed by the OS for low memory mid-`dotnet test` (IDE processes hold ~6 GB); rerun serially.

App full tests after all slices: **7701 passed / 0 failed / 1 skipped** (`%TEMP%/wavee-tests-resume5.log`; 7616 at the start of the day). Two verification chains were killed by the OS on commit charge (46 of 63 GB, IDE processes ~6 GB plus ten idle MSBuild nodes and a 2 GB VBCSCompiler from the day's builds); build servers were shut down gracefully and the Release build rerun with `-m:2 -nodeReuse:false` before the native republish.

App Release after all slices: 0 errors (29 warnings). Native republished 16:24 (`%TEMP%/wavee-arm64-aot-resume7-build.log`; two background attempts were OS-killed on commit charge during IL compilation, the foreground run succeeded).

Native tour 3 (16:25-16:29, `%TEMP%/wavee-perf-tour-native-3.{md,log,manifest.json}`, 19 navIds = 9 routes × 2 laps + startup, no user interaction, no crash): frames **FAIL** over83=31 of 20544 (tour 2: 60 incl. 24 user-click frames, i.e. 36 scripted), worst 77.9 ms (109.3), first route firstFrameMs 55.3 (128.3), 5 startup slow frames; memory **FAIL** launch 203.1 MB (203.8), end of cold lap 759 (811), OS peak 831.6 (896.0); reveals **FAIL** with exactly two breaches: startup navId=1 122.4 ms (149.2) and cold artist-A navId=4 131.9 ms (156.5) — every other cold reveal 59-74 ms, warm 3-65 ms. Startup activation phases now measured: Core 3.4 ms (8 steps), Worker 0.8 ms, Window 124.8 ms in 6 one-per-drain steps (smtc 40.8, jumplist 50.8, taskbar 17.9, power-os 11.0, toast-register 4.2) — off the timed frame but still UI-thread time in the first second. GPU census: 77.8 MB / 34 resources at launch (87.1 / 64 before) → 159.8 / 819 at the end of the cold lap → 1079 resources later (`images=1111 imagesReady=1051 imageBytes=54.6`); the new `rt:`/`upload:` tokens did not reach the app's mem.sample line (plumbing slice started). Working-set classifier on this binary (`%TEMP%/wavee-ws-attrib2-{launch,loaded}.md`): launch ws 435.4 MB (492.6 before), WC segments 5 = 82.9 MB (9 = 147.7), GC resident 177; after four routes ws 619.2 (510.3 in the earlier, differently timed capture), WC segments 11 = 179.7 MB, GC resident 217 — per-thumbnail 64 KiB-granule textures are the leading suspect for the segment growth (slice started: UMA thumbnail packing in ImageTextureStore).

Tour 3 attribution (raw log, pid 18012): 31 over-budget frames = 18 flush/reactive, 11 submit/fenceWait (GPU, worst seq=246 artist-A scroll 20.3 ms submit=17.7 fenceWait=13.9), 1 unaccounted (seq=38 first empty frame 36.7 ms, unaccounted=27.4 — the empty-first-frame pathology shrank from 106-134 ms), 0 layout-dominant. Worst frame seq=63 liked first content mount 77.9 ms flush=72.1 mounts=2753 (ItemsView×2, ExpandableRowSlot×30, BoundRowContent×30, SidebarPaneSlot×31, bindFires=1780); seq=51 shell mount 56.0 ms mounts=204; list pages repeat at 1479-1958 mounts per frame. The startup Window steps (smtc 40.8, taskbar 17.9, power-os 11.0, jumplist 50.8 = 120.5 ms) run on the UI thread in dispatch drains interleaved with the first five frames — not charged to any frame's phases but stretching `sinceNavMs` for the startup reveal. Reveals: navId=1 liked 122.4 ms waited on LikedSongsQuery growing 1640→2232 keys (coldKeys 328→562) across passes plus a 9.4 ms PublishJoin gate wait and the two heavy mount frames; navId=4 artist-A 131.9 ms waited on ArtistDetailQuery converging coldKeys 6→902→0 across three passes (0.1/12.0/11.3 ms) serialized with a 10.2 ms ReadTrack gate wait and a 12.0 ms PreloadAsync commit — the ArtistPopular fetch (133 ms) lands after the reveal. Memory: launch 203.1 = 36.7 managed + 77.8 gpu + 88.6 other; end of cold lap 762 = 305 + 160 + 297; peak 823 = 348 + 167 + 308. Largest steps: +327 MB during the startup liked reveal/settle (gen2 1.6→24, LOH 22→46, components 93→358), +77 MB artist-B cold (LOH 145→206), +50 MB playlist-B (catalog resident 3534→9917 with a disproportionately small ws delta). Sweep once (nothing evictable, probe 134.6 ms); commit.slow 15 / 505.7 ms, worst 97.6 ms AcceptAsync; gate.wait 9 / 130.2 ms, worst 46.8 ms `.ctor`. Slices started from this: per-frame realization budget for list first mounts (ItemsView + row hosts), startup Window ladder gated behind the first reveal + idle and budgeted per sub-step, and a design document for managed-heap retention (`docs/plans/wavee/memory-retention-design.md`).

The next foreground tour is awaiting the user's scheduling response; continue headless verification and concrete memory design while it is pending. Never proceed through navigation/input when startup readiness is missing. Preserve the incomplete report, raw log and manifest instead. Native startup already has known failures (266.4914 ms primary reveal, about 628 MiB observed WS); these are evidence for mandatory cold-start and memory fixes, not a complete workload baseline.

## Objective and evidence

User targets, clarified during planning: zero frames above **8.3 ms**, working set in the **100–200 MB** range from startup through the whole tour, and **every page reveal <=100 ms, cold and warm**. Startup, first visits, retained-page activation and background refresh all count; a cold path gets no relaxed performance threshold. Report actual refresh interval separately; do not silently substitute 16.67 ms or round 8.333 ms into the stricter 8.3 ms gate. Memory below 100 MB is acceptable.

Cold reveal measurements of 109–119 ms are failures, as is approximately 1183 MB working set. Record first-frame startup latency and end-to-end navigation latency as distinct user-visible delays alongside reveal latency; a quick route-to-reveal measurement cannot hide slow input/deep-link handoff. A skeleton alone is not a completed content reveal. When unavailable network data prevents meeting the target, report that failure and its wait attribution; do not redefine success or exclude the route.

The handoff's native ARM64 JIT run is the historical baseline: `%TEMP%/wavee-perf-tour-run2.md`, its `.log`, and `wavee-tour-31644.gcdump`. It reports 38 over-budget frames in navigation windows, 6 in scroll windows, a 45 ms worst navigation-window frame, and approximately 1183 MB working set at the end. Those windows can overlap; their counts are not a unique whole-tour total. Startup separately includes a 265 ms frame. These are historical measurements, not measurements of today's exact files.

Recorded warm reveals are 6–31 ms, but several warm routes have no reveal value. The current report's global reveal PASS is therefore not established. The x64 AOT run on ARM64 is invalid for the native performance baseline. The separate seven-route JIT profile has different heap totals from the nine-route tour; keep their measurements separate.

Read for this plan: both repositories' CLAUDE.md rules, engine AGENTS.md and fluentgpu skill, current tour/profile/publish scripts, NavigationFrameWatch, Responsive, track template/bound row, and relevant shelf code. The prior operation-ultra-fast progress document records completed implementation slices; it does not establish performance acceptance.

## 1. Repair measurement validity before changing performance behavior

Files: `ops/tools/perf-profile.ps1`, `ops/tools/perf-tour.ps1`, `src/apps/Wavee/App/NavigationFrameWatch.cs`, relevant page-reveal producers, and engine diagnostics only where needed.

### Heap ownership

The current profile script already parses ordinary dumpheap rows as Address, MT, Size. The handoff's proposed column swap is not justified by this version. Preserve the raw output and first establish whether commands, selected addresses, output filtering, or actual shared ownership explain repeated roots.

Extract parsing into a small reusable PowerShell helper. This is the intended row parser, retaining MT for verification:

```powershell
function ConvertFrom-DumpHeapRows([string[]]$Lines) {
    foreach ($line in $Lines) {
        if ($line -match '^\s*(?<Address>[0-9a-fA-F]{8,16})\s+(?<MT>[0-9a-fA-F]{8,16})\s+(?<Size>[\d,]+)\s*$') {
            [pscustomobject]@{
                Address = $Matches.Address
                MethodTable = $Matches.MT
                Bytes = [long]($Matches.Size -replace ',', '')
            }
        }
    }
}
```

Use command-output fixtures to cover headings, summaries, commas, multiple distinct addresses, malformed rows and empty results. Tests consume fixture data, never production source text. Select arrays by exact byte size with more than one address sampled from the repeated ~2 MB group; the existing rounded-MB grouping can hide distinct owners. Use `dumpobj ADDRESS` before `gcroot ADDRESS` to verify type/size, retain complete command output, and verify the root chain actually reaches the requested object. Root multiple BoxEl objects deterministically; skipping 200 addresses alone is not representative. Exclude `Free` from live-object totals and report heap fragmentation separately.

Add an analysis-only route through the script accepting existing dump/trace artifacts, so parser validation does not require another app launch. Check exit codes, artifact existence and command success. Report unresolved ownership explicitly.

### CPU attribution

Local `dotnet-trace report topN --help` offers number, inclusive and verbose options, but no thread filter. Inclusive sorting alone does not fix idle-wait dominance. Export/analyze samples with thread identity and timestamps using an available trace analyzer; verify its installed API/CLI before coding integration. Identify the UI OS thread by its AppHost/RunFrame stack and record that ID. Report UI work, UI waits and render-thread work separately, restricted to recorded navigation windows. Keep sample counts, total observed thread time and denominator beside inclusive/exclusive rankings. Inclusive parent/child times are overlapping, not additive.

Start by reanalyzing `%TEMP%/wavee-profile-20260909-1042.nettrace` and `.dmp` if present. If too few UI samples cover short row mounts, repeat those route transitions in a dedicated attribution capture. CPU sampling cannot resolve a single sub-millisecond function reliably from one mount. Use bounded always-on counters for measure calls, memo hits/misses, shape invalidations and mounts where samples leave uncertainty.

### Whole-tour verdict and route attribution

Current defects to address:

- Only the first nav/scroll rollup per step is consumed; later bursts and late refresh work can disappear from the verdict.
- Missing nav, scroll and reveal data can effectively pass. A wheel burst with no observed movement is unmeasured.
- Reveal selection uses time ordering alone. A delayed reveal from another parked page can still be credited to the current route.
- Working-set peak is the maximum of selected step-end samples, not all samples or the process peak.
- The final step extends into dump collection/shutdown; collection can perturb GC, memory and frame results.
- Rate-limited slow logs are useful exemplars, not authoritative total counts. Pending suppression and scroll windows need final flushing.

Add monotonic frame counters covering the session, with explicit tour-start/tour-end snapshots before any dump collection. Update counters once per observed frame, without per-frame allocation. Include raw-threshold over-8.3 count, worst frame, refresh-budget count and frame count. Maintain navigation/scroll detail as attribution; derive the verdict from counter deltas so overlaps cannot double count and late refresh frames cannot disappear. Ensure finalization emits pending windows even if no subsequent frame arrives. Retain full precision for verdicts and format only for display.

Carry navigation ID plus route/argument identity through reveal production and reporting. A cached kept-alive page needs an activation reveal marker tied to its new navigation, not only the original mount. Define reveal as ready content included in the active page's frame; distinguish an application marker from verified presentation. Record deep-link handoff latency separately. First lap means first visit this process, not cleared disk/network caches; log actual cache/readiness state and distinguish cached data from retained UI.

Use three-valued verdicts. Proposed helper for aggregate thresholds:

```csharp
internal enum PerfVerdict { Pass, Fail, Incomplete }

internal static class PerfGate
{
    public static PerfVerdict Evaluate(int observed, int expected, int violations)
        => violations > 0 ? PerfVerdict.Fail
         : expected <= 0 || observed != expected ? PerfVerdict.Incomplete
         : PerfVerdict.Pass;
}
```

Apply after validating unique expected observations, numeric values and interval coverage. A known violation remains FAIL even when other measurements are missing. Test no observations, partial coverage, violations, duplicate route IDs, delayed foreign reveals and all-complete success. Test frame aggregation independently from log formatting.

Sample all working-set/private readings during the defined interval and report process peak separately with its lifetime scope. Never call a 5-second sampled maximum an exact peak. Keep WS, private commit, managed live/committed heap, native reservations and GPU residency separate: they overlap and cannot simply be summed or subtracted into an exact native total.

## 2. Native ARM64 baseline and input coverage

Run prerequisite headless gates serially, then publish explicitly for ARM64:

```powershell
dotnet build Wavee.slnx
dotnet build Wavee.slnx -c Release
dotnet test src/apps/Wavee.Tests/Wavee.Tests.csproj
Invoke-Pester -Path ops/release/tests
dotnet build ../fluent-gpu/src/FluentGpu.slnx
dotnet build ../fluent-gpu/src/FluentGpu.slnx -c Release
dotnet run --project ../fluent-gpu/src/FluentGpu.VerticalSlice
powershell -File ops/build/publish-wavee-aot.ps1 -Arch arm64
powershell -File ops/tools/perf-tour.ps1 -Exe C:\wavee\WaveeMusic\src\apps\Wavee\bin\Release\net10.0\win-arm64\publish\Wavee.exe
```

No direct `NoteRoute(` call was found in Wavee.Tests during planning; builds and tests still need execution. Run any new performance-helper tests too. Do not run builds or dump collection concurrently with a timed tour. Verify actual target process architecture, not its directory name; record executable hash, app/engine revisions plus dirty-diff fingerprints, architecture, power state, refresh rate, window size, scale, playback state and route/cache state. Preserve timestamped raw artifacts outside source control. Keep scripts UTF-8 with BOM for Windows PowerShell 5.1.

Use three fresh-process repetitions, two laps each, with the same routes and conditions. Report each run and the worst result; averages do not override a single threshold breach. Capture startup separately for attribution and include it in frame and memory acceptance. Add a controlled cold-data case using an isolated supported test profile/cache fixture, preserving the user's actual data; the first lap of a warm disk cache cannot establish true cold-data performance. Apply the same <=100 ms reveal threshold and record network/cache waits explicitly. Shipping-like AOT uses ordinary Release, not `-Diag`. JIT remains the managed attribution target; do not transfer JIT timing or heap savings directly to AOT claims.

Input scenarios, reported separately:

1. Preserve the existing 16 ms / 120 wheel-delta burst as a reproducible stress scenario.
2. Add a repeatable lower-cadence wheel sequence with pauses, direction reversals and bounded pointer movement to exercise hover. Label it a chosen synthetic workload, not validated human behavior. Verify focus, SendInput success and observed viewport movement.
3. Add the proposed `-UserScroll N` recording mode for actual laptop touchpad input. Announce the route/recording interval; do not inject wheel events during it. Record gesture/inertia coverage and observed motion. This interactive run requires the user to participate when execution reaches it.

Before adding `FrameStats.InputMs/InputEvents`, trace the actual PAL dispatch and DirectManipulation callback boundaries. Measure active dispatch separately from blocking message waits, document which frame receives between-frame input, and avoid double counting time already in a phase. Correlate present cadence with active scrolling; audit the meaning of existing missed-vblank counters on an event-driven idle UI before interpreting every gap as a dropped frame. Input work and presentation may lie outside current FrameMs coverage.

## 3. Fix candidates, conditional on the corrected baseline

Each slice begins with its measured cause, exact files and intended numerical delta. Implement one causal change at a time and rerun the matching scenario, then the full tour. Do not promise that any candidate alone will reach 200 MB or zero slow frames. Memory reduction and cold reveal latency are mandatory work, not follow-up polish: collect their attribution with the first baseline, and promote a proven major memory or cold-path fix ahead of a smaller frame optimization. The ranking below is an initial investigation order, not permission to postpone either failing gate.

| Priority | Investigation and potential change | Required evidence and regression coverage |
|---|---|---|
| 1: row mount/layout | `Components/TrackRowTemplate.cs`, `Features/Detail/DetailTracks.cs`; engine `Layout/FlexLayout.cs`, span measurement. Count slot mounts, shape changes, actual measure work and memo misses before removing wrappers or changing cache keys. | Worst row-opening frame, mount bytes/row, measures/row, memo hits/misses. Preserve metadata links, recycled handlers, expansion, focus, selection, skeleton transitions and responsive layout. |
| 2: shelves | App shelf call sites plus engine `Controls/PagedShelf.cs`. It already has `ItemsView.CreateBound` and measured virtualization paths: identify which path each slow shelf uses, probe count, vertical offscreen shelf mounting and remount triggers. Use the existing bound source/virtualization mechanisms. | Mounted cards/probes versus visible cards, allocated bytes and scroll/reopen time. Preserve paging, snap, resize, keyboard focus, hover and current-item actions. Rendering alone virtualizes; page queries still demand their whole model. |
| 3: responsive/Home | Engine `Controls/Responsive.cs` already supports `Responsive.Of<TState>` with an equality gate. Inspect ungated closures, incomplete/overbroad state and width/shape churn; convert only proven hot callers to complete state-gated props. Trace Home's delayed feed publication by item identity. | Equal data causes no card/row remount; changed metadata still updates; tier transitions and resize remain correct. No frozen constructor values or metadata-based identity keys. |
| 4: retained scene/pages | Engine scene columns, recording snapshots, scroll storage; app `Features/Shell/ContentHost.cs` currently retains 8 entries. Attribute current live/capacity bytes and snapshot ownership before considering sparse scroll/animation/text storage or splitting paint storage. Evaluate page retention only with state restoration and cached reveal cost measured. | ARM64 AOT WS/commit and managed/native allocation ownership; no broken generations, snapshot lifetime, animation, text cache or scroll restoration. Memory and <=100 ms cached reveal must improve together. |
| 5: images/native memory | Root actual byte arrays, then inspect image/pixel pool occupancy and eviction. Profile native committed regions and GPU allocations using an available native memory tool, since the managed heap cannot explain the full WS. | Decode churn, hit rate, image quality, GPU residency and AOT WS before/after. Do not simply lower budgets until images continually decode. |
| 6: commit queue | `Backend/Catalog/DataCommitQueue.cs`, repository/query paths. Separate queued wait from lock hold and execution for SweepAsync/AcceptAsync; consider work outside the gate or bounded batches only after tracing transaction/publication boundaries. | Queue wait distribution, query latency, no loss of atomic consistency, ordering, cancellation or completeness; no return to fetch windows in pages. |

Current row entry point (actual code, preserve live item binding):

```csharp
public override Element Render()
{
    var shape = _o._rowShape!.Value;
    return TrackRowTemplate.Build(
        new BoundItemScope<RowPresentation>(_scope, _presentation),
        shape, _o.Handlers);
}
```

This subscribes to shape; investigate why shape changes before caching the returned Element. The row already uses bound text/spans and shape-selected cells. Removing those binds would regress metadata updates.

Component path and visual invariant for the first slice:

```text
ContentHost -> KeepAlive(8) -> detail page -> ItemsView.CreateBound
  -> ExpandableRowSlot -> BoundRowContent -> TrackRowTemplate.Build
    -> GridEl -> enabled cells -> title + SpanTextEl metadata

| number | heart | art | title / clickable metadata | optional facts | duration | actions |
|                         expanded detail when opened                                  |
```

The exact enabled cells vary by RowShape. Preserve column alignment, hit targets and expansion geometry. Write the actual proposed patch and regression cases into this plan before each performance slice; do not invent cross-cutting scene struct shapes before reading their canonical owners. Before engine design changes, read engine README, design README/SPEC-INDEX and subsystem ownership docs; update owners and run check-canon when contracts change.

## 4. Execution and acceptance

- First deliverable: corrected analysis of the existing artifacts, verified root targets and UI-thread attribution, plus a report that cannot pass missing coverage.
- Second: reproducible native ARM64 AOT baseline with full-tour counters, all route reveals and separately identified input scenarios. Publish the numbers before beginning performance fixes.
- Third: sequential measured fixes in the ranked order above, adjusted only when new measurements justify it. Implementation can use disjoint-file subagents as repo rules request; only the orchestrator builds, tests or launches, and no measurement overlaps agent/build load.
- After changes: app Debug/Release, Wavee.Tests, applicable PowerShell tests and engine Debug/Release plus full VerticalSlice for engine work. Include visual/interactive regression checks appropriate to the changed UI. No source-text tests or new environment-variable switches.
- Compare before/after on the same native architecture and workload. A later wavee-stable comparison needs matching architecture, viewport, data/cache and instrumentation; label unavailable metrics instead of inferring them. Stable comparison supplements the controlled current-tree baseline.
- Acceptance requires all three repetitions to satisfy every complete gate: 0 frames >8.3 ms including startup, WS <=200 MB from startup through the whole tour, every cold and warm reveal <=100 ms, and correct interactive behavior. No cold-start, first-visit or cache-warmup exemption. Any known breach is FAIL; missing evidence is INCOMPLETE. Startup results and instrumentation limitations stay visible.
- Preserve the large pre-existing dirty working trees. No stash/reset, commit, push or release is part of this plan. Associate actual fixes with existing issue references per repo rules when implementing; do not invent issue numbers or send external messages.

Immediate next action when executing this plan: validate raw dump commands and repair the report's coverage/attribution, then rerun the outstanding gates and collect the native ARM64 baseline. Performance implementation starts after that baseline has been reported.
