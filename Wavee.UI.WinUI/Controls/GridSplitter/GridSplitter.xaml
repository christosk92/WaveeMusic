<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Wavee.UI.WinUI.Controls">

    <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Default">
            <SolidColorBrush x:Key="GridSplitterBackgroundNormal" Color="Transparent"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPointerOver" Color="{ThemeResource SystemBaseLowColor}"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPressed" Color="{ThemeResource SystemBaseHighColor}"/>
        </ResourceDictionary>
        <ResourceDictionary x:Key="Light">
            <SolidColorBrush x:Key="GridSplitterBackgroundNormal" Color="Transparent"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPointerOver" Color="{ThemeResource SystemBaseLowColor}"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPressed" Color="{ThemeResource SystemBaseHighColor}"/>
        </ResourceDictionary>
        <ResourceDictionary x:Key="HighContrast">
            <SolidColorBrush x:Key="GridSplitterBackgroundNormal" Color="Transparent"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPointerOver" Color="{ThemeResource SystemColorHighlightColor}"/>
            <SolidColorBrush x:Key="GridSplitterBackgroundPressed" Color="{ThemeResource SystemColorHighlightColor}"/>
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <Style TargetType="controls:GridSplitter">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Background" Value="{ThemeResource GridSplitterBackgroundNormal}"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:GridSplitter">
                    <Grid x:Name="RootGrid"
                          Background="{TemplateBinding Background}"
                          CornerRadius="4">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource GridSplitterBackgroundPointerOver}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource GridSplitterBackgroundPressed}"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
