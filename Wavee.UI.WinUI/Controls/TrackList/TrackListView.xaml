<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Wavee.UI.WinUI.Controls.TrackList.TrackListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:contracts="using:Wavee.UI.WinUI.Data.Contracts"
    xmlns:appControls="using:Wavee.UI.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:behaviors="using:Wavee.UI.WinUI.Behaviors"
    xmlns:track="using:Wavee.UI.WinUI.Controls.Track"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Card shadow for rows -->
        <media:AttachedCardShadow x:Key="RowShadow" Offset="0,1" BlurRadius="4" Opacity="0.08" CornerRadius="6"/>
    </UserControl.Resources>

    <Grid>
        <!-- Main ListView with scrollable header -->
        <ListView
            x:Name="InternalListView"
            SelectionMode="Extended"
            CanDragItems="True"
            DragItemsStarting="InternalListView_DragItemsStarting"
            DragItemsCompleted="InternalListView_DragItemsCompleted"
            SelectionChanged="TrackListView_SelectionChanged"
            ContainerContentChanging="TrackListView_ContainerContentChanging"
            HorizontalContentAlignment="Stretch"
            Padding="16,0,18,0">

            <ListView.ItemContainerTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="False"/>
                    <AddDeleteThemeTransition/>
                </TransitionCollection>
            </ListView.ItemContainerTransitions>

            <!-- Scrollable Header: Page Header + Column Headers.
                 HorizontalAlignment=Stretch on the StackPanel is required: WinUI 3's
                 ListView.Header does NOT auto-stretch to viewport width even when the
                 ListView itself has HorizontalContentAlignment=Stretch. Without it the
                 header content sizes to its natural width and the column-header * column
                 collapses to its MinWidth, making every header column appear shifted
                 hundreds of pixels left of the actual row columns. -->
            <ListView.Header>
                <StackPanel HorizontalAlignment="Stretch">
                    <!-- Page-specific Header Content -->
                    <ContentPresenter x:Name="HeaderContentPresenter" Content="{x:Bind HeaderContent, Mode=OneWay}"/>

                    <!-- Column Headers (scrolls with page header initially) -->
                    <!-- Width is driven imperatively from TrackListView.xaml.cs (SyncHeaderWidthToListView,
                         hooked off InternalListView.SizeChanged). WinUI 3's ListView.Header ContentPresenter
                         doesn't propagate viewport width during measure (microsoft-ui-xaml issues #3682 / #9824),
                         so any * column in here would otherwise collapse to MinWidth and the header would
                         visually misalign with row content. Don't remove the code-behind assignment. -->
                    <Grid x:Name="ScrollableColumnHeaders" Padding="8,0,8,0" Margin="0,0,0,8" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
                        <!-- 7 columns matching TrackItem Row layout (artist is a subline under Title, no standalone column).
                             MaxWidth on the Title column MUST match TrackItem.xaml's RowContentGrid (currently 640) — without
                             it, the header's * column keeps expanding past 640 on wide displays while the row's column caps
                             out, and every column right of Title slides hundreds of pixels out of alignment with the rows. -->
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition x:Name="HeartColumnDef" Width="32"/>
                            <ColumnDefinition x:Name="ArtColumnDef" Width="42"/>
                            <ColumnDefinition Width="*" MinWidth="120" MaxWidth="640"/>
                            <ColumnDefinition x:Name="AlbumColumnDef" Width="180"/>
                            <ColumnDefinition x:Name="AddedColumnDef" Width="120"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="#" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Title (sortable) -->
                        <Button Grid.Column="3"
                                Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                                CommandParameter="Title"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="12,8,8,8"
                                HorizontalAlignment="Left"
                                HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_1" Text="Title" Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                          FontSize="10"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          Visibility="{x:Bind ViewModel.IsSortingByTitle, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Button>

                        <!-- Album (sortable) -->
                        <Button x:Name="AlbumHeaderScrollable" Grid.Column="4"
                                Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                                CommandParameter="Album"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0,8,8,8"
                                HorizontalAlignment="Left"
                                HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_3" Text="Album" Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                          FontSize="10"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          Visibility="{x:Bind ViewModel.IsSortingByAlbum, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Button>

                        <!-- Date Added (sortable) -->
                        <Button x:Name="AddedHeaderScrollable" Grid.Column="5"
                                Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                                CommandParameter="AddedAt"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0,8,8,8"
                                HorizontalAlignment="Left"
                                HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_4" Text="Date Added" Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                          FontSize="10"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          Visibility="{x:Bind ViewModel.IsSortingByAddedAt, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Button>

                        <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_5" Grid.Column="6" Text="Duration" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,8"/>
                    </Grid>
                </StackPanel>
            </ListView.Header>

            <ListView.Footer>
                <!-- StackPanel wrapper + HorizontalContentAlignment=Stretch mirror the Header pattern
                     above (see comment on ScrollableColumnHeaders). Without this, WinUI 3's implicit
                     ListView.Footer container doesn't propagate viewport width and anything the page
                     puts in FooterContent collapses to zero width → invisible. -->
                <StackPanel HorizontalAlignment="Stretch">
                    <ContentPresenter Content="{x:Bind FooterContent, Mode=OneWay}"
                                      HorizontalContentAlignment="Stretch"/>
                </StackPanel>
            </ListView.Footer>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel ItemsUpdatingScrollMode="KeepScrollOffset" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="contracts:ITrackItem">
                    <track:TrackItem
                        Track="{x:Bind}"
                        Mode="Row"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Sticky Column Headers Overlay (appears when original scrolls out of view) -->
        <Border x:Name="StickyColumnHeaders"
                VerticalAlignment="Top"
                Visibility="Collapsed"
                CornerRadius="8"
                Padding="2"
                BorderThickness="0"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid Padding="8,0,8,0">
                <!-- 7 columns matching TrackItem Row layout (artist is subline under Title).
                     Padding 8,0,8,0 + MaxWidth=640 on Title both intentionally mirror
                     ScrollableColumnHeaders above and TrackItem.xaml's RowContentGrid so
                     the sticky overlay sits flush with the rows it labels. -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition x:Name="StickyHeartColumnDef" Width="32"/>
                    <ColumnDefinition x:Name="StickyArtColumnDef" Width="42"/>
                    <ColumnDefinition Width="*" MinWidth="120" MaxWidth="640"/>
                    <ColumnDefinition x:Name="StickyAlbumColumnDef" Width="180"/>
                    <ColumnDefinition x:Name="StickyAddedColumnDef" Width="120"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="#" Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Title (sortable) -->
                <Button Grid.Column="3"
                        Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                        CommandParameter="Title"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="12,8,8,8"
                        HorizontalAlignment="Left"
                        HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_6" Text="Title" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                  FontSize="10"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind ViewModel.IsSortingByTitle, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Button>

                <!-- Album (sortable) -->
                <Button x:Name="StickyAlbumHeader" Grid.Column="4"
                        Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                        CommandParameter="Album"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0,8,8,8"
                        HorizontalAlignment="Left"
                        HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_8" Text="Album" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                  FontSize="10"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind ViewModel.IsSortingByAlbum, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Button>

                <!-- Date Added (sortable) -->
                <Button x:Name="StickyAddedHeader" Grid.Column="5"
                        Command="{x:Bind ViewModel.SortByCommand, Mode=OneWay}"
                        CommandParameter="AddedAt"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0,8,8,8"
                        HorizontalAlignment="Left"
                        HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_9" Text="Date Added" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <FontIcon Glyph="{x:Bind ViewModel.SortChevronGlyph, Mode=OneWay}"
                                  FontSize="10"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind ViewModel.IsSortingByAddedAt, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Button>

                <TextBlock x:Uid="Controls_TrackList_TrackListView__TextBlock_10" Grid.Column="6" Text="Duration" Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,8"/>
            </Grid>
        </Border>

        <!-- Loading Overlay (no progress ring — shimmer rows handle loading state) -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed"/>

        <!-- Empty State -->
        <ContentPresenter x:Name="EmptyStatePresenter"
                          Content="{x:Bind EmptyStateContent, Mode=OneWay}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Visibility="Collapsed"/>

        <!-- Error State -->
        <Grid x:Name="ErrorStateContainer" Visibility="Collapsed"
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <!-- Custom error content (shown when ErrorStateContent is provided) -->
            <ContentPresenter x:Name="ErrorStatePresenter"
                              Content="{x:Bind ErrorStateContent, Mode=OneWay}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Visibility="Collapsed"/>
            <!-- Default error state (shown when no custom ErrorStateContent provided) -->
            <appControls:ErrorStateView x:Name="DefaultErrorState"
                                     Message="{x:Bind ErrorMessage, Mode=OneWay}"
                                     RetryCommand="{x:Bind RetryCommand, Mode=OneWay}"/>
        </Grid>

        <!-- Selection Command Bar (appears when tracks selected) -->
        <Border x:Name="SelectionCommandBarBorder"
                VerticalAlignment="Bottom"
                CornerRadius="8,8,0,0"
                Margin="0,0,0,0"
                Visibility="Collapsed">
            <Border.Background>
                <AcrylicBrush TintColor="{ThemeResource LayerFillColorDefault}"
                              TintOpacity="0.9"
                              FallbackColor="{ThemeResource LayerFillColorDefault}"/>
            </Border.Background>
            <controls:TabbedCommandBar x:Name="SelectionCommandBar">
                <controls:TabbedCommandBar.MenuItems>
                    <controls:TabbedCommandBarItem x:Name="SelectionCommandBarItem">
                        <AppBarButton Icon="{ui:FontIcon Glyph=&#xE768;}"
                                      Label="Play selected"
                                      Command="{x:Bind ViewModel.PlaySelectedCommand, Mode=OneWay}"/>
                        <AppBarButton Icon="{ui:FontIcon Glyph=&#xE768;}"
                                      Label="Play after"
                                      Command="{x:Bind ViewModel.PlayAfterCommand, Mode=OneWay}"/>
                        <AppBarButton Icon="{ui:FontIcon Glyph=&#xE90B;}"
                                      Label="Add to queue"
                                      Command="{x:Bind ViewModel.AddSelectedToQueueCommand, Mode=OneWay}"/>
                        <AppBarSeparator/>
                        <AppBarButton Icon="{ui:FontIcon Glyph=&#xE710;}"
                                      Label="Add to playlist">
                            <AppBarButton.Flyout>
                                <MenuFlyout x:Name="AddToPlaylistFlyout"
                                            Placement="TopEdgeAlignedLeft"
                                            Opening="AddToPlaylistFlyout_Opening">
                                    <MenuFlyoutItem x:Uid="Controls_TrackList_TrackListView__MenuFlyoutItem_11" Text="New playlist..."
                                                    Icon="{ui:FontIcon Glyph=&#xE710;}"
                                                    Click="NewPlaylist_Click"/>
                                    <MenuFlyoutSeparator x:Name="PlaylistSeparator" Visibility="Collapsed"/>
                                    <!-- Playlist items added dynamically in code-behind -->
                                </MenuFlyout>
                            </AppBarButton.Flyout>
                        </AppBarButton>
                        <AppBarButton x:Name="RemoveButton"
                                      Icon="{ui:FontIcon Glyph=&#xE74D;}"
                                      Command="{x:Bind ViewModel.RemoveSelectedCommand, Mode=OneWay}"/>
                    </controls:TabbedCommandBarItem>
                </controls:TabbedCommandBar.MenuItems>
            </controls:TabbedCommandBar>
        </Border>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1100"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="Medium">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="AddedColumnDef.Width" Value="0"/>
                        <Setter Target="AddedHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyAddedColumnDef.Width" Value="0"/>
                        <Setter Target="StickyAddedHeader.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="AddedColumnDef.Width" Value="0"/>
                        <Setter Target="AddedHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="AlbumColumnDef.Width" Value="0"/>
                        <Setter Target="AlbumHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyAddedColumnDef.Width" Value="0"/>
                        <Setter Target="StickyAddedHeader.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyAlbumColumnDef.Width" Value="0"/>
                        <Setter Target="StickyAlbumHeader.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="VeryNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="AddedColumnDef.Width" Value="0"/>
                        <Setter Target="AddedHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="AlbumColumnDef.Width" Value="0"/>
                        <Setter Target="AlbumHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="ArtistColumnDef.Width" Value="0"/>
                        <Setter Target="ArtistHeaderScrollable.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyAddedColumnDef.Width" Value="0"/>
                        <Setter Target="StickyAddedHeader.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyAlbumColumnDef.Width" Value="0"/>
                        <Setter Target="StickyAlbumHeader.Visibility" Value="Collapsed"/>
                        <Setter Target="StickyArtistColumnDef.Width" Value="0"/>
                        <Setter Target="StickyArtistHeader.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
