<?xml version="1.0" encoding="utf-8"?>
<winuiex:WindowEx
    x:Class="Wavee.UI.WinUI.Views.MigrationErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winuiex="using:WinUIEx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Wavee — Library cache issue"
    Width="560"
    Height="420"
    MinWidth="480"
    MinHeight="360">

    <winuiex:WindowEx.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt" />
    </winuiex:WindowEx.SystemBackdrop>

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Headline -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                      Glyph="&#xE7BA;"
                      FontSize="28"
                      VerticalAlignment="Center"
                      Foreground="{ThemeResource SystemFillColorCautionBrush}" />
            <TextBlock x:Name="HeadlineText"
                       Text="Can't open your library cache"
                       Style="{ThemeResource TitleTextBlockStyle}"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Reason -->
        <TextBlock x:Name="ReasonText"
                   Grid.Row="1"
                   Margin="0,20,0,12"
                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                   TextWrapping="Wrap" />

        <!-- Details + guidance -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
                <TextBlock x:Name="ExplainText"
                           TextWrapping="Wrap"
                           Style="{ThemeResource BodyTextBlockStyle}" />
                <Border CornerRadius="6"
                        Padding="12"
                        Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <TextBlock x:Name="DetailsText"
                               FontFamily="Consolas"
                               FontSize="12"
                               TextWrapping="Wrap"
                               IsTextSelectionEnabled="True" />
                </Border>
                <TextBlock x:Name="PreservedText"
                           TextWrapping="Wrap"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </ScrollViewer>

        <!-- Actions -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,20,0,0">
            <Button x:Name="QuitButton"
                    Content="Quit Wavee"
                    Click="OnQuitClick" />
            <Button x:Name="RebuildButton"
                    Content="Rebuild library cache"
                    Style="{ThemeResource AccentButtonStyle}"
                    Click="OnRebuildClick" />
        </StackPanel>
    </Grid>
</winuiex:WindowEx>
