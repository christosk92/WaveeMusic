<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wavee.UI.WinUI.Views.CreatePlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="24"
            MaxWidth="400">

            <!-- Icon -->
            <Grid
                Width="120"
                Height="120"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                HorizontalAlignment="Center">
                <FontIcon
                    FontFamily="{StaticResource MediaPlayerIconsFontFamily}"
                    Glyph="{x:Bind ViewModel.IconGlyph, Mode=OneWay}"
                    FontSize="48"/>
            </Grid>

            <!-- Title -->
            <TextBlock
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                Style="{StaticResource TitleTextBlockStyle}"
                HorizontalAlignment="Center"/>

            <!-- Tracks Info (shown when adding tracks) -->
            <TextBlock
                Text="{x:Bind ViewModel.TracksInfoText, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.HasTracks, Mode=OneWay}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                HorizontalAlignment="Center"
                Margin="0,-16,0,0"/>

            <!-- Name Input -->
            <TextBox
                x:Name="NameTextBox"
                PlaceholderText="{x:Bind ViewModel.PlaceholderText, Mode=OneWay}"
                Text="{x:Bind ViewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                HorizontalAlignment="Stretch"
                Padding="12"
                FontSize="16"/>

            <!-- Buttons -->
            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Spacing="12">
                <Button
                    Content="Create"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                    Command="{x:Bind ViewModel.CreateCommand}"
                    MinWidth="120"/>
                <Button
                    Content="Cancel"
                    Command="{x:Bind ViewModel.CancelCommand}"
                    MinWidth="120"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
