<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wavee.UI.WinUI.Views.LocalLibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Wavee.UI.WinUI.ViewModels"
    xmlns:converters="using:Wavee.UI.WinUI.Converters"
    NavigationCacheMode="Disabled"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="32,24,32,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="6">
            <TextBlock Text="Local files" Style="{StaticResource TitleTextBlockStyle}" FontWeight="Bold"/>
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Style="{ThemeResource BodyTextBlockStyle}">
                <Run Text="{x:Bind ViewModel.TrackCount, Mode=OneWay}"/>
                <Run Text=" tracks · "/>
                <Run Text="{x:Bind ViewModel.AlbumCount, Mode=OneWay}"/>
                <Run Text=" albums"/>
            </TextBlock>
        </StackPanel>

        <!-- Empty state -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="8"
                    Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <FontIcon Glyph="&#xE8B7;" FontSize="48"
                      Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
            <TextBlock Text="No local files indexed yet"
                       Style="{ThemeResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Add a watched folder in Settings → Storage and Network → Local Files."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="420"
                       TextAlignment="Center"/>
        </StackPanel>

        <!-- Album list -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Albums}">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="24"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:LocalAlbumGroupViewModel">
                        <Grid ColumnSpacing="16" Padding="0,8,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Art -->
                            <Grid Grid.Column="0" Width="120" Height="120"
                                  Background="{ThemeResource SubtleFillColorTertiaryBrush}"
                                  CornerRadius="6">
                                <Image Source="{x:Bind ArtworkUri, Converter={StaticResource SpotifyImageConverter}, ConverterParameter=240, Mode=OneTime}"
                                       Stretch="UniformToFill"/>
                            </Grid>

                            <!-- Title + tracks -->
                            <StackPanel Grid.Column="1" Spacing="6">
                                <StackPanel Spacing="2">
                                    <TextBlock Text="{x:Bind AlbumTitle}"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind SubtitleDisplay}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Style="{ThemeResource CaptionTextBlockStyle}"/>
                                </StackPanel>

                                <ItemsControl ItemsSource="{x:Bind Tracks}" Margin="0,4,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:LocalTrackRowViewModel">
                                            <Grid Padding="6,6,6,6" ColumnSpacing="12"
                                                  Background="Transparent"
                                                  PointerEntered="TrackRow_PointerEntered"
                                                  PointerExited="TrackRow_PointerExited"
                                                  Tapped="TrackRow_Tapped"
                                                  Tag="{x:Bind}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="32"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind Index}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Right"/>
                                                <StackPanel Grid.Column="1" Spacing="0">
                                                    <TextBlock Text="{x:Bind Title}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxLines="1"/>
                                                    <TextBlock Text="{x:Bind Artist}"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxLines="1"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2"
                                                           Text="{x:Bind DurationDisplay}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
