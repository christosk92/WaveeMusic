<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Wavee.UI.WinUI.Views.ArtistsLibraryView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:tracklist="using:Wavee.UI.WinUI.Controls.TrackList"
    xmlns:track="using:Wavee.UI.WinUI.Controls.Track"
    xmlns:dto="using:Wavee.UI.WinUI.Data.DTOs"
    xmlns:vm="using:Wavee.UI.WinUI.ViewModels"
    xmlns:primitives="using:CommunityToolkit.WinUI.Controls"
    xmlns:toolkit="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:behaviors="using:Wavee.UI.WinUI.Behaviors"
    xmlns:controls1="using:Wavee.UI.WinUI.Controls"
    mc:Ignorable="d"
    Background="Transparent">

    <UserControl.Resources>
        <media:AttachedCardShadow x:Key="CardShadow"
                                   Offset="4"
                                   BlurRadius="16"
                                   Opacity="0.25"
                                   CornerRadius="8"/>
    </UserControl.Resources>

    <Grid>
        <Grid
            x:Name="RootGrid"
            Visibility="{x:Bind ViewModel.IsWideLayout, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200" MaxWidth="400"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="TracksPanelColumn" Width="0"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox x:Uid="Views_ArtistsLibraryView__TextBox_1"
                    Grid.Row="0"
                    PlaceholderText="Filter artists..."
                    Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,12"/>

                <ItemsView
                    x:Name="ArtistsView"
                    Grid.Row="1"
                    ItemsSource="{x:Bind ViewModel.FilteredArtists, Mode=OneWay}"
                    SelectionMode="Single"
                    SelectionChanged="ArtistsView_SelectionChanged"
                    DoubleTapped="ArtistsView_DoubleTapped">
                    <ItemsView.Layout>
                        <StackLayout Orientation="Vertical" Spacing="2"/>
                    </ItemsView.Layout>
                    <ItemsView.ItemTemplate>
                        <DataTemplate x:DataType="dto:LibraryArtistDto">
                            <ItemContainer>
                                <Grid Padding="8" ColumnSpacing="12" CornerRadius="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <controls:Shimmer
                                        Grid.Column="0"
                                        Width="48" Height="48" CornerRadius="24"
                                        Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <StackPanel
                                        Grid.Column="1"
                                        VerticalAlignment="Center" Spacing="6"
                                        Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="120"/>
                                        <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="80"/>
                                    </StackPanel>

                                    <Grid
                                        Grid.Column="0"
                                        Width="48"
                                        Height="48"
                                        CornerRadius="24"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                        Visibility="{x:Bind IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                        <FontIcon
                                            Glyph="&#xE77B;"
                                            FontSize="20"
                                            Opacity="0.3"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Visibility="{x:Bind ImageUrl, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                        <primitives:ConstrainedBox AspectRatio="1:1">
                                            <Grid CornerRadius="24">
                                                <Image
                                                    Source="{x:Bind ImageUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='48'}"
                                                    Stretch="UniformToFill"
                                                    Visibility="{x:Bind ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="96"/>
                                            </Grid>
                                        </primitives:ConstrainedBox>
                                    </Grid>

                                    <StackPanel
                                        Grid.Column="1"
                                        VerticalAlignment="Center" Spacing="2"
                                        Visibility="{x:Bind IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                        <TextBlock
                                            Text="{x:Bind Name}"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"
                                            TextTrimming="CharacterEllipsis"
                                            MaxLines="1"/>
                                        <TextBlock
                                            Text="{x:Bind AddedAtFormatted}"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            TextTrimming="CharacterEllipsis"
                                            MaxLines="1"/>
                                    </StackPanel>
                                </Grid>
                            </ItemContainer>
                        </DataTemplate>
                    </ItemsView.ItemTemplate>
                </ItemsView>

                <StackPanel
                    Grid.Row="1"
                    Spacing="2"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                            <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="120"/>
                            <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="80"/>
                        </StackPanel>
                    </Grid>
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                            <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="140"/>
                            <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="70"/>
                        </StackPanel>
                    </Grid>
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                            <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="100"/>
                            <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="90"/>
                        </StackPanel>
                    </Grid>
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                            <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="130"/>
                            <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="85"/>
                        </StackPanel>
                    </Grid>
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                            <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="110"/>
                            <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="75"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>

            <controls1:GridSplitter
                Grid.Column="1"
                Width="12"/>

            <Border Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    toolkit:Effects.Shadow="{StaticResource CardShadow}">
                <Grid Padding="12">
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="12"
                        Visibility="{x:Bind ViewModel.SelectedArtist, Mode=OneWay, Converter={StaticResource InverseNullToVisibilityConverter}}">
                        <FontIcon Glyph="&#xE77B;" FontSize="48" Opacity="0.3"/>
                        <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_2"
                            Text="Select an artist to see details"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Opacity="0.5"
                            TextAlignment="Center"/>
                    </StackPanel>

                    <Grid Visibility="{x:Bind ViewModel.SelectedArtist, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid
                                x:Name="DetailArtistImageContainer"
                                Grid.Column="0"
                                Width="80"
                                Height="80"
                                CornerRadius="40"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                <FontIcon
                                    Glyph="&#xE77B;"
                                    FontSize="32"
                                    Opacity="0.3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                <Image
                                    Source="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='80'}"
                                    Stretch="UniformToFill"
                                    Visibility="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="160"/>
                            </Grid>

                            <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="4" VerticalAlignment="Center">
                                <TextBlock
                                    Text="{x:Bind ViewModel.SelectedArtistName, Mode=OneWay}"
                                    Style="{StaticResource SubtitleTextBlockStyle}"
                                    TextTrimming="CharacterEllipsis"
                                    MaxLines="2"/>
                                <TextBlock
                                    Text="{x:Bind ViewModel.SelectedArtistAddedAt, Mode=OneWay}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <primitives:WrapPanel Orientation="Horizontal" HorizontalSpacing="8" VerticalSpacing="6" Margin="0,8,0,0">
                                    <Button
                                        Style="{StaticResource AccentButtonStyle}"
                                        Command="{x:Bind ViewModel.PlayArtistCommand}"
                                        Padding="12,6">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE768;" FontSize="14"/>
                                            <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_3" Text="Play"/>
                                        </StackPanel>
                                    </Button>
                                    <Button
                                        Command="{x:Bind ViewModel.ShuffleArtistCommand}"
                                        Padding="12,6">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE8B1;" FontSize="14"/>
                                            <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_4" Text="Shuffle"/>
                                        </StackPanel>
                                    </Button>
                                    <Button
                                        Click="ViewArtistButton_Click"
                                        Padding="12,6">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE7B3;" FontSize="14"/>
                                            <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_5" Text="View artist"/>
                                        </StackPanel>
                                    </Button>
                                    <controls1:FollowButton ItemUri="{x:Bind ViewModel.SelectedArtist.Id, Mode=OneWay}"/>
                                </primitives:WrapPanel>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="1">
                            <ScrollView>
                                <StackPanel Spacing="12">
                                    <ToggleButton
                                        IsChecked="{x:Bind ViewModel.ShowSavedOnly, Mode=TwoWay}"
                                        Padding="12,6"
                                        HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE728;" FontSize="14"/>
                                            <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_6" Text="Saved only"/>
                                        </StackPanel>
                                    </ToggleButton>
                                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.AlbumGroups, Mode=OneWay}">
                                        <ItemsRepeater.Layout>
                                            <StackLayout Orientation="Vertical" Spacing="24"/>
                                        </ItemsRepeater.Layout>
                                        <ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="vm:ArtistAlbumGroupViewModel">
                                                <StackPanel>
                                                    <Grid Margin="0,0,0,12">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock
                                                            Grid.Column="0"
                                                            Text="{x:Bind GroupName}"
                                                            Style="{StaticResource BodyStrongTextBlockStyle}"
                                                            VerticalAlignment="Center"/>

                                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                                            <Button x:Uid="Views_ArtistsLibraryView__Button_7"
                                                                Padding="8,4"
                                                                Background="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource InverseBoolToBackgroundConverter}}"
                                                                Command="{x:Bind ToggleViewModeCommand}"
                                                                ToolTipService.ToolTip="Grid view">
                                                                <FontIcon Glyph="&#xF0E2;" FontSize="14"/>
                                                            </Button>
                                                            <Button x:Uid="Views_ArtistsLibraryView__Button_8"
                                                                Padding="8,4"
                                                                Background="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource BoolToBackgroundConverter}}"
                                                                Command="{x:Bind ToggleViewModeCommand}"
                                                                ToolTipService.ToolTip="List view">
                                                                <FontIcon Glyph="&#xE8FD;" FontSize="14"/>
                                                            </Button>
                                                        </StackPanel>
                                                    </Grid>

                                                    <ItemsView
                                                        ItemsSource="{x:Bind Albums}"
                                                        SelectionMode="Single"
                                                        Visibility="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                                        <ItemsView.Layout>
                                                            <UniformGridLayout MinItemWidth="120" MinItemHeight="180"
                                                                               MinColumnSpacing="12" MinRowSpacing="12"
                                                                               ItemsStretch="None"/>
                                                        </ItemsView.Layout>
                                                        <ItemsView.ItemTemplate>
                                                            <DataTemplate x:DataType="vm:ArtistAlbumItemViewModel">
                                                                <ItemContainer>
                                                                    <Grid Width="120" RowSpacing="6" Padding="4"
                                                                        Tapped="{x:Bind OnTapped}">
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition Height="120"/>
                                                                            <RowDefinition Height="Auto"/>
                                                                            <RowDefinition Height="Auto"/>
                                                                        </Grid.RowDefinitions>

                                                                        <Border
                                                                            Grid.Row="0"
                                                                            CornerRadius="6"
                                                                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                                                            <Grid>
                                                                                <FontIcon
                                                                                    Glyph="&#xE93C;"
                                                                                    FontSize="32"
                                                                                    Opacity="0.3"
                                                                                    Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                                                                <Image
                                                                                    Source="{x:Bind Album.ImageUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='120'}"
                                                                                    Stretch="UniformToFill"
                                                                                    Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                                                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                                                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="240"/>
                                                                            </Grid>
                                                                        </Border>

                                                                        <TextBlock
                                                                            Grid.Row="1"
                                                                            Text="{x:Bind Album.Name}"
                                                                            Style="{StaticResource CaptionTextBlockStyle}"
                                                                            TextTrimming="CharacterEllipsis"
                                                                            MaxLines="2"/>

                                                                        <TextBlock
                                                                            Grid.Row="2"
                                                                            Text="{x:Bind Album.Year}"
                                                                            Style="{StaticResource CaptionTextBlockStyle}"
                                                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                                    </Grid>
                                                                </ItemContainer>
                                                            </DataTemplate>
                                                        </ItemsView.ItemTemplate>
                                                    </ItemsView>

                                                    <ItemsRepeater
                                                        ItemsSource="{x:Bind Albums}"
                                                        Visibility="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <ItemsRepeater.Layout>
                                                            <StackLayout Orientation="Vertical" Spacing="16"/>
                                                        </ItemsRepeater.Layout>
                                                        <ItemsRepeater.ItemTemplate>
                                                            <DataTemplate x:DataType="vm:ArtistAlbumItemViewModel">
                                                                <StackPanel>
                                                                    <Grid ColumnSpacing="12" Margin="0,0,0,8">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="48"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>

                                                                        <Border
                                                                            Grid.Column="0"
                                                                            Width="48"
                                                                            Height="48"
                                                                            CornerRadius="4"
                                                                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                                                            <Grid>
                                                                                <FontIcon
                                                                                    Glyph="&#xE93C;"
                                                                                    FontSize="20"
                                                                                    Opacity="0.3"
                                                                                    Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                                                                <Image
                                                                                    Source="{x:Bind Album.ImageUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='48'}"
                                                                                    Stretch="UniformToFill"
                                                                                    Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                                                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                                                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="96"/>
                                                                            </Grid>
                                                                        </Border>

                                                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                                            <TextBlock
                                                                                Text="{x:Bind Album.Name}"
                                                                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                                                                TextTrimming="CharacterEllipsis"/>
                                                                            <TextBlock
                                                                                Text="{x:Bind Album.Year}"
                                                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                                        </StackPanel>
                                                                    </Grid>

                                                                    <StackPanel
                                                                        Visibility="{x:Bind IsLoadingTracks, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                                        Spacing="8"
                                                                        Margin="0,0,0,8">
                                                                        <Grid Height="32" ColumnSpacing="12">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="24"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="40"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <controls:Shimmer Grid.Column="0"/>
                                                                            <controls:Shimmer Grid.Column="1"/>
                                                                            <controls:Shimmer Grid.Column="2"/>
                                                                        </Grid>
                                                                        <Grid Height="32" ColumnSpacing="12">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="24"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="40"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <controls:Shimmer Grid.Column="0"/>
                                                                            <controls:Shimmer Grid.Column="1"/>
                                                                            <controls:Shimmer Grid.Column="2"/>
                                                                        </Grid>
                                                                        <Grid Height="32" ColumnSpacing="12">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="24"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="40"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <controls:Shimmer Grid.Column="0"/>
                                                                            <controls:Shimmer Grid.Column="1"/>
                                                                            <controls:Shimmer Grid.Column="2"/>
                                                                        </Grid>
                                                                    </StackPanel>

                                                                    <ItemsRepeater
                                                                        ItemsSource="{x:Bind Tracks, Mode=OneWay}"
                                                                        Visibility="{x:Bind HasLoadedTracks, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                                        <ItemsRepeater.Layout>
                                                                            <StackLayout Orientation="Vertical" Spacing="4"/>
                                                                        </ItemsRepeater.Layout>
                                                                        <ItemsRepeater.ItemTemplate>
                                                                            <DataTemplate x:DataType="dto:AlbumTrackDto">
                                                                                <track:TrackItem
                                                                                    Track="{x:Bind}"
                                                                                    Mode="Row"
                                                                                    ShowAlbumArt="False"
                                                                                    ShowArtistColumn="False"
                                                                                    ShowAlbumColumn="False"
                                                                                    ShowDateAdded="False"
                                                                                    IsCompactRow="True"
                                                                                    PlayCommand="{Binding ViewModel.PlayTrackCommand, ElementName=Root}"/>
                                                                            </DataTemplate>
                                                                        </ItemsRepeater.ItemTemplate>
                                                                    </ItemsRepeater>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ItemsRepeater.ItemTemplate>
                                                    </ItemsRepeater>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsRepeater.ItemTemplate>
                                    </ItemsRepeater>
                                </StackPanel>
                            </ScrollView>

                            <StackPanel
                                Visibility="{x:Bind ViewModel.IsLoadingDetails, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                Spacing="24">
                                <StackPanel Spacing="12">
                                    <controls:Shimmer Height="20" Width="80" HorizontalAlignment="Left"/>
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <StackPanel Width="120" Spacing="6">
                                            <controls:Shimmer Height="120" CornerRadius="6"/>
                                            <controls:Shimmer Height="14" Width="100"/>
                                            <controls:Shimmer Height="12" Width="50"/>
                                        </StackPanel>
                                        <StackPanel Width="120" Spacing="6">
                                            <controls:Shimmer Height="120" CornerRadius="6"/>
                                            <controls:Shimmer Height="14" Width="90"/>
                                            <controls:Shimmer Height="12" Width="50"/>
                                        </StackPanel>
                                        <StackPanel Width="120" Spacing="6">
                                            <controls:Shimmer Height="120" CornerRadius="6"/>
                                            <controls:Shimmer Height="14" Width="110"/>
                                            <controls:Shimmer Height="12" Width="50"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Spacing="12">
                                    <controls:Shimmer Height="20" Width="100" HorizontalAlignment="Left"/>
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <StackPanel Width="120" Spacing="6">
                                            <controls:Shimmer Height="120" CornerRadius="6"/>
                                            <controls:Shimmer Height="14" Width="85"/>
                                            <controls:Shimmer Height="12" Width="50"/>
                                        </StackPanel>
                                        <StackPanel Width="120" Spacing="6">
                                            <controls:Shimmer Height="120" CornerRadius="6"/>
                                            <controls:Shimmer Height="14" Width="105"/>
                                            <controls:Shimmer Height="12" Width="50"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>

            <controls1:GridSplitter
                Grid.Column="3"
                Width="12"
                Visibility="{x:Bind ViewModel.IsTracksPanelVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <Border x:Name="TracksPanelBorder"
                    Grid.Column="4"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    toolkit:Effects.Shadow="{StaticResource CardShadow}"
                    MinWidth="280">
                <Grid x:Name="TracksPanel" Padding="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border
                            x:Name="DetailAlbumImageContainer"
                            Grid.Column="0"
                            Width="64"
                            Height="64"
                            CornerRadius="6"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <Grid>
                                <FontIcon
                                    Glyph="&#xE93C;"
                                    FontSize="24"
                                    Opacity="0.3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                <Image
                                    Source="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='64'}"
                                    Stretch="UniformToFill"
                                    Visibility="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="128"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="4" VerticalAlignment="Center">
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedAlbumName, Mode=OneWay}"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                TextTrimming="CharacterEllipsis"
                                MaxLines="2"/>
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedAlbumYear, Mode=OneWay}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button
                                Click="ViewAlbumButton_Click"
                                Padding="8,4"
                                Margin="0,4,0,0">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE7B3;" FontSize="12"/>
                                    <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_9" Text="View album" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <Button
                            Grid.Column="2"
                            Command="{x:Bind ViewModel.CloseTracksPanelCommand}"
                            Style="{StaticResource SubtleButtonStyle}"
                            Padding="8"
                            VerticalAlignment="Top">
                            <FontIcon Glyph="&#xE8BB;" FontSize="12"/>
                        </Button>
                    </Grid>

                    <tracklist:TrackListView
                        Grid.Row="1"
                        ViewModel="{x:Bind ViewModel}"
                        ItemsSource="{x:Bind ViewModel.SelectedAlbumTracks, Mode=OneWay}"
                        IsLoading="{x:Bind ViewModel.IsLoadingSelectedAlbumTracks, Mode=OneWay}"
                        IsCompact="True"
                        HorizontalAlignment="Stretch"
                        ShowColumnHeaders="False"
                        ShowAlbumArtColumn="False"
                        ShowArtistColumn="False"
                        ShowAlbumColumn="False"
                        ShowDateAddedColumn="False"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Visibility="{x:Bind ViewModel.IsNarrowLayout, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Margin="0,0,0,16"
                Padding="10,6"
                HorizontalAlignment="Left"
                CornerRadius="18"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <FontIcon
                        Glyph="&#xE77B;"
                        FontSize="14"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <BreadcrumbBar
                        ItemsSource="{x:Bind ViewModel.BreadcrumbItems, Mode=OneWay}"
                        ItemClicked="BreadcrumbBar_ItemClicked">
                        <BreadcrumbBar.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Text="{Binding}"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    TextTrimming="CharacterEllipsis"
                                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </DataTemplate>
                        </BreadcrumbBar.ItemTemplate>
                    </BreadcrumbBar>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1">
                <Grid
                    x:Name="NarrowArtistsStage"
                    Visibility="{x:Bind ViewModel.ShowNarrowArtistsStage, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBox x:Uid="Views_ArtistsLibraryView__TextBox_2"
                        Grid.Row="0"
                        PlaceholderText="Filter artists..."
                        Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,0,12"/>

                    <Grid Grid.Row="1">
                        <ItemsView
                            ItemsSource="{x:Bind ViewModel.FilteredArtists, Mode=OneWay}"
                            SelectionMode="Single"
                            SelectionChanged="NarrowArtistsView_SelectionChanged"
                            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                            <ItemsView.Layout>
                                <StackLayout Orientation="Vertical" Spacing="2"/>
                            </ItemsView.Layout>
                            <ItemsView.ItemTemplate>
                                <DataTemplate x:DataType="dto:LibraryArtistDto">
                                    <ItemContainer>
                                        <Grid Padding="8" ColumnSpacing="12" CornerRadius="4" Tapped="NarrowArtistItem_Tapped">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <controls:Shimmer
                                                Grid.Column="0"
                                                Width="48" Height="48" CornerRadius="24"
                                                Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center" Spacing="6"
                                                Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="120"/>
                                                <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="80"/>
                                            </StackPanel>

                                            <Grid
                                                Grid.Column="0"
                                                Width="48"
                                                Height="48"
                                                CornerRadius="24"
                                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                                Visibility="{x:Bind IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                                <FontIcon
                                                    Glyph="&#xE77B;"
                                                    FontSize="20"
                                                    Opacity="0.3"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Visibility="{x:Bind ImageUrl, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                                <primitives:ConstrainedBox AspectRatio="1:1">
                                                    <Grid CornerRadius="24">
                                                        <Image
                                                            Source="{x:Bind ImageUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='48'}"
                                                            Stretch="UniformToFill"
                                                            Visibility="{x:Bind ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                            behaviors:ImageFallbackBehavior.HideOnError="True"
                                                            behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                                            behaviors:ImageFallbackBehavior.DecodePixelWidth="96"/>
                                                    </Grid>
                                                </primitives:ConstrainedBox>
                                            </Grid>

                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center" Spacing="2"
                                                Visibility="{x:Bind IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                                <TextBlock
                                                    Text="{x:Bind Name}"
                                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                                    TextTrimming="CharacterEllipsis"
                                                    MaxLines="1"/>
                                                <TextBlock
                                                    Text="{x:Bind AddedAtFormatted}"
                                                    Style="{StaticResource CaptionTextBlockStyle}"
                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                    TextTrimming="CharacterEllipsis"
                                                    MaxLines="1"/>
                                            </StackPanel>
                                        </Grid>
                                    </ItemContainer>
                                </DataTemplate>
                            </ItemsView.ItemTemplate>
                        </ItemsView>

                        <StackPanel
                            Spacing="2"
                            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid Padding="8" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                                    <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="120"/>
                                    <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="80"/>
                                </StackPanel>
                            </Grid>
                            <Grid Padding="8" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <controls:Shimmer Width="48" Height="48" CornerRadius="24"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                                    <controls:Shimmer Height="16" HorizontalAlignment="Left" Width="140"/>
                                    <controls:Shimmer Height="12" HorizontalAlignment="Left" Width="70"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Grid>

                <Grid
                    x:Name="NarrowArtistDetailsStage"
                    Visibility="{x:Bind ViewModel.ShowNarrowArtistDetailsStage, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            Width="80"
                            Height="80"
                            CornerRadius="40"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <FontIcon
                                Glyph="&#xE77B;"
                                FontSize="32"
                                Opacity="0.3"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                            <Image
                                Source="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='80'}"
                                Stretch="UniformToFill"
                                Visibility="{x:Bind ViewModel.SelectedArtistImageUrl, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                                behaviors:ImageFallbackBehavior.HideOnError="True"
                                behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                behaviors:ImageFallbackBehavior.DecodePixelWidth="160"/>
                        </Grid>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="4" VerticalAlignment="Center">
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedArtistName, Mode=OneWay}"
                                Style="{StaticResource SubtitleTextBlockStyle}"
                                TextTrimming="CharacterEllipsis"
                                MaxLines="2"/>
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedArtistAddedAt, Mode=OneWay}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                            <primitives:WrapPanel Orientation="Horizontal" HorizontalSpacing="8" VerticalSpacing="6" Margin="0,8,0,0">
                                <Button
                                    Style="{StaticResource AccentButtonStyle}"
                                    Command="{x:Bind ViewModel.PlayArtistCommand}"
                                    Padding="12,6">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE768;" FontSize="14"/>
                                        <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_10" Text="Play"/>
                                    </StackPanel>
                                </Button>
                                <Button
                                    Command="{x:Bind ViewModel.ShuffleArtistCommand}"
                                    Padding="12,6">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE8B1;" FontSize="14"/>
                                        <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_11" Text="Shuffle"/>
                                    </StackPanel>
                                </Button>
                                <Button
                                    Click="ViewArtistButton_Click"
                                    Padding="12,6">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE7B3;" FontSize="14"/>
                                        <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_12" Text="View artist"/>
                                    </StackPanel>
                                </Button>
                            </primitives:WrapPanel>
                        </StackPanel>
                    </Grid>

                    <ScrollView Grid.Row="1">
                        <StackPanel Spacing="12">
                            <ToggleButton
                                IsChecked="{x:Bind ViewModel.ShowSavedOnly, Mode=TwoWay}"
                                Padding="12,6"
                                HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE728;" FontSize="14"/>
                                    <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_13" Text="Saved only"/>
                                </StackPanel>
                            </ToggleButton>
                            <ItemsRepeater ItemsSource="{x:Bind ViewModel.AlbumGroups, Mode=OneWay}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Vertical" Spacing="24"/>
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="vm:ArtistAlbumGroupViewModel">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock
                                                    Grid.Column="0"
                                                    Text="{x:Bind GroupName}"
                                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                                    VerticalAlignment="Center"/>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                                    <Button
                                                        Padding="8,4"
                                                        Background="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource InverseBoolToBackgroundConverter}}"
                                                        Command="{x:Bind ToggleViewModeCommand}">
                                                        <FontIcon Glyph="&#xF0E2;" FontSize="14"/>
                                                    </Button>
                                                    <Button
                                                        Padding="8,4"
                                                        Background="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource BoolToBackgroundConverter}}"
                                                        Command="{x:Bind ToggleViewModeCommand}">
                                                        <FontIcon Glyph="&#xE8FD;" FontSize="14"/>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>

                                            <ItemsView
                                                ItemsSource="{x:Bind Albums}"
                                                SelectionMode="Single"
                                                Visibility="{x:Bind IsListView, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                                <ItemsView.Layout>
                                                    <UniformGridLayout MinItemWidth="120" MinItemHeight="180"
                                                                       MinColumnSpacing="12" MinRowSpacing="12"
                                                                       ItemsStretch="None"/>
                                                </ItemsView.Layout>
                                                <ItemsView.ItemTemplate>
                                                    <DataTemplate x:DataType="vm:ArtistAlbumItemViewModel">
                                                        <ItemContainer>
                                                            <Grid Width="120" RowSpacing="6" Padding="4"
                                                                Tapped="{x:Bind OnTapped}">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="120"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>

                                                                <Border
                                                                    Grid.Row="0"
                                                                    CornerRadius="6"
                                                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                                                    <Grid>
                                                                        <FontIcon
                                                                            Glyph="&#xE93C;"
                                                                            FontSize="32"
                                                                            Opacity="0.3"
                                                                            Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                                                        <Image
                                                                            Source="{x:Bind Album.ImageUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='120'}"
                                                                            Stretch="UniformToFill"
                                                                            Visibility="{x:Bind Album.ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                                            behaviors:ImageFallbackBehavior.HideOnError="True"
                                                                            behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                                                            behaviors:ImageFallbackBehavior.DecodePixelWidth="240"/>
                                                                    </Grid>
                                                                </Border>

                                                                <TextBlock
                                                                    Grid.Row="1"
                                                                    Text="{x:Bind Album.Name}"
                                                                    Style="{StaticResource CaptionTextBlockStyle}"
                                                                    TextTrimming="CharacterEllipsis"
                                                                    MaxLines="2"/>

                                                                <TextBlock
                                                                    Grid.Row="2"
                                                                    Text="{x:Bind Album.Year}"
                                                                    Style="{StaticResource CaptionTextBlockStyle}"
                                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                            </Grid>
                                                        </ItemContainer>
                                                    </DataTemplate>
                                                </ItemsView.ItemTemplate>
                                            </ItemsView>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </StackPanel>
                    </ScrollView>
                </Grid>

                <Grid
                    x:Name="NarrowAlbumTracksStage"
                    Visibility="{x:Bind ViewModel.ShowNarrowAlbumTracksStage, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border
                            Grid.Column="0"
                            Width="64"
                            Height="64"
                            CornerRadius="6"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <Grid>
                                <FontIcon
                                    Glyph="&#xE93C;"
                                    FontSize="24"
                                    Opacity="0.3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                <Image
                                    Source="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter='64'}"
                                    Stretch="UniformToFill"
                                    Visibility="{x:Bind ViewModel.SelectedAlbumImageUrl, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                                    behaviors:ImageFallbackBehavior.HideOnError="True"
                                    behaviors:ImageFallbackBehavior.FadeInOnLoad="True"
                                    behaviors:ImageFallbackBehavior.DecodePixelWidth="128"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="4" VerticalAlignment="Center">
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedAlbumName, Mode=OneWay}"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                TextTrimming="CharacterEllipsis"
                                MaxLines="2"/>
                            <TextBlock
                                Text="{x:Bind ViewModel.SelectedAlbumYear, Mode=OneWay}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button
                                Click="ViewAlbumButton_Click"
                                Padding="8,4"
                                Margin="0,4,0,0"
                                HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE7B3;" FontSize="12"/>
                                    <TextBlock x:Uid="Views_ArtistsLibraryView__TextBlock_14" Text="View album" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <tracklist:TrackListView
                        Grid.Row="1"
                        ViewModel="{x:Bind ViewModel}"
                        ItemsSource="{x:Bind ViewModel.SelectedAlbumTracks, Mode=OneWay}"
                        IsLoading="{x:Bind ViewModel.IsLoadingSelectedAlbumTracks, Mode=OneWay}"
                        IsCompact="True"
                        HorizontalAlignment="Stretch"
                        ShowColumnHeaders="False"
                        ShowAlbumArtColumn="False"
                        ShowArtistColumn="False"
                        ShowAlbumColumn="False"
                        ShowDateAddedColumn="False"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
