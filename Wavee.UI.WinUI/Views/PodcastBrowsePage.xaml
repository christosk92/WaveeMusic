<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wavee.UI.WinUI.Views.PodcastBrowsePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="40,32,40,40">
        <StackPanel Spacing="18" MaxWidth="720" HorizontalAlignment="Left">
            <StackPanel Spacing="8">
                <TextBlock Text="Podcasts"
                           Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock x:Name="SubtitleText"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <controls:TokenView x:Name="TopicTokenView"
                                IsWrapped="True"
                                CanRemoveTokens="False"
                                SelectionMode="Single"
                                IsItemClickEnabled="False">
                <controls:TokenView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{x:Bind}" FontSize="13" FontWeight="SemiBold"/>
                    </DataTemplate>
                </controls:TokenView.ItemTemplate>
            </controls:TokenView>

            <InfoBar IsOpen="True"
                     Severity="Informational"
                     Title="Podcast browse is not built yet"
                     Message="This page is ready to receive a Spotify genre URI from show topic tokens."
                     IsClosable="False"/>

            <TextBlock x:Name="UriText"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Page>
