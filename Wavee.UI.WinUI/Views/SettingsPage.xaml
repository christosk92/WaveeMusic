<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wavee.UI.WinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///CommunityToolkit.WinUI.Controls.Segmented/Segmented/Segmented.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Padding="32,24,32,0" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Settings"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,4"/>

        <controls:Segmented x:Name="SettingsSelectorBar"
                            Grid.Row="1"
                            SelectedIndex="0"
                            Style="{StaticResource PivotSegmentedStyle}"
                            SelectionChanged="SelectorBar_SelectionChanged">
            <controls:SegmentedItem x:Name="GeneralItem" Content="General" Tag="general"/>
            <controls:SegmentedItem x:Name="PlaybackItem" Content="Playback" Tag="playback"/>
            <controls:SegmentedItem x:Name="AudioItem" Content="Audio" Tag="audio"/>
            <controls:SegmentedItem x:Name="StorageItem" Content="Storage &amp; Network" Tag="storage"/>
            <controls:SegmentedItem x:Name="DiagnosticsItem" Content="Diagnostics" Tag="diagnostics"/>
            <controls:SegmentedItem x:Name="AboutItem" Content="About" Tag="about"/>
        </controls:Segmented>

        <ContentControl x:Name="ContentHost"
                        Grid.Row="2"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
            <ContentControl.ContentTransitions>
                <TransitionCollection>
                    <ContentThemeTransition/>
                </TransitionCollection>
            </ContentControl.ContentTransitions>
        </ContentControl>
    </Grid>
</Page>
